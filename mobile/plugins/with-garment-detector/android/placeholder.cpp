// Wave R-A: empty placeholder so add_library has at least one source.
//
// All real C++ sources for the BursGarmentDetector .so are appended by the
// nitrogen-generated `BursGarmentDetector+autolinking.cmake` via
// `target_sources(... PRIVATE)` calls.
//
// Copied alongside CMakeLists.txt into `android/` by the Expo config plugin
// at prebuild time (see `mobile/plugins/with-garment-detector/index.js`).
