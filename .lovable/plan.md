

# Update Social Links in Landing Page Footer

## Changes

### File: `src/components/landing/LandingFooter.tsx`

1. Update the Instagram link `href` from `"#"` to `"https://www.instagram.com/burs_style"` and add `target="_blank"` and `rel="noopener noreferrer"` so it opens in a new tab.
2. Remove the Twitter/X icon and link entirely.

This is a small, straightforward change -- just two lines modified in one file.

