

# Add GDPR Cookie Consent Banner to Static Landing Page

## What
Add a cookie consent banner to `public/landing.html` that matches the existing React `CookieConsent` component's design — fixed bottom bar with Accept/Decline buttons, linking to the privacy policy. Uses `localStorage` key `burs-cookie-consent` (same as the React app) so consent persists across both.

## Changes