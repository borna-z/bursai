<!-- Extracted from CLAUDE.md (2026-05-03) to keep the always-loaded tracker tight. Append new entries here, not in CLAUDE.md. See CLAUDE.md "Launch Plan Update" + "Findings Discipline" sections for write rules. -->

### Findings Log

New findings discovered during implementation (not in the original audit). Agent MUST add here when spotted out of scope.

| Date | Prompt | Location | Description | Action |
|------|--------|----------|-------------|--------|
| 2026-05-07 | M30 PR #757 review (P2 deferred) | `supabase/migrations/20260507120000_push_provider_columns.sql` (RLS policies on `push_subscriptions`) | **`push_subscriptions` RLS policies lack explicit `WITH CHECK` clauses.** PG ≥ 15 falls back to using the `USING` predicate when `WITH CHECK` is omitted on UPDATE/INSERT policies, so the table is functionally protected today, but explicit `WITH CHECK (user_id = auth.uid())` is the canonical hardening pattern and removes the silent dependency on PG-version behaviour. | Track for: post-launch hardening PR. Migration-required (CLAUDE.md hard rule). Add explicit `WITH CHECK` on the INSERT and UPDATE policies for `push_subscriptions`. |
| 2026-05-07 | M30 PR #757 review (P2 deferred) | `supabase/functions/send_push_notification/index.ts` cleanup loop + Expo `DeviceNotRegistered` handling | **Stale-token cleanup is on-send only.** The function deletes a `push_subscriptions` row when Expo tickets a `DeviceNotRegistered` / `InvalidCredentials` error during a real send; no scheduled sweep runs. A user who uninstalls the app + never receives another push keeps their dead row indefinitely, polluting analytics counts. Acceptable for launch — the on-send path eventually catches every dead token the next time we attempt to push to it. | Track for: post-launch cron PR. Add a daily `cleanup_stale_push_tokens` cron that prunes rows older than N days with no successful send. Update `vault.secrets` per the cron-secrets discipline. |
| 2026-05-07 | M30 PR #757 review (P2 deferred) | `mobile/src/hooks/usePushNotifications.ts` `useRegisterPushToken` | **No `Notifications.addPushTokenListener` registered.** Expo's documentation supports a runtime listener that fires when the push token rotates server-side (rare on iOS APNs, more common on FCM). The current registration captures the token once per session; a mid-session rotation would leave the row stale until the next app launch. Expo Push tokens are long-lived in practice (months) so this is a low-risk gap. | Track for: post-launch hardening PR. Add `Notifications.addPushTokenListener((tokenData) => ...)` inside `usePushTokenRegistration` that re-runs the upsert with the new token. |
| 2026-05-07 | M25 PR #751 review (P2 deferred) | `mobile/src/screens/onboarding/StyleQuizV4Step.tsx` `PercentSlider` | **PercentSlider drag-only; web V4 quiz supports tap-to-set.** Mobile slider's PanResponder requires a drag (`onMoveShouldSetPanResponder` + dx>dy gate); a single tap on the track does nothing. Web `PercentSlider` (in `src/components/onboarding/StyleQuizV4.tsx`) treats a click as set-position. Cross-platform UX divergence — mobile users on small targets (formality floor/ceiling, lifestyle mix) can't precisely land a value without drag. | Track for: M25 follow-up polish PR. Add `onStartShouldSetPanResponder: () => true` and have `onPanResponderGrant` always commit the touch X. Drag continues working via the existing move handler. |
| 2026-05-07 | M25 PR #751 review (P2 deferred) | `mobile/src/components/Chip.tsx` (primitive) at all `ChipRow`/`Chip` call sites in onboarding quiz | **Chip primitive lacks `accessibilityRole="checkbox"` / `accessibilityState={{ selected: ... }}` semantics for multi-select use.** Single-select rows render Chips with `active` prop but no role beyond default; VoiceOver users hear the label without the selected state. Multi-select rows (archetypes, fabricSensitivities, occasions, color swatches in some places) need checkbox semantics. Primitive-level change ripples to every caller in the app. | Track for: a11y audit PR (post-launch). Audit `Chip` callers for single-select-radio vs multi-select-checkbox semantics; either thread a `role` prop through the primitive or split into `RadioChip` / `CheckChip`. |
| 2026-05-07 | M25 PR #751 review (P2 deferred) | `mobile/src/screens/onboarding/StyleQuizV4Step.tsx` `ColorGrid` Q5 | **18 color swatches at 44pt with `gap: 12` flex-wrap give 0px inter-target padding on the cross-axis when wrapping.** Tap-target adjacency below the 4px minimum gap recommended by Material guidelines means two adjacent swatches can be tapped near-together — accidental selection / deselection. | Track for: M25 follow-up polish PR. Bump `gap` from 12 to 16, OR split the row into a fixed-column CSS grid with explicit minimum spacing on both axes. |
| 2026-05-07 | M17 PR #743 review (P2.12) | `outfit_items` schema — RLS gap on inserted `garment_id` ownership | **`outfit_items` INSERT RLS doesn't verify the inserted `garment_id` belongs to the same user as the parent outfit's owner.** Codex review on PR #743 surfaced this while reviewing the M17 accessory-add path. The insert path (`supabase.from('outfit_items').insert({ outfit_id, garment_id, slot })`) trusts the client to pass a self-owned `garment_id`. A malicious authenticated user could attach another user's garment id to their own outfit, leaking the FK reference (the garment row itself remains protected by the garments RLS policies, but the relation surface is exposed). Pre-existing schema gap, NOT introduced by M17 — the wave only added a new caller of the insert path. Also noted: there's no UNIQUE constraint on `(outfit_id, garment_id)`, addressed at the screen layer in M17 via render-time filtering + defensive Set gate. | Track for: post-launch hardening PR. Migration-required (CLAUDE.md hard rule prevents shipping it without explicit user authorization). Two paths: (a) add a CHECK constraint via a SECURITY DEFINER trigger that resolves `outfits.user_id` from the outfit_id and asserts equality with `garments.user_id`; (b) add a UNIQUE on `(outfit_id, garment_id)` while at it so the dedupe gate moves into the DB. |
| 2026-05-05 | M1 (P1) | `mobile/src/navigation/RootNavigator.tsx` | **Most "real" screens under `mobile/src/screens/` stay registered as `Placeholders.*` despite the screen file existing.** Surfaced in Codex round 5 of PR #728 — `GarmentDetail` was a placeholder, so the M1 polling hook never mounted in the navigator and the image-swap behaviour couldn't run. M1 fixes only `GarmentDetail` (the screen its acceptance criteria depend on); the rest stay as placeholders. | Track for: dedicated screen-mount pass (likely a small PR before M8 / launch). Replace each placeholder with the real screen now that they're tested in isolation. |
| 2026-05-05 | M1 (P2) | `mobile/src/hooks/useRenderJobStatus.ts` query key | **`['render_job', garmentId]` is not user-scoped**, unlike `['garment', user?.id, id]` in `useGarments.ts`. Low risk in practice — the row is fetched fresh on every poll (`staleTime: 0`) and the hook is terminal-aware so a stale snapshot can't strand. Inconsistent with the rest of the mobile cache convention. | Track for: future tidy-up PR (M2 or later). Add `user.id` to the key. |
| 2026-05-05 | M1 (P2) | `mobile/src/hooks/useRenderJobStatus.ts` terminal handling | **No user-facing signal on `failed` or `poll_timeout`.** Hook returns the snapshot but `GarmentDetailScreen` discards it; the pill simply disappears once `render_status` flips off `pending`. Wave file didn't require a failure pill. UX gap if a render fails — user sees the original photo with no explanation. | Track for: post-launch UX polish or M8 (Add-garment UX polish). Add a "Render unavailable, tap to retry" banner driven by `render_status === 'failed'`. |
| 2026-05-05 | M1 (P2) | `render_jobs` RLS scope | **Hook polls `render_jobs` directly via `supabase-js`, presumes RLS scopes by user.** Not verified during M1 — relied on the existing edge-function pipeline + the assumption that table policies match `garments`. | Track for: M2 or M9 audit. Confirm `render_jobs` SELECT policy enforces `user_id = auth.uid()` (or equivalent via FK to `garments`). |
| 2026-05-06 | PR #738 review (migration-blocked) | `wear_logs` schema — needs UNIQUE on `(user_id, garment_id, worn_at::date)` | **Mobile review found `useMarkOutfitWorn` has a TOCTOU race window between the day-level `outfits.worn_at` check and the multi-write sequence.** Same-day writes from two devices (or two near-simultaneous taps that miss the synchronous in-flight Set) can both pass the SELECT and both run the full INSERT path, producing duplicate `wear_logs` rows + double wear_count bumps. Fully atomic dedupe requires either a UNIQUE constraint on `wear_logs(user_id, garment_id, worn_at::date)` (so the second insert 23505s and the catch path treats it as deduped) OR a Postgres `mark_outfit_worn(outfit_id, garment_ids)` RPC that wraps the read + bumps + log insert in a single transaction. Day-level dedupe + in-flight Set + screen-layer `wornToday` gate cover 99% of cases for a single-device user, but cross-device + worst-case timing slips through. | Track for: post-launch hardening PR. Migration-required (CLAUDE.md hard rule prevents shipping it without explicit user authorization). |
| 2026-05-06 | PR #738 review (migration-blocked) | `outfit_feedback` schema — needs UNIQUE on `(user_id, outfit_id)` | **`useRateOutfit` and `useSaveOutfitNote` use a SELECT-newest, UPDATE-or-INSERT, then DELETE-siblings pattern (PR #738 Codex round 8 fix) because the table lacks a UNIQUE constraint and PostgREST `.upsert(..., { onConflict: 'user_id,outfit_id' })` 4xxs.** The defensive sweep collapses duplicates created by races, but a clean upsert path requires the migration. The read tolerates duplicates via `.order(desc).limit(1)` so reads never throw, but the table can briefly hold 2+ rows under contention. | Track for: post-launch hardening PR. Migration-required. |
| 2026-05-06 | PR #738 review (migration-blocked) | `garments.wear_count` increment — needs `increment_wear_count(garment_id)` Postgres RPC | **`useMarkWorn` (single garment) and `useMarkOutfitWorn` (per-garment loop) both do read-modify-write on `wear_count`. Two concurrent same-garment wears lose an increment.** Acknowledged in code comments as accepted v1 trade-off. The atomic fix is a SECURITY DEFINER RPC `UPDATE garments SET wear_count = wear_count + 1, last_worn_at = NOW() WHERE id = $1 AND user_id = auth.uid()` that runs the increment server-side. Same RPC could feed the wear_logs insert + the outfit `worn_at` stamp for full atomicity (folds into the `mark_outfit_worn` tracker entry above). | Track for: post-launch hardening PR. Migration-required. |
| 2026-05-06 | PR #738 review (M33 i18n) | `mobile/src/screens/SettingsScreen.tsx`, `SettingsAccountScreen.tsx`, `SettingsPrivacyScreen.tsx` | **Settings screens carry hardcoded English strings throughout** (section headers, row labels, Alert copy, captions). Other onboarding/auth flows already use `tr()` from `mobile/src/lib/i18n.ts` and have keys appended to `mobile/src/i18n/en.ts`. Sweden launch needs `sv` translations for everything user-facing. | Track for: M33 (mobile i18n locale pass). Append all keys to `en.ts` + `sv.ts`; existing 8 other locales fall back to key strings until translator pass per existing convention. |
| 2026-04-27 | Wave 7 audit (P0 #1) | `supabase/migrations/20260426120000_onboarding_state.sql:243-272` | **`increment_onboarding_garment_count` RPC has no upper bound or step gate.** Any authenticated user can call the RPC unboundedly — no `WHERE onboarding_step != 'completed'`, no upper-bound check on `onboarding_garment_count`. Poisons future analytics, breaks any P48 reward keying on the counter, and serves as a tampering vector for the rate-limit boost. | Track for: Wave 7 hotfix PR A. Add `WHERE onboarding_step <> 'completed' AND onboarding_garment_count < 200` to the UPDATE in a follow-up migration. |
| 2026-04-27 | Wave 7 audit (P0 #2) | `supabase/functions/_shared/scale-guard.ts:30,204-209` | **`analyze_garment` doesn't get the onboarding boost.** Function is `noTierMultiplier: true` which skips both plan resolution AND multiplier. BatchCapture hammers analyze_garment 20-50 times per session — the exact function the boost was designed to protect. Onboarding users hit free-tier 30/min cap during BatchCapture. | Track for: Wave 7 hotfix PR A. Split `noTierMultiplier` into `skipPlanResolution` + `skipMultiplier` so `'onboarding'` plan can override the multiplier veto. |
| 2026-04-27 | Wave 7 audit (P0 #3) | `supabase/functions/_shared/scale-guard.ts:97-153` | **Plan cache holds onboarding boost up to 5 min past completion.** When `advance_onboarding_step('completed')` runs, the per-isolate plan cache (TTL 5min) keeps the user as `'onboarding'`. User completes onboarding, hammers 3x rate limits for ≤5 min, then drops to free. Soft quota bypass. | Track for: Wave 7 hotfix PR A. Invalidate cache on completion transition (or shorten TTL). Cleanest: cache-by-step-version. |
| 2026-04-27 | Wave 7 audit (P0 #4) | `src/lib/advanceOnboardingStep.ts`, `src/pages/Onboarding.tsx` `handleQuizComplete` / `handlePhotoTutorialComplete` / `handleBatchCaptureComplete` / `handleAchievementComplete` | **Profile cache never invalidated after `advanceOnboardingStep` for intermediate transitions.** `useProfile` has staleTime 10 min. After RPC bumps `onboarding_step` server-side, no cache invalidation fires. ProtectedRoute reads stale step on next render. Combined with the legacy-write-failure path, can cause redirect loops. | Track for: Wave 7 hotfix PR B. Wrap `advanceOnboardingStep` in a React Query mutation hook that auto-invalidates `['profile', user.id]` on success. |
| 2026-04-27 | Wave 7 audit (P0 #5) | `src/pages/Onboarding.tsx:62-66` | **Reload always lands user at quiz step regardless of server `onboarding_step`.** The 5 boolean `useState`s (`languageStepDone`/`quizDone`/`photoTutorialDone`/`batchCaptureDone`/`achievementDone`) all reset to `false` on mount. No `useEffect` hydrates from server step. User completes quiz → closes app → reopens → sees quiz again. CRITICAL UX regression for resume flow. | Track for: Wave 7 hotfix PR B. Replace the 5 booleans with a derived `currentStep` from `profile?.onboarding_step` via `useEffect`, OR hydrate the booleans on mount based on server step (e.g., step=`photo_tutorial` → `setQuizDone(true)`; step=`batch_capture` → both true; etc.). |
| 2026-04-27 | Wave 7 audit (P0 #6) | `src/pages/Onboarding.tsx:291` + `src/components/onboarding/LanguageStep.tsx` | **Non-admin users never advance from `'not_started'`.** `effectiveLanguageDone = isAdmin ? languageStepDone : true` skips the language step UI for non-admins, but `LanguageStep.onComplete` doesn't call `advanceOnboardingStep('language')` anyway. So non-admins sit at server `'not_started'` until quiz completion fires a multi-step skip directly to `'photo_tutorial'`. `onboarding_started_at` never gets set until that quiz call → onboarding boost activation delayed → BatchCapture (which hammers analyze_garment) hits free-tier limits. | Track for: Wave 7 hotfix PR B. Either (a) on Onboarding mount when step is `'not_started'`, advance to `'language'` immediately as a server-side bootstrap; or (b) have LanguageStep advance the server even for non-admins. |
| 2026-04-27 | Wave 7 audit (P0 #7) | `src/components/onboarding/BatchCaptureStep.tsx:56-84,195-201` + `src/lib/incrementOnboardingGarmentCount.ts` + `src/hooks/useProfile.ts:66` | **Counter never refreshes mid-batch.** `useProfile` staleTime is 10 min. After each successful `incrementOnboardingGarmentCount` RPC, no `queryClient.invalidateQueries` fires; `serverCount` stays frozen. Optimistic `optimisticBumps` carries the count locally. Mid-batch reload → server catches up but optimisticBumps resets to 0 → if server returns a count behind in-memory delta, displayed total drops abruptly. | Track for: Wave 7 hotfix PR C. Consume the RPC's return value (already returns the new count) into a local state hook OR `queryClient.invalidateQueries` after each RPC. |
| 2026-04-27 | Wave 7 audit (P0 #8) | `src/components/onboarding/BatchCaptureStep.tsx:195-201` | **Counter drift on RPC failure traps user with bad server count.** If `incrementOnboardingGarmentCount` throws (transient network, RLS denial, auth lapse), catch logs warning and continues — `optimisticBumps` still increments. Server count NOT bumped. User sees count=20 locally, taps "Continue", advances server step to `'achievement'` with server count maybe at 5. Downstream P49 (StudioSelection picks 3 from wardrobe) and analytics see wrong number. | Track for: Wave 7 hotfix PR C. In catch block, DON'T increment `optimisticBumps`. Either retry-with-backoff once OR surface toast and let user re-tap. Combined with audit P0 #7. |
| 2026-04-27 | Wave 7 audit (P0 #9) | `src/types/styleProfile.ts:610-665` (`migrateV4ToV3Compat`) + edge function consumers | **V3 mirror silently wiped for 5 same-name fields (gender / climate / layering / paletteVibe / primaryGoal).** `return { ...compat, ...v4 }` makes V4 win on every name collision. The intended V3 mirrors for these 5 fields are silently overwritten with raw V4 enum values. Concrete impact: `_shared/outfit-scoring.ts:1080-1084` substring-matches `primaryGoal` against `'comfort\|relaxed\|creative'` — no V4 value matches, so V4 users with comfort-leaning goals NEVER get the formality-down branch (silent style-quality regression). | Track for: Wave 7 hotfix PR D. Two paths: (a) **canonical** — teach the 5 known V3 readers (`burs_style_engine`, `style_chat`, `shopping_chat`, `suggest_outfit_combinations`, `_shared/outfit-scoring.ts`) to accept V4 vocab via `v4XToV3()` projection helpers at read time; (b) **defensive** — add distinct V3-only mirror keys (`genderV3`, `climateV3`, etc.) so V4 canonical and V3 mirror coexist without collision. Recommend (a). |
| 2026-04-27 | Wave 7 audit (P0 #10) | `src/pages/Onboarding.tsx:99-122` (`completeOnboarding`) | **Redirect-loop race when RPC succeeds but legacy preferences write fails.** Order: `advanceOnboardingStep('completed')` succeeds → `mutateAsync(preferences)` writes legacy flag AND invalidates profile cache. If legacy write fails after RPC succeeded, profile cache is stale → next route check redirects to `/onboarding` → Onboarding.tsx renders flow again → loop. | Track for: Wave 7 hotfix PR B. Invalidate profile query immediately after `advanceOnboardingStep` succeeds (before legacy write). Also folds into the audit P0 #4 mutation hook. |
| 2026-04-27 | Wave 7 audit (P1 cluster) | Various | **24 P1 findings spanning backend / state machine / P45 / P47.** Top items: pre-boost cache locks new users into `'free'` for 5 min after first AI call; backfill races against concurrent profile writes; `auth.role()` returns NULL inside Postgres triggers; RPC accepts skip-ahead transitions (no adjacency check); `useOnboarding` hook is dead code (runtime lives in `Onboarding.tsx`); local STEPS array collapses 3 future server states under `getstarted`; `StyleQuizV4.onSkip` doesn't advance server state; `Step ## of ##` / `Question ## of ##` hardcoded English; double-tap race on Continue handlers; `MAX_GARMENTS=50` race window in BatchCapture; sequential 50-file pipeline blocks UI 2-12 min; SettingsStyle ChipMulti vocab mismatches V4 archetype/occasion IDs; `migrateV3ToV4` doesn't normalize V3 archetype names ('streetwear' → 'street'); SettingsStyle re-edit lossy-collapses V4-only values (`mixed`/`desert`/`earth`/`professional_polish`/`fun_experimenting`); ColorSwatch ID divergence between V4 (18) and SettingsStyle (36). | Track for: Wave 7.9 closure (per Wave Closure Rule). Schedule into a P1 cleanup PR after the 4 P0 hotfix PRs land. Full per-finding detail in audit transcripts (subagent IDs `a1a4c30a81cbc6e1b`, `a9f12add4b27a2fe2`, `ae76df425b0cbc0fc`, `a3167f76dcef3535d`). |
| 2026-04-27 | Wave 7 audit (P2/P3 cluster) | Various | **17 P2 + 9 P3 findings.** Mostly polish: `comfortVsStyle = 100 - formalityCeiling` ignores `formalityFloor`; Q1 height accepts fractional values; `step === null` vs `step === undefined` conflated in ProtectedRoute fallback; `/admin`/`/privacy`/`/terms` listed exempt despite not being mounted; `useOnboarding` schema validation lenient (`z.string()` not `z.enum()`); ObjectURL leak on captured-thumbnails unmount; `BatchCaptureStep` manually constructs INSERT instead of using `buildGarmentInsert`; test coverage gaps on Q4 archetype gate, draft userId-change re-hydrate, MAX-reached state, RPC failure paths. | Defer post-launch. Schedule into a polish PR or fold into individual feature PRs as they touch the relevant files. |
| 2026-04-19 | P0a | `src/i18n/locales/en.ts`, `src/i18n/locales/sv.ts` | Duplicate keys `capsule.generating` and `common.delete` in both locale files — surfaced by Vite build warnings when the new pre-commit hook ran. Locale files are append-only per CLAUDE.md so this cannot be fixed inline. | **RESOLVED in PR #653** (Wave 0+1 cleanup sweep, 2026-04-21): duplicate later-occurrences removed from both locale files, earlier namespace-scoped definitions preserved. User-visible string change on en.ts: `capsule.generating` "Generating..." → "Building packing list..." (the earlier, more specific definition now wins). `common.delete` had identical values — no visible change. |
| 2026-04-19 | P0b | `.github/workflows/ci.yml:26` | Type-check step uses `bun run tsc --noEmit` without `--skipLibCheck`. CLAUDE.md pipeline + pre-commit hook both use `--skipLibCheck`. CI is stricter than local — a lib-type bump could fail CI without tripping local. | **RESOLVED in PR #653** (Wave 0+1 cleanup sweep, 2026-04-21): `--skipLibCheck` added to the CI type-check step so it matches the local pipeline + pre-commit hook. |
| 2026-04-19 | P0b | `.github/workflows/ci.yml:34-41` | Bundle-size check echoes `WARNING` on overflow but exits 0 → never blocks a merge. Silently passes regressions. | **RESOLVED in PR #653** (Wave 0+1 cleanup sweep, 2026-04-21): bundle-size check now exits 1 with `::error::` annotation on overflow, so regressions block the merge instead of passing with a silent warning. |
| 2026-04-19 | P0b | `.github/workflows/ci.yml` | CI has no `deno check supabase/functions/<fn>/index.ts` step. Fix Protocol requires it for edge-function changes but CI doesn't enforce. | **RESOLVED in PR #653** (Wave 0+1 cleanup sweep, 2026-04-21): added `deno-check` job with three-dot diff detection of changed `supabase/functions/<name>/index.ts`. Only runs when edge-function entry-points changed (not on `_shared/` — that would force check-all and spike CI time). Uses `denoland/setup-deno@v1` + `actions/cache` on `~/.cache/deno` so subsequent runs skip ESM URL resolution. |
| 2026-04-19 | P0d-ii | `supabase/migrations/` | **Schema drift** — earliest migration (`20260124173453_...`) `ALTER TABLE`s `public.garments`, but no `CREATE TABLE garments` (or other base tables) migration exists in the repo. Base schema was authored in Studio UI without a backfilled migration file. `supabase start` + `supabase db reset` against an empty local DB fails with `ERROR: relation "public.garments" does not exist`. Surfaced when P0d-ii's smoke-local CI job ran for the first time. | Dedicated fix: new prompt **P0d-iv — Schema baseline migration (drift repair)** inserted between P0d-iii and P0e. P0d-ii's `smoke-local` job is gated `if: false` until P0d-iv re-enables it. **Resolved in P0d-iv (PR #639, 2026-04-20).** |
| 2026-04-20 | P0d-iv | `supabase/migrations/2026*.sql` (deleted) | **Prod schema internal inconsistency** — multiple historical migrations had DDL statements that silently failed on prod over months of Studio UI edits. Examples: triggers referencing `public.update_updated_at_column()` (function exists only in `storage` schema), functions with `_role app_role` parameters where the column on prod is `text`, policies referencing `requester_id`/`addressee_id` where prod has `user_id`/`friend_id`. The 67 migration files describe a schema that never fully existed. | Resolved by Strategy V in P0d-iv: dump prod schema as baseline, delete the 67 files, repair remote tracking. Migration history preserved in git log. Future migrations build on the baseline. |
| 2026-04-20 | P0d-iv | `.github/workflows/ci.yml` smoke-prod step + `src/test/smoke/harness.ts` `shouldRunSmoke` gate | **Silent-skip CI hiding broken tests.** `shouldRunSmoke` returns false when `SUPABASE_SERVICE_ROLE_KEY_TEST` is missing; Vitest then reports every smoke `describe.skipIf` as skipped and the CI step exits 0 regardless. On main, the secret has never been provisioned, so every post-merge smoke run has been reporting green while actually running zero assertions. Surfaced when P0d-iv's local CI (with local credentials auto-exported) became the first environment that actually ran `signup.test.ts` end-to-end, revealing two assertions (`display_name` truthy, `preferences.onboarding.completed === false`) that never matched `handle_new_user()` behaviour. | **FULLY RESOLVED 2026-04-25**: PR #653 (2026-04-21) shipped fail-loud guard. User provisioned `SUPABASE_SERVICE_ROLE_KEY_TEST` as a GitHub Actions repo secret 2026-04-25. Verification: main CI run #24930703508 (push of P30 PR #675 squash) initially failed at Bun-installer 502, re-ran cleanly, smoke-prod step exited green — secret resolves correctly + smoke tests run for real on every push-to-main now. Both remediations complete. |
| 2026-04-20 | P0d-iv | `supabase/migrations/00000000000000_initial_schema.sql` — `public.init_render_credits()` | **Prod trigger function silently relies on undefined search_path behaviour.** `handle_new_user()` schema-qualifies every reference (`public.profiles`, `public.subscriptions`), so it works when fired as `supabase_auth_admin` (role-level `search_path=auth`). `init_render_credits()` uses the bare name `INSERT INTO render_credits ...`; under `search_path=auth` it should fail with `relation "render_credits" does not exist`, and it does locally. Prod evidently resolves it (5/5 recent users have render_credits rows) but the mechanism doesn't reproduce in a fresh Supabase CLI stack on identical PG/role config. | Minimum-deviation fix in P0d-iv: added `ALTER FUNCTION public.init_render_credits() SET search_path = public;` at the end of the baseline. This is a proconfig-only override — the dumped function body is untouched — and hardens local against the same footgun CLAUDE.md's "Secrets inside migrations" block warns about for SECURITY DEFINER functions. |
| 2026-04-20 | P0d-iv | `storage.buckets` on prod + `src/components/settings/ProfileCard.tsx` + `src/pages/PublicProfile.tsx` + `src/hooks/useAvatarUrl.ts` | **`avatars` storage bucket + its 4 policies were dropped from prod at an unknown date**, but client code still calls `supabase.storage.from('avatars')` for upload / delete / signed-URL reads. Avatar upload is currently broken for every new user; the 2 of 7 prod profiles with `avatar_path` populated point to objects in a bucket that no longer exists (their signed-URL reads 404). The old `20260308075837_192365b0-b62f-42b9-88b5-8b0091f3c2cf.sql` migration that created the bucket + policies was deleted in P0d-iv Strategy V — intentionally, because "baseline equals prod" and prod no longer has the bucket. Surfaced by Codex review on PR #639. | **Product decision: remove, not restore.** The baseline stays truthful (no `avatars` bucket, matching prod). **RESOLVED in PR #654** (Wave 0+1 cleanup sweep B, 2026-04-21): deleted `src/hooks/useAvatarUrl.ts` + its test file, stripped avatar upload/delete path from `src/components/settings/ProfileCard.tsx` (now renders initials-only Avatar), removed signed-URL fetch from `src/pages/PublicProfile.tsx`, dropped `avatar_path` from `src/lib/schemas.ts` + from 3 test fixture mocks, and shipped migration `20260421124000_drop_profiles_avatar_path.sql` that `ALTER TABLE public.profiles DROP COLUMN IF EXISTS avatar_path`. **Post-merge action**: run `npx supabase db push --linked --yes` to apply the migration, then regenerate `src/integrations/supabase/types.ts` via `supabase gen types typescript --linked` and commit the regen in a follow-up PR (types.ts is auto-generated per CLAUDE.md and intentionally stale in this PR — it still declares `avatar_path` but no code references it). |
| 2026-04-20 | P0d-iii | `supabase/functions/_shared/burs-ai.ts:17` | **`GEMINI_URL` is a hardcoded `const`**, not read from env. That makes edge-function Gemini calls impossible to reroute through the mock server booted by `src/test/smoke/mocks/mock-server.ts` — the whole reason P0d-ii's mock infrastructure exists. Surfaced when writing P0d-iii smoke tests: to hit `analyze_garment` / `render_garment_image` / etc. under local Supabase with a mocked Gemini, the edge-runtime needs `GEMINI_URL` overridable via `Deno.env.get("GEMINI_URL_OVERRIDE") ?? "https://generativelanguage..."`. **Resolved in P0d-iii (PR #640, 2026-04-20)** — approach expanded per user (CTO) direction. Three shared URLs made overridable (`GEMINI_URL_OVERRIDE` in `burs-ai.ts`, `GEMINI_IMAGE_URL_OVERRIDE` in `gemini-image-client.ts`, `GEMINI_TEXT_URL_OVERRIDE` in `render-eligibility.ts`), all backward-compatible (unset → original hardcoded value). 7 tests rewritten to invoke edge functions via `supabase.functions.invoke()`; CI workflow boots `start-mock-server.ts` + `supabase functions serve --env-file`. AI function redeploy **not completed this session** — Supabase deploy bundler hit `esm.sh 522 Origin unreachable` repeatedly on `import 'https://esm.sh/@supabase/supabase-js@2'`; external infra blocker. Backward-compat of the env-var pattern means prod is safe without deploy: functions keep using the hardcoded URLs until next successful deploy picks up the env-var fallback. Follow-up RESOLVED 2026-04-20: all 24 consumers successfully redeployed. |
| 2026-04-20 | P0a | `.husky/pre-commit` | File lacks `#!/usr/bin/env sh` shebang. On Windows, git.exe cannot exec the hook directly → pre-commit runs are skipped unless the agent wraps it via `core.hooksPath`. Surfaced by P0d-iv and P0d-iii agent sessions — both had to use a wrapper to run the full tsc + eslint + build pipeline. | **RESOLVED in PR #641 (P0a-ii, 2026-04-20)** — shebang prepended to `.husky/pre-commit`; Windows git.exe execs the hook directly. Tracker action backfilled in Wave 4.9 hygiene PR #697 (2026-04-28) — the underlying fix shipped 8 days earlier; the row's Action column was never flipped. |
| 2026-04-20 | P0d-iii | `supabase/functions/travel_capsule/index.ts:838` (resolveId) + `:762` (deterministic fallback) | `resolveId()` expects string ids, but the deterministic fallback at line 762 writes garment objects into capsule_items. Downstream `id.trim()` crashes on the object path. Surfaced when P0d-iii's `travel-capsule.test.ts` mock routing revealed the fallback path — test was tightened so happy path avoids the crash, but the production code path is latent. | **RESOLVED in PR #656** (Wave 0+1 cleanup sweep D, 2026-04-21): Option A chosen — fixed the fallback to emit IDs matching AI shape. `buildDeterministicFallback` now returns `capsule_items: Array.from(capsuleMap.keys())` (string[]) instead of `.values()` (GarmentRow[]). Downstream recovery at line 867 updated: `fallback.capsule_items.filter(id => validIds.has(id))` — no object→id mapping. outfits[i].items was already string[] via `candidate.items`. Post-merge: deploy `travel_capsule`. |
| 2026-04-20 | P1 | `supabase/functions/process_job_queue/index.ts`, `supabase/functions/daily_reminders/index.ts` | **Mistake pattern — cron-only vs user-facing auth.** Initial P1 agent brief included a `getUser()` fallback pattern copied from `detect_duplicate_garment` for cron-only functions. Codex caught that the fallback allowed any authenticated end-user to invoke service-role-escalated code (DoS against queue processing + notification storm). Correct pattern for cron-only endpoints is hard-reject of non-service-role callers via `timingSafeEqual(token, SERVICE_ROLE_KEY)`. User-facing functions continue to use the `getUser()` fallback. | Fixed in follow-up commit on PR #643. For future prompts: user-facing functions use `getUser()` fallback; cron-only functions use hard-reject only. |
| 2026-04-20 | P0e | process (not a file) | **Prompt ordering mistake.** P1 and P2 were executed before P0e because the P1 agent brief instructed "move CURRENT PROMPT to P2" (should have been "P0e"). No correctness impact — P0e is independent of P1/P2 scope — but Wave 0 safety net was briefly incomplete during Wave 1 work. | Future prompt briefs must verify CURRENT PROMPT against the prompt list and move to the earliest `[TODO]`, not just the next-numbered prompt. |
| 2026-04-20 | P2 | `supabase/functions/calendar/index.ts` (handleSyncAll) | **Possible dead code.** `handleSyncAll` has no discoverable caller — zero in-repo invokers, zero matching rows in `cron.job` for 'calendar' or 'sync_all' patterns. May be dead code OR called by a Supabase Dashboard scheduled function outside pg_cron. | **RESOLVED in PR #676** (Wave 4.9 follow-up cleanup, 2026-04-25): re-audited on main `f2cd0301` — `cron.job` table query for `command ILIKE '%calendar%' OR command ILIKE '%sync_all%'` returned 0 rows; `analytics_events` query for `metadata->>'fn' = 'calendar'` over 7-day window returned 0 rows; in-repo grep for `handleSyncAll` / `sync_all` returned only the function's own self-references (definition + dispatcher case + doc comment). Confirmed dead. Deleted: (a) the `handleSyncAll` function (~71 LOC at lines 398-468), (b) the `case 'sync_all':` dispatcher arm, (c) the `timingSafeEqual` import (was only used by handleSyncAll), (d) the `"sync_all"` line in the file's header doc comment. Net: -69 LOC. The dispatcher now returns the default 400 "Unknown action" for any future caller of `action: 'sync_all'`. Deploy: `calendar`. |
| 2026-04-21 | P4 | `supabase/functions/prefetch_suggestions/index.ts` (cron-batch path, lines 123+) | **Cron-batch path has no auth gate.** P4 hardened the single-user-trigger path but the else branch (cron batch mode) accepts any caller with no Authorization check. A drive-by anon POST with an empty body (or any body missing `trigger: "first_5_garments"`) falls through to the batch path and triggers up to 100 parallel AI calls — DoS / cost-amplification vector against Gemini. This is `verify_jwt = false` in config.toml, so there's no platform-level gate either. | **RESOLVED in PR #655** (Wave 0+1 cleanup sweep C, 2026-04-21): applied the P1 cron-only hard-reject pattern to the batch-mode else branch. Imported `timingSafeEqual` from `_shared/timing-safe.ts`. After the single-user-trigger block returns, the batch path now pre-checks `Authorization` header via `timingSafeEqual(token, SUPABASE_SERVICE_ROLE_KEY)` and 401s any non-service-role caller before entering the 100-user AI batch loop. Post-merge: deploy `prefetch_suggestions`. |
| 2026-04-21 | P4 | main repo `C:\Users\borna\OneDrive\Desktop\BZ\Burs\bursai-working` | **Stale mid-merge cruft in main repo.** Main repo is stuck on branch `prompt-p0d-iii-deploy-retry` at `6de4c09a` with an in-progress merge bringing `origin/main` (d02d6ac5) into the branch — CLAUDE.md conflict unresolved, 8 files auto-merged (all already on origin). Leftover from a prior agent session. P4 worked around it by using the existing `heuristic-swanson-e41ab7` worktree (clean on main at d02d6ac5) and `npm ci`-ing dependencies there. | **RESOLVED in PR #676** (Wave 4.9 follow-up cleanup, 2026-04-25): main repo now on `main` clean (synced to `f2cd0301`); legacy `tmp-db-push` branch deleted; stale partial `types.ts` regen with stderr-leak garbage discarded (verified the official regen against `f2cd0301` produces zero content diff vs the manually-de-noised W4.9-A version — only CRLF/LF differences). Worktree registry pruned 12→4 via `git worktree remove -f -f` (kept: main + heuristic-swanson-e41ab7 + amazing-jepsen-6ca359 vertex-migration-plan + quizzical-edison-f17334 current shell). Locked agent-* worktrees + claude/* + p0d-iv-drift-repair + p0d-smoke-poc registry entries removed; on-disk directories remain due to Windows file locks (held by stale processes that can't be killed without reboot) but git no longer tracks them. |
| 2026-04-21 | P5 | `src/hooks/__tests__/useDeepLink.test.tsx` lines 48, 66 | **Test fixtures still use legacy `app.bursai.com` domain.** Deep-link test fixtures construct URLs like `https://app.bursai.com/u/borna` and `https://app.bursai.com/outfit/abc-123`. Real prod domain is `burs.me` (via `app.burs.me`). Fixtures will match URL shape regardless of domain — no functional test regression today — but if the hook ever adds domain-whitelisting, these fixtures will silently diverge from prod behaviour. Surfaced by the P5 grep sweep for `bursai.com` across the repo. | **RESOLVED in PR #653** (Wave 0+1 cleanup sweep, 2026-04-21): both fixtures updated from `app.bursai.com` to `app.burs.me`. |
| 2026-04-21 | (cleanup B) | `src/i18n/locales/*.ts` — `settings.avatar_invalid`, `settings.avatar_too_large`, `settings.avatar_updated`, `settings.avatar_error`, `settings.change_photo` | **Orphaned avatar i18n keys in 14 locale files.** The 5 keys were only referenced by the avatar upload path in `ProfileCard.tsx`, which was removed in PR B (Wave 0+1 cleanup sweep). Keys remain in locale files per CLAUDE.md's append-only rule. No functional impact — `t()` just returns the key itself if called, but no code paths call these anymore. Surfaced by code-reviewer during PR B review. | **RESOLVED in PR #670 (W4.9-B, 2026-04-24)** — all 5 orphan `settings.avatar_*` keys deleted across all 14 locale files (70 instances total). Post-edit grep for `settings.avatar_` in `src/` returns zero. Tracker action backfilled in Wave 4.9 hygiene PR #697 (2026-04-28). |
| 2026-04-21 | P8 | `LAUNCH_PLAN.md` P8 spec (lines 714-717) | **Spec column-name error — and the approach itself is unfixable without schema change.** Spec's code snippet used `.like("cache_namespace", ...)` but that column doesn't exist on `ai_response_cache`. Initial P8 commit tried `.like("cache_key", ...)` as a fix — Codex P1 on PR #652 correctly flagged that cache_key is a SHA-256 hash (via `createBursAICacheKey` in `_shared/burs-ai.ts` line 235 — `hashKey(stableSerialize(...))`), so the UUID substring is hashed away and a LIKE matches zero rows. Both spec and first-fix approaches silently do nothing. Ai_response_cache cleanup is NOT possible without adding a `user_id` column to the table (requires migration + backfill + `storeCache` update in burs-ai.ts + redeploy of all 22 AI functions). GDPR-wise, the table's short TTLs (30 min – 12 h) make natural expiration-based decay acceptable ("without undue delay" is satisfied within a day). | Fixed in P8 PR (Codex commit): removed the broken line entirely, replaced with an honest comment explaining the TTL-decay mitigation and pointing to the schema-change follow-up. LAUNCH_PLAN.md P8 spec should be updated in a future doc-sweep. Dedicated schema-change follow-up prompt should add `user_id` column to `ai_response_cache` — likely bundled with P13 cache-namespace work since both touch the cache layer. |
| 2026-04-21 | (doc fix) | `LAUNCH_PLAN.md` P1 spec (lines 360-378) | **Stale spec advising insecure auth pattern.** LAUNCH_PLAN.md's P1 cron-only section still showed the `if (!isServiceRole) { require user JWT }` fallback pattern that Codex rejected on PR #643. Since LAUNCH_PLAN.md is the source-of-truth agents read for pattern reference (CLAUDE.md is the tracker), this would trap future cron-auth work — notably P7 on `process_job_queue` handlers. CLAUDE.md's earlier Findings Log row (2026-04-20, P1) documented the hard-reject correction in tracker-space only; the spec file was never updated. Surfaced when the user asked to sweep LAUNCH_PLAN.md for tracker-style drift after P5 shipped. | Fixed in PR #649 (doc-only): P1 cron-only section rewritten to show the hard-reject pattern (`timingSafeEqual` + return 401, no JWT fallback). Added a pattern-selection rule of thumb covering user-facing vs cron-only vs dual-mode. No code/status changes; `CURRENT PROMPT` stays P6. |
| 2026-04-21 | P9 | `supabase/functions/cleanup_ai_cache/index.ts` (pre-patch) | **P1 auth-gap miss.** The original P1 Wave 1 prompt hardened `summarize_day`, `process_job_queue`, and `daily_reminders` — but `cleanup_ai_cache` has the same cron-only, mass-DB-delete profile and was not included in P1's scope. Anon callers could invoke it repeatedly (`verify_jwt = false` in config.toml, zero auth in the function body) to both burn DB query budget and evict hot cache rows prematurely. Would also make P9's overload gate toothless (overload guard trips on error, not on successful-but-abusive repeated anon calls). Surfaced when P9 started adding overload to the function and noticed the missing auth. | **RESOLVED in this PR (Wave 2-A, P9 scope expansion)**: added the same `timingSafeEqual(token, SERVICE_ROLE_KEY)` hard-reject pattern that P1 shipped to the sibling cron-only functions. Imported `timingSafeEqual` from `_shared/timing-safe.ts`. Deploy: `cleanup_ai_cache`. |
| 2026-04-21 | P9 | `_shared/scale-guard.ts` | **Shared-module change triggers multi-consumer deploy.** P9 added 9 new `RATE_LIMIT_TIERS` entries to `_shared/scale-guard.ts` (10 originally planned; `seed_wardrobe` tier removed mid-PR alongside the function itself — see P11 row below). Per CLAUDE.md's Shared Module Deploy Map, any change to `_shared/scale-guard.ts` requires redeploying ALL AI functions because the module is bundled individually per function. Adding (never modifying) entries is technically safe — existing functions won't look up the new keys — but CI smoke-local bundles the same shared module into every function at test time, so bundler drift is possible. | Mitigation: the 12 touched functions in this PR are being redeployed anyway (they consume new tier entries or the new overload import). Existing untouched AI functions (`burs_style_engine`, `style_chat`, etc.) need NOT be redeployed in this PR — the additions are purely additive. This is the same pattern as prior shared-module changes (the P5 VAPID fix, the P7 `submitJob` signature tighten). Not a new finding per se — just a reminder to keep the deploy scope to the 12 functions that actually changed. |
| 2026-04-21 | P15 | `vitest.config.ts` (repo-wide full-suite flakes) | **Non-deterministic test suite on Windows.** Full-suite `npx vitest run` was flaky pre-P15 — each invocation failed a DIFFERENT subset of 5-20 tests at the default 5000ms per-test timeout, while every affected file passed cleanly when run in isolation (9/9 assertions in ~7s wall-clock). Root cause: vitest defaults to the `threads` pool with one worker per CPU core; on Windows, parallel workers hammer the filesystem during module resolution + jsdom bootstrap, and the worst-case combined transform + environment setup exceeds the 5s per-test budget, clipping legit component-render tests that actually take 1-3s. Confirmed pre-existing (not caused by P15) by stashing all P15 edits and reproducing on pristine main. User explicitly asked for a fix even though pre-existing. | **RESOLVED in this PR (Wave 3-A, P15 scope expansion)**: added three settings to `vitest.config.ts` — `testTimeout: 15000` (bumped from default 5000ms), `pool: "forks"` (process isolation instead of shared-memory threads), `maxWorkers: 2` (cap parallelism regardless of CPU count). Vitest 4's new top-level option names (was `poolOptions.forks.maxForks` in v2/v3). Full suite now runs 1104/1104 reliably across multiple invocations. Tradeoff: wall-clock end-to-end ~1.5-2x slower on cold cache (100-340s observed vs 368s pre-fix, but variance is high — not a regression, just the variance was hidden before because half the runs were bailing on timeouts). Longer-term cleanup (future test-infra PR): audit which tests legitimately take >3s and split into a dedicated slow-suite, OR move back to `threads` once the filesystem hotspots are identified. |
| 2026-04-21 | P15 | `supabase/migrations/00000000000000_initial_schema.sql:909` (schema baseline) | **Latent default-value footgun.** The baseline defines `garments.image_processing_status TEXT NOT NULL DEFAULT 'pending'`. P15 deleted `process_garment_image` (the only thing that transitioned `pending` → `ready`), but the column + default stay per spec. All live insert paths go through `buildGarmentIntelligenceFields` which passes `skipImageProcessing: true` (→ `'ready'`), so new rows are safe. Any FUTURE insert caller that bypasses that helper would create a row that polling gates no longer transition (though P15 also removed those gates — so the practical effect is benign per-user display, but outfit-scoring gate in `_shared/outfit-scoring-body.ts:467` would reject the garment from outfit generation until its default is changed). Surfaced by code-reviewer on P15 PR. | **RESOLVED in PR #669 (W4.9-A, 2026-04-24)** — 7 `image_processing_*` columns dropped from `public.garments` via `<ts>_drop_image_processing_columns.sql`; CHECK constraint + `idx_garments_processing_status` index dropped automatically via column-drop cascade. Orphan `job_queue` rows with `job_type='image_processing'` deleted in same migration. Outfit-scoring gate removed in lockstep. Tracker action backfilled in Wave 4.9 hygiene PR #697 (2026-04-28). |
| 2026-04-21 | Wave 3-B | `supabase/functions/render_garment_image/index.ts` + `_shared/gemini-image-client.ts` + `_shared/render-eligibility.ts` + `_shared/mannequin-presentation.ts` + `RenderPendingOverlay.tsx` (audit surface) | **Render-pipeline deep audit** — the user asked for "find every improvement, even the smallest, so rendering feels like magic and gets it right every time." End-to-end audit turned up 12 improvements beyond the P16-P19 spec scope: **F2** category-first prompt ordering, **F5** enriched mannequin presentation (per-sex body-shape hints), **F9** logo-preservation signal in validator, **F13** input-image preflight (magic-byte + dimension check before burning Gemini), **F15** exp-backoff retry on Gemini 5xx/429 in the client transport, **F16** output MIN_SIZE 10KB→30KB, **F17** output dimension check (512×512 min), **F18** output magic-byte validation, **F20** time-estimate hint on the pending overlay ("Takes about 20 seconds"), **F21** i18n for the overlay strings (added 7 `render.*` keys to en + sv), **F22** new `RenderFailedBanner` component surfacing `render_status='failed'` with a one-tap retry (was silently falling back to the original photo before). **RENDER_PROMPT_VERSION bumped v1→v2** — folded into the credit-ledger baseKey so in-flight v1 reservations keep their replay path intact while new requests mint fresh v2 reservations. | **RESOLVED in this PR (Wave 3-B)**. Shipped together because they all touch the same two edge functions + shared modules — splitting would have produced 12 conflicting PRs racing for the same files. Code-reviewer approved. Post-merge: deploy `render_garment_image` only (no migration; `_shared/gemini-image-client.ts` + `_shared/render-eligibility.ts` + `_shared/mannequin-presentation.ts` are consumed only by `render_garment_image` at runtime, so no other function needs redeploy — confirmed by grep). |
| 2026-04-21 | Wave 3-B (deferred) | Render-pipeline polish that didn't make P16-P19 scope | **Deferred follow-up findings from Wave 3-B audit** (do NOT block launch; tracked for future polish): **F3** prose-vs-bullets prompt experiment (needs A/B on a sample of users, not a one-shot decision), **F4** per-category negative-example enrichment, **F8** color-drift validation gate (currently unchecked — Gemini sometimes shifts navy→black, beige→tan), **F14** aspect-ratio output validation (config requests 4:5 but no output enforcement), **F19** server-side concurrent-reservation debounce (user hitting Regenerate twice with different nonces can still double-charge), **F23** per-attempt progress UI (currently user waits 60s with no signal during retries), **F24/F25/F26** observability (per-attempt telemetry + finer failure taxonomy + cost-per-render tracking). Code-reviewer Wave 3-B also flagged: **#5** RenderFailedBanner optimistic-rollback race (enqueue succeeds server-side but client never reads response → client reverts to `'failed'` but job is actually pending) — initially logged as low-risk "worker claim writes authoritative state," but **Codex P1 round 2 on PR #661 correctly elevated this**: before the worker reconciles, the banner re-renders, user clicks Try Again, fresh clientNonce creates a SECOND reservation, user is double-charged. **RESOLVED in PR #661 fix commit 3**: the catch block now classifies via `isRenderEnqueueRetryable(error.status)` — retryable/transport errors keep the optimistic `'pending'` flip in place (shimmer takes over, worker wins), only definitive 4xx denials or non-enqueue errors revert to `'failed'`. **#8** validator fail-open-on-first-attempt should emit a Sentry alert hook on `validator_unavailable` so aggregate outages are visible. | Track in this Findings Log; fold into a follow-up "render-polish v2" prompt after first 30 days of real-user telemetry informs priorities. Not launch-blocking. |
| 2026-04-21 | P15 | `src/hooks/useGarments.ts` + `useGarmentsByIds.ts` + `useAISuggestions.ts` (same defect pattern) | **Spec scope missing 3 of 4 defect sites.** P15 LAUNCH_PLAN.md spec named only `src/pages/GarmentDetail.tsx` for the `image_processing_status === 'pending' | 'processing'` polling-gate removal, but the identical defect existed in three sibling hooks: `useGarments.ts` (lines 104-105), `useGarmentsByIds.ts` (lines 24-25), `useAISuggestions.ts` (lines 167-168). Leaving them would mean any pre-P15 garment stuck at `'pending'` (from previously-broken stub runs) would cause these hooks to refetch indefinitely post-merge. Spec's acceptance criterion "polling terminates correctly based on render_status alone" applies equally to all four sites, so fixing all four is consistent with spec intent. | **RESOLVED in this PR (Wave 3-A, P15 scope expansion)**: all 4 polling sites updated with identical surgical removal (drop the 2 `image_processing_status` conditions; keep `render_status` conditions). Documented in PR body under "Scope expansion". Future prompts that name one file of a shared pattern should explicitly check for identical siblings before scoping. |
| 2026-04-22 | P23 | `supabase/functions/prefetch_suggestions/index.ts:54` | **Prompt-text drift surfaced by the `compactGarment` fix.** P23 dropped the `.slice(0, 8)` in `compactGarment()` so every caller now emits full UUIDs. `prefetch_suggestions` is the only direct caller — its prompt text told the model "Pick garments by their short ID (first 8 chars)", which became inconsistent once the wardrobe lines carried 36-char UUIDs. Model would have returned 8-char prefixes; nothing downstream resolves those to full IDs so the entire cached response (`daily_suggestions_${userId}`, TTL 43200s) would be unusable for the next 12h per user. Caught by code-reviewer as a blocker. Same pattern to watch for whenever a shared helper's output shape changes: grep the shared helper's callers and audit their surrounding prose, not just the function signature. | **RESOLVED in this PR (Wave 4-A scope expansion)**: prompt updated to "Pick garments by their ID." `prefetch_suggestions` now in the functional-change list alongside the 2 spec-named consumers. No new Findings — fully closed. |
| 2026-04-22 | P24 | `supabase/functions/_shared/burs-ai.ts` `waitForEnrichment` | **Minor hardening follow-up flagged by code-reviewer, then tightened twice by Codex.** (r3) `waitForEnrichment` accepted `opts.pollIntervalMs` and `opts.timeoutMs` without bounds-checking — `0` or negative values would turn the loop into a busy-poll against Supabase. (r4) `Math.max(50, NaN)` returns NaN and `Math.max(0, Infinity)` returns Infinity; `sleep(NaN)` resolves immediately and `remaining <= 0` never holds for NaN, so the first clamp pass was still vulnerable to non-finite inputs. Helpers are library-only in Wave 4-A but must be defensive before Wave 4-B consumers opt-in. | **RESOLVED in this PR (Wave 4-A, Codex P2 r3+r4)**: coerce via `Number.isFinite(raw) ? raw : default` FIRST, THEN apply `Math.max(0, ...)` for timeout and `Math.max(50, ...)` for poll. 50ms floor respects Supabase connection-reuse budget. Regression tests: `pollIntervalMs: 0` (≤6 calls in 200ms), `timeoutMs: -100` (fast exit), `pollIntervalMs: NaN` (≤3 calls in 200ms), `pollIntervalMs: Infinity` (≤3 calls in 200ms), `timeoutMs: Infinity` (<500ms on completed row — proves deadline math stays bounded). |
| 2026-04-22 | P24 | `garments.enrichment_status` writers (frontend vs job queue) | **Two spellings coexist for the "ready" terminal state.** Frontend enrichment paths (`src/lib/garmentIntelligence.ts:437` — the MAIN enrichment path, `src/pages/GarmentDetail.tsx:215`, `src/components/onboarding/QuickUploadStep.tsx:108`) write `'complete'`. Backend job-queue path (`supabase/functions/process_job_queue/index.ts:269`) writes `'completed'`. Pre-existing mirror bug also found: `_shared/outfit-scoring-body.ts:466` `const enrichmentReady = garment.enrichment_status === 'complete'` — job-queue-enriched garments were silently getting the 0.55 unenriched penalty in outfit scoring, regardless of actual ai_raw completeness. Surfaced by Codex P2 review on PR #663, warning that the `filterEnrichedGarments` helper (P24) written against `'completed'` alone would silently drop frontend-enriched garments when consumers adopt it. Also `'in_progress'` (frontend) vs `'processing'` (backend) spelling divergence present for the non-terminal state, but no reader discriminates those today so no live bug. | **RESOLVED in this PR (Wave 4-A scope expansion)**: (1) added `isEnrichmentReady(status)` + `isEnrichmentFailed(status)` helpers in `_shared/burs-ai.ts` accepting both `'complete'` and `'completed'` as ready; (2) `filterEnrichedGarments` + `waitForEnrichment` both use the helpers; (3) fixed `_shared/outfit-scoring-body.ts:466` inline to accept both spellings — burs_style_engine redeploy already in scope so no extra deploy cost. Tests extended with explicit dual-spelling cases. Future schema-cleanup PR should standardize writers on one spelling (recommendation: `'completed'` + `'processing'` — the longer forms — since they match the job_queue pattern used in `stripe_events`, `render_jobs`, and other internal state machines) + backfill existing `'complete'` rows to `'completed'`. Not this PR — requires a migration, data backfill, and a writer sweep. |
| 2026-04-23 | P20 | `supabase/functions/_shared/outfit-validation.ts:67,120` | **Pre-existing TS2677 caught by CI deno-check.** Two `presentSlots.filter((slot): slot is string => slot !== 'unknown')` calls had broken type predicates. `slot`'s parameter type is `CanonicalOutfitSlot` (a narrow union including `'unknown'`); claiming `slot is string` widens the type, which TS rejects — "type predicate's type must be assignable to parameter's type." Zero runtime impact (the filter works; `string` was just a bogus claimed output type), but strict deno check refuses to compile. Been latent since before PR #653 added the deno-check CI step — no earlier PR touched a function whose dependency graph includes `outfit-validation.ts`, so no deno-check ever ran against it. My Wave 4-B edit to `mood_outfit/index.ts` was the first PR to trigger `deno check supabase/functions/mood_outfit/index.ts`, which recursively type-checks `outfit-validation.ts`. Scope-expanded into the Wave 4-B PR because it was a direct CI blocker. | **RESOLVED in this PR (Wave 4-B scope expansion)**: dropped the broken type predicate on both lines — `.filter((slot) => slot !== 'unknown')`. Return type is `CanonicalOutfitSlot[]`, implicitly widens to the outer `presentSlots: string[]` return shape. Zero runtime change. The shared module's deploy radius expands to `_shared/outfit-validation.ts` consumers: `mood_outfit` (already in scope) + any other caller that imports it. Grep verified: `generate_outfit`, `style_chat`, `burs_style_engine`, and the shared modules that chain off it. Since the fix is type-only (no behaviour change), these consumers don't need new deploys unless they're already being deployed for other reasons. |
| 2026-04-23 | P20 | `supabase/functions/mood_outfit/index.ts:196-215` | **Codex P1 on PR #664 — slot-coverage regression from RETRIEVAL_LIMIT=40 truncation.** My initial Wave 4-B pre-filter ranked and truncated to 40 garments without preserving a complete-outfit path. On skewed wardrobes (e.g. 35 tops + 3 bottoms + 2 shoes), the top 40 by mood-score could drop ALL bottoms or ALL shoes — even though the outer `canBuildCompleteOutfitPath(garments)` check (line 204) confirmed the full wardrobe DOES have a valid path. Model would then receive a prompt from which no valid `top+bottom+shoes` or `dress+shoes` outfit could be built, `validateCompleteOutfit` would fail, and the user would see a "Not enough garments to build a complete mood outfit" error on a wardrobe that previously worked. A regression from the pre-P20 "send all garments" behaviour. | **RESOLVED in this PR (Wave 4-B, Codex P1 fix)**: after the initial `scoredPool.slice(0, RETRIEVAL_LIMIT)` cut, check `canBuildCompleteOutfitPath(promptGarments)`. If the subset can't build a path, greedily extend from the next-best-ranked garments (`scoredPool` outside the top 40) and then the unranked `garments` wardrobe fallback, stopping as soon as a complete path is restored. Worst case adds a handful of items — `estimateMaxTokens` uses `promptGarments.length` dynamically so the token budget tracks. New telemetry field `slot_coverage_forced` logs when this fired so we can measure real-world frequency. |
| 2026-04-23 | P21 | `supabase/functions/_shared/retrieval.ts:397-435` (scanEventHints) | **Codex P2 on PR #664 — substring collisions in event hint extraction.** `scanEventHints()` used `haystack.toLowerCase().includes(k)` against a 14-key FORMALITY_HINTS and 14-key SEASON_HINTS map. Two defect classes: (a) substring collisions — "informal" contains "formal" so an event described as "informal brunch" would set `formality: 'high'` instead of `'low'`; (b) common English words overlap season keys — "may" and "fall" are both modal verbs, "spring into action" is a common idiom. Because these hints are merged into the structured intent BEFORE the AI call AND before `intentToCacheKey()`, users could receive consistently mis-targeted gap/shopping recommendations AND those wrong recommendations would be cached under a mis-derived intent hash. | **RESOLVED in this PR (Wave 4-B, Codex P2 fix)**: (1) switched both hint maps to word-boundary regex matching — `new RegExp(\`\\b${escaped}\\b\`, 'i')` with module-level regex pre-compilation; (2) reordered FORMALITY_HINTS so longer multi-word phrases ("black tie", "smart casual", "business casual") precede their single-word component words, so whichever matches first wins the longest-phrase-wins race naturally; (3) intentionally DROPPED `"may"` and `"fall"` from the season map — word boundaries don't fix verb-ambiguity ("I may go", "don't fall"), and the cost of a wrong season-hint is higher than the benefit (it's merged into the cache key). `"april"` + `"autumn"` preserve the same coverage unambiguously. |
| 2026-04-23 | P21 | `supabase/functions/_shared/retrieval.ts` `computeWardrobeCoverage` | **Codex P2 round 2 on PR #664 — unnormalized category aliases leaked false gaps.** `computeWardrobeCoverage` recorded raw lowercase category keys from `g.category`, but the `gaps_derived` rule at the same line range only checks canonical singular names (`top`, `bottom`, `shoes`, `outerwear`, `dress`, `accessory`). User data carries both singular and plural/legacy variants (`tops`, `bottoms`, `shoe`, `accessories`, etc.). A wardrobe of 10 items all tagged `"tops"` would therefore see `by_category = { "tops": 10 }`, which means `by_category.top` is 0, which fires the `"no top in wardrobe"` gap-derived rule. That false gap then gets fed into the AI prompt AND into the fallback scorer's heuristics, cascading into wrong recommendations. The fallback scorer itself already guarded against this via `categoryCount(cats, ["top", "tops"])`-style pattern, but the coverage object upstream did not. | **RESOLVED in this PR (Wave 4-B, Codex round 2 P2 fix)**: added `CATEGORY_ALIAS_MAP` + exported `canonicalCategory()` helper in `_shared/retrieval.ts`. Maps `tops→top`, `bottoms→bottom`, `shoe→shoes`, `accessories→accessory`, `outerwears→outerwear`, `dresses→dress`. `computeWardrobeCoverage` now keys `by_category` on the canonical name, so `gaps_derived` sees `by_category.top = 10` regardless of whether the DB rows store `"top"` or `"tops"`. Existing fallback `coverage.by_category.shoes \|\| coverage.by_category.shoe` in `wardrobe_gap_analysis/index.ts` becomes redundant (second OR branch always 0) but is kept as belt-and-suspenders in case future aliases land outside the map. |
| 2026-04-23 | P21 | `supabase/functions/_shared/retrieval.ts` `stratifiedSample` round-robin fill | **Codex P2 round 2 on PR #664 — round-robin break disabled category rotation.** The remainder-fill path was documented as "round-robin across category buckets" but had an unconditional `break` at the end of the outer `offset` loop. The nested inner loop took ALL items from bucket 1, then ALL from bucket 2, etc. — that's bucket-order concatenation, not round-robin. Concrete impact: when the proportional quota pass under-fills `n`, the fill path favors whatever bucket comes first in `entries` (sorted by length desc), skewing the representative sample toward the largest category. Minority-category coverage suffered. | **RESOLVED in this PR (Wave 4-B, Codex round 2 P2 fix)**: rewrote the fill loop as a genuine round-robin — step one index per bucket per `offset` pass, bounded by `maxLen = max(bucket.length)` so the loop terminates. `taken.has(g.id)` skip preserves the dedup invariant. Result: minority buckets get proportional presence at each offset; no silent large-bucket bias. |
| 2026-04-23 | P21 | `supabase/functions/wardrobe_gap_analysis/index.ts` fallback candidates | **Codex P1 round 3 on PR #664 — fallback could return empty gaps.** After my P21 rewrite, `fallbackGapAnalysis` built its `candidates` array via conditional pushes (shoeCount < 2, bottomCount < 2 or skewed, topCount < 3 or skewed, outerwearCount < 1, accessoryCount < 2, by_formality.high === 0 && total >= 10). A reasonably complete wardrobe can pass every check, leaving `candidates = []`. The subsequent `.sort().slice(0,5)` then yields `gaps: []`. When Gemini is unreachable, the endpoint used to return a ranked fallback; now it could return nothing, giving the user no actionable recommendation. Regression vs pre-Wave-4-B behaviour (the old Markov-like list always returned 5 items). | **RESOLVED in this PR (Wave 4-B, Codex round 3 P1 fix)**: added 3 unconditional baseline candidates before the slice — "Dark wash straight-leg jeans" (score 30), "White cotton t-shirt" (score 25), "Simple leather watch" (score 20). Conditional items score 65-100 so they still win the top-5 slot on skewed wardrobes, but `gaps` is guaranteed to have ≥3 entries even when every conditional guard fails. If intent is provided, the shopping-recommendations path also benefits (it re-ranks the same `candidates` pool). |
| 2026-04-23 | P27 | `supabase/functions/clone_outfit_dna/index.ts:274-280` (top-40 slice) | **Code-reviewer IMPORTANT on PR #665 — top-40 cap can strip a whole slot.** Same defect pattern Codex flagged on P20 mood_outfit (PR #664). Repro: reference outfit is top+bottom+shoes, user has 60 tops scoring high and only 2 shoes scoring low (shoes fail color/archetype gates). All 40 slots go to tops; the model then can't produce a complete outfit per prompt rule #1 and either hallucinates shoe IDs or returns incomplete variations. | **RESOLVED in this PR (Wave 4-C, code-reviewer fix pre-push)**: imported `canBuildCompleteOutfitPath` from `_shared/outfit-validation.ts`. After the initial `scoredPool.slice(0, 40)` cut, checks `canBuildCompleteOutfitPath(ranked.map(r => r.g))`. If the subset can't build a complete path, greedily extends from the next-best-scored overflow (scoredPool beyond index 40), then from the unranked wardrobe as final fallback, stopping as soon as a complete path is restored. Mirrors exactly the `mood_outfit/index.ts:196-215` pattern shipped in PR #664. Worst case adds a handful of items — `estimateMaxTokens({inputItems: ranked.length, ...})` already uses dynamic input count so the budget tracks. |
| 2026-04-23 | P27 | `supabase/functions/clone_outfit_dna/index.ts:303` slot-coverage recovery loop | **Codex P1 on PR #665 — recovery loop iterated `gatedWardrobe` instead of full `wardrobe`.** When the enrichment gate is active (ratio ≥ 0.7), `gatedWardrobe = filterEnrichedGarments(wardrobe)` drops non-enriched items. `scoredPool` is derived from `gatedWardrobe`, so after the 40-item slice the greedy step-A loop iterates `scoredPool.slice(40)` (correct — still within the enriched pool), but the step-B fallback iterated `gatedWardrobe` — which is the same set `scoredPool` was derived from. On wardrobes where the ONLY candidate for a missing slot (often the one shoe or one bottom a user has) is non-enriched, step-B silently iterates items already in `seen` and `canBuildCompleteOutfitPath` stays false. The model then gets an impossible candidate set and either hallucinates IDs or returns incomplete variations. | **RESOLVED in this PR (Wave 4-C, Codex P1 fix)**: step-B fallback now iterates the full `wardrobe` (pre-gate set), matching exactly the `mood_outfit` pattern from PR #664. Also consolidated the `seen` Set into a single instance reused across step-A, step-B, and the new outerwear step — each extension step updates the same set so candidates aren't re-added. |
| 2026-04-23 | P27 | `supabase/functions/clone_outfit_dna/index.ts:293` outerwear coverage | **Codex P2 on PR #665 — `canBuildCompleteOutfitPath` doesn't require outerwear.** The helper validates base paths (top+bottom+shoes OR dress+shoes) but never requires outerwear. Prompt rule #1 asks the model to include outerwear whenever the reference outfit has it, so on outerwear-based references (e.g. coat + trousers + shoes) the pre-filter can return zero outerwear candidates and still pass the slot-coverage gate. The model then must either hallucinate an outerwear ID or violate rule #1. | **RESOLVED in this PR (Wave 4-C, Codex P2 fix)**: after the base slot-coverage pass, checks `referenceHasOuterwear` (any reference item classified as outerwear) and `ranked.some(slot === "outerwear")`. If reference has outerwear and ranked pool has none, greedy-adds the best-scored outerwear from `scoredPool.slice(40)` first (preserves scoring), falls back to the full `wardrobe` if the overflow also lacks outerwear. If neither has outerwear, accepts graceful degrade — user genuinely has no outerwear in their wardrobe, base outfit still valid. |
| 2026-04-23 | P27 | `supabase/functions/clone_outfit_dna/index.ts` enrichment gate | **Code-reviewer SUGGESTION on PR #665 — inlined dual-spelling check duplicated P24 helper.** The initial Wave 4-C rewrite inlined `g.enrichment_status === 'complete' || g.enrichment_status === 'completed'` when computing `enrichmentRatio`. P24 (PR #663) shipped `isEnrichmentReady()` in `_shared/burs-ai.ts` as the canonical predicate — accepts both spellings centrally so future schema-cleanup (W4.9-A) flips to a single canonical value without requiring a grep-and-replace across consumers. | **RESOLVED in this PR (Wave 4-C, code-reviewer suggestion applied pre-push)**: replaced inlined check with `isEnrichmentReady(g.enrichment_status)` import from `_shared/burs-ai.ts`. Single source of truth. |
| 2026-04-23 | P21 | `src/test/smoke/mocks/gemini.ts` `hasShoppingIntent` detector | **Codex P2 round 3 on PR #664 — intent detector fired on non-intent mode.** The mock's intent detection regex was `/shopping intent\|shopping_recommendations\|fills_gap/i`. Codex flagged that `shopping_recommendations` and `fills_gap` are present in the tool schema description text AND could bleed into the message content if a future caller serializes tool schema into the prompt (some callers inline tool descriptions for models that don't support structured tool-calling). Impact: the mock always emitted `shopping_recommendations` regardless of actual intent, so the smoke test's "without intent → gaps only" backward-compat case couldn't catch regressions in that code path. | **RESOLVED in this PR (Wave 4-B, Codex round 3 P2 fix)**: narrowed the regex to `/shopping assistant mode\|shopping intent/i` — both markers appear ONLY in the prompt text of intent-mode requests (`modeInstructions` emits "You are in SHOPPING ASSISTANT mode" only when intent is provided; the SHOPPING INTENT block header is gated the same way). Tool-schema field names can no longer trigger the intent branch. |
| 2026-04-24 | P27b | `src/components/garment/SecondaryImageManager.tsx` raw-file fallback + confirm-time busy recheck | **Codex P1+P2 round 5 on PR #668 (TWO findings).** (i) **P1 — `compressImage` fails hard** — my add flow did `const { file: compressed } = await compressImage(file)` and aborted on any throw. `compressImage` uses `createImageBitmap` + `OffscreenCanvas` — both known to be unavailable or flaky in some Median/WebView environments (documented in `useAddGarment`'s raw-file fallback). Users on those environments would have the add feature entirely broken instead of gracefully degrading to a larger raw upload. Fix: mirror `useAddGarment`'s pattern — `try/catch` around `compressImage`, fall back to raw `file` on failure, log the compression error. (ii) **P2 — Busy gate only enforced before dialog open** — `handleSwapConfirm` (and `handleDeleteConfirm`) had no busy recheck at confirm-time. If `render_status` / `enrichment_status` flipped to busy WHILE the dialog was open (background refetch or another tab), confirming still fired a swap/delete — potentially double-charging a render credit mid-flight. Fix: re-check `isBusy` at the top of both confirm handlers; if busy, close dialog + show busy-toast + return. | **RESOLVED in this PR (Wave 4.5-B, Codex P1+P2 round 5 fix)**: both applied. Tests: full vitest 1121/1121 passing; tsc + eslint clean. |
| 2026-04-24 | P27b | `src/components/garment/SecondaryImageManager.tsx` upload path + render ordering | **Codex P1+P2 round 4 on PR #668 (TWO findings).** (i) **P1 — Upload path collision after prior swap** — my `handleFileChange` wrote to a fixed `${user.id}/${garment.id}_secondary.${ext}` path. Scenario that collides: user adds secondary A → swaps (A's `_secondary` path moves to `image_path`) → removes secondary (deletes what was the original) → adds new secondary B → upload to the fixed path OVERWRITES A's content (the current primary). Subsequent "Remove" then deletes the live primary. Fix: unique crypto-random suffix per upload (`secondary_${uniqueTag}.${ext}`, `uniqueTag = crypto.randomUUID().slice(...)`) + `upsert: false` (defense-in-depth against any residual collision). Orphan objects from future replace flows cleaned by the post-launch storage-GC cron (already logged). (ii) **P2 — Render racing enrichment** — earlier design fired `triggerGarmentPostSaveIntelligence` with `skipRender:true` and a SEPARATE `startGarmentRenderInBackground` immediately after. But my swap UPDATE cleared `ai_raw` to null; the render worker could pick up the job BEFORE enrichment wrote fresh ai_raw, producing a render from stale/minimal metadata with no automatic redo. Fix: added `renderOptions: { force?: boolean }` to `TriggerGarmentPostSaveIntelligenceOptions` — when provided, the internal `startGarmentRenderInBackground` call that fires AFTER enrichment settles uses that option. Swap flow now passes `renderOptions: { force: true }` into the single `triggerGarmentPostSaveIntelligence` call and drops the parallel render trigger. Single source of truth for ordering; render guaranteed to see fresh ai_raw. | **RESOLVED in this PR (Wave 4.5-B, Codex P1+P2 round 4 fix)**: both applied. Tests: full vitest 1121/1121 passing; tsc + eslint clean. |
| 2026-04-24 | P27b | `src/components/garment/SecondaryImageManager.tsx` original_image_path + cache invalidation | **Codex P1+P2 round 3 on PR #668 (TWO findings).** (i) **P1 — Update `original_image_path` in swap** — my swap UPDATE only touched `image_path` and `secondary_image_path`. But `getPreferredGarmentImagePath` in `src/lib/garmentImage.ts` prefers `original_image_path` over `image_path` when no rendered image is ready. Post-swap: `render_status='pending'` (we set it) → falls through to `original_image_path || image_path`, and `original_image_path` STILL pointed at the old primary's raw upload. Result: wardrobe card + hero showed the OLD photo all the way through enrichment + render, and permanently if render failed. Whole point of the "Use as primary" feature broken until render completed. Fix: add `original_image_path: newPrimary` to the same UPDATE so it's tracked in lockstep with `image_path`. (ii) **P2 — Invalidate broader wardrobe query set** — my local `invalidate()` only busted `['garment', garment.id]`. But list screens read from `['garments', userId, filters]`, `['garments-by-ids']`, `['garments-count', userId]`, `['garments-smart-counts']`, plus `['ai-suggestions']`, `['insights']`, etc. — 10+ query keys with multi-minute stale windows. After a swap/add/delete, stale caches meant the wardrobe list + insights dashboard + AI suggestions all showed old images until their staleTime expired. Canonical helper `invalidateWardrobeQueries` in `src/hooks/useGarments.ts` (already used by `useUpdateGarment` + `useDeleteGarment` for this exact reason) busts the full set. Fix: replace the local one-key `invalidate` with `invalidateWardrobeQueries(queryClient, user?.id)`. | **RESOLVED in this PR (Wave 4.5-B, Codex P1+P2 round 3 fix)**: both applied. Tests: full vitest 1121/1121 passing; tsc + eslint clean. |
| 2026-04-24 | P27b | `src/components/garment/SecondaryImageManager.tsx` swap OC + delete race | **Codex P1 round 2 on PR #668 (TWO findings).** (i) **Swap OC narrow on only image_path** — `handleSwapConfirm` used `.eq('image_path', previousImagePath)` alone for optimistic concurrency, but the UPDATE also rewrites `secondary_image_path`. Concurrent-tab scenario: tab A reads garment → tab B adds + swaps its own secondary → tab A's stale `secondary_image_path` (now either orphaned or re-assigned by B's swap) gets promoted into primary. Fix: `.eq('secondary_image_path', previousSecondaryPath)` added alongside, so ANY concurrent write in EITHER column fails the OC guard cleanly. (ii) **Delete race — storage delete before DB clear** — `handleDeleteConfirm` removed the storage object at `path` BEFORE nulling the DB pointer. Under a concurrent swap in another tab that moved `path` from `secondary_image_path` to `image_path` (via the value-exchange UPDATE), our delete would remove the now-live hero asset. Fix: reordered to DB-first — `.update({ secondary_image_path: null }).eq('secondary_image_path', path)` commits the pointer clear ONLY if the path still occupies secondary; if the OC miss hits (zero rows matched, path was moved), we bail without touching storage. Only after the DB clear do we remove the storage object. | **RESOLVED in this PR (Wave 4.5-B, Codex P1 round 2 fix)**: both applied as described. Tests: full vitest 1121/1121 passing; tsc + eslint clean. |
| 2026-04-24 | P27b | `src/components/garment/SecondaryImageManager.tsx` swap recovery | **Codex P1 round 1 on PR #668 — non-retryable render enqueue failures stranded garment at `render_status='pending'`.** My swap flow wrote `render_status='pending'` in the single-statement UPDATE, then called `enqueueRenderJob(..., { force: true })` directly with a manual retry block. The retry block covered only the retryable-error path (5xx/transport via `isRenderEnqueueRetryable`); non-retryable errors (402 insufficient credits, 4xx other) were caught by an `else` branch that ONLY logged and moved on. No `render_jobs` row got inserted, so the worker had nothing to reconcile; the garment stayed at `'pending'` forever with no recovery. Exactly the defect class `resetGarmentRenderStateOnEnqueueFailure` exists to prevent in `startGarmentRenderInBackground` (Codex rounds 11 + 14 on PR #421). | **RESOLVED in this PR (Wave 4.5-B, Codex P1 fix)**: (a) `startGarmentRenderInBackground` in `garmentIntelligence.ts` exported + accepts `options: { force?: boolean }` param; tightened its `source` type from `string` to `RenderTriggerSource` and removed the interior `as RenderTriggerSource` casts. `force` threads through to both the initial `enqueueRenderJob` call AND the same-nonce retry. (b) `SecondaryImageManager.handleSwapConfirm` now delegates the render trigger to `startGarmentRenderInBackground(garment.id, 'manual_enhance', { force: true })` — inherits the full 402-reset / retryable-retry / server-state-check / non-retryable-reset tree for free. Net diff: -15 LOC in SecondaryImageManager (manual retry block removed), +5 LOC in garmentIntelligence.ts (export + param + thread). Single source of truth for render-enqueue recovery. |
| 2026-04-24 | P27b | `src/components/garment/SecondaryImageManager.tsx` | **Code-reviewer SUGGESTION on PR #668 — no dedicated unit tests for the new component.** The three handlers (add/swap/delete), the optimistic-concurrency miss path, and the busy-gate toast branch are each exercised only transitively via the GarmentDetail test (which mocks the component wholesale). A standalone `SecondaryImageManager.test.tsx` would lock in: (a) OC-miss path invalidates + surfaces `common.something_wrong` correctly, (b) busy-gate blocks clicks + toasts, (c) delete proceeds to DB null when storage delete throws, (d) swap re-fires the new `startGarmentRenderInBackground` helper correctly. Not launch-blocking — the existing GarmentDetail mock prevents the test file from regressing, and the handlers follow the same patterns already tested elsewhere. | Scheduled: future test-coverage PR. Not Wave 4.9 scope (4.9 is findings-cleanup + schema); track as nice-to-have. |
| 2026-04-24 | P27b | `garments` storage bucket orphan accumulation | **Code-reviewer SUGGESTION on PR #668 — storage GC cron for orphaned secondary images.** `SecondaryImageManager.handleDeleteConfirm` catches storage delete failures as non-fatal (logs + proceeds with DB null). Over months, transient Supabase-storage hiccups OR partial swap flows could accumulate orphaned `${userId}/${garmentId}_secondary.*` objects with no DB pointer — same risk class as orphaned render-job rows. A background cron (pg_cron + edge function) that sweeps `garments/${userId}/` for objects without a matching `image_path` OR `secondary_image_path` row would close the leak. Low urgency (orphan size is bounded by user × swap count and each object is ~100-500KB). | Scheduled: post-launch storage-hygiene pass. Not launch-blocking. |
| 2026-04-24 | W4.9-A | `supabase/functions/_shared/outfit-scoring.ts:889` + `:1336` | **Pre-existing deno-check blockers in shared module.** Two TypeScript errors in `outfit-scoring.ts` predated this PR (confirmed via `git stash + deno check` on main): (a) line 889 — inline stub literal cast `as GarmentRow` has been type-insufficient since day one (stub misses `created_at` / `enrichment_status` / `ai_raw`); (b) line 1336 — `missing.push('shoes')` against a `missing` variable that filter-narrowed to `('top' | 'bottom' | 'dress')[]` at line 1308. Both errors were invisible in CI because `deno-check` only runs on changed edge-function entry-points (standing rule from PR #653), and no PR since deno-check shipped has touched `burs_style_engine/index.ts` — until W4.9-A's SELECT-string edit. | **RESOLVED in this PR (Wave 4.9-A scope expansion, deno-check unblocker)**: (a) widened the cast to `as unknown as GarmentRow` per deno's error message guidance — safe double-cast since the stub is a narrow fallback for `items.find(slot==='shoes')?.garment`. (b) typed `missing: string[]` explicitly so the `missing.push('shoes')` compiles. Zero runtime behaviour change — the fallback literal is only read by `garmentText()` which ignores unset fields. |
| 2026-04-24 | P30 | `supabase/functions/style_chat/index.ts:1161-1164,1337` | **5 pre-existing deno-check blockers in style_chat/index.ts** (4× `TS2345` SupabaseClient type mismatch on `getCalendarContext` / `getRecentOutfitsContext` / `getRejectionsContext` / `getWardrobeContext` calls; 1× `TS2304` undefined `StyleChatIntentKind`). Verified pre-existing on main via `git stash + deno check`. Not introduced by P30 — P30 only touches `_shared/style-chat-classifier.ts`, and CI deno-check scopes to changed entry-points only (P0b convention), so this PR doesn't exercise the check. | Scheduled: whichever future PR directly modifies `supabase/functions/style_chat/index.ts` will inherit these as CI blockers (same pattern as W4.9-A's outfit-scoring.ts pre-existing fixes). Likely triggered by a Wave 5 P28+P29 follow-up once those prompts are re-scoped against the post-P26 code. Not launch-blocking — Supabase bundler transpiles regardless of deno check success. |
| 2026-04-24 | P30 | Wave 5 spec drift | **P28 spec is partially pre-P26 and describes already-fixed code** — the "fail-open with `slot: "unknown"`" defect called out at `unified_stylist_engine.ts:82-91` was RESOLVED in P26 (PR #665). Current `unified_stylist_engine.ts` uses `classifySlot` for `other_items` in swap mode; style_chat's refinement path already passes full `active_look_garment_ids` + `locked_garment_ids` at lines 1471-1479. The user's repro ("pressing refine button just anchors a garment instead of whole outfit") may be a narrower remaining edge case OR may be already fixed by P26. Need targeted repro before scoping a P28 code fix. P29 (activeLook persistence across turns) requires grepping for serialization paths across AIChat.tsx + styleChatContract.ts before a fix can be planned. P31 depends on P28 outcome (RefineChips/RefineBanner payload fix is a frontend verification of P28's backend scope). | **RESOLVED in PR #673 (P28 tracker, 2026-04-25)** — full end-to-end investigation on `main` (commit `8a240d0c`, post-P30 merge) confirms the refine flow correctly threads full outfit context at every layer: UI (`OutfitSuggestionCard.tsx:332` passes `garments.map(g => g.id)`, never anchor), client handler (`AIChat.tsx` `handleEnterRefine` stores full list and includes `active_look.garment_ids` + `locked_slots` on every refine-mode message), backend (`style_chat/index.ts:1471-1479` threads `active_look_garment_ids` + `locked_garment_ids` + `requested_edit_slots`; `unified_stylist_engine.ts:83` maps `mode:"refine"`→`"generate"` while preserving full context at line 124; `burs_style_engine` uses `buildActiveLookSlotMap` + `rankCombosForRefinement` to enforce locked-preservation + edit-slot-change). P28's intent fully delivered by P26 (slot mapping, PR #665) + P30 (classifier routing, PR #672). P28 status flipped to `[DONE-subsumed]`. **P29 + P31 follow-up audit (PR #674, 2026-04-25)** — both also `[DONE-subsumed]`: P29 serialization verified working at every layer (types match, client serializes, DB persists + readbacks, backend threads anchor through wardrobe-context lookup, response echoes anchor back); P31 verified architecturally subsumed (`RefineChips` + `RefineBanner` are intentionally stateless presentational components, parent `AIChat.tsx:810-841` owns payload construction and ships `active_look.garment_ids: refineMode.activeGarmentIds` plus `locked_slots` on every refine-mode message). Wave 5 fully complete (P28 + P29 + P31 subsumed, P30 shipped as PR #672). |
| 2026-04-24 | W4.9-A | `src/components/garment/GarmentEnrichmentPanel.tsx:78` | **Code-reviewer P1 on W4.9-A — stale `EnrichmentStatus` union type.** Declared `'none' \| 'pending' \| 'in_progress' \| 'complete' \| 'failed'` but the W4.9-A backfill migration canonicalizes writers to `'processing'` + `'completed'`. Runtime keeps working (JS doesn't enforce types; the `as EnrichmentStatus` cast at `GarmentDetail.tsx:77` widens), but the type lies. Line 82's `enrichmentStatus === 'processing'` check (newly added in W4.9-A) would have been dead-typed. | **RESOLVED inline (Wave 4.9-A, code-reviewer P1 fix pre-push)**: widened the union to include both pre- and post-backfill spellings: `'none' \| 'pending' \| 'in_progress' \| 'processing' \| 'complete' \| 'completed' \| 'failed'`. Mirrors `isEnrichmentReady` helper's dual-spelling acceptance — defensive programming through the transition window. |
| 2026-04-25 | P41 | `src/i18n/locales/{en,sv,...}.ts` — `occasion.{vardag,jobb,fest,dejt}` keys | **Orphan-key sweep candidate (code-reviewer Suggestion, non-blocker).** P41 dropped the 4 Swedish-keyed occasion strings from `UnusedOutfits.tsx`'s OCCASIONS array. The corresponding locale keys (`occasion.vardag`, `occasion.jobb`, `occasion.fest`, `occasion.dejt`) defined in en.ts:1704-1707 + sv.ts:1607-1610 + the other 12 locale files MAY still be reached by other callers — a grep sweep across `src/` showed many files referencing these strings (mood detection regexes, calendar event keyword matchers, etc.) so the keys probably stay reachable. Confirm via grep before any future locale-cleanup wave removes them. Not launch-blocking. | Scheduled: post-launch locale-cleanup wave that consolidates dual-keyed entries (related to other dual-spelling sweeps tracked in Findings Log: enrichment_status `complete`/`completed`, color names Swedish/English). |
| 2026-04-25 | P41 | Wave 6 audit-first miss | **Audit-first protocol caught a defect missed by the audit sub-agent.** Initial Wave 6 P41 audit verdict was `DONE-subsumed` based on consistent `t()` usage in UnusedOutfits.tsx — the agent searched for inconsistent t() namespaces and found none. But the agent missed `src/pages/UnusedOutfits.tsx:29`'s defective OCCASIONS array `['vardag', 'jobb', 'dejt', 'fest', 'casual', 'smart_casual']` (4 Swedish + 2 English mix) AND line 290's hardcoded `"unused pieces"` text. The defective OCCASIONS array is the actual "Swedish/English mixing" referenced by the prompt — it cycles through 6 generated outfits via `OCCASIONS[index % OCCASIONS.length]` and gets passed to `burs_style_engine` as the `occasion` field. Both en.ts (`occasion.vardag` AND `occasion.casual`) and `getOccasionLabel(t, occasion)` happily handle either key, so no user-visible defect — but the inconsistency violated the prompt's intent. Caught when the main agent independently read the file before the planned tracker-only flip per Wave 5 P28 precedent (always verify before flipping subsumed). | **RESOLVED in this PR (P41 actual code fix, not tracker-only flip)**: array replaced with canonical English-keyed 6-occasion vocabulary `['casual', 'work', 'date', 'party', 'workout', 'travel']` matching `OutfitGeneratePicker.tsx:29-36`, `QuickGenerateSheet.tsx:62-69`, `AdjustDaySection.tsx:10-15`. Line 290 localized via new key `insights.unused_pieces_label`. Lesson for future audits: grep-for-t()-coverage is not enough — also read constant arrays/maps and verify their values match canonical vocabularies elsewhere in the codebase. |
| 2026-04-25 | P36 | `src/lib/dateLocale.ts:51` `getDateFnsLocale` | **First-render flash for non-en locales — and async-load never re-renders consumers.** P36's date-fns weekday formatting depends on `dateFnsLocale` from `useLanguage()`. `getDateFnsLocale(locale)` returns `enUS` synchronously when the per-locale chunk hasn't loaded yet. The `loadDateFnsLocale(locale).catch(() => {})` fire-and-forget at LanguageContext.tsx:68 never updated component state, so consumers stayed pinned to enUS until some unrelated re-render happened. Codex P2 (PR #678) flagged this as a deterministic regression for non-en users in Insights and any other dateFnsLocale consumer. | **RESOLVED in PR #678 fix-up commit (Codex P2 round 1)**: LanguageProvider now tracks `dateFnsLocale` in state and updates it when `loadDateFnsLocale(locale)` resolves. First-render flash to enUS is now bounded to the chunk-load duration (~50ms) and consumers automatically re-render once the chunk lands. Same pattern applies to the no-provider fallback path. |
| 2026-04-25 | P33+P34+P36 | NotFound + ShareOutfit + PublicProfile + Insights via LanguageContext | **Codex P1 + P2 batch on PR #678.** (P1 NotFound) Replacing literal strings with `t('notfound.*')` made the component depend on LanguageProvider, but tests and any out-of-provider caller now render fallback humanized text ("Title" / "Return home") instead of intended copy. (P2 PublicProfile) `String.prototype.replace` treats `$&`/`$'`/`` $` `` in user-controlled `display_name` as JS regex special tokens, mangling `<title>` / og: / twitter: meta tags. (P2 ShareOutfit) `t('share.meta_title_template').replace(...)` returns the humanized template name 'Meta title template' on cold start before LanguageProvider's dict loads — meta tags briefly broken. (P2 Insights x2) Same async-locale issue as P36 row above. | **RESOLVED in PR #678 fix-up commit**: new shared `src/lib/i18nFallback.ts` with `safeT()` (humanize-fallback detection + explicit English fallback) and `interpolateMeta()` (function replacer + humanize-fallback detection + hardcoded fallback template). NotFound uses `safeT` for hardcoded `'Page not found'` / `'Return to Home'`. PublicProfile + ShareOutfit use `interpolateMeta` for safe interpolation of user-controlled `display_name` / `outfit.occasion` into meta-tag templates with hardcoded English fallbacks. LanguageContext date-fns rerender fix (above row) closes the Insights x2 P2. Verification: tsc 0, eslint 0, NotFound test 2/2 pass, full vitest 1328/1328 pass. |
| 2026-04-25 | P35 | `src/components/onboarding/AccentColorStep.tsx` | **Dead code — zero importers in `src/`.** Code-reviewer on PR #679 flagged that AccentColorStep.tsx has zero importers (verified via grep — only the file itself contains the string `AccentColorStep`). Component is unrendered. P35 localized it defensively (3 keys for live-preview labels at lines 61, 69, 75), but the work is wasted since the file never renders. The leftover Swedish "Favorit" (line 69 pre-fix) is also moot as a user-facing concern for the same reason. | Scheduled: post-launch dead-code sweep — delete AccentColorStep.tsx + remove the 3 `onboarding.accent.preview_*` keys from en.ts + sv.ts. Not launch-blocking. |
| 2026-04-25 | P32 | `supabase/functions/travel_capsule/index.ts:492-493` | **Pre-existing dead vars in travel_capsule.** `localeName` (line 492) and `isSv` (line 493) are declared but never read. Pre-existing on main — caught by code-reviewer during P32 review. The actual locale prompt-injection happens at line 647 via `LOCALE_NAMES[locale] || "English"` directly, NOT through `localeName`. ESLint config at `eslint.config.js:8` excludes `supabase/**` so CI never caught this. P32's LOCALE_NAMES extension is correct (line 647 picks up the new 14 entries) — these dead vars don't affect the fix. | Scheduled: post-launch dead-code sweep — delete the 2 unused declarations. Not launch-blocking. |
| 2026-04-25 | P40 | `src/pages/OutfitGenerate.tsx:78` + `supabase/functions/shopping_chat/index.ts:137` | **Translator-pass needed for full multi-locale regex accuracy.** P40 expanded FORMAL_KEYWORDS (~50 tokens, 12 Latin locales) and CHAT_SHORT_RE (~85 tokens, all 14 locales). Confidence: high on sv/en, reasonable on da/no/de/fr/es, low on fi/it/pt/nl/pl (single-best-guess), low on ar/fa (greetings only, no formality words for FORMAL_KEYWORDS). Native speakers should review for: (a) missing common variants; (b) over-matching on common words that aren't formality/greeting triggers in their context; (c) FORMAL_KEYWORDS expansion to ar/fa once `\b` Unicode boundary semantics are vetted with RTL scripts. Codex P2 (PR #681): locale-scope FORMAL_KEYWORDS via per-locale map (Polish "cena"=price no longer collides with Spanish/Italian "cena"=dinner); broaden CHAT_SHORT_RE punctuation to support Spanish ¿¡ and Arabic ،؟. | **PARTIALLY RESOLVED in PR #681 fix commit (Codex P2)**: locale-scoping + non-ASCII punctuation. Translator-pass for token coverage still scheduled post-launch. |
| 2026-04-25 | P37+P38+P39 | `_shared/burs-slots.ts`, `_shared/outfit-rules.ts`, `_shared/travel-capsule-planner.ts`, `_shared/day-intelligence.ts`, `src/pages/MoodOutfit.tsx`, `src/i18n/locales/en.ts`, `src/i18n/locales/sv.ts` | **Translator-pass needed (third Wave 6 batch).** P37 mood hint sv translations + P38 garment vocabulary across 3 shared modules + P39 calendar-event tags across 8 OCCASION_RULES + 3 inline arrays — all added by best-effort agent translation. Confidence pattern matches P40: high on sv/en/de/fr; reasonable on no/da/es/it/pt/nl; lower on fi/pl; ar/fa added but warrant native-speaker review. Cross-language collisions intentionally skipped (documented per-prompt in tracker rows). Pre-existing collision NOT fixed: `rock` in OUTERWEAR_TOKENS is sv/de=coat AND a substring of English "rock band" subcategories — pre-P38 problem. | Scheduled: post-launch translator review wave consolidates all Wave 6 token translations (P40 + P37/P38/P39). Not launch-blocking — sv + en accurate; others ship with imperfect-but-additive coverage. |
| 2026-04-26 | P44 | `src/hooks/useOnboarding.ts:17-25` | **Code-reviewer P1 on Wave 7 PR 1 — rollout-bridge: in-progress users get trapped between PR 1 and PR 2.** `completeOnboarding()` only writes the legacy `preferences.onboarding.completed=true` flag and never touches `onboarding_step`. The P42 backfill flips `step='completed'` for users whose legacy flag is true AT MIGRATION APPLY TIME — but anyone who is mid-OLD-flow when PR 1 ships, and finishes the OLD flow between PR 1 merge and PR 2 (P45) ship, would write the legacy flag (now ignored by the new gate) AND keep `onboarding_step='not_started'` (column default, never touched by old code). The P44 ProtectedRoute then redirects them to `/onboarding` indefinitely; the OLD Onboarding.tsx page still renders `Navigate to "/"` based on the legacy flag, creating a redirect loop. | **RESOLVED in this PR (Wave 7 PR 1, Fix Protocol exception (b) — shared-code defect that would silently regress the PR's acceptance criterion "non-completed users are redirected to /onboarding")**: `completeOnboarding()` now ALSO calls the new `advance_onboarding_step(p_user_id, p_to_step='completed')` RPC BEFORE the legacy preferences write. RPC error → throw + skip legacy write (no half-state). `{ok:false}` no-op (already at 'completed') → tolerate + continue. Test file `useOnboarding.test.tsx` adds 4 cases (RPC called with correct args; legacy write happens after RPC; RPC error blocks legacy write; ok:false tolerated). `as any` cast on `supabase.rpc` because the new RPC isn't in the auto-generated types.ts Functions union until post-merge regen (CLAUDE.md hard rule: never edit types.ts manually). |
| 2026-04-26 | P44 | `src/pages/Onboarding.tsx`, `src/hooks/useOnboarding.ts` | **Codex round 8 P1 on PR #683 — gate the swallow on column-existence, not just error code.** PGRST202 is also returned when PostgREST's schema cache is stale POST-migration (right after `db push` applies, before PostgREST notifies/reloads). Round-7's "swallow on PGRST202 OR 42883" matched in that window too, creating exactly the split-brain we worked through rounds 4-5 to prevent (column='not_started', legacy=true → ProtectedRoute redirects, Onboarding.tsx Navigates to /, loop). | **RESOLVED in this PR (Codex round 8 fix-up commit)**: added a column-existence gate. Both completion paths now compute `profileLacksOnboardingColumn = (profile as ...).onboarding_step === undefined` BEFORE the RPC call. The catch branch swallows ONLY when BOTH `profileLacksOnboardingColumn` AND `code in ('42883','PGRST202')` — i.e., the column genuinely doesn't exist on the loaded profile (pre-migration window) AND the error matches the deploy-window signal. Once `useProfile`'s `select('*')` returns the column (post-migration, including the rare stale-cache PGRST202 case), all RPC errors propagate, keeping legacy + column states aligned (neither set → user retries). 2 new tests cover the new "throw post-migration even on PGRST202/42883" guarantee; 2 existing pre-migration tests reaffirmed for clarity. tsc 0 / eslint 0 / build clean / vitest 1374/1374 (46 net new vs prior 1328: 19 scale-guard + 12 ProtectedRoute + 9 useOnboarding + 6 advanceOnboardingStep wrapper). |
| 2026-04-26 | P44 | `src/pages/Onboarding.tsx`, `src/hooks/useOnboarding.ts` | **Codex round 7 P1 on PR #683 — deploy-window swallow misses PostgREST error code.** My round-5 narrow-swallow only matched raw Postgres SQLSTATE `42883`, but supabase-js goes through PostgREST which returns its OWN error code `PGRST202` ("function absent from schema cache") when the RPC isn't deployed. Real-world deploy-window callers therefore never see `42883` — they see `PGRST202` and my `code !== '42883'` check rethrows, skips the legacy preferences write, and leaves first-time completers stuck (ProtectedRoute's pre-migration fallback only saves users who already had the legacy flag set). | **RESOLVED in this PR (Codex round 7 fix-up commit)**: extended the tolerated-codes condition from `code !== '42883'` to `code !== '42883' && code !== 'PGRST202'` in both completion paths. Both codes are tolerated to cover (a) the primary supabase-js → PostgREST path that real users hit (`PGRST202`) AND (b) direct-Postgres tests / local dev (`42883`). Only those two codes pass through; everything else (transient network, ownership mismatch, etc.) propagates per round-5's split-brain fix. Tests: existing `42883` test renamed for clarity; new test covers `PGRST202` (real-world deploy-window). tsc 0 / eslint 0 / build clean / vitest 1372/1372 (44 net new vs prior 1328: 19 scale-guard + 12 ProtectedRoute + 7 useOnboarding incl. 1 new + 6 advanceOnboardingStep wrapper). |
| 2026-04-26 | P43 | `supabase/migrations/20260426120000_onboarding_state.sql` (column lock block) | **Codex round 6 P1 on PR #683 — column REVOKE no-op against table-level GRANT ALL.** Round 2's `REVOKE UPDATE (cols) ON profiles FROM authenticated, anon` was supposed to lock the 4 onboarding columns, but the baseline grants `ALL ON public.profiles TO authenticated, anon` (initial schema lines 2532-2534). PostgreSQL's column-level REVOKE only removes column-level grants — a table-level UPDATE permission continues to permit writes on every column regardless. So clients could still directly modify `onboarding_step` / `onboarding_started_at` and re-enter the 3x boost at will, undermining both onboarding-gate integrity and the rate-limit boost. | **RESOLVED in this PR (Codex round 6 fix-up commit)**: replaced the column-only REVOKE with the canonical "REVOKE table-level UPDATE + GRANT column-level UPDATE on the explicitly allowed columns" pattern. (a) `REVOKE UPDATE ON public.profiles FROM authenticated, anon` — strips the baseline table-level UPDATE. (b) `GRANT UPDATE (display_name, avatar_url, home_city, preferences, created_at, updated_at, body_image_path, height_cm, weight_kg, ics_url, last_calendar_sync, stripe_customer_id, username, is_premium, mannequin_presentation, last_active_at) ON public.profiles TO authenticated` — re-grants UPDATE on the 16 pre-existing user-facing columns (everything in baseline minus `avatar_path` which was dropped in 20260421124000, and minus the 4 new onboarding columns). Pre-PR write surface preserved for non-onboarding columns. The 4 new onboarding columns are now genuinely server-controlled — only the SECURITY DEFINER RPCs (`postgres` owner, bypasses column GRANTs) and `service_role` (BYPASSRLS) can write them. anon ends up with no UPDATE permission, which is defense-in-depth (RLS already blocks anon writes). tsc 0 / eslint 0 / build clean / `db push --dry-run` clean. |
| 2026-04-26 | P44 | `src/pages/Onboarding.tsx:64-71`, `src/hooks/useOnboarding.ts:17-25` | **Codex round 5 P1 on PR #683 — narrow the swallow to deploy-window only.** My round-4 fix swallowed ALL RPC errors. Codex flagged the post-migration risk: a transient network/ownership error at the RPC site would still let the legacy preferences write succeed, creating split-brain — DB column stays `'not_started'` while client legacy flag becomes `true`. ProtectedRoute (column-based, post-migration) keeps redirecting to `/onboarding` while `Onboarding.tsx`'s `onboardingCompleted` (preferences-based) Navigates to `/`. Loop again, this time triggered post-migration on every refresh. | **RESOLVED in this PR (Codex round 5 fix-up commit)**: narrowed the catch in both completion paths to swallow ONLY Postgres `42883 function does not exist` (the deploy-window signal). All other RPC errors propagate (transient network, ownership mismatch, invalid step). Onboarding.tsx's outer `handleGetStartedAction` already has its own try/catch keeping navigation moving for the user — the failure is observable via console.warn + Sentry but not user-blocking. Tests: existing "swallow on RPC error" assertion narrowed to `code:'42883'`; 2 new tests cover (a) non-42883 code (e.g. 08006 network) → throws + skips legacy write, (b) error without `code` field → throws (defensive). tsc 0 / eslint 0 / build clean / vitest 1371/1371 (43 net new vs prior 1328: 19 scale-guard + 12 ProtectedRoute + 6 useOnboarding incl. 2 new + 6 advanceOnboardingStep wrapper). |
| 2026-04-26 | P44 | `src/pages/Onboarding.tsx:64-71`, `src/hooks/useOnboarding.ts:17-25` | **Codex round 4 P1 on PR #683 — RPC failure during deploy window blocks legacy completion.** My round-1 fix called `advanceOnboardingStep(...)` BEFORE the legacy preferences write and threw on RPC error. During the deploy window, the RPC doesn't exist on the DB yet (Postgres `42883 function does not exist`); the throw aborts before the legacy write happens. ProtectedRoute's round-3 fallback only passes users whose legacy flag is ALREADY true — so a user finishing onboarding for the FIRST TIME during the window writes nothing and gets stuck in `/onboarding` indefinitely (no flag → fallback redirects). | **RESOLVED in this PR (Codex round 4 fix-up commit)**: both `Onboarding.tsx`'s LOCAL `completeOnboarding()` and `useOnboarding.completeOnboarding()` now wrap `advanceOnboardingStep` in try/catch + `console.warn`. RPC failures (deploy window, transient network) no longer block the legacy preferences write. Real-world non-deploy-window errors (ownership mismatch, invalid step) are unreachable for a user completing their own onboarding with the hardcoded `'completed'` target. Console warning preserves observability. The previous `useOnboarding.test.tsx` "RPC error blocks legacy write" assertion is inverted — new test asserts RPC error → legacy write still happens + console.warn fires. tsc 0 / eslint 0 / build clean / vitest 1369/1369. |
| 2026-04-26 | P44 | `src/components/auth/ProtectedRoute.tsx` | **Codex round 3 P1 on PR #683 — deploy-window redirect loop.** Vercel auto-deploys the frontend on PR merge but `npx supabase db push --linked --yes` happens manually post-merge. During that window the new `onboarding_step` column doesn't exist on profile rows, so `profile.onboarding_step` is `undefined`. My round-2 gate treated undefined as "not completed" and redirected EVERY authenticated user to `/onboarding`. The OLD `Onboarding.tsx:135-137` then reads `preferences.onboarding.completed === true` and `Navigate to "/"` for already-completed users — bouncing them back. Result: redirect loop for the entire deploy window for users who completed onboarding under the legacy schema. | **RESOLVED in this PR (Codex round 3 fix-up commit)**: `ProtectedRoute` now distinguishes `step === null` (column undefined → pre-migration window) from `step !== 'completed'` (column exists but mid-flow → redirect). When step is null, the gate falls back to the legacy `preferences.onboarding.completed` flag — already-completed users pass through; non-completed users still redirect. Once the migration applies, every row has `step='not_started'|'completed'` (NOT NULL DEFAULT) and the fallback never fires again. 2 new test cases cover (a) undefined-step + legacy completed → renders children, (b) undefined-step + legacy incomplete → redirects, (c) undefined-step + null preferences → redirects. tsc 0 / eslint 0 / build clean / vitest 1369/1369 (41 net new vs prior 1328: 19 scale-guard + 12 ProtectedRoute incl. 2 fallback + 4 useOnboarding + 6 advanceOnboardingStep wrapper). |
| 2026-04-27 | Wave 7 audit | All 10 P0 audit findings (full file list above in the prompt entry) | **Wave 7 pre-launch audit (PR #689 docs the 10 P0s + 12 Findings Log rows + Session Workflow Patterns).** Surfaced by 4-parallel-subagent audit run on the post-P48 onboarding surface. Spans backend (increment bound, analyze_garment boost, plan-cache TTL), state machine (server-step hydration on reload, lang RPC gate, redirect-loop race), counter (BatchCapture optimistic pattern), and V4 compat (migrateV4ToV3Compat merge order). Read-only audit doc PR #689 stayed open while user triaged → user directive "688 is not merged so fix the findings in it then update 689" → fixes folded into PR #688. | **RESOLVED in PR #688** (Wave 7 Pre-Launch Audit Fixes entry above). 4-parallel sub-agent fix run + 1 code-reviewer P1 round + tracker update — all atomic in PR #688 commit. PR #689 separately updated to mark all 10 must-fix rows RESOLVED in PR #688. |
| 2026-04-26 | P42+P43 | `supabase/migrations/20260426120000_onboarding_state.sql` (backfill + REVOKE block) | **Codex round 2 P1+P2 on PR #683.** (a) **P1 quota bypass via tampered onboarding columns** — the scale-guard `resolveUserPlan` reads `profiles.onboarding_started_at` + `onboarding_step` to grant the 3x onboarding tier. The existing `Users can manage own profile` RLS policy lets authenticated users UPDATE their own profile row freely, so a malicious user could keep refreshing `onboarding_started_at` to NOW() and holding `onboarding_step != 'completed'` to lock in 3x rate limits indefinitely. (b) **P2 backfill brittleness** — the predicate `(preferences->'onboarding'->>'completed')::boolean IS TRUE` text-casts to boolean. Any non-boolean literal at that path (untyped JSONB lets a misbehaving client write strings/numbers/etc.) raises `invalid input syntax for type boolean` and rolls back the entire migration. | **RESOLVED in this PR (Codex round 2 fix-up commit)**: (a) Added a `REVOKE UPDATE (onboarding_step, onboarding_garment_count, onboarding_started_at, onboarding_completed_at) ON public.profiles FROM authenticated, anon` block at the end of the migration. SECURITY DEFINER RPCs run as `postgres` (the function owner) and bypass column-level GRANTs, so `advance_onboarding_step` + `increment_onboarding_garment_count` continue to work — they're now the ONLY legitimate writers. service_role bypasses via `BYPASSRLS`. INSERT permission left intact: the client-side profile auto-create in `useProfile.ts` doesn't set these columns (relies on NOT NULL DEFAULT). No current code reads/writes these columns directly outside the RPCs, so the REVOKE breaks zero callers. (b) Replaced the predicate with `preferences->'onboarding'->'completed' = 'true'::jsonb` — direct JSONB equality. Returns FALSE for string/number/null values without raising. Same matching behavior for actual JSONB-true rows. tsc 0 / eslint 0 / build clean / `db push --dry-run` clean. |
| 2026-04-28 | Wave 7.9 followup audit P1 #8 (V3→V4 archetype norm) | `src/types/styleProfile.ts:350` `migrateV3ToV4` | **V3 archetype names (`streetwear`, `scandinavian`, `elegant`, `vintage`, `artsy`) were emitted raw into V4 `archetypes[]` without normalization, breaking V4 readers that filter against `ARCHETYPE_OPTIONS`.** PR #694 deferred — needed an archetype translation table. | **RESOLVED in PR #696 (Wave 7.9 followup)**: added `V3_ARCHETYPE_TO_V4` map + `v3ArchetypeToV4` helper. `migrateV3ToV4` dedupes + normalizes; unknown tokens dropped. 18 new tests. |
| 2026-04-28 | Wave 7.9 followup audit P1 #10 (ColorSwatch V4=18 vs SettingsStyle=36 divergence) | `src/types/styleProfile.ts` (COLOR_SWATCHES) + `src/pages/settings/SettingsStyle.tsx` (COLOR_MAP) | **V4 has 18 curated swatches; SettingsStyle historically rendered 36. `migrateV3ToV4` passed favoriteColors through raw, polluting V4 vocab with V3-only names (`cobalt`, `coral`, `gold`, ...) that V4 readers can't parse.** PR #694 deferred. | **RESOLVED in PR #696**: added `V3_COLOR_TO_V4` map + `v3ColorToV4` helper for 19 V3-only color names → closest V4 swatch. SettingsStyle V4-aware: renders V4's 18 IDs when `isV4=true`, legacy 36 for V3 records. ChipMulti for `styleWords` mirrors writes to both `styleWords` AND `archetypes` on V4 records. |
| 2026-04-28 | Wave 7.9 followup audit P2 #7 (BatchCaptureStep buildGarmentInsert) | `src/components/onboarding/BatchCaptureStep.tsx:172-195` | **Manual 22-field INSERT instead of the `buildGarmentInsert` shared helper. Code drift between LiveScan/AddGarment and BatchCapture.** PR #694 deferred — `buildGarmentInsert` required non-null `analysis`. | **RESOLVED in PR #696**: extended `GarmentIntakeCandidate.analysis` to nullable + null-safe defaults in `buildGarmentInsert`. New `BuildGarmentInsertOptions.importedVia` preserves the `'batch_capture'` analytics taxonomy. ~50 LOC removed from BatchCaptureStep. |
| 2026-04-28 | Wave 7.9 followup audit polish #1 (RevealStep all-failed copy) | `src/components/onboarding/RevealStep.tsx` | **Cooking copy + shimmer read as a quiet lie when all 3 trial-gift renders failed and auto-retry didn't recover.** Per-PR #693 acceptance review. | **RESOLVED in PR #696**: new `allFailed` derived state + 3rd ternary branch (`title_all_failed` / `subtitle_all_failed` keys in en+sv). Shimmer hidden when allFailed. |
| 2026-04-28 | Wave 7.9 followup audit B.P0.2 (RevealComplete redirect loop) | `src/pages/Onboarding.tsx` `handleRevealComplete` | **`navigate('/')` was unconditional even when `completeOnboarding` threw. The throw path navigated to `/` while `onboarding_step` stayed `'reveal'`, ProtectedRoute redirected back to `/onboarding`, hydration showed Reveal again — redirect loop on every retry.** | **RESOLVED in PR #696**: `handleRevealComplete` re-throws on failure; explicit navigate dropped (relies on `onboardingCompleted` short-circuit's `<Navigate to="/" replace />`). RevealStep's `handleAdvance` now async + awaits `Promise.resolve(onComplete())` + resets `advancing` on rejection so the user can retry without page reload. |
| 2026-04-28 | Wave 7.9 followup audit D.P1.3 (RevealStep render_jobs status filter) | `src/components/onboarding/RevealStep.tsx:79-96` | **Watched-IDs query lacked the `status IN (...)` filter that CoachTour applies. Could pick up older terminal rows from prior sessions on accounts that retried.** | **RESOLVED in PR #696**: added `.in('status', ['pending','in_progress','succeeded','failed'])` parity with CoachTour but kept succeeded+failed in scope (Reveal needs both for display + auto-retry). |
| 2026-04-28 | Wave 7.9 followup audit A.P0 (grant_trial_gift error routing) | `supabase/functions/grant_trial_gift/index.ts` | **(a) Outer catch never called `recordError` — overload guard silently never tripped on transport-storm. (b) Every `ok:false` returned HTTP 500, masking deterministic 4xx validation errors.** | **RESOLVED in PR #696**: imports `recordError`; new `statusForReason()` maps `'invalid_amount'`/`'invalid_user'` → 400, others → 500. `recordError` called on outer catch + the 5xx branch only (4xx are deterministic and shouldn't trip overload). |
| 2026-04-28 | Wave 7.9 followup audit C.P1.2 (SettingsStyle V4 vocab corruption on edit) | `src/pages/settings/SettingsStyle.tsx:409` ChipMulti `styleWords` | **On V4 records, ChipMulti for `styleWords` wrote raw V3 archetype names (`streetwear`, `scandinavian`, `elegant`, `vintage`, `artsy`) which V4's `archetypes[]` doesn't recognize. Editing through Settings silently corrupted V4 vocab.** | **RESOLVED in PR #696**: SettingsStyle imports `ARCHETYPE_OPTIONS` + `COLOR_SWATCHES`. ChipMulti renders V4's 12 archetypes when `isV4=true`, legacy 12 V3-named for non-V4. Toggle writes to BOTH `styleWords` AND `archetypes` on V4 records so the V4 canonical field doesn't drift. |
| 2026-04-28 | Wave 7.9 followup housekeeping | `supabase/config.toml` + `supabase/functions/_shared/scale-guard.ts:526` + `src/components/onboarding/StudioSelectionStep.tsx` | **Dead-code remnants:** (a) `[functions.seed_wardrobe]` stanza orphaned in config.toml after PR #657 deleted the function; (b) `attempts: supabase.rpc ? undefined : undefined` dead ternary in `claimJob`; (c) `if (err instanceof RenderEnqueueError && err.clientNonce) { /* placeholder */ }` empty branch in StudioSelection's catch (pre-flight nonce capture handles this above). | **RESOLVED in PR #696**: deleted all three. |
| 2026-04-28 | P52 (spec divergence) | `supabase/functions/start_trial/index.ts` (intentional omission) | **Wave 8 spec said `start_trial` should also set `profiles.onboarding_started_at = NOW()`. Implementation INTENTIONALLY does not.** Two reasons: (a) Wave 7's migration `20260426120000_onboarding_state.sql` REVOKE'd UPDATE on `onboarding_started_at` from authenticated roles; only the `advance_onboarding_step` SECURITY DEFINER RPC writes it. Letting `start_trial` (service-role) write it would bypass the defense-in-depth design. (b) `advance_onboarding_step('language')` already sets the column on first onboarding transition via `COALESCE(onboarding_started_at, NOW())`. The 24h "onboarding boost" rate-limit window (P43) was designed to clock from user engagement (entering onboarding), not from signup — setting it at signup would mean a user who signs up and returns 23h later gets ~1h of boost instead of the intended 24h. Net: Stripe trial start time and onboarding-boost start time are intentionally decoupled. | Documented in code comment (lines 49-58 of start_trial/index.ts) and PR body. No future action needed unless the Wave 8 spec is later revised. |
| 2026-04-29 | P54 (deferred) | `supabase/functions/prefetch_suggestions/index.ts` | **Hybrid cron+single-user-trigger function intentionally NOT gated by `enforceSubscription` in Wave 8 PR 2.** The function has two execution paths: (a) cron batch mode (no caller — processes top-N users in one invocation) which MUST NOT be gated (it'd block the cron); (b) single-user-trigger mode (caller's JWT user.id must match body.user_id) which SHOULD be gated like other AI functions. Naive injection at the top of the handler would break the cron path. Needs a per-path gate AFTER the cron-vs-user-trigger branch — gate only the user-trigger else branch (same shape pattern P4 used for the parallel auth-gate split on this exact function). | Track for: Wave 8 follow-up prompt (after P55–P57 land). **Scheduled: post-launch** — Wave 8.9 closure deferred per user direction 2026-04-29; non-blocking. Per-path `enforceSubscription` gate on `prefetch_suggestions` can ship as a small follow-up PR before Wave 11. |
| 2026-04-29 | P53 (deferred polish) | `src/components/PremiumSection.tsx` (non-premium branch UX) + `src/components/LinkImportForm.tsx:55` (`wouldExceedLimit` rename) + `src/pages/LiveScan.tsx` (`SlotsPill` "0 left") | **Cosmetic / naming polish flagged by Wave 8 PR 2 code-review (all P3, none blocking).** (a) `PremiumSection` non-premium branch is now visually thin (only renders for locked / past_due users; should redesign alongside the dedicated Paywall page in P55 — combine inline upgrade CTA with full paywall presentation). (b) `LinkImportForm`'s `wouldExceedLimit = !isPremium` variable is misleading post-P53 (it reflects "user is locked" not a count overflow); rename to `isLocked` for clarity. (c) `SlotsPill` in LiveScan still renders "0 left" for locked users — semantically correct since paywall opens on first scan tap, but stylistically odd; could hide entirely when `state === 'locked'`. | Track for: Wave 8 P55 covers (a); post-launch UX polish PR for (b) + (c). **Scheduled: post-launch UX polish PR** — Wave 8.9 closure deferred per user direction 2026-04-29; non-blocking pure cosmetic. |
| 2026-04-29 | P56 (deferred — cross-currency display sync) | `src/lib/localizedPricing.ts` lines 22-23 (`no` / `da`) + lines 24-31 (`fi` / `de` / `fr` / `es` / `it` / `pt` / `nl` / `fa`) + lines 32-35 (`en` / `en-gb` / `pl` / `ar`) | **Localized display prices diverge from the SEK ledger amount that Stripe actually charges.** Stripe is configured with a single SEK price ID per plan tier (119 SEK/month, 899 SEK/year) — every user, regardless of locale, is charged that exact SEK amount. The `localizedPricing` map currently shows: `no` 79 NOK / 699 NOK, `da` 59 DKK / 499 DKK, EU-€ locales 7.99 € / 69.99 €, `en` $7.99 / $69.99, `en-gb` £7.99 / £69.99, `pl` 29.99 zł / 249 zł, `ar` 25 د.إ / 219 د.إ. None of these correspond to the actual ledger amount post-Wave-8 — a Norwegian user sees "79 kr/month" but their card gets charged 119 SEK ≈ 122 NOK. P56 only updated `sv` to match the SEK ledger. **Three resolution paths**: (1) Configure Stripe with multi-currency prices (one Price per locale's currency) and let `create_checkout_session` pick the right Price by locale — proper localized billing but adds a 14-currency Price-ID matrix; (2) Drop locale-specific display and show "119 kr (~$11)" everywhere with a contextual exchange-rate hint — honest about the SEK billing but breaks the localized-feel; (3) Keep current 14-locale display BUT update each entry to the SEK-equivalent amount in the local currency at a fixed exchange rate (e.g. `no: 119 NOK / 899 NOK`) — visually consistent but legally questionable since they're charged in SEK. | Track for: Wave 8.5 or post-launch — needs business decision (multi-currency vs SEK-everywhere vs FX-equivalence). Sweden launch market unblocked by P56's `sv` update. **Scheduled: post-launch** — Wave 8.9 closure deferred per user direction 2026-04-29; needs business decision on multi-currency vs SEK-everywhere vs FX-equivalence before any code change. |
| 2026-05-05 | M1 PR 1 (add-piece parity) | `mobile/src/screens/AddPieceStep1.tsx`, `mobile/src/screens/AddPieceStep2.tsx`, `mobile/src/screens/AddPieceStep3.tsx`, `mobile/src/components/GarmentSaveChoiceSheet.tsx` | **Codex round 11 P1 on PR #725 — entire add-piece flow uses hardcoded English strings, not `t(...)` from `mobile/src/lib/i18n.ts`.** `grep -c useTranslation` returns 0 across all three Step screens; the new `GarmentSaveChoiceSheet` matches the surrounding pattern. Onboarding/auth/paywall/splash already use t() so the infra exists; the add-piece flow predates the i18n adoption. Wrapping just the choice sheet would put the user on a localized sheet between two English screens — net regression, not progress. | Track for: dedicated mobile add-piece i18n PR. Scope: thread `useTranslation` through Step 1/2/3 + the choice sheet, add a new `addPiece.*` namespace to `mobile/src/i18n/locales/en.ts` (and any other locale files mobile ships before launch). Not blocking M1 PR 1 — the surface is already English-only, this PR doesn't regress that. |
| 2026-04-26 | P42+P44 | `src/lib/schemas.ts:profileSchema`, `src/pages/Onboarding.tsx:64-71`, `supabase/migrations/20260426120000_onboarding_state.sql` (advance_onboarding_step RPC) | **Codex round 1 P1+P1+P2 on PR #683.** Three findings, all real: (a) **P1 schema strip** — `useProfile()` runs `safeParse(profileSchema, ...)`. `profileSchema` (`src/lib/schemas.ts:29-48`) did NOT define `onboarding_step` (or use `.passthrough()`), so zod's default strip-mode silently dropped the key. The new ProtectedRoute gate then read `step` as `undefined` for every parsed profile and redirected ALL users (incl. ones with DB row already `'completed'`) to `/onboarding` — exactly the loop the rollout-bridge fix tried to prevent. (b) **P1 actual completion path** — my Wave 7 PR 1 rollout-bridge fix went into `useOnboarding.ts` but `useOnboarding` is NOT used by runtime app screens (grep `useOnboarding\(` confirmed only the hook file + tests). The actual completion happens in `src/pages/Onboarding.tsx:64-71`'s LOCAL `completeOnboarding()` function — that's the runtime path. So users finishing the OLD flow under the new gate would still write only the legacy preferences flag. (c) **P2 SECURITY DEFINER race** — `advance_onboarding_step` did read-then-write without locking. Two concurrent forward transitions (multi-tab, retry storm) could both pass the read-then-validate check from the same `v_current` snapshot, then race the UPDATE — slower writer overwrites with a LOWER target step, breaking forward-only. | **RESOLVED in this PR (Codex round 1 fix-up commit)**: (a) Added `onboarding_step` (string), `onboarding_garment_count` (number), `onboarding_started_at` (string), `onboarding_completed_at` (string), all `.nullable().optional()`, to `profileSchema`. zod now preserves these keys through `safeParse`. (b) Extracted the RPC call into `src/lib/advanceOnboardingStep.ts` (typed wrapper with `OnboardingStep` union + `AdvanceOnboardingStepResult` interface). Both `useOnboarding.completeOnboarding()` AND `Onboarding.tsx`'s LOCAL `completeOnboarding()` now call the wrapper — single source of truth. The wrapper holds the transitional `as any` cast (auto-gen types.ts regens post-merge per CLAUDE.md hard rule). 6 dedicated wrapper tests cover correct args, success/no-op/backwards/error/null-data paths. (c) Added `FOR UPDATE` to the SELECT inside `advance_onboarding_step` — concurrent transitions on the same row now serialize: the second call blocks until the first commits, re-reads the post-write step, and validates against the new state. Acceptable cost — onboarding flow is low-frequency, lock window is tiny. Verification: tsc 0 errors, eslint 0 warnings, build clean, vitest 1367/1367 (39 net new tests vs prior 1328: 19 scale-guard + 10 ProtectedRoute + 4 useOnboarding + 6 advanceOnboardingStep wrapper), `db push --dry-run` clean post-edit. |
| 2026-05-05 | V0 | `mobile/` (jest-expo + RN testing library) | **Unit-test infrastructure (jest-expo + React Native Testing Library) deferred per V0 wave spec.** V0 ships CI gates only; no unit-test plumbing. The 6 V0 jobs (typecheck / lint / expo-doctor / bundle-size / deno-check / migration-smoke) cover regressions for now. | Track for: post-launch testing wave. Add jest-expo + `@testing-library/react-native` once mobile is in production and unit-test ROI justifies the maintenance. |
| 2026-05-05 | V0 | `mobile/` typecheck baseline | **`feat/mobile-rn-app` HEAD shipped with 130 unresolved TypeScript errors and 5 missing runtime npm deps the source already imports.** Surfaced when V0's typecheck gate ran for the first time. Errors: 11 missing `ThemeTokens` fields (destructive×11, scrimBg×6, scrimFg×4, destructiveSoft×1), 19 missing icon exports across `mobile/src/components/icons.tsx`, 4 missing `RootStackParamList` routes (Auth/Onboarding/Paywall/OutfitGenerate) read by 6+ screens, 5 missing modules (`expo-haptics`, `expo-localization`, `expo-image-manipulator`, `expo-file-system`, `@sentry/react-native`). Branch had been merging without typechecking. | **RESOLVED in V0 PR**: scope expanded under user authorization "quality and zero error margin before we start." Added missing deps via `npx expo install`, added all 19 icons + 4 theme tokens + 4 routes, refined route param types, exported `AddPiecePhoto` + `WardrobeFilters` types. V0's typecheck job now passes with 0 errors. Going forward, the V0 CI gate prevents regression. |
| 2026-05-05 | V0 | `mobile/eslint.config.js`, wave file `m-v0-ci-foundations.md` | **V0 wave spec called for `mobile/.eslintrc.cjs` (legacy ESLint config), but eslint-config-expo v10 (the version expo-doctor demands for SDK 54) requires ESLint v9, which uses flat config (`eslint.config.js`).** Wave's `--ext .ts,.tsx` flag also doesn't exist in v9. Implementer used flat config with `eslint-config-expo/flat`, dropped `--ext`, replaced with `"src/**/*.{ts,tsx}"` glob. | Justified deviation, documented in `eslint.config.js` header comment. Wave file should be updated to reflect actual ESLint version pinning if a future agent re-implements V0. |
| 2026-05-05 | V0 | `.github/workflows/mobile-ci.yml` bundle-size step | **Wave snippet uses `expo export -p ios -o /tmp/expo-export`. Expo CLI 54 removed the `-o` short-flag in favour of `--output-dir`.** The wave's snippet would fail with "unknown option: -o". | Replaced with `--output-dir /tmp/expo-export` in the workflow + comment explaining the change. |
| 2026-05-05 | V0 | `mobile/eas.json` | **`appVersionSource: "remote"` + production `autoIncrement: true` added beyond V0 wave skeleton.** The wave's eas.json doesn't specify these but EAS Build needs them for App Store submission tracking. Carried over from the existing `eas.json` (pre-V0 file already had them). | Intentional. Better to keep them than strip and re-add at M43. |
| 2026-05-05 | V0 | `mobile/src/components/icons.tsx` (`MoreIcon`) | **`MoreIcon` lacks `strokeLinecap` prop, minor stylistic drift from the file's other icons (most use `strokeLinecap="round"`).** | Cosmetic, no functional impact. Three filled circles render correctly without stroke caps. Defer to a polish pass. |
| 2026-05-05 | V0 | `.github/workflows/mobile-ci.yml` migration-smoke step | **`supabase db push --linked --dry-run --password "$SUPABASE_DB_PASSWORD"` lacks `--yes` flag.** With `--dry-run` no DB write occurs, but a future Supabase CLI version could start prompting for confirmation even on dry-runs. | Pin Supabase CLI version `1.226.4` (matches root `ci.yml`) prevents the prompt today. Revisit if/when CLI is bumped. |
| 2026-05-05 | V0 | `mobile/src/navigation/RootNavigator.tsx` | **4 new routes (Auth, Onboarding, Paywall, OutfitGenerate) registered as `PlaceholderScreen` despite real `AuthScreen.tsx`, `OnboardingScreen.tsx`, `PaywallScreen.tsx`, `OutfitGenerateScreen.tsx` existing in `mobile/src/screens/`.** Matches the file's existing 32-placeholder pattern ("swap in real implementations as they ship"). Means navigate('Auth') etc. mount placeholders, not the real screens, at runtime. | Track for: dedicated screen-mounting pass that wires all ready-to-mount real screens (would also affect StyleChat, Settings, OutfitDetail, etc.). Out of V0 scope (which is CI-only); not regression because runtime behaviour pre-V0 was navigate-to-typecheck-error. |
| 2026-05-05 | V0 | `eslint.config.js` (root) | **Root eslint ignored `dist`, `supabase/**`, `mobile/**`, `design_handoff_burs_rn/**` but not `.claude/**`** — agent-managed git worktrees there hold stale source trees that surface thousands of `no-explicit-any` / `no-console` / `prefer-const` errors when the husky pre-commit `npx eslint . --max-warnings 0` runs. Pre-existing config gap; surfaced for the first time when V0's commit (largest mobile-only change in a while) ran the hook. | **RESOLVED in V0 PR**: added `.claude/**` to root `eslint.config.js` ignores following the same pattern as the existing excludes. Root eslint now skips agent worktrees; web pipeline pre-commit hook runs clean again. |

| 2026-05-05 | M2 (P2) | `mobile/src/hooks/useSignedUrl.ts` `fetchAndCacheSignedUrl` | **`AbortSignal` doesn't reach `supabase.storage.createSignedUrl`.** supabase-js's storage client doesn't accept `AbortSignal` on `createSignedUrl` / `createSignedUrls`. The `signal?.aborted` check inside the cache layer only short-circuits the post-await write into the Map; the network round-trip to the storage REST endpoint still completes. Acceptable today — soft-failure contract returns `null` to consumers which render gradient placeholders — but worth knowing if we ever surface progress UI per-URL. | Track for: future supabase-js bump that exposes `AbortSignal` on storage operations (or for a manual `fetch` rewrite if the Supabase team doesn't add it). Low priority. |
| 2026-05-05 | M2 (P2) | `mobile/src/hooks/__tests__/useSignedUrlCache.test.ts` | **Wave file mentions a vitest test file for the cache hook; deferred per the wave file's "skip if no jest infra yet; track in findings-log.md" allowance.** Mobile RN doesn't have a jest/vitest harness wired yet. | Track for: mobile test-infra pass (TBD). Add cache-hit / dedup / TTL-expiry / clear-on-signOut cases once a runner is in place. |
| 2026-05-05 | M2 (P2) | `mobile/src/hooks/useSignedUrl.ts` inflight latch | **`inflight.set(key, promise)` runs after the IIFE has already started executing.** JS is single-threaded so the first `await` inside the IIFE is what yields control, and that always happens after the synchronous `inflight.set`. Worth a comment for future readers — the ordering relies on V8 microtask scheduling rather than explicit synchronization. | Track for: code clarity polish. Add a comment OR refactor to a `Deferred<T>` shape if a similar pattern lands elsewhere. |
| 2026-05-05 | M2 (P1) | `mobile/src/hooks/useSignedUrl.ts` `fetchAndCacheSignedUrl` + `useSignedUrls` queryFn | **Sign-out race repopulating the module-scope cache.** `clearSignedUrlCache()` only wiped the currently-known `urlCache` + `inflight` entries; in-flight `createSignedUrl` promises started before sign-out still resolved afterwards and ran the post-await `urlCache.set(...)`, landing user A's signed URL (with embedded JWT) in the cache during user B's session. Surfaced by Codex P1 round 1 on PR #729. | **RESOLVED in PR #729 (Codex round 1)**: added monotonic `cacheGeneration` counter; `clearSignedUrlCache()` increments it before `urlCache.clear()`; `fetchAndCacheSignedUrl` snapshots the generation pre-await and skips the post-await `urlCache.set` when the counter has moved; same guard threaded through the bulk `useSignedUrls` queryFn. In-flight callers still receive the URL transiently (their queryFn fulfils), but `queryClient.clear()` drops that result; the module-scope cache stays clean across sessions. |
| 2026-05-05 | M2 (P2) | `mobile/src/hooks/useSignedUrl.ts` cache key vs `render_garment_image` storage path | **50-min cache TTL serving stale URL after render regeneration.** `render_garment_image` upserts the rendered image at the stable path `${user_id}/${garment.id}/rendered.ext`. The cache is keyed on path alone, so a regenerate flow that fires while the cache entry is still fresh would re-mint the same path and the M2 hook would hand `<Image>` the prior signed URL — RN's native image cache could then keep serving the old bytes until TTL expiry. Surfaced by Codex P2 round 1 on PR #729. | **RESOLVED in PR #729 (Codex round 1)**: new `bustSignedUrlCache(path \| path[])` export removes the entry from `urlCache` + `inflight`. Wired into `mobile/src/hooks/useRenderJobStatus.ts`'s terminal-success effect — after the worker reaches a terminal status, the snapshot's `resultPath` is busted before the garment-cache invalidation refetches the row, guaranteeing the next `useSignedUrl(rendered_image_path)` read mints a fresh signed URL. Future regenerate flows on other surfaces should call the same helper. |
| 2026-05-05 | M2 (P2) | `mobile/src/hooks/useSignedUrl.ts` inflight latch ordering | **`inflight.set(key, promise)` runs after the IIFE has already started executing.** Original code-reviewer note flagged the implicit reliance on V8 microtask scheduling; future readers might miss why this is safe. | **RESOLVED in PR #729 (Codex round 1)**: explanatory comment added at the inflight-set call site documenting that the IIFE body up to its first `await` runs synchronously, so concurrent callers always see the inflight entry before joining. |
| 2026-05-06 | M2 (P2) | `mobile/src/hooks/useSignedUrl.ts` `bustSignedUrlCache` | **Module-cache bust didn't invalidate React Query.** `bustSignedUrlCache(path)` only deleted from `urlCache` + `inflight`; observers of `['signed-url', BUCKET, path]` kept their `query.data` URL fresh until `staleTime` (50 min) elapsed, so the M1 render-complete bust took no visible effect. Surfaced by Codex P2 round 2 on PR #729. | **RESOLVED in PR #729 (Codex round 2)**: signature changed to `bustSignedUrlCache(queryClient, path \| path[])`. Module-cache delete is now followed by `queryClient.invalidateQueries({ queryKey: ['signed-url', BUCKET, p] })` per path, plus a predicate-based sweep over `['signed-urls', BUCKET, ...]` bulk entries whose path-array contains a busted path. M1's terminal effect now passes its `qc` into the call. |
| 2026-05-06 | M2 (P2) | `mobile/src/hooks/useSignedUrl.ts` `useSignedUrl` / `useSignedUrls` `staleTime` | **Static `staleTime: TTL_MS` outlived the underlying signed URL.** A static 50-min freshness window started fresh on each cache-hit observation, so a screen remounted near the end of the module cache's TTL kept serving the same URL well past Supabase's 60-min expiry, surfacing as broken `<Image>`s until the query happened to go stale. Surfaced by Codex P2 round 2 on PR #729. | **RESOLVED in PR #729 (Codex round 2)**: `staleTime` is now a function that reads the cached entry's remaining lifetime and returns `Math.max(0, expiresAt - now - 30 s)` so refetch fires before the URL 401s. Same logic for the bulk variant — picks the soonest-expiring entry in the path list. Returns `TTL_MS` when the cache is not yet populated so the queryFn gets a chance to run. |
| 2026-05-06 | M2 (P2) | `mobile/src/hooks/useSignedUrl.ts` `fetchAndCacheSignedUrl` IIFE | **Single observer's abort poisoned joined callers.** The in-flight Promise closed over the first caller's `AbortSignal`; if that observer unmounted while Supabase was still responding, every caller that joined `inflight.get(key)` received `null` because the IIFE's `if (signal?.aborted) return null` short-circuit ran. In a fast-scrolled list this manifested as visible placeholder gaps for cells that hadn't unmounted. Surfaced by Codex P2 round 2 on PR #729. | **RESOLVED in PR #729 (Codex round 2)**: dropped the `signal?.aborted` short-circuit inside the shared IIFE — the IIFE always resolves to a real URL (or `null` on transport error) so joined observers receive the value. React Query handles per-observer abort externally for the unmounting caller. Same change applied to the bulk `useSignedUrls` queryFn for consistency. The `signal` parameter is no longer threaded into `fetchAndCacheSignedUrl`. |
| 2026-05-06 | M2 (P2) | `mobile/src/hooks/useSignedUrl.ts` `bustSignedUrlCache` vs in-flight pre-bust fetches | **Per-path bust didn't cancel pre-bust in-flight writes.** A `useSignedUrl(rendered_path)` request issued before the render completed would still resolve after `bustSignedUrlCache(qc, rendered_path)` had cleared the maps and run its `urlCache.set(...)`, repopulating the module cache with a URL minted pre-regeneration. Codex P2 round 3 on PR #729. | **RESOLVED in PR #729 (Codex round 3)**: added per-path generation counter `pathGenerations: Map<key, number>`. `bustSignedUrlCache` now `bumpPathGeneration(key)` before deleting the map entries. Both `fetchAndCacheSignedUrl` and the bulk `useSignedUrls` queryFn snapshot each touched path's counter pre-await and gate the post-await `urlCache.set(...)` on both the global session counter AND the per-path counter staying unchanged. `clearSignedUrlCache()` also wipes the per-path map (the global bump already invalidates every pending write). |
| 2026-05-06 | M14 (deferred) | `mobile/src/hooks/useRecordMemoryEvent.ts` | **Free-text "forget I prefer X" path NOT shipped.** The `reject_outfit` signal requires an `outfit_id`; "forget that I prefer warm tones" has no target outfit. There's no `preference_correction` signal type server-side. Garment-level forget (`never_suggest_garment`) is fully wired. | Track for: post-M14 follow-up. Either add a `preference_correction` signal type to memory_ingest's accepted set + extend `style-memory-signals.ts`, OR teach `style_chat`'s preference-extraction to honor an in-chat correction phrase that emits a more targeted signal. |
| 2026-05-07 | M19 PR #745 (deferred — locale parity) | `mobile/src/i18n/en.ts` (visualSearch.* namespace) + `mobile/src/i18n/locales/*` (all non-en) | **M19 visual-search i18n keys ship en-only.** The `visualSearch.*` namespace in `mobile/src/i18n/en.ts` adds ~25 new keys (eyebrow/title/CTAs/permissions/error copy/accessibility hints/empty-row copy). `DICTIONARIES` in `mobile/src/lib/i18n.ts` falls every non-en locale back to en until per-locale dictionaries land — mobile has no `mobile/src/i18n/locales/` directory yet, only a single `en.ts` source. M19 is the third wave (after M14, M17, M18) to add user-facing strings without parity translations. Sweden-first launch needs sv at minimum. | Track for: M33 owns the locale pass per existing convention. This entry consolidates the M14/M17/M18/M19 i18n debt so the M33 implementer has a single inventory; check the keys actually surfaced by each wave's screen against the en.ts namespace headers (M14 outfit rules, M17 OutfitDetail composition helpers, M18 photoFeedback.*, M19 visualSearch.*). |
| 2026-05-07 | M19 PR #745 (deferred — sentinel collision) | `mobile/src/lib/edgeFunctionClient.ts` + 26 caller files (hooks/screens) emitting `'subscription_required'` | **The `'subscription_required'` paywall sentinel is a plain English string** that any error-message surface could collide with. M19 review surfaced the risk while looking at `useVisualSearch` + `VisualSearchScreen.tsx`'s sentinel comparison. The string appears in 26 files across hooks (`useGenerateOutfit`, `useOutfitPool`, `useWeekGenerator`, `usePhotoFeedback`, `useStyleChat`, `useMoodOutfit`, `useVisualSearch`, etc.) and consumer screens. A future server payload that returns `error: 'subscription_required'` literally (instead of a 402) would route the user into Paywall when the actual problem is something else; conversely a translation collision could swallow a real paywall trigger. Risk is low today — `EdgeFunctionSubscriptionLockedError` is the only producer in the M9 wrapper — but the sentinel should be hardened to a non-collidable value (e.g. `'__BURS_SUBSCRIPTION_REQUIRED__'`) the first time the cross-cutting refactor cost is acceptable. | Track for: dedicated centralization PR. Scope: rename the literal in `edgeFunctionClient.ts`'s thrown error and every consumer's comparison; add a single exported constant `SUBSCRIPTION_SENTINEL` from a shared module so future agents can't reintroduce string literals. Not blocking M19 — single-source defines + grep coverage make a future migration mechanical. |
| 2026-05-07 | M20 PR #TBD (deferred — post-launch) | `mobile/ios/` (no Share Extension target yet) + `mobile/src/screens/ImportFromLinkScreen.tsx` route (accepts `initialUrl`) | **Native iOS Share-sheet integration deferred per the M20 wave file** ("iOS Share extension consideration: deferred — track in findings-log.md as 'post-launch: native share-sheet integration.'"). The mobile screen already accepts an `initialUrl?: string` route param so a future Share Extension can deep-link straight into the textarea pre-filled, but the actual native target / app group / deep-link wiring (`burs://import-from-link?url=...` or universal link) is not built. Android equivalent (`android:intent-filter` for `android.intent.action.SEND` text/plain) likewise out of scope. M20 ships the in-app paste flow only; users on a product page in Safari paste the URL manually. | Track for: post-launch (separate PR after M44). Scope: (1) iOS Share Extension target with App Group entitlement to write the shared URL into AsyncStorage; (2) cold-start handler in `App.tsx` that reads the shared URL and navigates to `ImportFromLink` with `{ initialUrl }`; (3) Android `SEND` intent-filter parity; (4) `RootNavigator.linking` config extension to route the deep link. Acceptance: tap Share → BURS → garment lands in wardrobe without leaving Safari. |
| 2026-05-07 | M20 PR #746 (deferred — doc drift) | `CLAUDE.md` ("Hard rules" → "`mobile/src/i18n/locales/{en,sv}.ts` and `mobile/src/i18n/locales/*` are append-only") vs reality at `mobile/src/i18n/en.ts` | **Doc drift: CLAUDE.md references `mobile/src/i18n/locales/*` but the only mobile locale file in the tree is `mobile/src/i18n/en.ts` (single source, no `locales/` directory yet).** Surfaced by Codex round 1 P2.7 on PR #746 while reviewing M20's i18n additions. The append-only intent applies fine to `mobile/src/i18n/en.ts` today; the path drift just means agents looking for the directory get an empty match. (Ties into the M19 P-deferred locale-parity entry above — the `locales/*.ts` per-language files don't exist yet because every wave so far has appended into the single en.ts source.) | Track for: a docs hygiene PR that updates CLAUDE.md's "Hard rules" bullet to read `mobile/src/i18n/en.ts` (and any future per-locale files in `mobile/src/i18n/locales/`) so the path matches reality without losing the future-proofing for when the M33 locale pass introduces `locales/sv.ts` etc. Not blocking; not behavioral. |
| 2026-05-07 | M23 PR #749 (deferred — wave gate) | `supabase/functions/shopping_chat/index.ts` (server-side product-tool emission) + `mobile/src/lib/styleChatContract.ts` (`shopping_results` parser path, forward-compat) | **M23 manual acceptance gate ("3+ result cards") not satisfiable today.** The deployed `shopping_chat` edge function emits text-only output; it does not yet emit a `shopping_results` envelope with structured product cards. Mobile shipped the full forward-compat path (`parseShoppingResultCards` in styleChatContract.ts + `ShoppingResultCard.tsx` primitive + render path under the assistant bubble), so the moment the server starts emitting cards the UI lights up without a mobile redeploy. R1 P1-1. | Track for: server-side wave that wires product-search tool calling into `shopping_chat` (likely a Gemini function-call schema + an external product-search shim or a curated catalog). Acceptance: a "buy me X" prompt returns a payload whose `shopping_results` array has 3+ entries that mobile renders inline. Not a code bug in M23 — purely a wave-file drift item where the manual gate runs ahead of the server contract. |
| 2026-05-07 | M23 PR #749 (deferred — defense-in-depth) | `mobile/src/lib/styleChatContract.ts` (URL allowlist for `product_url`) + Linking.openURL surface in `StyleChatScreen.tsx` | **URL allowlist permits private-IP/loopback https hosts.** The current `parseShoppingResultCards` accepts any `https://...` URL — including `https://127.0.0.1`, `https://10.x.x.x`, `https://192.168.x.x`, `https://[::1]`, `https://localhost`. M19 visual-search precedent established the same shape (https-only, no IP block) and the M23 wave file followed that precedent. Today the server controls `product_url` end-to-end so the attack surface is theoretical, but if external scrapers or third-party product catalogs ever feed into `shopping_chat`, an attacker-controlled response could redirect a tap to a private network endpoint. R2 P2. | Track for: shared URL-hardening PR (covers M19 + M23 simultaneously). Scope: a small reject-list helper in a shared module that strips loopback/private-IP/IPv6-link-local hosts, called from `parseShoppingResultCards` and any future structured-link parser. Trigger: when external scrapers / non-server-curated `product_url` values land in the contract. |
| 2026-05-07 | M23 PR #749 (deferred — locale-aware price) | `mobile/src/components/ShoppingResultCard.tsx` (`shoppingChat.cardPriceTemplate`) + `mobile/src/i18n/en.ts` | **Hardcoded `{amount} {currency}` template ignores per-locale price formatting conventions** (separator characters, currency-symbol position, decimal precision). Sweden launch wants `199 kr` for SEK rendered before the amount in some contexts; en-US wants `$199.00`; en-GB wants `£199.00`. Today the card renders `199 SEK` for every locale. R2 P2. | Track for: M33 (mobile i18n locale pass) — folds into the same locale-parity backlog the M14/M17/M18/M19 entry already covers. The fix is `Intl.NumberFormat(locale, { style: 'currency', currency: card.price.currency })` once the locale dictionaries land. Not blocking M23. |
| 2026-05-07 | M18 (deferred) | `mobile/src/lib/memoryEvents.ts` + `supabase/functions/_shared/style-memory-signals.ts` | **`try_on_outfit` memory-event creator NOT shipped.** Wave file asked for a `tryOnOutfitEvent(outfitId, source)` typed creator emitted from the photo-feedback flow so the comparison signal could ride into pair-memory. Verification of the canonical taxonomy (`CanonicalStyleMemorySignal` union, 11 entries: `save_outfit`, `unsave_outfit`, `rate_outfit`, `wear_outfit`, `skip_outfit`, `reject_outfit`, `swap_garment`, `quick_reaction`, `never_suggest_garment`, `like_pair`, `dislike_pair`) shows `try_on_outfit` is NOT in the union or the legacy→canonical map. The `memory_ingest` edge function would 4xx the event before it reached `feedback_signals`, AND emitting an unsupported `signal_type` from a typed creator would violate the file's "All future writers MUST emit one of these" contract. The photo-feedback UI is the bulk of M18's value and shipped without the memory-event part. | Track for: a follow-up that either (a) extends the canonical taxonomy with `try_on_outfit` (server-side `style-memory-signals.ts` + `memory_ingest` validation + downstream `burs_style_engine` / `style_chat` reads decide what pair-memory delta a try-on signal should produce — likely a small positive nudge weaker than a worn signal but stronger than a save), then ships the mobile `tryOnOutfitEvent` creator + emit from `PhotoFeedbackScreen` on a successful analyze; or (b) reuse an existing signal (e.g., `quick_reaction` with `value: 'try_on'`) — but that conflates orthogonal axes (try-on vs preference) and isn't recommended. Path (a) is the right one. |
| 2026-05-07 | M24 PR #750 (deferred — schema versioning) | `mobile/src/hooks/usePickMustHaves.ts` (`ShoppingList` shape + `parseShoppingList`) + `profiles.preferences.shopping_list_jsonb` JSONB | **M24 shopping_list JSONB shape has no `version` field.** The shipped `ShoppingList` interface is `{ entries, updated_at }` with no schema version marker. Future shape migrations (e.g. adding `target_price`, `purchased`, source-link fields) will require heuristic sniffing on read (presence/absence of fields) to decide which migration step to apply. Acceptable today — shape is small and parser is permissive — but the cost of adding `version: 1` is near-zero and pays off at the first non-trivial change. | Track for: add a `version: 1` field on the next non-trivial shape change (e.g. when M24's follow-ups introduce `purchased: boolean` or external store links). Bump to `version: 2` going forward. Mobile parser needs a `version === 1 ? parseV1 : parseLegacy` switch at that point. |
| 2026-05-07 | M24 PR #750 (deferred — concurrency) | `mobile/src/hooks/usePickMustHaves.ts` `useSaveShoppingList` mutationFn + any other writer of `profiles.preferences.*` | **Read-modify-write race on `profiles.preferences` across concurrent writers.** M24's `useSaveShoppingList` reads `preferences`, merges in `shopping_list_jsonb`, writes back. Any other writer (`OnboardingScreen` style preferences, `useUpdatePreferences`, future `useUpdateLanguage`, etc.) hitting the same row in parallel can clobber the other's update. Mobile is mostly sequential (single user, single device, single screen at a time) so collisions are rare in practice; multi-device sessions or future RPC fan-out would surface the race. | Track for: introduce a `update_user_preferences(key TEXT, value JSONB)` Postgres RPC that does an atomic `jsonb_set` server-side. All client preference writers route through it; no read-modify-write needed. Safe migration: ship the RPC + dual-write path → migrate writers one at a time → drop client RMW. Not blocking launch. |
| 2026-05-07 | M24 PR #750 (deferred — forward-compat) | `mobile/src/hooks/usePickMustHaves.ts` `parseEntry` | **`parseEntry` strips unknown fields on read.** The defensive parser whitelists `{id, gap_id, category, priority, notes, added_at}` and reconstructs a fresh `ShoppingListEntry` object from those keys only. If web (or a future mobile version) adds `target_price` / `purchased` / `vendor` to the shape, an older mobile build round-tripping a save would silently drop those fields back to `null` on every read-modify-write. Acceptable today — single-platform writer (mobile is the only writer of this JSONB key) — but locks in a one-way version step. | Track for: switch to spread-preserve (`{ ...obj, ...validatedFields }`) when web/mobile shape diverges, OR (preferred) couple to the schema-version migration above so the parser knows which fields are valid per version. Trigger: when the web app or any other writer starts mutating `shopping_list_jsonb`. |
| 2026-05-07 | M24 PR #750 (deferred — locale parity) | `mobile/src/i18n/en.ts` (pickMustHaves namespace) + `mobile/src/i18n/locales/sv.ts` (does not exist) | **M24 pickMustHaves i18n keys ship en-only.** New keys added in M24 review fix-up: `loadError`, `remove`, `removeAriaLabel`, `savedCountTemplate.one`, `savedCountTemplate.other`, `selectedCountTemplate.one`, `selectedCountTemplate.other`. Mobile is en-only at present per existing wave-file convention; the M33 locale pass owns translation parity for the entire mobile namespace. This entry consolidates M24 into the existing M14/M17/M18/M19 i18n debt for the M33 implementer. | Track for: M33 (mobile i18n locale pass). Sweden-first launch needs `sv.ts` parity for at least the user-facing pickMustHaves strings. Append to the cross-wave inventory the M19 entry above already maintains. |
| 2026-05-07 | M27 PR #753 review (deferred — design assets) | `mobile/src/screens/onboarding/PhotoTutorialStep.tsx` `ExampleTile` | **PhotoTutorial uses placeholder iconography (TshirtIcon / CameraIcon in colored Views) for the good vs bad example tiles; real garment-photo PNGs needed before public launch.** The wave file authorized "simple colored View placeholders with iconography for now" — semantic meaning reads (accent-tinted = good, destructive-tinted = bad), but a real photo of a flat-lay garment vs a cluttered moody shot would land the message faster. No `mobile/assets/photo-tutorial-good.png` / `photo-tutorial-bad.png` exist yet. | Track for: design-driven follow-up. Once design produces the two PNG assets (good + bad reference images sized for ~140pt square at @3x), drop them in `mobile/assets/onboarding/` and replace the inline `<TshirtIcon />` / `<CameraIcon />` blocks with `<Image source={require(...)} />`. Component shape (`ExampleTile`) is locally scoped so the swap is single-file. |
| 2026-05-07 | M27 PR #753 review (deferred — replay UX) | `mobile/src/screens/SettingsScreen.tsx` (no entry) + `mobile/src/hooks/useFirstRunCoach.ts` (no `replayTour()` exposed) | **Coach tour skip is irreversible without a Settings → Replay entry.** M27 R1 added a confirm dialog so accidental skip taps no longer kill the tour, but a user who skips intentionally and later wants to revisit has no surface to do so. Out of scope for M27 (Settings is its own wave). The hook persists `coach_tour_completed_at` in `profiles.preferences` and `currentStep` in AsyncStorage — a future Replay action would clear both. | Track for: M-Settings (post-launch). Add a `replayCoachTour()` method on `useFirstRunCoach()` that nulls `preferences.coach_tour_completed_at`, removes the AsyncStorage step key, and resets the cached step to 0; surface it as a `SettingsRow` under "Help" or "Onboarding". Acceptance: tap → dismiss Settings → next tab change replays the 4-step sequence. |
| 2026-05-07 | M27 PR #753 review (deferred — a11y device testing) | `mobile/src/components/CoachOverlay.tsx` (`accessibilityViewIsModal` + caption block focus order) | **VoiceOver focus order tuning needs device testing.** The overlay sets `accessibilityViewIsModal` so screen readers are scoped to the modal content, but the tab order (progress pill → target descriptor → caption block → Skip → Next) hasn't been verified on hardware. iOS may differ from Android — RN's `Modal` focus-trap implementations diverge per platform. Defer because device testing isn't gated by code review and the existing semantics (target rect labeled with caption, dialog role via `accessibilityRole="alert"` fallback since RN doesn't expose 'dialog') are best-available without empirical validation. | Track for: a11y audit pass on physical hardware (iOS + Android, VoiceOver + TalkBack). If focus order is wrong, add `accessibilityElementsHidden` / `importantForAccessibility="no-hide-descendants"` on the four scrim quadrants OR explicit `accessibilityElementsOrder` (Android) so the reading order is progress → caption → buttons. |

| 2026-05-07 | M28 PR #754 review (deferred — atomic JSONB writes) | `mobile/src/hooks/useTravelCapsules.ts` `useUpdateTravelCapsuleMustHaves` + `useUpdateTravelCapsulePackedState` | **Read-modify-write race on `travel_capsules.result` JSONB.** Both must_haves and packed_state mutations select `result` then write the merged blob back. Cross-device or rapid same-device writes can clobber each other. M28 review fix added a defensive `updated_at`-eq guard in the WHERE clause + cache refresh + `TRAVEL_CAPSULE_SAVE_CONFLICT` sentinel + screen-level Alert/refetch on conflict. The ideal fix is an atomic `jsonb_set` RPC that takes `(capsule_id, key, value)` and writes the field server-side without RMW. | Track for: post-launch migration when the user is open to one. Scope: Postgres function `update_travel_capsule_field(capsule_id uuid, key text, value jsonb)` using `jsonb_set` + `SECURITY INVOKER` so RLS still enforces `auth.uid() = user_id`; client mutations switch to `supabase.rpc` and drop the read-then-write pattern. The defensive `updated_at` guard stays in place until the RPC ships. |
| 2026-05-07 | M28 PR #754 review (deferred — UX) | `mobile/src/screens/TravelCapsuleScreen.tsx` `generate.isPending` overlay | **`travel_capsule` generation is a 60s call with no cancel button.** The Build-my-capsule CTA disables itself + renders a loading Card while `useGenerateTravelCapsule` runs (~25-45s typical, 60s timeout in the wrapper). The hook's per-call `AbortController` aborts on unmount but there is no in-screen cancel surface — a user who taps Build then changes their mind has to background the app or wait the full timeout. | Track for: post-launch UX polish. Add a Cancel button to the generating Card that calls a hook-exposed abort method (e.g. `generate.abort()` returning the underlying AbortController), and dim the disabled CTA with secondary copy "Cancelling—" while the abort drains. Not blocking — >30s waits are explained by the generatingBody copy and Sentry already filters AbortError noise. |
| 2026-05-07 | M28 PR #754 review (deferred — cross-platform divergence) | mobile `useTravelCapsules.ts` (nested `result.must_haves`/`result.packed_state`) vs web `src/hooks/useTravelCapsule.ts` (raw `capsuleResult` in `result`, packed-state component-local) | **Web/mobile travel-capsule state cross-pollination.** Mobile persists must_haves + packed_state by nesting under the `result` JSONB key (no migration required). Web stores `capsuleResult` as-is in `result` and keeps packed-state in component-local `useState<Set<string>>` (transient, never persisted). A capsule edited on web won't surface mobile's persistent state, and a capsule edited on mobile won't show its must-haves / packed-state on web. M28 PR #754 rewrote the doc comments to be honest about this rather than lying about web parity. | Track for: when web M28 ships. Either web adopts the nested-keys convention so both platforms read/write the same shape, or both platforms migrate to a dedicated JSONB column / atomic RPC (ties into the `jsonb_set` RPC entry above). Pre-launch impact is low — Sweden-first is mobile-only. |
| 2026-05-07 | Audit follow-up — M25/M27/M28 (PR #TBD) | `mobile/src/hooks/useV3CompatBackfill.ts` (new) + `mobile/src/contexts/AuthContext.tsx` mount + `mobile/src/screens/OnboardingScreen.tsx` (`onboarding.completed_at` write) + `mobile/src/hooks/useFirstRunCoach.ts` (retro gate signal) + `mobile/src/hooks/useTravelCapsules.ts` (combined RMW + retry-on-conflict) + `mobile/src/screens/TravelMustHavesScreen.tsx` (conflict alert) + `mobile/src/hooks/useGenerateTravelCapsule.ts` (gap-id collision hedge) | **Audit follow-up — M25 V3-compat backfill, M27 retro-gate stronger signal, M28 dual-mutation race + gap-id collision hedge — all P0/P1 fixed in PR #TBD.** Pre-M25 users now back-fill `preferences.styleProfile` once per session so AI consumers get populated prompt lines without a quiz retake; M27's retro gate now keys on `onboarding.completed_at` (with legacy proxy via `style_profile_v4_jsonb` + `onboarding.completed === true` and a 1h anti-replay window) instead of the false-positive-prone `created_at > 7 days` heuristic; M28 mutations consolidated into a single combined RMW with one retry on `updated_at` conflict to absorb legitimate races, with distinct alert copy for the post-retry conflict path; and gap-id synthesis now appends a per-code counter so duplicate AI gap codes don't collide React keys. | RESOLVED in PR #TBD. |
| 2026-05-07 | M31 PR A #758 review (deferred — enterprise pass after PR B) | `mobile/src/hooks/useSubscription.ts` PREMIUM_PLAN_VALUES vs `src/hooks/useSubscription.ts` `subscription.plan === 'premium'` check | **State-machine plan-value asymmetry — mobile accepts `'monthly'`/`'yearly'`/`'premium'`; web only `'premium'`.** Mobile widened the accepted plan vocab in M31 because RevenueCat writes the subscription period (`monthly`/`yearly`) into `plan` while the legacy Stripe writer wrote `'premium'`. Web's `useSubscription.deriveState` still hard-checks `subscription.plan === 'premium'`. A user who purchased on iOS via RevenueCat will read as `'locked'` on web until the cross-platform writer normalises the plan value. Pre-launch impact: low (Sweden-first is mobile-only); long-term impact: mismatched gating once web ships RevenueCat parity. | Track for: M43 cleanup (or the M31 PR B → enterprise pass). Either widen `src/hooks/useSubscription.ts` to accept the same set, or normalise plan to `'premium'` at the webhook write site so both clients agree. |
| 2026-05-07 | M31 PR A #758 review (deferred — enterprise pass after PR B) | `mobile/src/hooks/useSubscription.ts` (no `canAddGarment` / `canCreateOutfit` helpers) | **`canAddGarment` / `canCreateOutfit` not ported from web's `useSubscription`.** Web exposes `canAddGarment()` / `canCreateOutfit()` / `remainingGarments()` / `remainingOutfits()` gating helpers. Mobile's `useSubscription` exposes only `state` / `isPremium` / `isLocked` and lets every screen open-code the gating logic. Onboarding-boost bypass is now in mobile (this PR) so the helpers would correctly return `true` during the boost window — porting is mechanical. | Track for: enterprise pass after M31 PR B. Mirror web's helper signatures so screens (Wardrobe, Outfits, Studio) can switch from manual `state === 'locked'` checks to `canAddGarment()`. |
| 2026-05-07 | M31 PR A #758 review (deferred — cosmetic) | `mobile/src/lib/revenuecat.ts` `loadPurchasesModule` doc comment | **Dynamic `import('react-native-purchases')` comment misleading.** The comment claims the dynamic import "gates web / simulator paths from loading the native bridge", but Metro statically resolves the specifier so the bundle still includes the module — only the runtime `isPurchasesSupported()` guard skips invocation. Functionally correct (no native calls fire on simulator/web), but the comment overstates the bundling benefit. | Track for: enterprise pass cosmetic cleanup. Reword to "we still gate execution behind `isPurchasesSupported()` even though Metro will include the module in the bundle." |
| 2026-05-07 | M31 PR A #758 review (deferred — pre-M44 acceptable) | `mobile/src/lib/revenuecat.ts` `resolveApiKey` (no UI surface for missing keys) | **No dedicated UI when `EXPO_PUBLIC_REVENUECAT_*_API_KEY` is absent.** Sandbox keys are provisioned in M44. Until then a Subscribe tap on a build without the key throws `REVENUECAT_PACKAGE_UNAVAILABLE` and surfaces the generic-error alert. Acceptable pre-M44 because dev builds aren't on the App Store path; production builds will fail closed once M44 wires the key into EAS secrets. | Track for: M44. If the key absence persists into a build that reaches TestFlight, add an explicit "Subscriptions are temporarily unavailable" empty-state screen so reviewers don't see a generic error. |
| 2026-05-07 | M31 PR A #758 review (deferred — autolinking) | `mobile/app.json` `plugins[]` (`react-native-purchases` not registered) | **`react-native-purchases` v10 ships without an Expo config plugin.** No `expo-module.config.json`, no `app.plugin.js` in the package. The library relies on Expo's autolinking (`react-native.config.js` + native module resolution) — registering it explicitly in `plugins[]` would fail at `expo prebuild` since there's no plugin export to invoke. Verified via `mobile/node_modules/react-native-purchases/` listing. The reviewer's request to add it would have broken the build, so we documented the autolinking path here instead. | Track for: a future `react-native-purchases` major (v11+) — re-check whether the package adds an Expo config plugin and register it then. Until then, autolinking is the supported path. |
| 2026-05-07 | M31 PR B #759 review (deferred — schema change required) | `supabase/functions/revenuecat_webhook/index.ts` CANCELLATION branch + `subscriptions` schema | **No `cancel_at` column for scheduled-cancel UX.** RevenueCat's CANCELLATION event carries the user-initiated unsubscribe *intent* alongside `expiration_at_ms` — the entitlement remains active through Apple's grace period, then EXPIRATION fires and the row flips to `canceled/free`. Today the webhook just touches `updated_at` on CANCELLATION; nothing in `subscriptions` distinguishes "active and renewing" from "active but already cancelled, ending on <date>". The Settings → Subscription screen therefore can't show "Your premium ends on Mar 14" until the EXPIRATION lands. Migration-required (CLAUDE.md hard rule prevents shipping it without explicit user authorization). | Track for: post-launch UX wave. Add a nullable `cancel_at timestamptz` column to `subscriptions`. On CANCELLATION, set `cancel_at = expiration_at_ms`. On UNCANCELLATION, set `cancel_at = NULL`. Update `useSubscription` (mobile + web) to surface the scheduled-cancel state. Stripe webhook would need parity (`cancel_at_period_end` → `cancel_at`) so both pipelines populate the same column. |
| 2026-05-07 | post-launch theme-6 cleanup (PR #760) | `mobile/src/lib/edgeFunctionClient.ts` + ~24 callers | **M19 `'subscription_required'` sentinel was inlined as a bare literal or local `const SUBSCRIPTION_SENTINEL` declaration in 24 mobile hooks/screens.** Drift risk: a future server-side rename would silently miss every caller. | RESOLVED in PR #760: exported `SUBSCRIPTION_SENTINEL` from `edgeFunctionClient`; all callers import the canonical constant. |
| 2026-05-07 | post-launch theme-6 cleanup (PR #760) | `mobile/src/screens/onboarding/AccentColorStep.tsx` + onboarding step machine | **M26 step persisted to client-only `preferences.accentColor` with no live consumer (mobile theme is light/dark only).** Dead code with 18 i18n keys. | RESOLVED in PR #760: file + 18 i18n keys deleted; STEP_COUNT 8→7; `PersistedState.v` bumped 1→2 so existing in-flight drafts reset cleanly under the new step ordering. |
| 2026-05-07 | post-launch theme-6 cleanup (PR #760) | `mobile/src/screens/NotificationsScreen.tsx` route | **M41 inbox stream not yet shipped; route reachable from HomeScreen weather pill + push-deep-link allowlist.** Render path was fixtures-only `[]`. | RESOLVED in PR #760: route registration + import + push allowlist entry hidden until M41; HomeScreen weather pill converted from no-op `Pressable` to plain `View`. File retained for M41. |
| 2026-05-07 | post-launch theme-6 cleanup (PR #760) | `mobile/src/screens/ShareOutfitScreen.tsx` + entry button on OutfitDetail + push allowlist | **Hardcoded mock `https://burs.me/o/mock` SHARE_URL — every production share opened a 404.** Direct violation of "no hardcoded fake info / stubs" rule. User confirmed the share surface is unused at launch. | RESOLVED in PR #760: file + RootNavigator entry + 5 i18n keys + 2 OutfitDetail callers + push allowlist entry deleted. `useGenerateFlatlay` retained (no live importer post-deletion; kept in case M-future re-introduces the surface). |
| 2026-05-07 | post-launch theme-6 cleanup (PR #760) | `mobile/src/screens/onboarding/AchievementStep.tsx` `grant_trial_gift` wiring | **TODO(server-write) — `grant_trial_gift` never wired on mobile; web parity (`src/components/onboarding/AchievementStep.tsx`) fires it on mount.** Mobile users were silently missing the 3 free render credits granted at onboarding completion. Stripe is being removed entirely (IAP-only future) but the gift fires regardless of payment provider. | RESOLVED in PR #760: fire-and-forget `callEdgeFunction('grant_trial_gift', { body: {} })` once per `user.id` on mount; invalidates `['render_credits', userId]` only when `data.ok === true` (mirrors web); guarded against unmount via `cancelled` ref; transient errors and `{ ok: false }` responses log to console.warn rather than burning Sentry quota (web parity). |
| 2026-05-07 | post-launch theme-6 cleanup (PR #760) | `mobile/src/screens/OnboardingScreen.tsx` header comment | **TODO(server-write) referenced unwritten `advance_onboarding_step` path; that path actually shipped in W2 and the screen merges `preferences.styleProfile` + `style_profile_v4_jsonb` correctly today.** Stale comment; doc drift. | RESOLVED in PR #760: TODO removed. |
| 2026-05-07 | post-launch theme-5 V3/V4 vocab fidelity (PR #761) | `supabase/functions/_shared/outfit-scoring.ts:1080-1085` `resolveOccasionSubmode` | **Substring matchers `primaryGoal.includes('formal'\|'professional')` / `primaryGoal.includes('comfort'\|'relaxed'\|'creative')` were dead code for ALL users.** Wave 7 audit P0 #9 (`migrateV4ToV3Compat` merge order, fixed in PR #688) made the V3 mirror reliable, but didn't fix the readers themselves: V3 vocab values (`save_time \| better_style \| wardrobe_org \| reduce_waste \| plan_outfits`) contain none of those substrings, so the formality push-up / push-down branches NEVER fired on either V3 native users or V4-migrated users. The V4 numeric formality bounds (`formalityCeiling` / `formalityFloor`) flowed through to the merged profile but were entirely ignored by the scoring engine. Mobile `migrateV4ToV3Compat` was verified byte-for-byte identical to web — no port gap. | RESOLVED in PR #761: replaced the dead V3 substring matchers with V4-native `formalityCeiling` / `formalityFloor` midpoint reads (0-100 scale → 1-5 occasion scale). Midpoint ≥ 3.8 (≈ 70/100) pulls work-occasion target up to 4.5 → "Formal Office"; midpoint ≤ 2.3 (≈ 32/100) pulls target down to 2.8 → "Relaxed Office". Single signal per profile (vs the two-bound version that fired both branches for default V4 users). V3-native users (no V4 fields) fall back to occasion-default `formalityTarget` — preserves prior observed behaviour (the dead branches gave them nothing either). New `_shared/__tests__/outfit-scoring-formality.test.ts` covers 13 equivalence classes (8 core + 5 defensive: non-work, meeting override, V3 native, V4 default, V4 formal-leaning, V4 comfort-leaning, styleVector override, partial-V4 incomplete-signal, plus wrapped-prefs (`{ styleProfile: { ... } }`) `getStylePrefs` unwrap, extreme bounds (0/0 and 100/100), wide-range user (10/95) → no-adjustment by-design, NaN bound, string-typed bound). |
