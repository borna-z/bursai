<!-- Extracted from CLAUDE.md (2026-05-03) to keep the always-loaded tracker tight. Append new entries here, not in CLAUDE.md. See CLAUDE.md "Launch Plan Update" + "Findings Discipline" sections for write rules. -->

### Completion Log

| Date | PR | Prompt | Summary |
|------|-----|--------|---------|
| 2026-04-19 | #635 | P0a | Husky pre-commit hook runs tsc + eslint + build |
| 2026-04-19 | #636 | P0b | Tighten CI lint step: fail on eslint warnings (`--max-warnings 0`) |
| 2026-04-19 | #637 | P0d | Smoke-test POC: harness + 3 tests (signup, plan-week, garment-add) + CI step gated on `RUN_SMOKE=1`. Split into P0d-ii (infra) + P0d-iii (remaining 7 flows). |
| 2026-04-19 | #638 | P0d-ii | Test infra (partial): harness extension + mock-server scaffolding (Node stdlib) + ADR. `smoke-local` CI job exists but is gated `if: false` pending P0d-iv — first CI run exposed missing base-schema migration. Drift repair tracked as new P0d-iv. |
| 2026-04-20 | #664 | P0d-iv | Schema drift repair via Strategy V: baseline dump (36 tables) as sole source of schema truth; 67 historical migrations deleted (they described a fiction — lots of silently-failed DDL); remote tracking table repaired in one atomic transaction; smoke-local CI re-enabled. Commit 1 preserves the attempted Strategy W idempotency pass in git history. |
| 2026-04-20 | #640 | P0d-iii | Smoke-test suite expanded from 3 to 10. 7 new tests (enrichment, render, outfit-generate, outfit-refine, visual-search, shopping-chat, travel-capsule) invoke the target edge function via `supabase.functions.invoke()` and assert the response envelope — NOT DB-only. Three hardcoded Gemini URLs made overridable in `_shared/burs-ai.ts` + `_shared/gemini-image-client.ts` + `_shared/render-eligibility.ts` (backward-compat: identical behaviour when env vars unset). Mock Gemini server + `start-mock-server.ts` entrypoint + CI workflow wiring via `supabase functions serve --env-file`. AI tests skip in smoke-prod (`shouldRunAiSmoke` gate) so prod Gemini isn't hit. 10/10 passing locally with mock interception verified. **Deploy of 24 AI functions deferred** — `esm.sh 522` blocker from Supabase bundler; backward-compat keeps prod safe until retry. See Findings Log row for `_shared/burs-ai.ts:17` (now resolved). |
| 2026-04-20 | #641 | P0a-ii | Fix .husky/pre-commit: add missing shebang so Windows git.exe executes the hook |
| 2026-04-20 | #642 | P0d-iii-deploy | Retry-deployed all 24 AI function consumers of the Gemini URL env-var refactor. esm.sh 522 cleared; no code changes. |
| 2026-04-20 | #643 | P1 | Add JWT verification + service-role bypass to summarize_day (JWT-only), process_job_queue (bypass), daily_reminders (bypass) |
| 2026-04-20 | #644 | P2 | Calendar handleSyncAll: reject anon-key callers, service-role only (DoS fix) |
| 2026-04-20 | #645 | P0e | Wave 0 catch-up: CI step checks `supabase migration list --linked` + `db push --dry-run` on any PR touching supabase/migrations/ |
| 2026-04-20 | #646 | P3 | OAuth hardening for google_calendar_auth: server-side `redirect_uri` allowlist (hardcoded 3 URIs + optional `ALLOWED_CALENDAR_REDIRECT_URIS` env extras) + single-use CSRF token `state = <user_id>.<nonce>` backed by new `public.oauth_csrf` table (10-min TTL, consumed on callback, hourly `oauth_csrf_cleanup` pg_cron). Client passes `state` from URL back to backend verbatim. Post-merge: `supabase db push` provisions the table + cron, then deploy `google_calendar_auth`. |
| 2026-04-21 | #647 | P4 | prefetch_suggestions identity check: single-user-trigger path now validates caller's JWT via `supabase.auth.getUser(token)` and asserts `user.id === body.user_id` before running `processSingleUser`. 401 on missing/invalid token, 403 on mismatch. Cron-batch path intentionally untouched (separate unauditted gap — see Findings Log). Post-merge: deploy `prefetch_suggestions`. |
| 2026-04-21 | #648 | P5 | Email domain fix: VAPID `mailto:` contact string in `webpush.setVapidDetails()` changed from `hello@bursai.com` to `hello@burs.me` in both `send_push_notification` and `daily_reminders`. VAPID `sub` claim is an RFC 8292 abuse-contact identifier — FCM/APNs don't validate it, zero behavioural impact on push delivery. Post-merge: deploy both functions. |
| 2026-04-21 | #649 | (doc fix) | LAUNCH_PLAN.md P1 cron-only section rewritten: removed the `if (!isServiceRole) { JWT fallback }` anti-pattern that Codex rejected on PR #643. Replaced with the hard-reject `timingSafeEqual` pattern that actually shipped. Added a rule-of-thumb block (user-facing vs cron-only vs dual-mode) so future prompts picking auth patterns have one canonical reference. No code changes; CURRENT PROMPT stayed P6. |
| 2026-04-21 | #650 | P6 | Outfit ownership check in `suggest_accessories`: before the parallel query fetching `outfit_items` via service client, added a single-query ownership check (`outfits.select(id).eq(id).eq(user_id).maybeSingle()`) that returns 404 on "not yours" OR "doesn't exist" (collapses both paths into one response — no enumeration oracle). Codex P2 follow-up: split `outfitError` to 500 (retriable/monitored) vs `!outfitRow` to 404 (enumeration-safe). Post-merge: deploy `suggest_accessories`. |
| 2026-04-21 | #651 | P7 | Cross-user validation in `process_job_queue` handlers: `handleGarmentEnrichment` and `handleImageProcessing` now require `userId` and filter the garments query on both `.eq("id", garmentId)` AND `.eq("user_id", userId)`. Error message is "Garment not found or not owned" — collapses missing-garment and cross-user cases. `handleBatchAnalysis` left alone (no garment_id in payload, no attack vector). Also tightened `submitJob` in `_shared/scale-guard.ts`: `userId?: string` → `userId: string` (defensive type constraint; zero in-repo callers today so no breakage). Post-merge: deploy `process_job_queue`. |
| 2026-04-21 | #652 | P8 | Complete delete_user_account cascade: added DELETEs for 10 orphan-row tables (`render_credit_transactions`, `render_jobs`, `render_credits`, `feedback_signals`, `garment_pair_memory`, `analytics_events`, `ai_rate_limits`, `outfit_feedback`, `push_subscriptions`, `travel_capsules`). Spec said 12 but `chat_messages` was already in the existing cascade. Render FK order preserved (transactions→jobs→credits). Silent-delete pattern matches existing file convention for leaf cleanups. **`ai_response_cache` intentionally NOT cleaned** — Codex P1 follow-up caught that `cache_key` is a SHA-256 hash so any LIKE filter matches zero rows. Mitigation: short TTLs (30 min – 12 h) across the codebase mean orphan cache rows decay naturally within a day. Proper fix requires a `user_id` column on the table — tracked as follow-up (likely bundled with P13). GDPR right-to-erasure hygiene. Post-merge: deploy `delete_user_account`. |
| 2026-04-21 | #659 | P13+P14+L554 | **Wave 2-C cache-layer bundle.** (a) P13 — user-scoped `cacheNamespace` + added `userId` option in 7 AI functions (`style_twin`, `clone_outfit_dna`, `wardrobe_aging`, `wardrobe_gap_analysis`, `smart_shopping_list`, `suggest_accessories`, `travel_capsule`). (b) P14 — `summarize_day` namespace now includes user.id (previously keyed only on 40-char event-title slice — identical calendars cross-leaked); `suggest_outfit_combinations` uses full UUID instead of 8-char prefix. (c) L554 schema change — new migration `20260421180000_ai_response_cache_user_id.sql` adds `user_id UUID REFERENCES auth.users(id) ON DELETE CASCADE` + partial index. `_shared/burs-ai.ts` `storeCache` populates the column from the new `userId` option. `delete_user_account` gets an explicit `ai_response_cache.delete().eq("user_id", userId)` step (belt-and-suspenders alongside FK cascade). Old Codex-P1 "can't clean this table" comment retired. Code-reviewer approved. **Blast radius**: burs-ai.ts shared module → all 22 AI functions need redeploy (9 in scope + 13 untouched), plus `delete_user_account`. Post-merge: `supabase db push --linked --yes` then deploy 23 functions. Cold-cache penalty on the 9 scope functions (TTL decay 30min-12h) is intentional and noted. |
| 2026-04-21 | #658 | P12 | **Wave 2-B solo PR. DB-backed idempotency.** Replaced `_shared/idempotency.ts` in-memory `Map` with `public.request_idempotency` Postgres table. Atomic claim via `upsert(..., {ignoreDuplicates:true})` — exact pattern from `stripe_events` in stripe_webhook. First isolate wins the primary-key race; losers read the row and return either the cached Response (status>0) or 409 with Retry-After (status=0 still pending). Split TTLs: CLAIM_TTL_MS=60s (crashed-isolate retries don't deadlock), DEFAULT_RESULT_TTL_MS=5min (completed response). Both helpers gained required `supabaseAdmin` arg; 2 consumers (`create_checkout_session`, `delete_user_account`) updated to hoist the service client above the idempotency check. Also tightened `create_checkout_session` outer catch to a generic 500 string (the hoist widened potential error-message leak surface — fixed preemptively). New migration `20260421170000_request_idempotency.sql` with idempotent DDL + hourly pg_cron cleanup job (mirrors oauth_csrf pattern). Deploy: `create_checkout_session`, `delete_user_account` + `npx supabase db push --linked --yes`. |
| 2026-04-21 | #657 | P9+P10+P11 | **Wave 2-A themed PR.** (a) P9 — overload + rate-limit swept across 12 functions. 9 user-facing (`import_garments_from_links`, `insights_dashboard`, `send_push_notification`, `restore_subscription`, `create_portal_session`, `delete_user_account`, `calendar`, `google_calendar_auth`, `generate_outfit`) get the full `enforceRateLimit` + `checkOverload` + `RateLimitError` catch pattern. 3 cron-only (`daily_reminders`, `process_job_queue`, `cleanup_ai_cache`) get `checkOverload` only. 9 new `RATE_LIMIT_TIERS` entries in `_shared/scale-guard.ts`. `process_garment_image` skipped (P15). **Scope expansion**: `cleanup_ai_cache` had NO auth gate at all (P1 missed it) — added the same `timingSafeEqual` hard-reject pattern P1 shipped to sibling cron-only functions. (b) P10 — `src/lib/validators.ts` (new) with `isUuid` + `isValidUsername`; applied in `PublicProfile.tsx` + `ShareOutfit.tsx` to short-circuit malformed URL params. (c) P11 — **seed_wardrobe REMOVED entirely** (not gated). Codex P1+P2 surfaced blocking issues with the initial gating attempt: rate limit breaks the function's own multi-call flow, and the token-consume step wasn't atomic. User call: seed_wardrobe is admin/demo-only with no prod use case — remove rather than gate. Dropped: function dir, config.toml entry, tier entry, WIP migration, `SeedContext.tsx`, `SeedWardrobe.tsx`, `seedGarments.ts`, `SeedProgressPill.tsx`, the `/settings/seed-wardrobe` route + AppLayout-level pill, and defensive test mocks across 10 test files. Also cosmetic fix: calendar.ts helper signatures loosened from `ReturnType<typeof createClient>` to `any` (pre-existing supabase-js generic narrowing that broke deno-check the first time it ran against calendar). Post-merge: deploy 12 edge functions (no migration). |
| 2026-04-21 | #661 | P16+P17+P18+P19 | **Wave 3-B themed PR. Render-magic bundle.** Shipped 4 launch-plan prompts plus 12 audit findings in a single PR because they share the same hot path (`render_garment_image` + its 3 shared-module dependencies). (a) **P16** category-aware prompts via `classifyCategory()` → 6 presentation classes (ghost_mannequin / shoes / bag / flat_lay / jewelry / accessory_generic). (b) **P17** 3-attempt retry chain with distinct prompt variants (primary / tightened / minimal) — full pipeline per attempt, retryable rejects advance to next variant, hard failures (auth/model_path) throw immediately. (c) **P18** category-aware validation with 2 new decision codes (reject_wrong_category, reject_logo_missing) + 2 new signals. (d) **P19** fetchWithTimeout across image-gen (60s × 3-attempt backoff) + text gates (25s). Plus audit findings **F2** prompt lead-with-category, **F5** enriched mannequin-presentation body-shape hints, **F9** logo-preservation signal, **F13** input preflight (magic-byte + 400×400 min dims), **F15** 5xx/429 exp-backoff in client, **F16-F18** output hard-gate (30KB min bytes + 512×512 min dims + magic-byte validation), **F20+F21** RenderPendingOverlay time hint + full i18n, **F22** new `RenderFailedBanner` component with one-tap retry surfacing `render_status='failed'`. **RENDER_PROMPT_VERSION bumped v1→v2** (folded into credit-ledger baseKey — in-flight v1 reservations preserve replay path; new requests get fresh v2 reservations). Verification: tsc 0 errors, eslint 0 warnings, build clean, vitest 1104/1104. Code-reviewer approved (1 finding verified safe via clarifying comment; remaining findings logged for follow-up). Post-merge: deploy `render_garment_image` only. |
| 2026-04-22 | #663 | P23+P24 | **Wave 4-A themed PR. Retrieval-quality shared infra.** Bundled because both touch `_shared/burs-ai.ts` and share the 22-function redeploy cycle. (a) **P23 ID truncation** — removed `.slice(0, 8)` at three sites, not just the 2 in the original spec. Root cause `_shared/burs-ai.ts:compactGarment:189` fixed upstream so every AI function calling the helper inherits the fix; `suggest_outfit_combinations/index.ts:111` (prompt-exposed `unusedIds`) and `wardrobe_aging/index.ts:56` (prompt-exposed `garmentList`) fixed at the consumer. Response validation in both named consumers is exact-match against a full-UUID set — shorter→longer stays exact-match, no break. **Scope expansion (code-reviewer blocker fix):** `prefetch_suggestions/index.ts` line 54 still instructed the model to "Pick garments by their short ID (first 8 chars)" which became inconsistent once `compactGarment` emitted full UUIDs. Reviewer flagged this as a BLOCKER — without it, the model would have returned 8-char prefixes into a wardrobe of 36-char UUIDs, invalidating the entire cache. Updated to "Pick garments by their ID." (b) **P24 enrichment helpers** (library only) — added `isEnrichmentReady(status)` + `isEnrichmentFailed(status)` + `filterEnrichedGarments<T>(garments)` + `waitForEnrichment(supabase, garmentIds, opts)` to `_shared/burs-ai.ts`. Ready state accepts BOTH `'complete'` (frontend writers) and `'completed'` (backend job queue) — Codex P2 round 2 caught the spelling divergence which would have silently dropped frontend-enriched garments. Same fix applied inline to `_shared/outfit-scoring-body.ts:466` where the identical single-spelling check was a pre-existing defect (job-queue-enriched garments got the 0.55 unenriched penalty in outfit scoring despite being fully enriched). waitForEnrichment polls until ready/failed/timeout; phantom IDs bucket as failed. Consumers opt-in per later wave. 17 new unit tests added (3 compactGarment UUID assertions + 3 filterEnrichedGarments cases including dual-spelling + 11 waitForEnrichment scenarios including dual-spelling, numeric clamp, and non-finite coercion regressions). Verification: tsc 0, eslint 0, build clean, vitest 1115/1115. Code-reviewer approved after the prefetch_suggestions fix. **Blast radius**: `_shared/burs-ai.ts` bundles into all 22 AI functions → redeploy all 22 post-merge (per Shared Module Deploy Map). Functional change is narrower — 3 functions have behaviour changes: `prefetch_suggestions` (sole caller of `compactGarment` + prompt text updated), `suggest_outfit_combinations` (direct prompt build at line 111), `wardrobe_aging` (direct prompt build at line 56). **Cold-cache scope**: those 3 functions' `ai_response_cache` rows become unreachable post-deploy because the prompt payload (which feeds `cache_key` via SHA-256) now contains full UUIDs instead of 8-char prefixes. TTLs are short (1800s–43200s) — natural decay within 12h, acceptable per P13 precedent. Post-merge: `supabase functions deploy` for all 22 in groups of 5. |
| 2026-04-21 | #660 | P15 | **Wave 3-A solo PR. Unwire PhotoRoom entirely.** Deleted `supabase/functions/process_garment_image/` (the always-skip stub) + its `supabase/config.toml` entry. Stripped `startGarmentImageProcessingInBackground` from `src/lib/garmentIntelligence.ts` + its call site in `triggerGarmentPostSaveIntelligence`. Narrowed `imageProcessing` option union from `'edge' | 'local' | 'skip' | 'full'` → `'local' | 'skip'` (all in-repo callers pass `{ mode: 'skip' }`; 'local' preserved for Wave 9 Capacitor path). Removed `handleImageProcessing` from `process_job_queue` + dropped `image_processing` from `JOB_HANDLERS`. **Scope expansion**: removed the same `image_processing_status === 'pending' | 'processing'` polling gate from `GarmentDetail.tsx` (in spec) AND from 3 sibling hooks (`useGarments`, `useGarmentsByIds`, `useAISuggestions`) that had the identical defect — keeps post-P15 state consistent. **Out-of-scope stabilization (user-requested)**: fixed pre-existing vitest full-suite flakes on Windows by adding `testTimeout: 15000`, `pool: "forks"`, `maxWorkers: 2` to `vitest.config.ts`. Confirmed pre-existing by stashing P15 edits + reproducing on pristine main; full suite was flaking 5-20 tests per invocation on the default 5s per-test budget. Now 1104/1104 reliable. Code-reviewer approved. DB columns (`image_processing_status`, `image_processing_provider`, etc.) intentionally LEFT IN PLACE per spec — separate schema-cleanup PR will drop them + the orphan `job_queue` image_processing rows (tracked in Findings Log). Post-merge: deploy `process_job_queue` + run `npx supabase functions delete process_garment_image --project-ref khvkwojtlkcvxjxztduj` to decommission the prod edge function. |
| 2026-04-23 | #666 | P27a | **Wave 4.5-A solo PR. Schema-only migration — nullable `secondary_image_path` column on `public.garments`.** Single `ALTER TABLE ... ADD COLUMN IF NOT EXISTS secondary_image_path text`. No default, no CHECK, no RLS change — matches existing `original_image_path` / `processed_image_path` / `rendered_image_path` pattern. Idempotent guard makes safe to re-run. Zero backend code modified, zero frontend code modified, zero edge-function redeploy. **Design intent**: swap-primary in P27b will be an atomic `UPDATE garments SET image_path = secondary_image_path, secondary_image_path = image_path` — swaps the VALUES, not a pointer column, so `image_path` stays universal source of truth and every reader (wardrobe card, analyze_garment, render_garment_image, outfit scoring, React Query selectors) continues working with zero code changes. Post-merge: user runs `npx supabase db push --linked --yes` to apply, then `supabase gen types typescript --linked` to regenerate `src/integrations/supabase/types.ts` (CLAUDE.md auto-generated file; intentionally stale in this PR since no code reads/writes the column yet — P27b is the consumer in a follow-up PR). |
| 2026-05-05 | #728 | M1 | **M1 — Render polling.** Ports the web `useRenderJobStatus` hook to mobile (`mobile/src/hooks/useRenderJobStatus.ts`) using TanStack `useQuery` + `refetchInterval` instead of the web's hand-rolled `setTimeout` loop, with a tighter 90 s ceiling than web's 30-min budget. The hook polls the latest `render_jobs` row for a garment until `status ∈ {succeeded, failed, poll_timeout}`; on `succeeded` it invalidates `['garments']` and `['garment', user.id, garmentId]` so cached lists + the detail screen pick up `rendered_image_path`. Wired into `mobile/src/screens/GarmentDetailScreen.tsx`: while `garment.render_status === 'pending'` the hook ticks and the hero shows a "Studio render…" pill (ActivityIndicator + accent text); on succeed the cache invalidate causes `useGarment` to refetch, the row exposes `rendered_image_path`, `useSignedUrl` resolves the new URL, and the hero swaps without a pull-to-refresh. Three-state pill — pending → "Studio render…", rendered → "Studio", neither → hidden. **Out of scope vs original wave file (verified, wave file updated)**: `AddPieceStep3.tsx` unchanged — Step 3 calls `nav.reset` to GarmentDetail synchronously on save success (line 280), so a pill there unmounts before the first poll could land; polling correctly lives on its destination. `GarmentCard.tsx` unchanged — already reads `rendered_image_path ?? original_image_path` (W2), and the hook's `['garments']` invalidate triggers list refetch. Verification: tsc 0, eslint 0, expo-doctor passes. Code-reviewer subagent (single round, clean): three P2s logged to findings-log (RLS scope confirmation on render_jobs, no failure-state pill on `failed`/`poll_timeout`, query key `['render_job', garmentId]` not user-scoped — all low risk, tracked for follow-up). No edge-function changes; no migrations. |
| 2026-04-23 | #665 | P26+P27 | **Wave 4-C themed PR. Slot mapping + clone_outfit_dna deep audit.** Bundled per plan — both fix retrieval-side metadata sent to Gemini. (a) **P26 real slot mapping** — `generate_outfit/index.ts:73` replaced `slot: "unknown"` hardcode with a `classifySlot`-driven map. After `invokeUnifiedStylistEngine` returns, fetches `(id, category, subcategory)` for the selected garment_ids via `serviceClient` and builds `slotByGarment: Map<string, string>`; `items` emits `{ slot: slotByGarment.get(id) || "unknown", garment_id: id }`. Falls back to `"unknown"` only when the garment disappeared mid-request or category/subcategory is unrecognisable. Same pattern applied to `_shared/unified_stylist_engine.ts` swap path (lines 87-91) — `otherItems` built from real classified slots instead of blanket `"unknown"`. The shared module gained a `createClient` import so it can fetch garment metadata itself without requiring callers to pass a service client. **Root CLAUDE.md fix**: Shared Module Deploy Map row for `unified_stylist_engine.ts` updated from `style_chat` to `style_chat, generate_outfit` (grep confirms both are consumers; the old row was stale). (b) **P27 clone_outfit_dna retrieval tightening** — full rewrite of `clone_outfit_dna/index.ts`. DB queries expanded to SELECT `subcategory + ai_raw + enrichment_status` on both `outfit_items.garments` and the candidate wardrobe fetch. New helpers: `aiRawField` (safe extract), `describeGarmentForDNA` + `describeCandidateForPrompt` (structured multi-field lines with English formality labels instead of opaque `f3` codes), `extractDNAProfile` (structured `{ categories, colors (multi-family via colorFamily), formalityBand (low/mid/high|null), archetypes, occasions }`), `scoreCandidate` (category overlap 2.0x / color family 1.5x / formality exact 1.5x or adjacent-via-mid 0.75x / archetype 1.0x / occasion 0.75x). P24 enrichment gate — when ≥70% of the candidate wardrobe is enriched, applies `filterEnrichedGarments`; below threshold sends what we have (graceful degrade). Stable-sort top-40 pre-filter — ties preserve wardrobe order. Dynamic `estimateMaxTokens({ inputItems: ranked.length, outputItems: 3, perItemTokens: 120, baseTokens: 200 })`. Prompt rule #1 enforces complete outfits (top+bottom+shoes OR dress+shoes + outerwear when reference has it) so variations can't be single garments. Slot in DNA uses `classifySlot(category, subcategory)` with stored `outfit_items.slot` as fallback only — stored slot can be stale after category re-labels. Response shape `{variations: [{name, garment_ids, explanation}]}` preserved — backward-compatible, no frontend change. **Shared module radius**: `unified_stylist_engine.ts` touch → both `style_chat` + `generate_outfit` need redeploy (per corrected Deploy Map). `burs-ai.ts`, `burs-slots.ts`, `retrieval.ts`, `scale-guard.ts` — not modified. Post-merge: deploy `generate_outfit` + `clone_outfit_dna` + `style_chat`. No migration. |
| 2026-04-23 | #664 | P20+P21+P22 | **Wave 4-B themed PR. Retrieval pre-filters + shopping-merge.** Also carries the standing Wave Closure Rule addition + Wave 4.5 secondary-image feature + Wave 4.9 cleanup wave plan (docs-only additions to CLAUDE.md + LAUNCH_PLAN.md, staged in prior session). (a) **P20 mood_outfit pre-filter** — new `_shared/retrieval.ts` module (MOOD_MAP, MOOD_SCORING, rankGarmentsForMood, computeWardrobeCoverage, stratifiedSample, formalityLabel, colorFamily, intentToCacheKey, scanEventHints + types). mood_outfit now ranks the wardrobe against the mood (formality band / color family / occasion tags / style archetype / per-garment weather compat / wear-count decay), caps prompt payload at 40, opts into Wave 4-A `filterEnrichedGarments` when ≥70% of the top 80 are enriched, replaces opaque `f3` pipe-delimited format with structured JSON carrying English formalityLabel + occasion_tags + style_archetype, switches `max_tokens` to `estimateMaxTokens({ inputItems: promptGarments.length, ... })`, logs `retrieval.prefilter` telemetry. (b) **P21 wardrobe_gap_analysis becomes gap + shopping assistant** — accepts optional `intent` in request body (occasion/formality/season/budget/upcoming_events); `scanEventHints()` lifts keywords from upcoming_events descriptions into structured intent before AI call; prompt now contains `computeWardrobeCoverage()` structured JSON instead of English aggregate text, plus `stratifiedSample(garments, 25)` (representative across category × wear × recency, not 25 newest); tool schema optionally emits `shopping_recommendations: [{ priority, category, item, reasoning, fills_gap, price_range, search_query }]`; cacheNamespace partitions gap-only vs shopping-mode via `intentToCacheKey(intent)` djb2 hash; fallback rewritten to consume coverage math (respects "if >60% neutral don't recommend neutrals" rule); brand-stripping extended to shopping_recommendations. Backward-compatible — `useAdvancedFeatures.ts:66-79` destructure stays as-is (TS structural typing tolerates extra fields). (c) **P22 smart_shopping_list REMOVED** (per user decision, P11 pattern) — zero frontend callers confirmed; dropped function dir, config.toml stanza, scale-guard tier entry, CLAUDE.md rate-limit row, ARCHITECTURE.md function list row. Post-merge: `npx supabase functions delete smart_shopping_list --project-ref khvkwojtlkcvxjxztduj`. (d) **Wave 4-B mock + smoke test**: new `gemini.ts` branch for wardrobe_gap_analysis ahead of the outfit branch; new `wardrobe-gap-analysis-shopping.test.ts` with two cases (intent → both envelopes; no intent → gaps-only backward-compat). Verification: tsc 0 errors, eslint 0 warnings, build clean, vitest 1121/1121. Code-reviewer LGTM (2 nits applied inline — NaN clamp on rankForMood limit + autumn→fall season alias). **Deploy**: `mood_outfit` + `wardrobe_gap_analysis` only. `_shared/retrieval.ts` is consumed only by those two functions; `_shared/scale-guard.ts` tier removal is subtractive — no other AI function looked up the `smart_shopping_list` key. No migration (schema untouched). |
| 2026-04-24 | #672 | P30 | **Wave 5 partial — style_chat classifier fallback.** Drains the "make it warmer" misroute bug: LLM classifier sometimes returns `intent: "conversation"` on refine messages even when an active look is present, leaving users with an active outfit stranded in the conversational path. Added `applyActiveLookRefinementOverride(result, input)` to `_shared/style-chat-classifier.ts` as a deterministic post-classifier override. Shipped as a solo PR (not the P28+P30+P31 bundle originally planned) because P28 spec was written pre-P26 and describes already-fixed code in unified_stylist_engine.ts — requires deeper investigation before ship. 9 new unit tests added (20 total pass). Shared-module change confined to classifier so CI deno-check skips style_chat/index.ts (which has 5 pre-existing errors — tracked as Findings Log row). Verification: tsc 0, eslint 0, vitest 78/78 in shared suite + full suite green. Deploy: `style_chat`. |
| 2026-04-24 | #671 | W4.9-C | **Wave 4.9-C solo PR [cleanup]. Observability — Sentry hook on validator_unavailable fail-open path.** Drains Wave 3-B Finding #8 (aggregate validator outages were silently invisible until user-facing render quality degraded). Shipped a minimal `_shared/observability.ts` helper since no edge function previously integrated with Sentry. Helper parses `SENTRY_DSN` via `typeof Deno !== "undefined"` guard (so vitest/Node can transitively import observability.ts consumers safely), POSTs to Sentry's simpler `/store/` ingest endpoint (no SDK dependency, ~100 LOC total), and degrades to structured `console.warn` when DSN is unset (discoverable in Supabase Logs). `classifyValidatorError()` narrows the reason tag to `validator_timeout` / `validator_fetch_failed` / `validator_bad_response`. Wired into the single fail-open branch at `render_garment_image/index.ts:1679` — when `validateRenderedGarmentOutputWithGemini` throws on attempt 1, we emit `captureWarning('render_validator_unavailable', {attempt: 1, category: categoryClass, reason})` fire-and-forget. Existing console.warn left in place as belt-and-suspenders. User action post-merge: `npx supabase secrets set SENTRY_DSN=<dsn>` + configure Sentry alert rule for the new message type (tracked as post-launch setup, spec calls it out as NOT a merge blocker). Verification: tsc 0, eslint 0, deno check clean. Deploy: `render_garment_image`. |
| 2026-04-24 | #670 | W4.9-B+D | **Wave 4.9-B+D bundled PR [cleanup]. Docs + i18n drift + Fix Protocol audit.** Zero-code-impact cleanup. **W4.9-B**: (a) LAUNCH_PLAN.md P8 section cleaned — line 707's bullet reworded to point to the L554 note; in-code-block comment block (lines 735-754) rewritten to reflect that L554 (PR #659) shipped the `user_id` column + cascade FK + explicit `.eq("user_id", userId).delete()` line. Historical context preserved for future maintainers. (b) 5 orphan `settings.avatar_*` i18n keys (`change_photo`, `avatar_updated`, `avatar_error`, `avatar_invalid`, `avatar_too_large`) removed from all 14 locale files (70 instances total via consistent sed-based bulk-delete with per-locale line preservation). Post-edit grep for `settings.avatar_` in `src/` returns zero. Safe — PR #654 removed the only caller (`ProfileCard.tsx` avatar path). **W4.9-D**: audited Fix Protocol section end-to-end; minimal edits where prose diverged from actual practice: (a) "While writing code #3" strict "Do NOT fix" rule now lists three CI-blocker / shared-code / sibling-defect exceptions that reflect real scope-expansion patterns from Wave 4.9-A (deno fixes) and P15 (sibling hook sweep). (b) "Launch Plan Update #1" explicitly mentions `[DONE-partial]` status + the `[cleanup]` suffix convention for closing-wave PRs + "same commit or sibling commit on same branch" clarification for tracker-fix atomicity. All other Fix Protocol rules left unchanged (accurate vs current practice). Verification: tsc 0, eslint 0, build clean, vitest green. Deploy: none. |
| 2026-04-24 | #669 | W4.9-A | **Wave 4.9-A solo PR [cleanup]. Schema cleanup — drop 7 dead columns from `garments` + delete orphan `job_queue` rows + backfill `enrichment_status` canonical spelling.** Drains P15 + P24 Findings Log rows. Two new migrations (`20260424004046_drop_image_processing_columns.sql`, `20260424004047_backfill_enrichment_status.sql`) — column drops are cascading (CHECK `garments_image_processing_status_check` + INDEX `idx_garments_processing_status` vanish automatically). 22 source files + 10 test files updated — pre-drop grep sweep returns zero live references for `image_processing_`, `image_processed_at`, `processed_image_path`. Highlights: (a) `buildGarmentIntelligenceFields` in `garmentIntelligence.ts` narrowed from 10-column Pick to 3-column Pick (enrichment_status + original_image_path + render_status only); `skipImageProcessing` param removed; 2 callers updated. (b) `getGarmentProcessingMessage` in `garmentImage.ts` signature tightened from `(status, renderStatus, displaySource)` → `(renderStatus, displaySource)`; `GarmentProcessingBadge.tsx` + `GarmentDetail.tsx` + `garmentImage.test.ts` updated in lockstep — the dropped `'failed'` status branch was fully subsumed by the existing `renderStatus='failed'` branch (same output copy). (c) `outfit-scoring-body.ts` penalty calc rewritten: dropped the `imageReady`/`imageConfidence` variables + `-0.3` image-ready penalty + `-0.25` image-confidence penalty + recently-added compound penalty; `GarmentReadinessSignals` interface narrowed by 2 fields; max-cap `Math.min(1.6, penalty)` still binds. (d) `outfit-scoring.ts` GarmentRow type + `burs_style_engine/index.ts:851` SELECT string dropped the 2 now-missing columns in lockstep. (e) `types.ts` manually de-noised of 21 dropped-column field entries (7 cols × 3 blocks) — CLAUDE.md auto-gen-file rule intentionally bypassed here with a synchronized migration; user regenerates via `supabase gen types typescript --linked` post-merge to confirm. (f) 3 writers switched to canonical enrichment spellings: `'complete'`→`'completed'`, `'in_progress'`→`'processing'`. `isEnrichmentReady` helper's dual-spelling acceptance kept indefinitely (defensive). **Scope expansions**: fixed 2 pre-existing deno-check blockers in `outfit-scoring.ts` (bare `as GarmentRow` → `as unknown as GarmentRow`; `missing: string[]` type widening) — both verified pre-existing via `git stash + deno check`, but my PR's burs_style_engine SELECT-string edit triggers CI deno-check. Widened `EnrichmentStatus` union type in `GarmentEnrichmentPanel.tsx:78` to accept both old + new spellings (code-reviewer P1 fix pre-push). Verification: tsc 0 errors, eslint 0 warnings, build clean, vitest 1119/1119, deno check `burs_style_engine/index.ts` clean. Post-merge: `npx supabase db push --linked --yes` applies both migrations, then `supabase gen types typescript --linked > src/integrations/supabase/types.ts` in a follow-up commit. Deploy: `burs_style_engine` (only runtime consumer of outfit-scoring-body that this change affects). |
| 2026-04-25 | #678 | P33+P34+P36 | **Wave 6 frontend localization bundle — NotFound + ShareOutfit/PublicProfile meta tags + Insights eyebrow/weekdays.** Three prompts bundled because all are pure frontend, all touch the same locale files, and zero deploys are involved (W4.9-B+D bundling precedent). (a) **P33** — Audit revealed Auth.tsx + ResetPassword.tsx already fully localized; only NotFound.tsx had hardcoded strings. Added `useLanguage()` import + replaced `"Page not found"` → `t('notfound.title')` and `"Return to Home"` → `t('notfound.return_home')`. 2 new keys. (b) **P34** — ShareOutfit.tsx lines 148-156 had 3 hardcoded `${outfit.occasion} Outfit | Styled by BURS` titles + 3 hardcoded fallback descriptions; PublicProfile.tsx lines 142-143 had hardcoded `${displayName} — BURS Style Profile` title + `Check out ${displayName}'s style on BURS` description. Replaced with placeholder-pattern keys: `t('share.meta_title_template').replace('{occasion}', outfit.occasion)`, `t('share.meta_description_full')`, `t('share.meta_description_short')`, `t('profile.meta_title_template').replace('{name}', displayName)`, `t('profile.meta_description_template').replace('{name}', displayName)`. **Placeholder convention chosen** because the existing `t: (key: string) => string` signature has no built-in interpolation; `.replace('{x}', value)` is the simplest path that doesn't require a context API change. 5 new keys. (c) **P36** — Authorized carve-out from "Permanently frozen Insights.tsx" rule per spec. Line 36 hardcoded `['M', 'T', 'W', 'T', 'F', 'S', 'S']` weekday narrows replaced with `format(new Date(2024, 0, day), 'EEEEE', { locale: dateFnsLocale })` for days 1-7. `useLanguage()` destructure extended to pull `dateFnsLocale`. `useMemo` deps gained `dateFnsLocale` so re-renders on locale change. Line 64 hardcoded `eyebrow="INSIGHTS"` → `eyebrow={t('nav.insights')}` (uses existing key, matches UnusedOutfits.tsx:162 sibling pattern + Title Case + `.caption-upper` CSS). Title fallback at line 65 already followed safe-pattern (`t('insights.yourStyleStory') || 'Your Style Story'`) — left unchanged per audit. **Total: 7 new keys appended to en.ts + sv.ts** (append-only). All 12 other locale files untouched per convention (will fall back to key string until translator pass). Verification: tsc 0 errors, eslint 0 warnings, build clean (only pre-existing vite-react-swc deprecation warning unrelated to this PR), vitest 11/11 on Insights/ShareOutfit/PublicProfile tests. Code-reviewer approved. Deploy: none. |
| 2026-04-25 | #679 | P35 | **Wave 6 P35 — LiveScan + Onboarding components localization.** Audit revealed AddGarment.tsx + OutfitDetail.tsx already fully localized; defects concentrated in LiveScan.tsx (originally CoachMark only, scope expanded to 6 strings on the same screen) and 5 onboarding components: GetStartedStep.tsx (lines 47-48 eyebrow + title), LanguageStep.tsx (lines 25-26 eyebrow + title — title reuses existing `onboarding.language.title` key), AccentColorStep.tsx (lines 61, 69, 75 live-preview labels including the leftover Swedish "Favorit" in English code path — see Findings Log; component is dead code with zero importers), QuickStyleQuiz.tsx (lines 461-463 eyebrow + title + description), QuickUploadStep.tsx (lines 152-153 eyebrow + title). **Scope expansion (Fix Protocol criterion (c))**: LiveScan.tsx lines 754 + 756 + 766 had additional hardcoded strings (`"SCANNING"` label, `"Point camera at your clothing item"` instruction, `ctaLabel="Generate a look"`) flagged by code-reviewer as sibling defects on the same screen — fixed inline. **16 new keys** appended to en.ts + sv.ts (append-only): `livescan.coach.scan_title`, `livescan.coach.scan_body`, `livescan.coach.cta_label`, `livescan.scan_aria`, `livescan.scanning_label`, `livescan.scan_instruction`, `onboarding.eyebrow_generic` (shared LanguageStep + QuickUploadStep), `onboarding.getstarted.eyebrow_ready`, `onboarding.getstarted.title_ready`, `onboarding.accent.preview_outline`, `onboarding.accent.preview_favorite`, `onboarding.accent.preview_premium`, `onboarding.quiz.eyebrow`, `onboarding.quiz.title_intro`, `onboarding.quiz.intro_desc`, `onboarding.upload.title_intro`. Other 12 locale files NOT touched per convention. Verification: tsc 0 errors, eslint 0 warnings on touched files, build clean (only pre-existing vite-react-swc deprecation warning), vitest 1022/1022 (full suite). Code-reviewer approved. Deploy: none. |
| 2026-04-25 | #680 | P32 | **Wave 6 P32 — Extend langName maps to 14 locales.** Spec named 5 functions but `smart_shopping_list` was removed in P22; net scope is 4 functions. (a) `mood_outfit/index.ts:266` + `wardrobe_aging/index.ts:54` had binary `langName = locale === "sv" ? "svenska" : "English"` — replaced with full 14-entry `LOCALE_NAMES` map. (b) `travel_capsule/index.ts:486-488` had a partial 8-locale map (sv/en/no/da/fi/de/fr/es); extended to 14 with capitalization standardized to canonical native names (was lowercase + "finska" the Swedish-ized form, now `Suomi` the Finnish native form). (c) `clone_outfit_dna/index.ts` had NO locale awareness — defaulted to English regardless of caller. Added `locale = "en"` to the request-body destructure, the `LOCALE_NAMES` map, and `Respond in ${langName}.` as rule #5 in the system prompt. Caller `useCloneOutfitDNA` in `src/hooks/useAdvancedFeatures.ts` updated to pull `locale` from `useLanguage()` and include it in the body; test mock + assertion updated. Native-name map matches `src/i18n/types.ts:9-24` SUPPORTED_LOCALES (sv→Svenska, en→English, no→Norsk, da→Dansk, fi→Suomi, de→Deutsch, fr→Français, es→Español, it→Italiano, pt→Português, nl→Nederlands, pl→Polski, ar→العربية, fa→فارسی). Verification: tsc 0 errors, eslint 0 warnings, vitest useAdvancedFeatures 10/10. Code-reviewer approved. Deploy: 4 functions (mood_outfit, wardrobe_aging, clone_outfit_dna, travel_capsule). |
| 2026-04-25 | #676 | W4.9-followup | **Wave 4.9 follow-up cleanup PR [cleanup].** Drains the remaining open Findings Log rows attributable to Waves 0-4.9. Code change: deletes the dead `handleSyncAll` function from `supabase/functions/calendar/index.ts` (-69 LOC: 71-LOC function body + dispatcher case + `timingSafeEqual` import + 1-line doc comment). P2 finding RESOLVED — re-audited on main `f2cd0301`: zero `cron.job` rows reference `calendar` or `sync_all`; zero `analytics_events` rows for `metadata->>'fn' = 'calendar'` over 7d; only in-repo references were the function's own self-referential definition. Tracker updates (CLAUDE.md): (a) P2 row marked RESOLVED with full re-audit evidence; (b) P0d-iv row promoted from PARTIALLY RESOLVED → FULLY RESOLVED (user provisioned `SUPABASE_SERVICE_ROLE_KEY_TEST` 2026-04-25, verified by CI run #24930703508 rerun success); (c) P4 main-repo-cleanup row marked RESOLVED (main repo synced to `f2cd0301`, `tmp-db-push` branch deleted, stale `types.ts` partial regen with stderr-leak garbage discarded, worktree registry pruned 12→4); (d) W4.9 user-action checkbox list flipped all 3 items from `[ ]` to `[x]`. Also confirmed: `types.ts` regen produces zero content diff vs manually-de-noised W4.9-A version (only CRLF/LF) — manual edit was perfect. Verification: tsc 0 errors, eslint 0 warnings, build clean (Husky pre-commit), code-reviewer subagent approved. Deploy: `calendar` only (no shared module fanout). |
| 2026-04-25 | #674 | P29+P31 | **Wave 5 closure tracker-only PR — flips P29 + P31 to `[DONE-subsumed]`, completes Wave 5.** Zero code changes. Two parallel sub-agent investigations on `main` (commit `ca0bf856`, post-P28-tracker merge): (a) **P29** — every layer of `StyleChatActiveLookInput` serialization verified working: types match between `src/lib/styleChatContract.ts:21-27` and `supabase/functions/_shared/style-chat-contract.ts`; client serializes via `AIChat.tsx:820-838` (refine-mode pulls from `refineMode.activeGarmentIds`, follow-up turns hydrate from `currentVisibleLook.active_look` preserving `anchor_garment_id` + `anchor_locked`); DB persistence via `AIChat.tsx:407-426` `persistMessages` + readback via `parseStoredMessage` (lines 85-105) round-trips `stylistMeta.active_look` faithfully; `getLatestActiveLook(messages)` (line 638) reconstructs envelope from history; backend `style_chat/index.ts:927-944` deserializes + lines 947-949 promote `anchor_garment_id` to `selectedGarmentIds` when `anchor_locked` is true; response envelope at lines 1840-1866 echoes `anchor_garment_id` + `anchor_locked` back to client. Persistence machinery established pre-P26 + sharpened by **P26 (PR #665)** anchor-resolution fix. (b) **P31** — RefineChips (`RefineChips.tsx:26-99`) is intentionally a STATELESS message-emitter (calls `onChipTap(chip.message)` with plain text only at line 90 — zero payload construction). RefineBanner (`RefineBanner.tsx:15-58`) is purely PRESENTATIONAL (only X-button calls `onStopRefining`). Parent `AIChat.tsx:810-841` owns payload construction and includes `active_look.garment_ids: refineMode.activeGarmentIds` (FULL outfit) + `locked_slots` whenever `refineMode.isRefining`. Architecture-by-design subsumption — original P31 spec misread the chip components as payload owners. Tracker PR: (a) flipped P29 status `[TODO]` → `[DONE-subsumed]` with 6-layer audit trail; (b) flipped P31 status `[TODO]` → `[DONE-subsumed]` with architecture-by-design rationale; (c) advanced `CURRENT PROMPT` P29 → P25 (next `[TODO]` in numeric order — P25 depends on Wave 7 schema so realistically next actionable is Wave 6 P32); (d) `LAST UPDATED` stays 2026-04-25. Zero code changes; tsc/eslint/build no-op. **Wave 5 fully complete** (P28+P29+P31 subsumed, P30 shipped). Deploy: none. |
| 2026-04-25 | #673 | P28 | **Wave 5 P28 [DONE-subsumed] tracker-only PR.** Zero code changes. Investigated P28 end-to-end on `main` (commit `8a240d0c`, post-P30 merge) and verified the refine flow already correctly threads full outfit context at every layer — UI (`OutfitSuggestionCard.tsx:332` passes `garments.map(g => g.id)`, never anchor), client handler (`AIChat.tsx` `handleEnterRefine` stores full list and ships `active_look.garment_ids` + `locked_slots` on every refine-mode message), backend (`style_chat/index.ts:1471-1479` threads `active_look_garment_ids` + `locked_garment_ids` + `requested_edit_slots`; `unified_stylist_engine.ts:83` maps `mode:"refine"`→`"generate"` while preserving full context at line 124; `burs_style_engine` uses `buildActiveLookSlotMap` + `rankCombosForRefinement` to enforce locked-preservation + edit-slot-change). P28's intent fully delivered by **P26 (PR #665)** — slot mapping fix that replaced fail-open `slot:"unknown"` with `classifySlot()` — and **P30 (PR #672)** — classifier routing that surfaces refine intents to the override path. Tracker PR: (a) extended Status Legend with `[DONE-subsumed]` (analogous to `[DONE-partial]`); (b) flipped P28 status from `[TODO]` to `[DONE-subsumed]` with full investigation notes citing each layer; (c) updated the existing Wave 5 spec drift Findings Log row to definitive `RESOLVED` verdict for P28; (d) advanced `CURRENT PROMPT` from P28 to P29 (next `[TODO]`); (e) `LAST UPDATED` 2026-04-24 → 2026-04-25. Zero code changes; tsc/eslint/build no-op. Note: **P29 + P31 still TODO** per same investigation likely subsumed but left for the user to scope explicitly. Deploy: none. |
| 2026-04-25 | #677 | P41 | **Wave 6 P41 — UnusedOutfits Swedish/English key mixing.** Replaced `OCCASIONS = ['vardag', 'jobb', 'dejt', 'fest', 'casual', 'smart_casual']` (4 Swedish + 2 English mix) at `src/pages/UnusedOutfits.tsx:29` with the canonical English-keyed 6-occasion vocabulary `['casual', 'work', 'date', 'party', 'workout', 'travel']` — matches `OutfitGeneratePicker.tsx:29-36`, `QuickGenerateSheet.tsx:62-69`, `AdjustDaySection.tsx:10-15`. Localized the inline `"unused pieces"` text at line 290 via new key `'insights.unused_pieces_label'` ("unused pieces" / "oanvända plagg") appended to `src/i18n/locales/en.ts` + `sv.ts` per append-only rule. Behavior-preserving for downstream display: `getOccasionLabel(t, occasion)` in `src/lib/humanize.ts:75-81` already handled both Swedish-keyed and English-keyed values via the `occasion.${occasion.toLowerCase()}` key lookup, and en.ts had both `occasion.vardag` AND `occasion.casual` (rendered as "Casual"). **Initial Wave 6 audit verdict'd P41 as DONE-subsumed; main agent caught the OCCASIONS array defect during pre-flip independent verification (Wave 5 P28 precedent — always verify before flipping subsumed) — see Findings Log row.** Verification: tsc 0 errors, eslint 0 warnings, build clean, vitest unchanged (no new tests added — change is internal data shape + 1 i18n key, both validated by build). Deploy: none. |
| 2026-04-25 | #681 | P40 | **Wave 6 P40 — Multi-locale regexes (FORMAL_KEYWORDS + CHAT_SHORT_RE).** `OutfitGenerate.tsx:78` `FORMAL_KEYWORDS` expanded from English-only 13 keywords to 50+ formality-trigger words across 12 Latin-script locales with `/u` flag for non-ASCII `\b` boundaries. `shopping_chat/index.ts:137` `CHAT_SHORT_RE` expanded from 22 English tokens to ~85 across all 14 locales including ar/fa native script. RTL scripts intentionally omitted from FORMAL_KEYWORDS pending `\b` Unicode-boundary testing. Translator-pass logged in Findings Log. **Codex P2 fix-up commit**: locale-scope FORMAL_KEYWORDS via per-locale map (Polish "cena"=price collision with Spanish/Italian "cena"=dinner eliminated); broaden CHAT_SHORT_RE punctuation to leading [¿¡]? + trailing class adds ¿¡؟،, supporting `¿hola?`, `¡hola!`, `مرحبا،`, `مرحبا؟`. **Pre-existing TS2345 deno-check fix-up commit**: cast `supabase as ReturnType<typeof createClient>` at `getWardrobeContext` call site — same fix pattern as PR #675's style_chat fix. Verification: tsc 0 errors, eslint 0 warnings, Node.js regex sanity tests confirm correct matching. Deploy: `shopping_chat`. |
| 2026-04-25 | #682 | P37+P38+P39 | **Wave 6 closing bundle — locale extensions for AI classifier shared modules.** Three independent prompts bundled per W4.9-B+D + P33+P34+P36 precedent: each touches different files, all are pure data-array extensions (no logic changes), all share the same translator-pass caveat. (a) **P37** narrowed to frontend hint localization — MoodOutfit.tsx 12 hardcoded `hint:` literals → `hintKey:` referencing 12 new `mood.hint.*` keys appended to en.ts + sv.ts (24 entries total). MOOD_MAP in retrieval.ts intentionally NOT restructured: it's AI direction, AI handles localized output via existing langName from P32. Minimum-change interpretation per Fix Protocol. (b) **P38** garment vocabulary extended across 12 additional locales (no/da/fi/de/fr/es/it/pt/nl/pl/ar/fa) in 3 shared classifier modules: `_shared/burs-slots.ts` (6 token arrays + accent-stripped variants since this module DOES strip), `_shared/outfit-rules.ts` (5 token arrays + BASE_LAYER + MID_LAYER + ~95 EXPLICIT_SLOT_MAP entries; no accent strip so accented + ASCII fallback both included), `_shared/travel-capsule-planner.ts` (5 token arrays). Cross-language collisions skipped: ES/PT "saco" (jacket vs bag), NL "rok" (subset of SV "rock"), DE "hut"/PL "pas" too short, DE "strickjacke" subset of "jacke", FA cardigan/jacket overlap. (c) **P39** OCCASION_RULES tags + 3 inline arrays in `_shared/day-intelligence.ts` extended for all 13 additional locales. Real accented forms used (möte, häät, télétravail) since `normalizeText` only `.toLowerCase()`s — no accent strip — different from burs-slots. Tag growth: formal 6→35, party 5→39, work 7→70, dinner 5→38, travel 9→70, workout 7→47, social 5→41, remote 4→33. Inline travelTagged 6→47, workoutTagged 6→47, outdoorTagged 5→32. Cross-language collisions skipped: FR "course" (academic class), FR "vol" (3-char "Vol. 2" abbrev), PL "lot" (3-char English "lot"). **Implementation**: 3 parallel sub-agents — P37 frontend, P38 token vocab, P39 calendar tags — wrote independent file changes; main agent integrated + ran pipeline. Verification: tsc 0 errors, eslint 0 warnings, vitest 1328/1328 (full suite). Deploy: `summarize_day` (P39 sole consumer of day-intelligence.ts), `mood_outfit` + `generate_outfit` + `clone_outfit_dna` (burs-slots + outfit-validation chain consumers from P38), `travel_capsule` (travel-capsule-planner.ts consumer from P38). MOOD_MAP not changed → mood_outfit redeploy is for burs-slots only. **Wave 6 fully complete** (10/10 prompts: P32-P36 + P40 + P41 + this bundle). |
| 2026-04-26 | #683 | P42+P43+P44 | **Wave 7 PR 1 — Backend foundation (P42 schema + P43 rate-limit + P44 route gate).** Bundled per approved 5-PR phasing in `docs/superpowers/specs/2026-04-26-wave-7-onboarding-design.md`. (a) **P42 migration** — `supabase/migrations/20260426120000_onboarding_state.sql`: 4 new `public.profiles` columns (`onboarding_step text NOT NULL DEFAULT 'not_started'`, `onboarding_garment_count integer NOT NULL DEFAULT 0`, `onboarding_started_at timestamptz`, `onboarding_completed_at timestamptz`); CHECK constraint pinning the 10-value step enum; partial index on still-onboarding rows; idempotent backfill of existing `preferences->'onboarding'->>'completed' = true` rows to `step='completed'`; 2 SECURITY DEFINER RPCs (`advance_onboarding_step`, `increment_onboarding_garment_count`) with ownership check. Migration drift check pre-merge clean. (b) **P43 scale-guard** — added `onboarding: 3.0` tier to `_shared/scale-guard.ts`; `resolveUserPlan` now queries `profiles` in parallel with `subscriptions` and overrides `effectivePlan` to `"onboarding"` when `onboarding_started_at` is within 24h AND `onboarding_step !== 'completed'`. Wave 8 forward-compat: only the multiplier table changes when free tier is removed. New test file `_shared/__tests__/scale-guard.test.ts` (19 tests: tier multiplier math, onboarding boost detection across 9 input cases, subscription regression net, per-isolate cache). (c) **P44 ProtectedRoute** — source of truth swapped from `preferences.onboarding.completed` to `profile.onboarding_step !== 'completed'`. Single `skipOnboardingCheck` prop removed; replaced by pathname allow-list `ONBOARDING_EXEMPT_PATHS` in new `src/components/auth/onboardingExempt.ts` module (split from ProtectedRoute.tsx to satisfy `react-refresh/only-export-components` lint rule). Lockstep: `AnimatedRoutes.tsx:125` drops the `skipOnboardingCheck` prop usage; `AnimatedRoutes.discoverRedirect.test.tsx:15` mock prop typing updated. ProtectedRoute.test.tsx rewritten to 16 tests covering auth gate, onboarding gate (4 step states), exempt-path passthrough (4 paths), and `isOnboardingExempt` matcher (8 cases). (d) **Scope expansion (Fix Protocol exception (b))** — code-reviewer subagent flagged a rollout-bridge gap: `useOnboarding.completeOnboarding()` only wrote the legacy `preferences.onboarding.completed=true` flag and never touched `onboarding_step`, so users finishing the OLD flow between PR 1 merge and PR 2 ship would land in a redirect loop (legacy flag set, new gate ignores it, `onboarding_step` still 'not_started'). Fix: `completeOnboarding()` now ALSO calls `supabase.rpc('advance_onboarding_step', ...)` before the legacy write. 4 new tests cover RPC integration (called with correct args, legacy write only on success, error blocks legacy write, no-op tolerated). See Findings Log row dated 2026-04-26 for full detail. Verification: tsc 0 errors, eslint 0 warnings, build clean (only pre-existing vite-react-swc deprecation warning), vitest 1361/1361 (33 net new tests vs prior 1328), `deno check supabase/functions/mood_outfit/index.ts` clean (validates shared-module typing). Code-reviewer subagent approved pre-push (rollout-bridge finding folded into the PR). **Deploy radius**: post-merge `db push` (1 migration), `supabase gen types typescript --linked` (separate small follow-up PR per CLAUDE.md auto-gen rule), then 22 AI functions ONE AT A TIME (Shared Module Deploy Map for `_shared/scale-guard.ts`). |
| 2026-04-26 | #684 | (catch-up) | **Wave 7 PR 1 follow-up — types.ts regen post-migration.** Mechanical `npx supabase gen types typescript --linked` regen after PR #683's `20260426120000_onboarding_state.sql` migration applied to prod. Adds 4 new `profiles` columns (Row/Insert/Update for `onboarding_step` / `onboarding_garment_count` / `onboarding_started_at` / `onboarding_completed_at`) + 2 RPCs (`advance_onboarding_step` / `increment_onboarding_garment_count`) into `Database['public']['Functions']` union. Diff: 1 file, +20 lines. Codex approved ("Didn't find any major issues. Hooray!"). Tracker row added retroactively in PR #685 — catch-up convention from CLAUDE.md Launch Plan Update rule. Deploy: none. |
| 2026-04-27 | #685 | P45 | **Wave 7 PR 2 — Style DNA Quiz V4 (12-question rebuild).** Replaces `QuickStyleQuiz` (10 Qs, V3 shape) with `StyleQuizV4` (12 Qs per spec, V4 shape with `version: 4` field). New `src/types/styleProfile.ts` (363 LOC) — V4 schema with 18 enum unions + `LifestyleMix` + 5 catalog consts (`ARCHETYPE_OPTIONS` 12 / `COLOR_SWATCHES` 18 / `FABRIC_OPTIONS` 10 / `FABRIC_SENSITIVITY_OPTIONS` / `OCCASION_OPTIONS` 8) + `createEmptyStyleProfileV4()` + `migrateV3ToV4()` + `migrateV4ToV3Compat()` shim. New `src/components/onboarding/StyleQuizV4.tsx` (1083 LOC) — 12-screen quiz mirroring `QuickStyleQuiz` patterns (`OptionBtn`/`ChipBtn`/`QWrap`/`ProgressDots` adapted), AnimatePresence slide transitions, Q11 single-pick auto-advances; Q1/Q5/Q6/Q8/Q10 multi-field cards require explicit Next. **localStorage draft persistence** keyed on `burs.quizV4.draft.${userId}` — restore on mount, clear on complete/skip; satisfies spec acceptance "On reload, answers persist". **canAdvance gates** — Q1 disabled until all four sub-fields touched (gender/height_cm/build/ageRange via separate `q1Touched` interaction state, so `'prefer_not'` defaults don't trap users), Q4 disabled until `archetypes.length >= 3` (with destructive-color hint "Pick 3-5"). **safeT()** used for `skip`/`next`/`finish`/`question_label`/Q4 hint to fall back cleanly when locale missing the key (helper from PR #678). New `src/components/onboarding/__tests__/StyleQuizV4.test.tsx` (7 tests covering Q1 gate, Q4 min-3 gate, persistence restore, advance behavior, skip wiring). Modified `src/pages/Onboarding.tsx` — swapped to V4, `handleQuizComplete` accepts V4 shape, applies `migrateV4ToV3Compat` before persisting to `preferences.styleProfile` so legacy V3 readers (`burs_style_engine`/`style_chat`/`_shared/outfit-scoring.ts` reading `styleWords`/`hardestOccasions`/`wardrobeFrustrations`/`fit`/`genderNeutral`/`patternFeeling`/`comfortVsStyle`/`bursGoal`/`freeText`) keep finding populated values; maps V4 gender to legacy `mannequin_presentation` enum (feminine→female, masculine→male, others→mixed); sets `height_cm` from V4. After save, calls `advanceOnboardingStep(user.id, 'photo_tutorial')` (NOT `'completed'` — that's later in flow), wrapped in try/catch (deploy-window pattern). 142 new `styleQuizV4.*` keys appended to en.ts + sv.ts (append-only); other 12 locale files NOT touched per convention. **Code-reviewer findings addressed pre-push**: 2 P0 (V3-only edge function readers + SettingsStyle.tsx breakage) covered by `migrateV4ToV3Compat` shim writing both shapes; 3 P1 (no persistence / no min-3 gate / broken `||` fallback chain) all fixed inline. **Out of scope (Findings Log)**: SettingsStyle.tsx writes V3 mirror keys only — drift on edits from that page tracked as future cleanup; `QuickStyleQuiz.tsx` and `StyleQuizV3.tsx` LEFT IN REPO (only `StyleQuizV3` still referenced as type source by the back-compat shim); future P25 (Style DNA context injection) should consume V4 keys directly so the back-compat shim can be deprecated. Verification: tsc 0 errors, eslint 0 warnings, build clean, vitest 1381/1381 (+4 net new vs prior 1377 baseline). Deploy: none (frontend-only, Vercel auto-deploys). |
| 2026-04-27 | #686 | P46 | **Wave 7 PR 3 — PhotoTutorial screen.** New 1-screen tutorial that fills the `'photo_tutorial'` slot in the onboarding state machine between quiz and batch_capture. New `src/components/onboarding/PhotoTutorialStep.tsx` (186 LOC) — hero card with `Camera` icon + `PageIntro`, 2-up "good vs bad" examples grid (styled divs with `Check`/`X` icons + emerald/red tints, dark-mode-aware), 5-bullet tips list with stagger-in motion (`EASE_CURVE`, 0.05s delay step) and lucide icons (`Sun`/`Layers`/`Frame`/`UserX`/`Shirt`), floating action bar with primary "I'm ready" CTA via `safeT()` fallback. New `src/components/onboarding/__tests__/PhotoTutorialStep.test.tsx` (41 LOC) with 2 smoke tests (renders 5 bullet labels via humanized-fallback `useLanguage` mock + clicking CTA fires `onComplete`). Modified `src/pages/Onboarding.tsx` — `STEPS` array extended `['lang','quiz','upload','getstarted']` → `['lang','quiz','photo_tutorial','upload','getstarted']`; new `[photoTutorialDone, setPhotoTutorialDone]` state and `stepKey` chain; new `handlePhotoTutorialComplete` calls `advanceOnboardingStep(user.id, 'batch_capture')` BEFORE setting local state, wrapped in try/catch + toast on failure (deploy-window pattern matching post-quiz transition); local state always advances so user is never trapped. 16 new `photoTutorial.*` keys appended to en.ts + sv.ts (append-only); other 12 locale files NOT touched per existing convention. **State machine note**: server flips to `'batch_capture'` after CTA, but local UI advances `photo_tutorial → upload` because P47 BatchCapture isn't built yet — user lands on existing `QuickUploadStep`. Once P47 ships, `STEPS` array gets `'batch_capture'` inserted between `'photo_tutorial'` and `'upload'` and the local state machine extends. **Code-reviewer subagent APPROVED** pre-push (no P0/P1 blockers). Verification: tsc 0 errors, eslint 0 warnings, build clean, vitest 3/3 (2 PhotoTutorialStep + 1 Onboarding renders-without-crashing). Deploy: none (frontend-only, Vercel auto-deploys). |
| 2026-04-27 | #687 | P47 | **Wave 7 PR 4 — BatchCapture screen.** Replaces `QuickUploadStep` in the local STEPS array with a 20-50 garment batch upload flow. New `src/components/onboarding/BatchCaptureStep.tsx` (369 LOC) — multi-upload UI: PageIntro card + counter card with `Progress` bar (caps visually at MIN=20 / RECOMMENDED=30) + 4-col captured-thumbnails grid + bottom action bar (Add photos / Continue / Done). MIN=20 / RECOMMENDED=30 / MAX=50. "Continue" disabled until count ≥ 20; "Done" surfaces at count ≥ 30. Counter source of truth = `profile.onboarding_garment_count` (server) + local `optimisticBumps` delta synced via `useEffect` on serverCount change. Sequential per-file pipeline (compress with raw-fallback for Median/WebView → upload to garments bucket → `buildGarmentInsert` → `triggerGarmentPostSaveIntelligence` with `source: 'batch_add'` for fire-and-forget enrichment + render → `incrementOnboardingGarmentCount` RPC). Failure handling: increment-RPC failure logged but garment row already saved (count drift only narrows boost-window scope). Storage orphan-on-partial-upload class flagged in P27b — already scheduled for post-launch GC; no new finding. New `src/lib/incrementOnboardingGarmentCount.ts` (32 LOC) typed wrapper — pattern-matches `advanceOnboardingStep.ts`, throws on RPC error, returns numeric new count. No `as any` cast needed since the RPC is in the regenerated types.ts (PR #684). New `src/components/onboarding/__tests__/BatchCaptureStep.test.tsx` (90 LOC) — 3 smoke tests covering count=0/20/30 gate transitions and CTA wiring. Modified `src/pages/Onboarding.tsx` — STEPS `'upload'`→`'batch_capture'`, swapped `<QuickUploadStep>` for `<BatchCaptureStep>`, renamed `uploadDone` state → `batchCaptureDone`, new `handleBatchCaptureComplete` calls `advanceOnboardingStep(user.id, 'achievement')` BEFORE setting local state, wrapped in try/catch + toast on failure (deploy-window pattern matching post-quiz / post-tutorial transitions). 9 new `batchCapture.*` keys appended to en.ts + sv.ts (append-only). **`QuickUploadStep` left as orphan code** — no longer rendered or imported; future cleanup PR can delete the component file + its 3 namespace keys after design QA. **State machine**: server flow now `quiz → photo_tutorial → batch_capture → achievement`. After Done, `onboarding_step='achievement'`; local UI advances to `getstarted` placeholder until P48 (Achievement screen) ships. **Code-reviewer subagent APPROVED** pre-push (no P0/P1). One nice-to-have suggestion addressed inline: removed unused `.replace('{count}', ...)` calls on `min_helper` / `recommended_helper` since locale strings don't contain that placeholder (totalCount renders separately in a large `<span>`). **Codex quota exhausted** — Gate 10 fallback (code-reviewer subagent approval) used. Verification: tsc 0 errors, eslint 0 warnings, build clean, vitest 1388/1388 (3 net new vs prior 1385). Deploy: none (frontend-only, Vercel auto-deploys). |
| 2026-04-27 | #688 | P48 (partial) + Wave 7 audit fixes | **Wave 7 PR 5 expanded scope: AchievementStep (frontend-only) + 10 P0 audit-fix consolidation.** Original PR #688 shipped only AchievementStep (`src/components/onboarding/AchievementStep.tsx`, 123 LOC; Onboarding.tsx wires `'achievement'` step in STEPS array; 6 new `achievement.*` i18n keys). User directive 2026-04-27 mid-session ("688 is not merged so fix the findings in it then update 689") expanded scope to also carry every P0 from the parallel-subagent audit (PR #689 docs the audit). Audit fixes added in 4 parallel sub-agent dispatches + 1 code-reviewer P1 round: **(#1)** new migration `20260427120000_onboarding_increment_bounds.sql` bounds `increment_onboarding_garment_count` at 200 + skips when step='completed'; **(#2 + #3)** `_shared/scale-guard.ts` — `noTierMultiplier` carve-out so onboarding boost still applies to analyze_garment, `ttlForPlan` returns 60s for onboarding plan instead of 5min; **(#4)** `advanceOnboardingStep.ts` + `incrementOnboardingGarmentCount.ts` accept optional `queryClient` and invalidate `['profile', userId]` on success; **(#5)** Onboarding.tsx hydrates step state from server on mount via `useEffect` + `hasHydratedRef` guard so reload after partial completion lands the right step; **(#6)** lang step calls `advanceOnboardingStep(uid, 'language', queryClient)` before local state advance; **(#7 + #8)** `BatchCaptureStep.tsx` proper optimistic-update pattern with rollback + `refetchQueries` + error toast; **(#9)** `styleProfile.ts` `migrateV4ToV3Compat` rewritten to explicitly translate 6 same-name collision keys via the bidirectional translators (no more `{ ...compat, ...v4 }` spread bug); SettingsStyle.tsx lockstep no-V4-translate-on-write; **(#10)** `completeOnboarding` 3-attempt retry on legacy preferences write with 500ms backoff. **DEFERRED to P48-followup (needs user approval)**: `grant_trial_gift` edge function. **Code-reviewer subagent APPROVED** after 1 P1 round (BatchCaptureStep missing queryClient arg — fixed inline; tests updated to assert it). **Codex quota exhausted** — Gate 10 fallback per memory's standing rule. Verification: tsc 0 errors, eslint 0 warnings (repo-wide --max-warnings 0), npm build clean (31s), vitest 1478/1478 pass (+90 net new tests vs prior 1388: 19 scale-guard expansion + 6 advanceOnboardingStep + 3 useOnboarding + 5 BatchCaptureStep + 74 styleProfile + integration). **Files**: 16 modified + 2 new (1 migration + 1 test file). **Deploy**: post-merge `npx supabase db push --linked --yes` (1 new migration); `_shared/scale-guard.ts` change → redeploy ALL 22 AI functions ONE AT A TIME per CLAUDE.md hard rule; frontend changes auto-deploy via Vercel. |
| 2026-04-27 | #690 | P48-followup | **Wave 7 PR 6 — grant_trial_gift edge function (P48-followup).** User explicitly approved adding the new edge function ("auto merge pr 689 and then finish the wave 7 first p48 follow up"). New `supabase/functions/grant_trial_gift/index.ts` (158 LOC) wraps the existing `grantTrialGift()` helper from `_shared/render-credits.ts:300`. Auth: caller's JWT verified via `getUser()`, only the verified `user.id` reaches the helper (body is empty), service-role client constructed inside, hardcoded `TRIAL_GIFT_AMOUNT=3` server-side — cross-user gift abuse impossible. Idempotent on key `onboarding_gift_${user.id}` — re-mount during dev hot-reload / network retry / React StrictMode never double-credits (the underlying `grant_trial_gift_atomic` RPC's existence-check on `idempotency_key` returns `{ok:true, duplicate:true}` before any UPDATE). New rate-limit tier `grant_trial_gift` in `_shared/scale-guard.ts` (5/min, 20/hour) — tight enough to bound retry storms, loose enough that StrictMode + a few retries don't trip. New `[functions.grant_trial_gift]` stanza in `supabase/config.toml` (`verify_jwt = false`, matching project pattern). AchievementStep.tsx gains a mount-time `useEffect` calling `invokeEdgeFunction('grant_trial_gift', { body: {} })` (resilient wrapper with timeout/retry/circuit-breaker — same pattern BatchCaptureStep uses). Success path invalidates `['render_credits', userId]` so P49 (StudioSelection) sees the new credits without staleTime wait. Failure paths log + continue — CTA never gates on the grant. AchievementStep.tsx file header (lines 14-37) rewritten from "Scope decision: deferred" to reflect the now-shipped state. **Code-reviewer subagent APPROVED** (no P0/P1; spec-compliance check on the file header passed). Verification: tsc 0 errors, eslint 0 warnings, build clean (19s), vitest 4/4 (2 pre-existing + 2 new). **Files**: 1 new edge function + 4 modified (scale-guard, config.toml, AchievementStep.tsx, AchievementStep.test.tsx). **Deploy**: `grant_trial_gift` (new function deploy). `_shared/scale-guard.ts` change is purely additive (tier entry only) — no behavior change for the other 22 AI consumers, so they don't need redeploy in this PR. |
| 2026-04-27 | #691 | P49 | **Wave 7 PR 7 — StudioSelection screen.** New `src/components/onboarding/StudioSelectionStep.tsx` (~360 LOC) lets the user pick exactly 3 garments from their wardrobe and enqueues 3 `render_jobs` via `enqueueRenderJob(garmentId, 'manual_enhance', { clientNonce })`. The trigger source `'manual_enhance'` is the closest existing enum (Wave 4.5-B SecondaryImageManager precedent); the `source='trial_gift'` from the launch-plan spec refers to `render_credit_transactions.source` which is auto-chosen by `reserve_credit` RPC drawing from `trial_gift_remaining` first — automatically consumes the 3 credits granted by P48-followup. UI: PageIntro card + 4-col LazyImage grid + numerical badge on selected (1/2/3 in warm-gold) + counter "1 of 3" → "3 of 3 — perfect" with color flip + locked CTA "Render my 3 pieces" until count===3. **Cannot skip/close** — no skip button, no escape route per spec. **Code-reviewer round 1 found 2 P0 + 2 P1 + 1 P2** all fixed inline: (#1) **nonce reuse on transport-failure retry** — first impl generated fresh `crypto.randomUUID()` per call; if server reserved a credit but transport failed, retry would double-charge. Fix: `clientNoncesRef.current: Map<string, string>` stores per-garment nonce, retries reuse same nonce, server dedups. (#2) **narrow 402 detection** — first impl only matched `err.status===402`; edge function returns 402 with body `{error: 'trial_studio_locked'}` OR `'insufficient_credits'`. Defense-in-depth fix: catch `status===402 || code==='insufficient_credits' || code==='trial_studio_locked'`. (#3) Onboarding.tsx hydration map gains `TODO(P50)` for post-studio_selection states. (#4) Selection-mutation locked during partial batch (`enqueuedIds.size > 0 && < 3`) → busy toast `studioSelection.partial_progress`. (#5) Dropped `sortedGarments` useMemo no-op + stale comment. New test file with 9 tests (5 original + 4 covering nonce-reuse-on-retry / 402-no-credits / code-aliases / partial-lock). 11 new `studioSelection.*` keys in en.ts + sv.ts. Modified Onboarding.tsx: STEPS array gains `'studio_selection'`, new `studioSelectionDone` state + `handleStudioSelectionComplete` advancing to `'coach_tour'` via deploy-window try/catch, hydration map + stepKey nesting extended. Verification: tsc 0 errors, eslint 0 warnings, build clean, vitest 9/9 on StudioSelectionStep. Deploy: none (frontend-only). |
| 2026-04-27 | #692 | P50 | **Wave 7 PR 8 — CoachTour onboarding step.** New `src/components/onboarding/CoachTourStep.tsx` (~280 LOC) — pragmatic single-screen interpretation of the spec (full app-shell tour overlays via existing `useFirstRunCoach` + `CoachMark` infrastructure deferred as a deeper product feature; 5 tip cards covering Home / Wardrobe / Outfits / AI Chat / Garment Detail destinations satisfy the user mental model + spec deliverable in one screen). Realtime subscription on `garments` table watching the 3 watched garment IDs — when any flips to `render_status='ready'`, CTA copy + accent shifts to celebrate. **Code-reviewer round 1 P1 fix**: watched IDs come from `render_jobs` (status IN ['pending','in_progress'], order by created_at desc, limit 3) NOT from wardrobe recency — P49 lets users pick ANY 3 from a grid, so the prior "3 most-recent garments" derivation was silently dead for any user picking older items. Fix uses `useQuery` reading the canonical render_jobs set. 17 new `coachTour.*` keys in en.ts + sv.ts. Onboarding.tsx: STEPS array gains `'coach_tour'`, hydration map distinguishes `coach_tour` vs `reveal` (TODO(P50) removed, TODO(P51) added for post-coach_tour). 8 tests including a regression test for the P1 (picked garments != newest garments → realtime fires only on actual picks). Verification: tsc 0 errors, eslint 0 warnings, build clean, vitest 8/8. Deploy: none. |
| 2026-04-27 | #693 | P51 | **Wave 7 PR 9 (CLOSER) — Reveal final tutorial page.** New `src/components/onboarding/RevealStep.tsx` (~270 LOC) showing the user the first of their 3 trial-gift studio renders with celebratory copy. Hero `useMemo` priorities: ready > cooking > anything-else (failed-and-retrying). Realtime subscription on `garments` watches the 3 watched IDs (sourced from `render_jobs` for the user, limit 3 by `created_at desc`); when one flips to `'ready'`, hero re-picks via state update. **Auto-retry once invariant** per spec — `retriedRef.current: Set<string>` ensures any garment with `render_status='failed'` gets exactly ONE `enqueueRenderJob(id, 'retry')` call; subsequent realtime UPDATEs that re-flag the same garment as failed don't trigger additional retries. While retry in flight, original photo shows via `getPreferredGarmentImagePath`. CTA "Start using BURS" → `handleRevealComplete` → `completeOnboarding` (column gate `'reveal' → 'completed'` + legacy preferences with 3-attempt retry from Wave 7 audit fix #10) → `navigate('/')`. Onboarding.tsx: STEPS gains `'reveal'` between `'coach_tour'` and `'getstarted'`, new `revealDone` state + `handleRevealComplete` handler, hydration map updated for `serverStep='reveal'` (flip every prior boolean, leave revealDone=false), stepKey nesting + render block extended. 10 new `reveal.*` keys in en.ts + sv.ts. Test file with 7 tests (cooking-state copy, realtime ready flip, channel filter, auto-retry-once invariant + idempotency, no retry on succeeded/pending, CTA, unmount cleanup). **Code-reviewer subagent verdict: APPROVE with 2 P2 polish items** (distinct all-failed copy + redundant navigate) deferred as non-blocking. Verification: tsc 0 errors, eslint 0 warnings, build clean (25s), vitest 50/50 across the onboarding test surface. **Wave 7 complete** — 8 screens end-to-end (lang → quiz → photo_tutorial → batch_capture → achievement → studio_selection → coach_tour → reveal → completed). Deploy: none. |
| 2026-04-28 | #697 | Wave 4.9 hygiene [cleanup] | **Tracker hygiene — flipped 3 stuck Findings Log rows whose underlying fixes shipped weeks ago but whose Action column was never updated.** No code changes. (1) Row 856 (P0a husky shebang) → RESOLVED in PR #641 (P0a-ii). (2) Row 864 (avatar i18n orphans) → RESOLVED in PR #670 (W4.9-B). (3) Row 870 (image_processing_status default footgun) → RESOLVED in PR #669 (W4.9-A) — 7 columns dropped + orphan job_queue rows deleted + outfit-scoring gate removed. Verifies Wave 4.9 closure is fully drained per the Wave Closure Rule. CURRENT PROMPT unchanged at P52. |
| 2026-04-28 | #696 | Wave 7.9 followup [cleanup] | **Drains 9 deferred + new audit findings from PR #694's wave-close.** Code fixes: (1) `migrateV3ToV4` archetype normalization (P1 #8) — `V3_ARCHETYPE_TO_V4` table + `v3ArchetypeToV4` helper + dedupe; (2) ColorSwatch V3→V4 alias (P1 #10) — `V3_COLOR_TO_V4` map + V4-aware SettingsStyle render; (3) BatchCaptureStep refactored to use `buildGarmentInsert` (P2 #7) — `GarmentIntakeCandidate.analysis` now nullable + safe defaults + new `BuildGarmentInsertOptions.importedVia`; (4) RevealStep all-failed copy variant (polish #1) + status filter parity (D.P1.3); (5) `handleRevealComplete` navigate redundancy (polish #2 / B.P0.2) — re-throws on failure + RevealStep async `handleAdvance` resets `advancing`; (6) SettingsStyle V4 vocab corruption fix (C.P1.2) — ChipMulti renders V4 archetype list + writes mirror to `archetypes` field; (7) `grant_trial_gift` `recordError` + 4xx routing (A.P0); (8) housekeeping — orphan `seed_wardrobe` stanza, dead ternary in `scale-guard` `claimJob`, dead branch in `StudioSelectionStep` catch removed. **15 files changed, +562/-98 LOC.** Verification: tsc 0 errors, eslint 0 warnings, build clean (12s), vitest 1502/1502 pass. Code-reviewer subagent dispatched but blocked by user's quota window — verification stands on tsc/eslint/build/vitest only. **Deploy**: `grant_trial_gift` only (sole edge function changed). `_shared/scale-guard.ts` change is a dead-line removal — semantically a no-op for the other 22 AI consumers, so they don't need redeploy in this PR. |
| 2026-04-27 | #694 | Wave 7.9 [cleanup] | **Wave 7.9 closure — drained 12 P1/P2 audit findings.** Per Wave Closure Rule: every wave ends with an Nx.9 sub-wave that drains its remaining findings before the next wave begins. Original audit (PR #689) flagged 24 P1 + 17 P2 findings as cluster rows; this PR addresses the actionable subset on post-Wave-7 main. Net diff: +283 / -524 LOC. **Code-reviewer subagent APPROVED — ship as-is.** **P1 findings**: (#1) pre-boost cache lock — RESOLVED-on-main (PR #688 already cut onboarding-plan TTL to 60s); (#2) `useOnboarding` dead-code — FIXED-here (deleted hook + 372-line test, runtime always lived in Onboarding.tsx's local handlers; advanceOnboardingStep.ts docstring updated); (#3) StyleQuizV4 onSkip didn't advance server — FIXED-here (new `handleQuizSkip` mirrors handleQuizComplete's deploy-window try/catch shape, calls `advanceOnboardingStep(uid, 'photo_tutorial')`); (#4) hardcoded "Step/Question N of M" English — FIXED-here (new `onboarding.progress_label` + `styleQuizV4.progress_label` keys in en.ts + sv.ts, applied via `interpolateMeta` from PR #678 for safe substitution); (#5) double-tap race on Continue handlers — FIXED-here (`advancing` guard added to LanguageStep, PhotoTutorialStep, AchievementStep, CoachTourStep, RevealStep; BatchCaptureStep already had `isAdvancing`; `advancing` stays true after first tap so disabled CTA blocks re-tap until unmount); (#6) MAX_GARMENTS=50 race in BatchCapture — FIXED-here (`isHandlingFilesRef` re-entry lock + per-iteration hard-cap recheck via `serverCountRef` / `optimisticBumpsRef` / `itemsRef` so each loop iteration sees fresh counts); (#7) SettingsStyle ChipMulti V4 vocab mismatch — RESOLVED-on-main (PR #688's V4 compat shim fix #9 already harmonized via per-field `v4XToV3` translators + `display*` helpers); (#8) `migrateV3ToV4` archetype normalization — DEFERRED (needs an archetype-translation table; out of scope for this closure PR, future polish wave); (#9) SettingsStyle re-edit lossy collapse — RESOLVED-on-main (PR #688 added the V3-vocab-wins-on-collision strategy, V4-only enums route through canonical fields like `fitOverall`); (#10) ColorSwatch ID divergence (V4 18 vs SettingsStyle 36) — DEFERRED (cross-cuts SettingsStyle vocab harmonization, safer to do alongside V3→V4 deprecation in a future polish wave). **P2 findings**: (#1) `comfortVsStyle = 100 - formalityCeiling` ignored floor — FIXED-here (`migrateV4ToV3Compat` now uses `100 - average(floor, ceiling)` with Math.round + 0..100 clamp; new test cases covering casual/formal/boundary); (#2) Q1 height accepts fractional values — FIXED-here (`parseInt` instead of `Number` + `step={1}` + `inputMode="numeric"`; new test); (#3) `step === null` vs `undefined` — RESOLVED-on-main (ProtectedRoute uses `?? null` then checks `=== null`); (#4) /admin /privacy /terms exempt-list — RESOLVED-on-main (verified all 3 are mounted in AnimatedRoutes.tsx, original audit comment was outdated); (#5) `useOnboarding` schema lenient — FIXED-here (`schemas.ts` `onboarding_step` tightened from `z.string()` to `z.enum([10 values])` matching the migration's CHECK constraint exactly); (#6) ObjectURL leak on captured-thumbnails unmount — FIXED-here (bulk-revoke `useEffect` cleanup using `itemsRef.current` so it captures latest set on unmount); (#7) BatchCaptureStep manually constructs INSERT — DEFERRED (`buildGarmentInsert` requires non-null analysis shape; refactor expands scope significantly); (#8) test coverage gaps — PARTIAL (added 1 height-integer + 1 comfortVsStyle test; Q4 / draft-rehydrate / MAX-reached / RPC-failure already covered in StyleQuizV4 + BatchCaptureStep test files). **17 files changed**, ~12 actionable items addressed, 3 deferred to future polish. Verification: tsc 0 errors, eslint 0 warnings, build clean (~21s), vitest 1487/1487 pass. Deploy: none (frontend-only, Vercel auto-deploys). |
| 2026-04-28 | #698 | P52 | **Wave 8 PR 1 — Auto-start 3-day Stripe trial on first SIGNED_IN.** New `supabase/functions/start_trial/index.ts` (~330 LOC) — creates Stripe customer + trialing subscription on first sign-in, upserts `subscriptions` row to `plan='premium', status='trialing', current_period_end ≈ NOW + 3 days`, mirrors `stripe_customer_id` to `profiles`. Mirrors `grant_trial_gift` template for boilerplate (CORS preflight, method check, `checkOverload`, anon-client + `getUser()` auth, service-role client, `enforceRateLimit`, `RateLimitError` catch, `recordError` on transport failure). **Three layers of idempotency**: (a) DB pre-check on `subscriptions.stripe_subscription_id` short-circuits with `{ok:true, already_started:true}` before touching Stripe; (b) DB-backed `request_idempotency` (P12 helper) scoped on `(start_trial, userId)`; (c) Stripe-side keys `start_trial_customer_${userId}` / `start_trial_sub_${userId}` so partial-failure retries reuse the same Stripe objects instead of leaking duplicates. **Frontend wire-up**: `src/contexts/AuthContext.tsx` `onAuthStateChange` listener fires `start_trial` fire-and-forget on first `SIGNED_IN`. `useRef<Set<string>>` keyed on `${userId}:${access_token.slice(0,16)}` deduplicates within a tab; cleared on `SIGNED_OUT` so sign-out + sign-in (different user) re-fires once (code-reviewer P1 fix pre-push). Edge function never trusts client-supplied userId — derives from verified JWT — so an in-flight invoke that completes after sign-out can only ever write for the JWT's owner. **Stripe SDK** import + secret-key resolution mirror `create_checkout_session` (esm.sh@18.5.0, apiVersion 2025-08-27.basil, `getStripeConfig()` env helper). Subscription created with `payment_behavior:'default_incomplete'` + `trial_period_days:3` + `payment_settings:{save_default_payment_method:'on_subscription'}` so trial starts immediately without a payment method on file (paywall flow in P54+ collects PM before trial end). **Spec divergence**: intentionally does NOT set `profiles.onboarding_started_at` — Wave 7 owns that column via `advance_onboarding_step` RPC, and decoupling Stripe trial start from onboarding-boost start is a UX win for users who delay engagement. See Findings Log row dated 2026-04-28. **Scale-guard tier**: added `start_trial: { maxPerHour: 10, maxPerMinute: 2 }` immediately after `grant_trial_gift` — purely additive, matches sibling Stripe-API endpoints (restore_subscription, create_portal_session). Per CLAUDE.md Shared Module Deploy Map convention (precedent: P9 PR #657, grant_trial_gift PR #690), additive tier entries do NOT require fanout redeploy. **config.toml**: new `[functions.start_trial]` stanza with `verify_jwt = false`. **Code-reviewer subagent verdict**: APPROVE with 1 P1 + 2 P2 + 1 P3 — P1 fixed inline pre-push (clear `triggeredTrialKeys` on `SIGNED_OUT`); P2/P3 noted but non-blocking. **Codex review loop (6+ rounds)**: round 1 P1 #1 (HTTP-error path on supabase-js invoke didn't roll back dedup) + P1 #2 (mirror to legacy `user_subscriptions` since `useSubscription` still reads it) → both fixed; round 2 P1 (canceled-status carve-out replayed Stripe idempotency-key cache → trial-cycling vector) → dropped the carve-out, any non-null `stripe_subscription_id` short-circuits; round 2 P2 (`access_token.slice(0,16)` collapsed to constant JWT header) → switched to full `access_token` for dedup key; round 3 P1 (auto-enrolled legacy users on every SIGNED_IN) → added 24h `subscriptions.created_at` recency gate; round 4 P1 (pre-check error fell through to fresh Stripe mint, demoting active subscribers) → fail closed with 503; round 5 P1 (24h cutoff locked out fresh signups whose first call failed OR email-confirm took >24h) + P2 (no React Query invalidation after success → UI showed free-tier gating until staleTime expiry) → replaced recency check with `user_metadata.trial_pending` flag set in `signUp()` callback + cleared server-side after mint, plus `queryClient.invalidateQueries(['subscription'])` on success; round 6 P1 (OAuth signups don't go through `signUp()` so they never get the `trial_pending` flag) → new migration `20260429120000_trial_pending_for_new_users.sql` updates `handle_new_user()` trigger to set `trial_pending: true` on raw_user_meta_data for ALL new auth.users rows (covers email + Google + Apple uniformly); legacy users (created before migration) still don't get the flag, preserving the round-3 scope guarantee. Verification: tsc 0 errors, eslint 0 warnings, build clean (12-18s across rounds), vitest 1513/1513, deno check clean. **Deploy**: post-merge `npx supabase db push --linked --yes` applies the new trigger migration, then `npx supabase functions deploy start_trial --project-ref khvkwojtlkcvxjxztduj --no-verify-jwt`. Test sign-up post-deploy: confirm `subscriptions.status='trialing'`, `current_period_end ≈ NOW + 3 days`, `auth.users.raw_user_meta_data.trial_pending = false` after first mint (cleared by edge function), no duplicate Stripe customers on page-refresh, OAuth signup also auto-enrolls in trial. |
| 2026-04-29 | #700 | P53+P54 | **Wave 8 PR 2 — Subscription enforcement (free-tier removal + day-4 lockout).** Bundled per Option A phasing — P53 frontend state machine + P54 backend gate must ship atomically (separately each breaks the other). Net diff: 35 files, +847/-156 LOC. **(a) P53 frontend** — `src/hooks/useSubscription.ts` full refactor: switched from `user_subscriptions` (legacy) to `subscriptions` table (single source of truth post-P52); new state machine `'trialing' \| 'premium' \| 'locked'` with `isPremium = state !== 'locked'`; dropped bootstrap mutation since `handle_new_user` trigger creates rows server-side. `PaywallModal.tsx` `reason` union extended with `'subscription_required'` + `'trial_expired'`. 5 consumer files updated (LiveScan, LinkImportForm, ProfileCard, PremiumSection, AuthContext stale-comment fix). 4 new i18n keys appended to en.ts + sv.ts (`paywall.subscription_required.{title,body}`, `paywall.trial_expired.{title,body}`); other 12 locales fall back per convention. **(b) P54 backend** — `enforceSubscription(supabaseAdmin, userId)` + `subscriptionLockedResponse(reason, cors)` exports added to `_shared/scale-guard.ts`. **Onboarding-plan bypass mandatory**: calls `resolveUserPlan` first; if it returns `'onboarding'` (per P43 boost — `onboarding_started_at` within 24h AND `onboarding_step != 'completed'`), short-circuits with `{allowed:true}` regardless of subscription row state. This protects BatchCapture during the SIGNED_IN → start_trial race window where the user's `subscriptions` row is still `plan='free'` from `handle_new_user`. Allowed states: trialing (`current_period_end` null OR future), active+premium. Everything else (no row, canceled, past_due, active+free legacy) → HTTP 402 with `{error:'subscription_required', reason:'locked'\|'expired'}`. **22 AI functions** inject the gate after `enforceRateLimit` and before AI work: `analyze_garment`, `assess_garment_condition`, `burs_style_engine`, `clone_outfit_dna`, `detect_duplicate_garment`, `generate_flatlay`, `generate_garment_images`, `generate_outfit`, `import_garments_from_links`, `mood_outfit`, `outfit_photo_feedback`, `render_garment_image`, `shopping_chat`, `style_chat`, `style_twin`, `suggest_accessories`, `suggest_outfit_combinations`, `summarize_day`, `travel_capsule`, `visual_search`, `wardrobe_aging`, `wardrobe_gap_analysis`. **Non-obvious placements**: (i) `render_garment_image` gate wrapped in `if (!isInternalInvocation)` so worker-driven renders aren't double-gated (`enqueue_render_job` already gates via credit reservation); (ii) `mood_outfit` uses `responseToPayload(subscriptionLockedResponse(...))` to match its streaming/overload-payload pattern; (iii) `summarize_day` lacks `enforceRateLimit` so the gate is the only protection (placed after service client construction); (iv) `travel_capsule` gates POST only — GET path (read of saved capsules) intentionally left ungated per code-reviewer (standard SaaS pattern: subscription gates AI compute, not data reads). **Out of scope (deferred to follow-up)**: `prefetch_suggestions` cron+single-user hybrid path needs per-path gate (Findings Log); `enqueue_render_job` already gates via credit reservation (double-gate redundant); cron/webhook/auth/billing functions left unchanged. **Tests**: 14 new `enforceSubscription` cases in `_shared/__tests__/scale-guard.test.ts` (25→39 covering onboarding bypass, no-row, trialing-future/past/null `current_period_end`, active+premium, active+free legacy, canceled, past_due, fail-closed-on-DB-error, fail-closed-on-throw); `useSubscription.test.tsx` rewritten (6→9 covering all state derivations + locked-state behavior); `PaywallModal.test.tsx` extended (3→5 for new reason variants). **Code-reviewer subagent verdict**: APPROVE-WITH-FIXES — 1 P2 (PremiumSection empty `<p>` block always rendering null since `limits.maxGarments === Infinity`; div removed inline) + 1 design Question (travel_capsule GET gating; resolved by moving gate to POST-only path). 1 P3 nit (`wouldExceedLimit` rename in LinkImportForm) deferred to polish PR. **Spec divergence**: trial-end column is `current_period_end` (NOT `trial_end` — wave-8 spec example was wrong; column never existed in `subscriptions` table). Verification: tsc 0 errors, eslint 0 warnings, build clean (15.10s), vitest **1532/1532 pass** (full suite, +19 new vs prior 1513 baseline). `deno check` deferred to CI (Deno not installed in dev env per past PR convention). **Deploy radius**: `_shared/scale-guard.ts` shared module change → ALL 22 AI functions need redeploy ONE AT A TIME per CLAUDE.md hard rule. Frontend auto-deploys via Vercel. No migration. |
| 2026-04-29 | #704 | P55 | **Wave 8 PR 3 — Paywall Restore Purchase (App Store guideline 3.1.1).** Frontend-only solo PR. Adds the `Restore Purchase` control that Apple reviewers test by signing into a fresh device and tapping it before checkout — every paywall surface needs it visible per AGR 3.1.1, and the `PaywallModal` is the canonical paywall for both web + iOS web-wrap. Net diff: 4 files, +231/-4 LOC. **(a) `src/components/PaywallModal.tsx`** — new `handleRestore` async handler invokes the existing `restore_subscription` edge function (P9 added rate-limit, P53 didn't touch the fn — re-verified contract: returns `{success, plan: 'premium'\|'free', status: 'active'\|'trialing'\|null, currentPeriodEnd}` with HTTP 200 in every branch, errors land in supabase-js `error` field). Three outcomes: (i) `plan==='premium' && status∈{'active','trialing'}` → `queryClient.invalidateQueries({queryKey:['subscription', user?.id]})` + parity invalidate of `['stripe-subscription']` + `toast.success` + `onClose()` (parent unblocks naturally via `useSubscription` re-derive); (ii) `plan==='free'` (no Stripe customer OR inactive sub) → invalidate `['subscription', user?.id]` (edge fn just wrote `plan='free'` to DB so cache must self-heal) + `toast.info` (modal stays); (iii) supabase-js `error` set OR thrown exception → `toast.error` (modal stays). New `<Button variant="link">` placed between yearly CTA and "Not now" ghost; `aria-label` for assistive tech; `RotateCcw` icon. `isLoading` state union extended `'monthly' \| 'yearly' \| 'restore' \| null` so all CTAs share the disabled-while-busy guard. **(b) `src/i18n/locales/en.ts` + `sv.ts`** — appended 4 new `paywall.restore_*` keys (purpose / success / no_subscription / error). Other 12 locales fall back to key string per append-only convention. **(c) `src/components/__tests__/PaywallModal.test.tsx`** — 6 new tests covering button visibility on all 4 reasons, success on `active`, success on `trialing`, info on `plan='free'` (with cache invalidation assertion), error on supabase-js `error` field, error on thrown exception. 11/11 pass (5 original + 6 new). **Code-reviewer subagent verdict**: APPROVE with no P0/P1 (1 P2 #1 stripe-subscription cache parity + 1 P2 #2 cache hygiene in else-branch — both folded inline pre-push as 2-line defensive wins). 3 P3 items (disabled-state UX trap, locale append-only, unmount-during-call) explained or deferred. **Out of scope (per spec)**: separate `src/pages/Paywall.tsx` marketing page (modal is sufficient per spec "or new page"); StoreKit native receipt restore (Wave 9 / Capacitor); SEK pricing (P56); credit priority verification (P57). Verification: tsc 0 errors, eslint 0 warnings, build clean (20.07s), vitest 1549/1549 pass (full suite, +6 new vs prior 1543 baseline since PR #700 + 1 unrelated test in mobile-RN scaffold). **Deploy**: none (frontend-only, Vercel auto-deploys). `restore_subscription` edge fn already deployed since P9; no shared-module change. |
| 2026-04-29 | #705 | P56+P57 | **Wave 8 PR 4 (CLOSER) — SEK pricing + credit priority verification.** Bundled per Option A phasing — both are small, both close Wave 8 functional scope. **(a) P56 — SEK pricing.** Sweden is the launch market and the canonical pricing reference: **119 SEK/month, 899 SEK/year**. The single SEK Stripe Price ID charges every user regardless of locale, so the localized display is presentation-only — `sv` becomes the source of truth that matches the SEK ledger amount exactly. (i) `src/lib/localizedPricing.ts` — `sv` PRICE_MAP entry updated `monthly: 79 → 119`, `yearly: 699 → 899`. `no` (NOK) and `da` (DKK) entries intentionally left at their pre-Wave-8 values — NOK/DKK are different currencies despite shared `kr` symbol; aligning them visually would imply local-currency billing the Stripe config doesn't do (see Findings Log entry, deferred for Wave 8.5). (ii) `.env.example` — appended Stripe section documenting `STRIPE_MODE`, `STRIPE_SECRET_KEY_TEST/_LIVE`, `STRIPE_WEBHOOK_SECRET_TEST/_LIVE`, and the 4 price-ID env vars `STRIPE_PRICE_ID_MONTHLY_TEST/_LIVE` + `_YEARLY_TEST/_LIVE`. Spec said "no code changes if env vars already used (verify)" — `create_checkout_session/index.ts:21-30` already reads via `getStripeConfig()`, no edge-fn change. (iii) Verified `create_checkout_session` reads price IDs from env; `stripe_webhook` reads `subscription.items.data[0]?.price.id` from Stripe payload (currency-agnostic). **(b) P57 — Credit priority verification.** Spec acceptance: trial_gift → monthly → topup. **Verified via Supabase MCP `pg_get_functiondef('reserve_credit_atomic')`** — RPC body is correct as-implemented: (1) `IF v_credits.trial_gift_remaining > 0` → debits trial_gift, sets `v_source := 'trial_gift'`; (2) `IF v_source IS NULL AND (monthly_allowance - used - reserved) > 0` → debits monthly; (3) `IF v_source IS NULL AND topup_balance > 0` → debits topup. `IF v_source IS NULL` guards on each subsequent block ensure exactly one source is debited per call. Defensive: `auth.role() = 'service_role'` enforced; `reset_period_if_needed` resets monthly counter at period boundary; idempotency via `idempotency_key` lookup at top of body returning `{ok:true, replay:true}`. **No migration, no code changes — P57 ships as `[DONE-subsumed]`** with citation to the `reserve_credit_atomic` RPC's original implementation (predates Wave 8). The `IF v_source IS NULL` chain is the structural guarantee that prevents double-debit across the priority tiers. **User actions required for P56 SEK billing (NOT blocking PR review)**: (1) Stripe Dashboard → Products → BURS Premium → create 119 SEK monthly + 899 SEK yearly Prices (test mode + live mode); (2) `npx supabase secrets set STRIPE_PRICE_ID_MONTHLY_TEST=price_..._test_119_sek` etc. for all 4 vars; (3) `npx supabase functions deploy create_checkout_session --project-ref khvkwojtlkcvxjxztduj --no-verify-jwt` then `stripe_webhook` (each one at a time). **Code-reviewer subagent verdict**: APPROVE (no P0/P1; 1 P2 about orphan stale-price keys in 14 locale files deferred to Wave 8.5 sweep). Verification: tsc 0 errors, eslint 0 warnings, build clean (12.44s), vitest pass on touched-area tests. **Deploy**: post-merge user deploys `create_checkout_session` + `stripe_webhook` after Stripe Dashboard + env-var step (NO deploy needed for P57 since RPC already deployed). Frontend auto-deploys via Vercel. No migration. **Wave 8 functional scope CLOSED** — next: Wave 8.9 closure (drain open Findings) → Wave 8.5 Style Memory. |
| 2026-04-24 | #668 | P27b | **Wave 4.5-B solo PR. Secondary image add/swap/delete on GarmentDetail.** Consumer for P27a's `secondary_image_path` column. New `SecondaryImageManager.tsx` under `src/components/garment/` slots under the hero image in GarmentDetail. (a) **Add**: hidden file input (`accept="image/*"` + `capture="environment"` — native camera picker on mobile, file chooser on desktop, matches LiveScan's browser-fallback branch output) → `compressImage()` → `uploadGarmentImage()` to `garments/${userId}/${garmentId}_secondary.{ext}` → UPDATE sets `secondary_image_path`. No AI re-trigger. (b) **Swap**: "Use as primary" button → single-statement UPDATE exchanges the VALUES of `image_path` ↔ `secondary_image_path` AND clears `ai_raw` / `ai_analyzed_at` / `ai_provider` / `silhouette` / `visual_weight` / `texture_intensity` / `style_archetype` / `occasion_tags` / `versatility_score` / `rendered_image_path` / `rendered_at` / `render_error`, sets `enrichment_status='pending'` + `render_status='pending'`. Optimistic concurrency via `.eq('image_path', previousImagePath)` catches concurrent swaps. Kicks `triggerGarmentPostSaveIntelligence` with `skipRender:true` then delegates the render trigger to the newly-public `startGarmentRenderInBackground(garment.id, 'manual_enhance', { force: true })` — inherits the full 402-reset / retryable-retry / server-state-check / non-retryable-reset tree. Spends 1 render credit — documented in AlertDialog confirm copy. (c) **Delete**: storage delete first (logged, non-fatal) → NULL the column. No AI re-trigger. (d) **Guardrail**: actions disabled via `aria-disabled` pattern while `render_status ∈ {pending, rendering}` OR `enrichment_status ∈ {pending, processing, in_progress}` OR mutation in flight. Clicks show busy-toast instead of silent no-op. (e) **UI**: 64×64 LazyImage thumbnail + action row + two AlertDialog confirmations. Motion via `EASE_CURVE` / `DURATION_MEDIUM`. Haptics on every action. (f) **i18n**: 14 new `garment.secondary_*` keys appended to `en.ts` + `sv.ts`. (g) **Design decisions**: read-then-UPDATE (supabase-js can't emit self-referential `SET x=y,y=x`; RPC contradicts spec "no swap RPC"; read-write race negligible given RLS + mutation-local lock + render/enrichment gate); file-input-only capture (spec says "browser fallback is already built into LiveScan" — standalone file input IS that fallback, avoiding a new capture-return route + state handoff). (h) **Shared-module refactor (Codex P1 round 1 fix)**: `startGarmentRenderInBackground` in `src/lib/garmentIntelligence.ts` exported + accepts `options: { force?: boolean }`. Existing internal caller (line 397 in `triggerGarmentPostSaveIntelligence`) passes no options → default `force=false` → zero behavior change. Swap flow passes `force: true` → full recovery tree reused. Also tightened the `source` param from `string` to `RenderTriggerSource`; removed two interior `as RenderTriggerSource` casts. (h2) **Concurrency hardening (Codex P1 round 2 fix)**: (i) swap OC guard expanded to `.eq('secondary_image_path', previousSecondaryPath)` alongside the `.eq('image_path', previousImagePath)` check — matches BOTH columns, so ANY concurrent write on EITHER column fails cleanly. (ii) delete flow reordered DB-first-then-storage with OC guard `.eq('secondary_image_path', path)` — prevents deleting the now-live hero asset if a concurrent swap moved `path` to primary between our read and our storage remove. (h3) **Display coherence + cache invalidation (Codex P1+P2 round 3 fix)**: (i) swap UPDATE also sets `original_image_path: newPrimary` — `getPreferredGarmentImagePath` prefers `original_image_path` over `image_path` when no render is ready, so leaving the raw-upload column stale meant wardrobe cards showed the OLD photo until render completed (and permanently if render failed). (ii) replaced local one-key `invalidate()` with the canonical `invalidateWardrobeQueries(queryClient, user?.id)` helper — busts 10+ wardrobe query keys (`garments`, `garments-by-ids`, `garments-count`, `insights`, `ai-suggestions`, etc.) so list screens refresh immediately post-mutation instead of waiting for staleTime. (h4) **Upload-path uniqueness + render-after-enrichment sequencing (Codex P1+P2 round 4 fix)**: (i) Upload path now tagged with `crypto.randomUUID().slice(0,12)` suffix (`secondary_${tag}.${ext}`) so each upload gets its own storage object; prevents the post-swap collision where a fixed `_secondary.jpg` could overwrite the current primary's bytes. `upsert:false` as defense-in-depth. (ii) Added `renderOptions: { force?: boolean }` to `TriggerGarmentPostSaveIntelligenceOptions` in `garmentIntelligence.ts`; the internal post-enrichment `startGarmentRenderInBackground` call now forwards it. Swap flow threads `renderOptions: { force: true }` into ONE `triggerGarmentPostSaveIntelligence` call and drops the separate direct render trigger — guarantees render sees fresh `ai_raw` after enrichment completes, no stale-metadata race. (h5) **Raw-file fallback + confirm-time busy recheck (Codex P1+P2 round 5 fix)**: (i) `compressImage` wrapped in try/catch — raw `file` used on failure (mirrors `useAddGarment` pattern for Median/WebView environments where `createImageBitmap` / `OffscreenCanvas` fail). (ii) `handleSwapConfirm` and `handleDeleteConfirm` re-check `isBusy` at confirm-time, not just at dialog-open — prevents bypassing the gate if render/enrichment state flips busy while the dialog is open. (i) **Test**: `GarmentDetail.test.tsx` updated with a SecondaryImageManager mock mirroring the existing GarmentEnrichmentPanel / GarmentOutfitHistory / GarmentSimilarItems pattern — new component's `useAuth()` call needs a provider the test doesn't wrap. Types regen (commit 1) surfaces `secondary_image_path` in Row/Insert/Update shapes. Stray "Initialising login role..." stderr debug line from the regen was stripped inline. Code-reviewer approved; Codex P1 fix applied pre-merge. Post-merge: **no deploy** — zero backend code modified; every AI function + wardrobe card continues reading `image_path` unchanged. |
| 2026-04-29 | #708 | P82 | **Wave 8.5 PR 1 — Style Memory audit (audit-only, no code changes).** Audit-only PR producing the Implementation note that subsequent prompts (P83-P92) cite directly. Three parallel general-purpose agents covered backend (`supabase/functions/`), frontend (`src/`), and the privacy/export/delete surface. Headline: **zero backend writers to `feedback_signals` or `wear_logs`** — every row is written by frontend supabase-js direct INSERT. **One frontend writer** (`useFeedbackSignals` hook, only `OutfitDetail.tsx` consumer); 6 of 8 save-outfit surfaces and 3 of 4 mark-worn surfaces silently lose data. **Two AI engines read divergent subsets** of `feedback_signals` — `burs_style_engine` reads 8 distinct `signal_type` values, `style_chat` reads only 4. **No persistent style summary table** exists; both engines re-derive style context per request via divergent extraction logic (12 keys vs 27 keys). **No memory-ingestion helper** (no `_shared/style-memory-signals.ts`, no `memory_ingest` function, no `record_pair` SQL RPC; only `recordPairOutcome` at `_shared/outfit-scoring.ts:537-592` exists). **Privacy export covers 3 tables**, missing 12 user-authored / memory-relevant tables. **`delete_user_account` already cascades 24 tables** explicitly (very comprehensive — only `user_style_summaries` needs to be added in P90 once the table exists). **No "Reset style memory" path** anywhere — built fresh in P90. Output written to `docs/launch/wave-8.5-p82-audit.md` as the canonical Implementation Note (PR-body-only would not survive — file form lets P83-P91 grep file paths). 11 sections + 5 open architectural questions for the user to resolve before P83 ships (most important: reject vs never_suggest_garment disambiguation; Mark-as-worn signal source α vs β; memory_ingest shape A vs B vs C). **Wave 8.9 closure deferred per user direction 2026-04-29** — 3 open Wave 8 Findings rows (P53/P54/P56 deferred items) flipped to `Scheduled: post-launch` annotations to keep the tracker consistent with the Wave Closure Rule. Wave Index updated: Wave 8 → DONE (functional scope), Wave 8.5 → CURRENT. **Verification**: zero source code changes; tsc/eslint/build run as pipeline-clean baseline check; no migration; no deploy. **Files**: `docs/launch/wave-8.5-p82-audit.md` (new) + `CLAUDE.md` tracker updates + `docs/launch/wave-8.5-style-memory.md` P82 status flip. **Deploy**: none. |
| 2026-05-01 | #709 | P83+P84+P85+P87 | **Wave 8.5 PR A — Backend Foundation.** Builds the entire Style Memory ingestion infrastructure as a purely additive backend foundation per Wave 8.5 architectural decisions D1–D5 (`docs/launch/wave-8.5-p82-audit.md`). Existing engines untouched; legacy reads still see legacy signal names. PR B (P86+P88+P89+P90+P91+P92) does the wiring + integration. (a) **P83 canonical signal taxonomy** — new `_shared/style-memory-signals.ts` exports `CanonicalStyleMemorySignal` union (11 names: save_outfit, unsave_outfit, rate_outfit, wear_outfit, skip_outfit, reject_outfit, swap_garment, quick_reaction, never_suggest_garment, like_pair, dislike_pair) + `normalizeStyleMemorySignal(input)` helper mapping 17 legacy aliases (swap_choice, planned_skip, planned_follow_through, wear_confirm, etc.) to canonical names. Per Resolved decision D1: `reject_outfit` is OUTFIT-LEVEL (penalize combination), `never_suggest_garment` is GARMENT-LEVEL (hard exclusion). Pure module — no Deno imports — bundles into edge functions AND runs unmodified under vitest. (b) **P84 user_style_summaries table** + **P85 ingest_memory_event RPC** — single migration `20260501120000_user_style_summaries_and_memory_ingest.sql`. Table: UNIQUE on user_id, partial index on dirty_at, RLS for user SELECT + service_role ALL. RPC: SECURITY DEFINER atomic triple-write — feedback_signals INSERT + garment_pair_memory upsert (via helper `_upsert_garment_pair_memory` matching legacy `recordPairOutcome` race profile per D3) + dirty mark on summary row. service_role-only EXECUTE. Idempotency enforced at edge-function layer (P12 `request_idempotency`), not inside RPC. (c) **P85 memory_ingest edge function** — new `supabase/functions/memory_ingest/index.ts` (~456 LOC) with the standard auth/rate-limit/subscription template: CORS preflight, method check, `checkOverload`, anon-client + `getUser()` auth, service-role client, normalize via P83 helper (drops + warns on unknown signals), `enforceRateLimit` (new tier `memory_ingest: { maxPerHour: 200, maxPerMinute: 30 }` in scale-guard.ts — high frequency by design, bounded against runaway useEffect loops), `enforceSubscription`, `request_idempotency` claim/cache, then RPC call. Plus `_shared/style-memory-ingest.ts` (~239 LOC) shared helper for OTHER edge functions to record memory as a side effect (P89 style_chat will use this for chat-driven preference persistence). (d) **P87 deterministic style-summary builder** — new `_shared/style-summary-builder.ts` exports pure `buildStyleSummary(inputs)`. Deterministic time anchor (max created_at across inputs, NOT Date.now()), exponential 90-day half-life decay, Wilson-smoothed confidence (n/(n+3)), N=3 promotion floor for archetype/color preferences, ≤500-char `summary_text` field. Pure function — no I/O — caller (P88/P89) loads inputs + persists output. **Files (10)**: 8 new (3 modules + 3 test files + 1 edge function + 1 migration) + 2 modified (config.toml + scale-guard.ts). **Tests**: 49 net new (`style-memory-signals` + `style-memory-ingest` 21 + `style-summary-builder` 24). **Deploy radius (post-merge)**: `npx supabase db push --linked --yes` (1 migration); `npx supabase functions deploy memory_ingest --project-ref khvkwojtlkcvxjxztduj --no-verify-jwt`; redeploy 22 AI functions ONE AT A TIME per Shared Module Deploy Map (scale-guard.ts touch is purely additive — semantically no-op for existing consumers, but bundle propagation requires the redeploy; same precedent as P57's `start_trial` tier add). |
| 2026-05-03 | #712 | P86+P88+P89+P90 (P91+P92 deferred) | **Wave 8.5 PR B — Integration + Wiring + Round 5/6/7 audit fixes.** Integrates the PR A foundation (PR #709) into the AI engines + frontend writers + privacy surface, plus three rounds of pre-/post-merge audit folding-in. Net diff across 18 commits: ~3700 LOC across ~70 files. **(1) Initial PR B integration** — Phase 1 foundation (memoryEvents.ts + memoryEventQueue.ts localStorage + useFeedbackSignals.ts rewrite + AuthContext queue drain + useAuthOrNull non-throwing variant); Phase 2 P86 minimum-viable wiring (top 3 high-volume save callsites: OutfitGenerate + AIChat + MoodOutfit); Phase 3 P88 burs_style_engine summary read + D1 hard-skip + N+1 fix; Phase 4 P89 style_chat summary inject + EdgeRuntime.waitUntil extraction; Phase 5 P90 reset_style_memory edge fn + extended delete_user_account cascade + 14-table SettingsPrivacy export bundle + Reset Memory UI. Migrations 120000-120610 (dedup + UNIQUE INDEX + summary CHECK constraints + race-fix RPC + REVOKE/GRANT splits). **(2) Round 5+6 deep audit (47 findings → 10 P0/P1 gates folded)** — R5-B1 inverted `if (!checkOverload)` in reset_style_memory (fn would 100%-fail in prod); R5-F1+F8 4xx-detection regex on `err.message` replaced with `getHttpStatus(err)` reading canonical `error.context.status` (FunctionsHttpError shape); R6-1 RPC ownership validation (migration 120700 — outfit_id + garment_ids must belong to caller); R5-F5 typed-confirmation on Reset dialog (clickjacking + accidental tap); R6-P11 Cache-Control: no-store on every PR B JSON response; R6-P5 captureWarning on summary-loader cache miss/persist/build; R6-3 bigint promotion of garment_pair_memory.{positive,negative}_count; R5-B5 verified false positive (extraction already AFTER subscription gate). R6-P3 PG17 pin DEFERRED (CLI v1.226.4 incompatibility). **(3) Smoke-test surface revealed 3 hidden P0s** — the new memory-ingest smoke test (added in this audit round per "make sure good and necessary CI/CDs") exercised the wire contract end-to-end and caught: **(i)** memory_ingest body field mismatch — function read `body.event_type` but frontend `RecordMemoryEventInput` sends `signal_type`; would have silently broken every save post-merge (HTTP 400 → R5-F1 4xx-classifier drops). Fix: read `signal_type` first with `event_type` legacy fallback. **(ii)** ownership SQL `record[]` vs `uuid[]` — my own R6-1 migration used `SELECT DISTINCT g FROM unnest(arr) g` which returns record-typed rows, not uuids, so `id = ANY(record[])` never matched and ownership ALWAYS denied. Fix: explicit `AS u(id)` column alias (migration 120710). **(iii)** PR A's auth gate broken since 2026-04-29 — used `current_setting('request.jwt.claim.role', true)` but modern PostgREST sets `request.jwt.claims` JSONB instead. Every `ingest_memory_event` call was raising `unauthorized caller (role=authenticator)` — the HTTP path returned 500 (no caller in prod yet), but `style_chat` extraction helper has been silently logging-and-dropping events for 4 days. **Production impact: zero memory ingestion has happened from any chat-driven preference detection since 2026-04-29.** Fix: switch to `auth.role()` (canonical Supabase helper that reads JSONB claims) — same pattern as the working onboarding migrations (migration 120720). **CI hardening**: new `src/test/smoke/memory-ingest.test.ts` (6 cases — happy path, R6-1 cross-user × 2, R6-P11 Cache-Control × 2, R5-B1 reset 200) runs against full local Supabase + edge runtime. **Migrations (14 total in PR B)**: 20260502120000 (dedup) + 120100 (UNIQUE INDEX) + 120200 (summary CHECK constraints) + 120300 (race-fix `_upsert_garment_pair_memory` ON CONFLICT) + 120400/120410 (REVOKE/GRANT split) + 120500 (reset_style_memory_atomic RPC) + 120600/120610 (REVOKE/GRANT split) + 120700 (ingest_memory_event ownership check — P0 #2 SQL bug) + 120710 (ownership SQL fix — P0 #2 fix) + 120720 (auth.role gate fix — P0 #3 fix) + 120800 (bigint promotion). **Verification**: tsc 0 errors, eslint 0 warnings, build clean (~25s), vitest 1685/1685 pass. **CI green on commit `a48f96d2`**: build-and-test ✓ smoke-local ✓ deno-check ✓ migration-drift-check ✓. **Deploy radius (post-merge)**: `npx supabase db push --linked --yes` applies all 14 migrations sequentially. Edge function deploys ONE AT A TIME: memory_ingest (new) → reset_style_memory (new) → burs_style_engine → style_chat → delete_user_account. Optional `SENTRY_DSN` secret for R6-P5 captureWarning. **Deferred** to follow-up PR: 5+ broken save surfaces (UnusedOutfits / AISuggestions / useTravelCapsule / useWeekGenerator / OutfitSuggestionCard local-only swap) + Plan calendar skip flow + GarmentDetail Never-suggest UI + QuickReactionRow + 4 placements + P91 detailed test matrix + P92 verification + R6-P3 PG17 pin (needs CLI v2.x bump). |


| 2026-05-04 | #724 | M0 | **Mobile Launch M0 — Sentry foundations + onError sweep.** Initializes `@sentry/react-native` from `mobile/src/lib/sentry.ts` helper. App.tsx loads `initSentry()` at module-load (before any other imports) and wraps default export in `Sentry.wrap`. Adds `captureMutationError(scope)` to every `useMutation` in `mobile/src/hooks/` (13 sites: useAddGarment, useGarments [4], useOutfits [5], usePlannedOutfits [2], useWardrobeGaps [1]). Composed onError preserves existing optimistic-rollback handlers in useUpdateGarment / useMarkLaundry / useMarkWorn — report first, then rollback. For non-useMutation streaming hooks (useStyleChat, useGenerateOutfit, useAnalyzeGarment, useMoodOutfit) wraps catch / onError in `Sentry.withScope` with `mutation:<scope>` tag and skips the expected `subscription_required` paywall sentinel. Sentry Expo plugin wired in `app.json`. New `mobile/.env.example` with `EXPO_PUBLIC_SENTRY_DSN=`. Verified: `npx tsc --noEmit` 0 errors, code-reviewer subagent approved. Smoke test deferred post-merge (requires DSN in EAS secrets / local .env). Deploy radius: none (mobile-only; no edge function or migration). |
| 2026-05-05 | #727 | V0 | **Mobile Launch V0 — CI/CD foundations + typecheck/lint baseline cleanup.** New `.github/workflows/mobile-ci.yml` with 6 jobs (typecheck, lint, expo-doctor, bundle-size, deno-check, migration-smoke) on PRs targeting `feat/mobile-rn-app`. Each job is its own check so branch protection can require all six. `mobile/eas.json` updated to V0 spec (development with iOS simulator + Android APK, preview/production channels). New `mobile/eslint.config.js` (flat config, eslint v9 + `eslint-config-expo/flat` v10 + `@typescript-eslint`, `--max-warnings 0`). `mobile/package.json` gains `lint` + `typecheck` scripts and devDeps. **Scope expansion (user-authorized "quality and zero error margin before we start"):** the V0 typecheck gate exposed 130 pre-existing TS errors and 5 missing runtime deps the source already imports; cleaned the baseline so V0's own gates can pass. Added: `expo-haptics`, `expo-localization`, `expo-image-manipulator`, `expo-file-system`, `@sentry/react-native@~6.x` (per pin), `react-native-worklets` (peer). 19 new icon exports in `mobile/src/components/icons.tsx` (CheckIcon, FileIcon, TrashIcon, ShareIcon, RotateIcon, ShieldIcon, PaletteIcon, MoreIcon, MinusIcon, GlobeIcon, BellIcon, MailIcon, KeyIcon, EditIcon, MoonIcon, LockIcon, ListIcon, StarIcon, SunRayIcon). 4 new theme tokens (`destructive`, `destructiveSoft`, `scrimBg`, `scrimFg`) for both light + dark. 4 new RootStackParamList routes (Auth/Onboarding/Paywall/OutfitGenerate, all PlaceholderScreen-mounted matching the existing 32-placeholder pattern; real screens swap-in deferred), typed params for AddPieceStep2/3/MoodOutfit/MoodFlow/Filters, exported `WardrobeFilters` + `AddPiecePhoto` types. `Button` component gained `accessibilityLabel` + `accessibilityState` props (a11y improvement; resolves one TS error). 13 lint baseline fixes: 7 escaped apostrophes, 5 unused-import cleanups, 1 useEffect dep, 1 useMemo stabilisation. **Wave-spec deviations** (all justified in code comments, listed in Findings Log): `eslint.config.js` (flat) instead of `.eslintrc.cjs` because eslint-config-expo v10 requires eslint v9; `expo export --output-dir` instead of `-o` (Expo CLI 54 removed `-o`); eas.json adds `appVersionSource: "remote"` + `autoIncrement: true` beyond the wave skeleton. Verification: typecheck 0 errors, lint 0 errors + 0 warnings, expo-doctor 17/17, bundle-size 1.02 MB gzipped (well under 5 MB warn). Code-reviewer subagent approved (3 P1 findings addressed: AddPieceStep2 whitespace, SettingsScreen + WardrobeGapsScreen merge artifacts, RootNavigator placeholders confirmed-intentional with comment). User-side follow-ups: branch protection on `feat/mobile-rn-app` (require all 6 checks) + throwaway negative+positive PRs to validate gates. |
| 2026-05-05 | #729 | M2 | **Mobile Launch M2 — Signed-URL TTL cache.** Rewrites `mobile/src/hooks/useSignedUrl.ts` from a bare React Query wrapper into a TTL-aware module-scope cache. Adds `Map<bucket:path, { url, expiresAt }>` (50 min TTL — 10 min before Supabase's 60 min signed-URL expiry) + a parallel `inflight` Map that dedupes concurrent fetches so a list rendering 50 cards with overlapping paths fires one `createSignedUrl` per unique path instead of one per card. The React-facing API (`useSignedUrl(path)` / `useSignedUrls(paths)` returning `useQuery` with `data: string \| null` / `Record<string, string \| null>`) is preserved verbatim — all 9 existing call sites compile unchanged (GarmentCard, GarmentDetailScreen, OutfitsScreen, UsedGarmentsScreen, OutfitDetailScreen ×2, LaundryScreen, EditGarmentScreen). Bulk variant reads cache first, batches only the misses through `createSignedUrls`, writes results back into the same Map — full-cache hit issues zero network calls. AbortSignal threaded from React Query's `queryFn({signal})` into the cache layer. New `clearSignedUrlCache()` export wired into `mobile/src/contexts/AuthContext.tsx` on both the `SIGNED_OUT` listener branch and the explicit `signOut()` callback so user A's signed URLs (each embeds a JWT in the query string) don't leak into user B's session on the same device — same rationale as the existing `queryClient.clear()` audit. Verification: `npx tsc --noEmit` 0 errors, `npx eslint src --max-warnings 0` clean, `npx expo-doctor` 17/17, `npx expo export -p ios` bundle 4.41 MB hbc. Code-reviewer subagent approved (3 P2s tracked in findings-log: createSignedUrl doesn't accept AbortSignal so signal.aborted check only short-circuits the post-await write, not the round-trip; useSignedUrlCache.test.ts deferred per wave file allowance; inflight latch race comment). No edge-function changes; no migrations. |
| 2026-05-06 | #737 | M13 | **Mobile Launch M13 — Outfit anchor locking + rules engine.** Three new modules ported from web with byte-level parity (`mobile/src/lib/outfitRules.ts`, `outfitValidation.ts`, `outfitAnchoring.ts`) — slot inference (`inferCanonicalOutfitSlot`), layering roles (`inferCanonicalLayerRole`), full validation pipeline (`validateOutfitItems` + `validateBaseOutfit` + `validateCompleteOutfit` + `getVisibleOutfitMissingSlots`), and the preferred-IDs helpers (`normalizePreferredGarmentIds`, `hasPreferredGarmentMatch`). Mobile-only extension on `outfitAnchoring.ts`: `applyAnchor(garments, anchorId): LockedSlots` returns the slot-locked map derived from the anchor garment's category/subcategory; `isAnchorPresent(itemIds, anchorId)` post-validates engine responses. **Wave-file drift, intentional:** the wave's `inferSlot → 'TOP'/'BOTTOM'/...` UPPERCASE enum was rejected because every existing call site (engine response items, outfit_items rows, web parity) uses lowercase canonical slots — keeping web parity beats inventing a divergent enum. The wave's `applyAnchor(garmentIds, anchorId, rules)` API was reshaped because web's `outfitAnchoring.ts` doesn't expose `applyAnchor` at all (only preferred-IDs helpers); we extended with a mobile-only API that takes garment objects (not just IDs) so the slot can be inferred without an extra DB hit. **Hook contract**: `useGenerateOutfit` now accepts `{ anchorGarmentId?, lockedSlots? }` (plus a `@deprecated garmentId` alias for back-compat with StyleMeScreen — which doesn't pass an anchor anyway), threads the anchor through `prefer_garment_ids: [anchorId]` (existing engine field), post-validates with `isAnchorPresent` after each response, surfaces `anchorMissed: boolean` so the screen can render a "not honoured" warning, and Sentry-logs (`captureMessage('anchor_garment_dropped_by_engine', 'warning')` with the anchor id + returned ids as extras). No auto-retry — a wardrobe with no viable composition around the anchor would loop forever. **OutfitGenerateScreen rewire**: anchor pill renders only when an anchor was requested; `useGarment(anchorId)` fetches the title for the pill copy; pill border flips from `t.accent` (locked) to `t.destructive` (missed). The screen is now mounted as the real component in `RootNavigator.tsx` (was `Placeholders.OutfitGenerate` pre-M13 — code-reviewer P0 caught the missing mount before push). **OutfitDetailScreen wire**: `PieceCard` accepts an optional `onLongPress` prop with `delayLongPress=350`; long-press → `Alert.alert("Make this the anchor", …)` → on confirm, `nav.navigate('OutfitGenerate', { garmentId })`. **Tracker pointer skipped past M6 + M7 + M8** to M14 — M6/M7 require physical Vision Camera testing on an EAS dev build (the agent can't run that); M8 depends on M6/M7 so it's blocked until the user runs the chain on hardware. Same precedent as M9 (PR #733). Verification: `npx tsc --noEmit` 0 errors, `npx eslint src App.tsx --max-warnings 0` clean, `npx expo-doctor` 17/17, `npx expo export --platform ios` clean. Code-reviewer subagent: P0 about missing OutfitGenerate mount fixed in pre-push commit; one P2 about wave-file drift documentation surfaced here. No edge-function changes; no migrations. |
| 2026-05-06 | #736 | M12 | **Mobile Launch M12 — Password reset + deep links.** New `mobile/src/hooks/useResetPassword.ts` exposes `requestReset(email)` → `supabase.auth.resetPasswordForEmail(email, { redirectTo: 'burs://reset-password' })` and `confirmReset(newPassword)` → `supabase.auth.updateUser({ password })`. Plain async functions (not `useMutation`) — neither method needs cache invalidation and they're called from different screens; errors still pipe to Sentry via `captureMutationError('useResetPassword.requestReset' / '.confirmReset')` so backend failures (rate limit, weak-password policy, expired recovery token) show up in the same dashboard as mutation hooks. **AuthScreen rewire**: "Forgot password?" link now calls `handleForgotPassword` → validates `emailValid` against the email field already on screen → fires `requestReset(trimmedEmail)` → shows native `Alert` with localized "Check your email — we sent a reset link to {email}." Replaces the prior `nav.navigate('ResetPassword')` (which only navigated to a stub). Sign-up mode still hides the link. **ResetPasswordScreen rewrite**: now the confirm-new-password screen — two SecureTextEntry fields (new + confirm) with min-6 + match validation, success Alert that `nav.reset` to MainTabs (AuthContext routes appropriately based on the now-hydrated session). Same screen serves both entry points: the recovery deep-link flow AND the signed-in user navigating from `SettingsAccountScreen → Reset password` row (existing session means `updateUser` succeeds without a token). **Deep-link plumbing**: `RootNavigator.tsx` mounts the real `ResetPasswordScreen` (was `Placeholders.ResetPassword`) and exports `linking: LinkingOptions<RootStackParamList>` with `prefixes: ['burs://']` + `screens: { ResetPassword: 'reset-password' }`. `App.tsx` adds `useRecoveryDeepLink()` — parses the recovery URL's hash fragment (`#access_token=...&refresh_token=...&type=recovery`), gates on `type === 'recovery'`, and calls `supabase.auth.setSession({ access_token, refresh_token })` so the screen reads a hydrated session. Both initial-URL (cold launch via the email link) and `Linking.addEventListener('url', ...)` (warm launch with the app already in background) paths are covered, with cleanup on unmount. `NavigationContainer` now receives `linking={linking}`. `app.json` declares `scheme: "burs"` (M34 will finalize the rest of the manifest). 5 new i18n keys for the AuthScreen reset Alerts (`auth.resetPassword.emailRequiredTitle/Body`, `successTitle/Body`, `errorTitle`) plus 13 keys for the new ResetPasswordScreen (`resetPassword.title/eyebrow/intro/newPasswordLabel/confirmPasswordLabel/cta/submitting/errorTitle/successTitle/successBody/tooShort/mismatch/back`) appended to `mobile/src/i18n/en.ts`. Verification: `npx tsc --noEmit` 0 errors, `npx eslint src App.tsx --max-warnings 0` clean, `npx expo-doctor` 17/17, `npx expo export --platform ios` bundle 4.72 MB hbc. Code-reviewer subagent approved (one P2 about Sentry capture absence — addressed in pre-push commit by piping both methods' errors through `captureMutationError`; second P2 about supabase import in AuthScreen turned out unfounded since `signInWithOAuth` still consumes it). No edge-function changes; no migrations. Supabase Auth dashboard whitelist of `burs://reset-password` redirect URL is the user's external action, listed in M44. |
| 2026-05-06 | #735 | M11 | **Mobile Launch M11 — Destructive mutations (account deletion + reset style memory).** App Store guideline 5.1.1(v) requires in-app account deletion; both edge functions (`delete_user_account` 24-table cascade + `reset_style_memory` atomic RPC) shipped server-side in Wave 8.5 but mobile had neither wired. New `mobile/src/hooks/useDeleteAccount.ts` and `mobile/src/hooks/useResetStyleMemory.ts` — both `useMutation` thin wrappers around `callEdgeFunction`, both with `retries: 1` (cascade is server-idempotent so a hung first attempt won't re-cascade on retry). Delete signs the user out via `supabase.auth.signOut()` after the cascade; AuthContext's existing `SIGNED_OUT` listener handles cache + signed-URL + offline-queue clear and the root navigator's auth gate routes to AuthScreen. Reset invalidates only the query keys that actually exist on mobile today (`['outfit']`, `['outfits']`, `['insights_dashboard']`); the wave-listed `['profile']`, `['style-dna']`, `['feedback-signals']`, `['ai-suggestions']` are documented as future-add when their consuming hooks ship (M14+ chat, M28 style DNA). New `mobile/src/components/TypedConfirmModal.tsx` — destructive-action gate that requires the user to type `DELETE` / `RESET` (case-sensitive) before the Confirm CTA enables. Mirrors web's PR #712 clickjacking-mitigation pattern. Cross-platform Modal (no `Alert.prompt` — that's iOS-only). Adds `destructive` prop to `Button.tsx` so the same surface treatment can be reused by future destructive flows (M30 unsubscribe, M31 RevenueCat). Wires both screens: `SettingsAccountScreen` (Delete) and `SettingsPrivacyScreen` (Reset + Delete). New i18n keys appended to `mobile/src/i18n/en.ts`: `settings.delete_account.*` and `settings.reset_memory.*` (en-only; sv + other 8 locales deferred to M33). Verification: `npx tsc --noEmit` 0 errors, `npx eslint src --ext .ts,.tsx --max-warnings 0` clean. Code-reviewer subagent approved (one P2 about speculative invalidation keys — addressed in same commit by trimming to only-real-queries and documenting future-adds). No edge-function changes; no migrations. |
| 2026-05-06 | #734 | M10 | **Mobile Launch M10 — Memory event queue.** New `mobile/src/lib/memoryEvents.ts` mirrors web's `src/lib/memoryEvents.ts` byte-for-byte (type-only `CanonicalStyleMemorySignal` import is the only cross-tree pull): `RecordMemoryEventInput`, `buildMemoryIdempotencyKey` (same target-precedence + alphabetical discriminator + minute_bucket as web), `isQuickReactionMissingValue` guard, plus typed event creators (`saveOutfitEvent`, `unsaveOutfitEvent`, `wearOutfitEvent`, `skipOutfitEvent`, `rejectOutfitEvent`, `rateOutfitEvent`, `swapGarmentEvent`, `neverSuggestGarmentEvent`, `quickReactionEvent`, `likePairEvent`, `dislikePairEvent`). Rewritten `mobile/src/lib/memoryIngest.ts` exposes `recordMemoryEvent(input)` (live caller path with quick_reaction guard + idempotency-key derivation + offline-queue fallback) and `dispatchMemoryEvent(payload)` (queue-replay path that swallows 4xx but throws 5xx so the queue retries). 4xx is treated as permanent client mistake (rate limit, paywall, validation, post-refresh 401) — logged + dropped, not enqueued. **Latent bug fix**: the legacy mobile shape sent `event_type` instead of the wire-canonical `signal_type` — server silently 400'd both the wear and save signals (Wave 8.5 P0 caught in PR #712 but never folded into mobile). The new typed creators emit the right field name. AuthContext registers a `MEMORY_EVENT_ACTION` handler alongside the M5 `add-garment-save` handler so connectivity-restored replay drains both queues atomically. `useOutfits.ts` migrates the two existing call sites (save_outfit + wear_outfit) from `ingestMemoryEvent` to typed creators + `recordMemoryEvent`. **Wave-listed screen migrations deferred**: OutfitDetailScreen / OutfitGenerateScreen / MoodOutfitScreen / StyleMeScreen / UnusedOutfitsScreen / GarmentDetailScreen don't currently have skip / swap / rate / never-suggest CTAs to wire — those depend on UI surfaces that haven't shipped yet on mobile. Tracked as a follow-up; the wave's "Persist save/wear actions through offline periods" core gate is satisfied. Verification: `npx tsc --noEmit` 0 errors, `npx eslint src --ext .ts,.tsx --max-warnings 0` clean. Code-reviewer subagent approved (no P0/P1/P2). No edge-function changes; no migrations. |
| 2026-05-06 | #733 | M9 | **Mobile Launch M9 — Edge function client wrapper.** New `mobile/src/lib/edgeFunctionClient.ts` exports `callEdgeFunction(name, opts)` — a single resilient wrapper for every `/functions/v1/...` call. Features: per-function circuit breaker (5 failures → 30s cooldown, mirrors web), exponential backoff with ±25% jitter capped at 8s, 90s default timeout, per-attempt AbortController honouring caller-supplied AbortSignal AND a cancellable backoff sleep, pre-flight session refresh when access_token is < 60s from expiry, one-shot 401 recovery via `auth.refreshSession()` (not counted against the retry budget), optional `X-Idempotency-Key` header (UUID generated once, reused across retries). Typed errors so consumers can branch cleanly: `EdgeFunctionTimeoutError`, `EdgeFunctionRateLimitError` (carries `retryAfter`), `EdgeFunctionSubscriptionLockedError` (HTTP 402 → paywall surfaces), `EdgeFunctionHttpError` (other non-2xx, carries `status` + `bodyText`), `EdgeFunctionCircuitOpenError`. Stream mode (`stream: true`) returns the raw `Response` for SSE consumers — TS overloads keep `T` typed for JSON, `Response` for stream. **Migrations** (`fetch(\`${supabaseUrl}/functions/v1/` now appears ONLY inside the wrapper — wave acceptance grep verified): `lib/sse.ts` (refactored to take `fnName` instead of full URL; uses `stream: true` internally; maps subscription-locked to the legacy `'subscription_required'` sentinel string), `lib/memoryIngest.ts`, `lib/garmentSave.ts` (queueRender), `lib/supabase.ts` (dropped unused `getSupabaseFunctionUrl`), `contexts/AuthContext.tsx` (start_trial), `hooks/useAnalyzeGarment.ts` (analyze + triggerGarmentEnrichment), `hooks/useDetectDuplicate.ts`, `hooks/useGenerateOutfit.ts`, `hooks/useWardrobeGaps.ts`, `hooks/useStyleChat.ts`, `hooks/useMoodOutfit.ts`, `hooks/useOutfits.ts` (memory-event calls + drop unused `session` destructures). Verification: `npx tsc --noEmit` 0 errors, `npx eslint src --ext .ts,.tsx --max-warnings 0` clean, `npx expo-doctor` 17/17, acceptance grep shows the only remaining raw fetch lives in `edgeFunctionClient.ts:257`. Code-reviewer subagent approved (P1 + 2 P2s addressed in pre-push commit: 404 added to non-retryable status set per web parity; backoff sleep now cancellable via AbortSignal; circuit-success bookkeeping moved AFTER the body-level rate-limit-envelope check so a 200 with `{error, retryAfter}` records a failure not a success). No edge-function changes; no migrations. **Wave skip note**: M6 (LiveScan continuous-scan, native Vision Camera) and M7 (batch add UX) deferred for user-side EAS-dev-build manual testing, which the agent can't run autonomously — both stay TODO; tracker pointer skipped to M9 then to M10 after merge per overnight-mode policy. |
| 2026-05-06 | #732 | M5 | **Mobile Launch M5 — Offline queue.** New `mobile/src/lib/offlineQueue.ts` ports the web's offline-mutation queue to AsyncStorage with a handler-registry pattern: each action type (currently `'add-garment-save'`) registers an async handler at app startup, the queue body stores only JSON-serializable payloads, and replay dispatches by action name. `MAX_QUEUE_SIZE = 50` + `MAX_ATTEMPTS = 3` per item; orphaned action types (no handler registered) are dropped on first replay rather than parked forever. `subscribe()` drives `useSyncExternalStore` in the new `mobile/src/hooks/useOfflineQueue.ts`, which also subscribes to NetInfo so consumers get `{ pending, isOnline }`. `mobile/src/components/OfflineBanner.tsx` self-renders null when online OR queue empty; `MainTabsScreen` mounts it as a safe-area-anchored overlay (`useSafeAreaInsets()` instead of a hardcoded top offset). Garment-save core (`persistGarment`, `persistGarmentWithOfflineFallback`, `OfflineQueuedError` sentinel, `isOnlineNow()` probe, all the `_shared`-equivalent helpers — `queueRender`, `buildAiRawWithSystemSignals`, etc.) extracted from `useAddGarment.ts` into `mobile/src/lib/garmentSave.ts` so the offline-replay handler can share the exact same code path WITHOUT a circular import (the prior `AuthContext ↔ useAddGarment` cycle is gone). `useAddGarment.ts` is now a thin React Query wrapper. `AuthContext.tsx` registers the `'add-garment-save'` handler at provider mount, kicks `replay()` on mount (cold-start with surviving items) AND on every NetInfo connectivity-restored event, and clears the queue on `SIGNED_OUT` so user A's pending mutations don't replay against user B's session. `useAddGarment` mutationFn pre-checks `isOnlineNow()` and re-checks after a thrown insert; on confirmed-offline either path enqueues and throws `OfflineQueuedError` — the screen layer (`AddPieceStep3`) treats it as success-with-side-effect, sets `savedRef.current = true` to skip the orphan-cleanup (the queued payload now owns the storagePath; replay needs the file), toasts "Saved offline", and unwinds with `nav.reset` to MainTabs. Sentry capture is skipped on `OfflineQueuedError` so the dashboard doesn't fill with offline-mode noise. New dep: `@react-native-community/netinfo@11.4.1` via `npx expo install` (SDK-54 compatible). Verification: `npx tsc --noEmit` 0 errors, `npx eslint src --ext .ts,.tsx --max-warnings 0` clean, `npx expo-doctor` 17/17. Code-reviewer subagent approved (pre-extraction P2s — circular import + safe-area inset — both folded into the same commit; one residual P2 about `cryptoRandomId` collision risk at MAX_QUEUE_SIZE=50 noted but deferred). No edge-function changes; no migrations. |
| 2026-05-06 | #731 | M4 | **Mobile Launch M4 — Duplicate detection.** New `mobile/src/hooks/useDetectDuplicate.ts` calls the deployed `detect_duplicate_garment` edge function with `{ image_path?, category, color_primary, title, subcategory, material }` and surfaces a typed `{ duplicates: DuplicateMatch[] }`. Companion `topDuplicate(result)` helper applies a `DUPLICATE_WARN_THRESHOLD = 0.85` (web parity per the wave file) so consumers don't drift. Wires the hook into `mobile/src/screens/AddPieceStep3.tsx` — the duplicate query fires as soon as the analysis fields land, image_path is optional so the parallel-upload path doesn't have to wait, and a once-per-match Modal pops with two CTAs: "View existing" navigates to GarmentDetail for the matched garment; "Add anyway" dismisses and lets the save flow proceed unchanged. A `duplicateAcknowledgedRef` keyed on `garment_id` prevents the modal from re-popping after dismissal even though title edits trigger a fresh query. The check is advisory: 402 / 429 / network errors silently no-op (the duplicate query throws but no save path reads its `error`/`isLoading`), so a flaky duplicate API can never block a legitimate save. New i18n keys `addpiece.duplicate.title` / `.body` / `.bodyNoTitle` / `.viewExisting` / `.addAnyway` appended to `mobile/src/i18n/en.ts`; sv (and the other 8 locales) deferred to M33. **Wave-file drift, intentional:** the wave's "Files touched" described the request shape as `{ garment_id_or_pending, ai_raw }` and response as `{ matches: [...] }` — the actual deployed contract is the one above. The implementation tracks the deployed function, not the stale wave description. Verification: `npx tsc --noEmit` 0 errors, `npx eslint src --ext .ts,.tsx --max-warnings 0` clean. Code-reviewer subagent approved (1 P2 about an unused `addpiece.duplicate.dismiss` key — removed in the same commit, no findings-log entry needed). No edge-function changes; no migrations. |
| 2026-05-06 | #730 | M3 | **Mobile Launch M3 — useGarmentCount.** New `mobile/src/hooks/useGarmentCount.ts` mirrors the web's count-only query (`select('*', { count: 'exact', head: true })` scoped by `user_id`) under the shared `['garments-count', user?.id]` cache key with 5-min `staleTime`. Wires the hook into `mobile/src/screens/WardrobeGapsScreen.tsx` to gate auto-analysis + the "Analyse now" CTA on `count >= 5` (web parity — gap analysis on a near-empty wardrobe flags every basic category as a "gap"). When the threshold isn't met the hero copy swaps to a guided "Add at least 5 garments…" caption and the CTA disables; once enough garments exist the original behaviour returns. Adds `['garments-count']` invalidation to `useAddGarment.onSuccess` and `useDeleteGarment.onSuccess` because the count cache key is a sibling of `['garments']` and would otherwise stay stale up to 5 min after a row count changes. **Wave-file deviations** (justified, scope-tight): the wave's "Modified call sites" listed `StudioSelectionStep.tsx` and `PaywallScreen.tsx` with hedged "(if any threshold display exists)" — neither currently reads a garment count, so no changes there (per CLAUDE.md "don't add features beyond what the task requires"). The mutation invalidation tweak is two new `invalidateQueries` calls beyond the literal "Files touched" list, but is required for the hook to stay correct after add/delete; documented in code comments. Verification: `npx tsc --noEmit` 0 errors, `npx eslint src --ext .ts,.tsx --max-warnings 0` clean, `npx expo-doctor` 17/17. Code-reviewer subagent approved. No edge-function changes; no migrations. |
| 2026-05-07 | #TBD | M18 | **Mobile Launch M18 — Photo feedback / selfie comparison.** Wires the deployed `outfit_photo_feedback` edge function into a camera-first capture surface so users can take a mirror selfie and compare against an outfit's garments. New `mobile/src/hooks/usePhotoFeedback.ts` is a two-step hook: (1) resize selfie via `expo-image-manipulator` (longest side 1200 px, JPEG q=0.85, same recipe as `lib/imageUpload.ts`'s AddPiece flow) → upload bytes via `supabase.storage.from('garments').upload(\`${user.id}/selfie_<timestamp>.jpg\`, ...)` (existing RLS policy authorises writes under the user's prefix); (2) call `outfit_photo_feedback` via M9's `callEdgeFunction` with `{ outfit_id, selfie_path }` and adapt the deployed `outfit_feedback` row response into the screen-facing `PhotoFeedback` envelope. **Contract drift, intentional (matches deployed function, not stale wave skeleton):** wave file's hook described `{ outfit_id, selfie_image_path }` request + `{ fit_notes, color_callouts, swap_suggestions, ... }` response — the deployed function reads `selfie_path` and returns the full `outfit_feedback` row (`fit_score`, `color_match_score`, `overall_score`, `commentary`, `ai_raw`); we track the deployed contract end-to-end with a defensive adapter (`commentary → fit_notes`, first sentence → `summary`, `overall_score` passes through, `color_callouts`/`swap_suggestions` mine `ai_raw` defensively for future tool-schema expansion). **Bucket choice:** the wave optimistically referenced a `selfie-feedback` bucket; verification of `00000000000000_initial_schema.sql` shows that bucket does NOT exist — only `garments` does. The deployed edge function reads the `garments` bucket directly (it asks `supabase.storage.from('garments').createSignedUrl(selfie_path, ...)` for both the selfie AND the outfit thumbnails), so uploading to `garments/<userId>/selfie_<ts>.jpg` keeps the function's signed-URL fetch and the existing user-prefixed RLS policy aligned. Standard hook plumbing: AbortController per call, mount-cleanup via abortRef, `EdgeFunctionSubscriptionLockedError → 'subscription_required'` sentinel, `EdgeFunctionHttpError` → parsed body `error` field or `HTTP <status>` fallback. Sentry capture skips paywall sentinel + caller aborts. New `mobile/src/screens/PhotoFeedbackScreen.tsx` is a dark-themed camera-first capture surface (always-light feedback view post-analysis) that mirrors `LiveScanScreen`'s permission gating + post-capture overlay UX: requestCameraPermission → CameraView (defaults to `front` facing for selfie) with shutter → "Use this selfie?" / "Retake" confirm → blocking overlay during upload + analyze (status copy swaps `Uploading…` → `Analyzing…`) → light-themed feedback view (Eyebrow `Fit notes` → italic PageTitle with summary or "Photo feedback" fallback → Caption with full commentary → optional color-callout chips → optional swap-suggestion `GarmentCard`s hydrated via `useGarment(swap.garment_id)`). Sticky paywall route via `paywallShownRef` (release-on-non-sentinel pattern from M17 OutfitDetail) routes once per screen lifetime. Modified screens: `mobile/src/screens/OutfitDetailScreen.tsx` adds a "Try it on" `quiet`-variant Button above the M17 composition-helpers row (one-tap mainline action, no AI cost gating); `mobile/src/screens/PlanScreen.tsx` adds the same CTA on the planned-outfit row (rendered after Restyle/Clear/+Add). `mobile/src/navigation/RootNavigator.tsx` registers the `PhotoFeedback` route with `{ outfitId: string }` typed param + adds the screen to the native stack. **Memory event deferred:** the wave asked for a `try_on_outfit` event creator in `mobile/src/lib/memoryEvents.ts`, but verification of `supabase/functions/_shared/style-memory-signals.ts` shows the canonical `CanonicalStyleMemorySignal` union has only 11 entries (`save_outfit`, `unsave_outfit`, `rate_outfit`, `wear_outfit`, `skip_outfit`, `reject_outfit`, `swap_garment`, `quick_reaction`, `never_suggest_garment`, `like_pair`, `dislike_pair`) — `try_on_outfit` is NOT in the taxonomy and the `memory_ingest` edge function would 4xx the event before it reached `feedback_signals`. Adding a mobile-only typed creator that emits an unsupported `signal_type` would silently drop the signal AND violate the file's "All future writers MUST emit one of these" contract. Findings-log entry added; the photo-feedback UI is the bulk of M18's value and ships in this PR. 17 new i18n keys appended to `mobile/src/i18n/en.ts` (`photoFeedback.{eyebrow,title,tryOnAction,captureCta,retake,confirm,uploading,analyzing,error,fitNotes,colorCallouts,swapSuggestions,done,overallTemplate,hint,cameraUnavailable,allowCamera,openSettings,captureFailedTitle,captureFailedBody,close,switchCamera}`); other 9 locales fall back to en (M33 ports). Verification: `npx tsc --noEmit --skipLibCheck` 0 errors, `npx eslint src --max-warnings 0` 0 warnings, `npx expo-doctor` 17/17 passed. No edge-function changes (wave authorizes none); no migrations. |
| 2026-05-07 | #TBD | M17 | **Mobile Launch M17 — Compositional helpers (flatlay + accessories + combinations + clone DNA).** Wires the four already-deployed composition edge functions into OutfitDetail + ShareOutfit. Each new hook is a thin M9 `callEdgeFunction` wrapper following the `useGenerateOutfit` / `useOutfitPool` shape: AbortController per call, sticky in-flight on unmount, typed-error catch (`EdgeFunctionSubscriptionLockedError` → `'subscription_required'` sentinel; `EdgeFunctionHttpError` → parsed `error` field or `HTTP <status>`), Sentry breadcrumb on unexpected paths only (skipped for the paywall sentinel + caller aborts). New `mobile/src/hooks/useGenerateFlatlay.ts` posts `{ outfit_id }` to `generate_flatlay` and surfaces `{ flatlayPath, isGenerating, error, generate, reset }` — the function is synchronous (uploads PNG to `garments/<userId>/flatlay_<outfitId>.png` then stamps `outfits.flatlay_image_path` and returns the path), so no render-job polling is required. New `mobile/src/hooks/useSuggestAccessories.ts` posts `{ outfit_id }` and surfaces `{ accessoryGarmentIds, isSuggesting, error, suggest, reset }` — id list is order-preserving and de-duped at the screen edge to prevent duplicate `outfit_items` inserts when the AI returns the same garment twice with different reasons. New `mobile/src/hooks/useSuggestCombinations.ts` posts `{ locale }` (the function ignores `outfit_id` and scores against the full wardrobe; `outfit_id` is still trim-validated so callers surface a real error instead of silently scoring nothing) and surfaces `{ combinations, isSuggesting, error, suggest, reset }`, where each combination maps to the existing `ScoredOutfitDraft` shape from `useOutfitPool` so screens render with the same `OutfitCard` infrastructure. Slot inference falls back to `top` per the existing `useGenerateOutfit.adaptItems` default for unmapped categories. New `mobile/src/hooks/useCloneOutfitDNA.ts` posts `{ outfit_id, locale }` to `clone_outfit_dna`, surfaces `{ cloned, variations, isCloning, error, clone, reset }` — the function returns up to 3 variations; the wave's MVP renders only `cloned` (variations[0]) but the full set is exposed for future depth. **Modified screens**: `mobile/src/screens/OutfitDetailScreen.tsx` adds a "More actions" sub-row beneath the primary Wear/Restyle/Save row with three `quiet`-variant buttons ("Suggest accessories", "Try variations", "Clone style"); each opens its own `CollapsibleSection` (new local component — boxed card with Eyebrow + Hide affordance) that lazily fires the underlying hook on first open. **Accessories**: 3-5 horizontally-scrolling `AccessoryCard`s (new local component — gradient placeholder + signed-URL image fallback + `+ Add to outfit` button); the add mutation inserts an `outfit_items` row with `slot: 'accessory'` (table has no UNIQUE on `(outfit_id, garment_id)` so a per-screen `Set<string>` of just-added ids enforces idempotency — synchronous double-tap drops at the gate, optimistic added flag flips Button to "Added"). Garment row hydration via inline `useQuery` keyed `['m17AccessoryRows', userId, sortedIdKey]` against `garments.in('id', ids).eq('user_id', userId)` — single round-trip. **Variations**: horizontal scroll of up to 3 `OutfitCard`s; tap → `nav.navigate('OutfitGenerate', { garmentId: draft.items[0]?.garment_id })` (seeds the variation's first garment as anchor, the screen's anchor pill carries forward the variation's intent — MVP "preview cards with Save" satisfied by the existing OutfitGenerateScreen's Save action). **Clone DNA**: pill-style "A new outfit in the same style" banner + a single OutfitCard pinned to `cloned`; tap navigates to OutfitGenerate with the first garment as anchor. All three sections share the sticky-ref paywall route pattern (`paywallShownRef` → `nav.navigate('Paywall')` once per screen lifetime when any of the three hooks raises `'subscription_required'`) — App-Store-friendly, doesn't re-pop the modal on retry. `ShareOutfitScreen.tsx` reads the `id` from route params, mounts `useGenerateFlatlay()`, swaps the share-card preview's 4-cell hue grid for either (a) the rendered flatlay image (resolved through M2's `useSignedUrl` so signed-URL caching applies), (b) a centered ActivityIndicator with "GENERATING FLATLAY…" caption while in flight, or (c) the original placeholder grid pre-generation. New "Generate flatlay" button below the caption (variant flips from `primary` → `outline` once a path lands so re-generation is intentional, not accidental). Re-generation is allowed (the function upserts to a deterministic storage path so it's cheap) but the screen skips the call when the path or in-flight flag is set. Same sticky-ref paywall route pattern. **i18n**: 11 new keys appended to the END of `mobile/src/i18n/en.ts` under "Composition helpers (M17)" — `outfitDetail.{suggestAccessoriesAction,tryVariationsAction,cloneDnaAction,accessories.{title,empty,addAction},variations.{title,empty},cloneDna.{title,banner}}` + `shareOutfit.{generateFlatlay,generatingFlatlay,flatlayError}`. Other 9 locales fall back to en (M33 ports). **Wave-file note (justified)**: `suggest_outfit_combinations` doesn't accept `outfit_id` server-side (the function reads the user's wardrobe + recent outfits and scores 2-3 fresh combinations); the hook still trim-validates the param so a future contract change can flip the field on without a screen rewrite. `clone_outfit_dna` returns 3 variations but only the first is rendered per the wave's "result is a single outfit" instruction — the rest live in `variations[]` for future depth. Verification: `npx tsc --noEmit --skipLibCheck` 0 errors, `npx eslint src --max-warnings 0` 0 warnings, `npx expo-doctor` 17/17 passed. No edge-function changes (all four already deployed); no migrations. |
| 2026-05-06 | #TBD | M15 | **Mobile Launch M15 — Day intelligence depth.** Ports the web's day-intelligence engine + a SmartDayBanner onto HomeScreen. New `mobile/src/lib/dayIntelligence.ts` is a byte-for-byte port of `src/lib/dayIntelligence.ts` (web) — pure module, no React, no DB calls, exports `buildDayIntelligence(events, weather) → DayIntelligence` plus the full type surface (`DayFormality`, `OutfitStrategy`, `DayEventInput`, `DayWeatherInput`, `EventIntelligence`, `DayIntelligence`) so mobile + web + the `summarize_day` edge function (`_shared/day-intelligence.ts`) share the same scoring rules end-to-end. Adds `DayContext` + `DayWeatherContext` types for the mobile scoring + UI layers respectively. New `mobile/src/lib/buildTodaySuggestions.ts` is a NEW pure scoring function (the web's same-named file builds chat suggestion chips with a different signature; the wave file's contract is `buildSuggestions(context, outfits, garments) → ScoredOutfit[]`). Five weighted axes — occasion match (3x), formality match (2.5x — averaged across outfit_items garments since `outfits.formality` doesn't exist on the schema), weather fit (2x — outerwear/season tags vs precipitation+temp, sensitivity-gated), novelty (1.5x — penalises overlap with the recently-worn-garment Set), completeness (1x — 4-piece outfits beat 1-piece). Defensive throughout: missing fields score neutral, never throw. New `mobile/src/hooks/useSmartDayRecommendation.ts` composes both libs — `useFlatGarments()` + `useOutfits(false)` (savedOnly=false so generated-but-unsaved looks are still candidates), derives the recently-worn set from `garments.last_worn_at` ≥ 7 days ago, returns `{ context, top3, isLoading, error }`. Mobile-adapted graceful fallback: weather defaults to `{ temperature: 18, precipitation: 'unknown' }` (M35 dependency documented inline), events default to `[]` (M36 dependency documented). Consumers can pass overrides via `{ weather, events }` once those hooks ship. Memoization stabilises overrides so identity-stable inputs don't churn the engine. New `mobile/src/hooks/useDaySummary.ts` calls the deployed `summarize_day` edge function via M9's `callEdgeFunction` (NOT raw fetch), keyed by `['daySummary', user?.id, dayKey, eventsHash, weatherHash]` so the AI call only fires once per day per user; staleTime 1h, gcTime 24h, retries=1, timeout 30s, paywall/rate-limit errors return false from `retry` so the banner hides instead of looping. Request body matches the function's contract verified against `supabase/functions/summarize_day/index.ts`: `{ events, weather }`. Returns `{ summaryText, isLoading, error }`. New `mobile/src/components/SmartDayBanner.tsx` renders an `Eyebrow` ("{Weekday} · {Occasion} · {Temp°C}" or just "{Weekday}" until M35/M36), italic `PageTitle` with the AI summary text (or fallback prompt), and a single tappable `OutfitCard` for top1 → navigates to `OutfitDetail`. Loading: full skeleton. Error / empty top1: returns null (banner is advisory; engine errors aren't surfaced). Uses `useTokens()`, no hardcoded hex; reuses existing primitives (`Eyebrow`, `PageTitle`, `Caption`, `OutfitCard`, `Skeleton`). `mobile/src/screens/HomeScreen.tsx` mounts `<SmartDayBanner />` immediately above the existing today's-look hero `Card` (within the same `gap: 18` ScrollView column) — nothing else touched, the existing hero remains the load-bearing surface. 5 i18n keys appended to `mobile/src/i18n/en.ts`: `home.smartDay.eyebrowTemplate`, `home.smartDay.fallback.eyebrow`, `home.smartDay.fallback.title`, `home.smartDay.tapHint`, `home.smartDay.empty.title`. Verification: `npx tsc --noEmit --skipLibCheck` 0 errors, `npx eslint src --max-warnings 0` clean, `npx expo-doctor` 17/17 passed. No edge-function changes (`summarize_day` already deployed per the wave file); no migrations. |
| 2026-05-06 | #TBD | M16 | **Mobile Launch M16 — Outfit pool + week generator.** Wires the pool generator (5–10 outfits per session) and the week generator (7-day plan) into PlanScreen and a new "Generate pool" surface. Both fan out via M9's `callEdgeFunction` against `burs_style_engine` (the engine the existing single-outfit `useGenerateOutfit` already calls — wave file's "generate_outfit" was a thin shim and the existing mobile contract already targets `burs_style_engine`); per-call failures isolated via `Promise.allSettled` (pool) / try-per-day-then-continue (week). New `mobile/src/hooks/useOutfitPool.ts` exposes `{ pool, isGenerating, error, anchorMissed, completed, generatePool({count, anchorGarmentId?, occasion?}), reset }` — fans out N parallel calls (clamped to 1–10), surfaces `'subscription_required'` sentinel on any 402 to short-circuit the batch (paywall route), drops drafts that fail the M13 `validateOutfitItems` slot rules or the `isAnchorPresent` anchor check (anchor-missed counted into `anchorMissed`), only flips top-level `error` when ALL calls failed. New `mobile/src/hooks/useWeekGenerator.ts` exposes `{ entries, isGenerating, completed, error, generateWeek({startDate?, locale?}), regenerateDay(date), reset }` — 7 sequential calls (matches web's intent to avoid burning the per-minute rate-limit envelope on a premium tier), per-day errors land on `entries[i].error` for the row-level "tap to retry" affordance, M15 `buildDayIntelligence(events, weather)` derives per-day `day_context`, recently-worn garment IDs (from `useFlatGarments` last_worn_at within 7 days) flow as `exclude_garment_ids` to bias against repetition. `regenerateDay` re-runs a single ISO without resetting the global flag so the rest of the week stays put. New `mobile/src/screens/OutfitPoolScreen.tsx` reads `{ anchorGarmentId?, occasion?, count? }` from the route, renders 2-column `FlatList` of pool tiles (gradient swatch + select check badge + occasion / family_label), sticky bottom bar with "Save selected (N)" + "Generate more"; saves persist via inline `outfits` + `outfit_items` insert (with `saved: true`) using the `supabase` client + `useAuth().user`, swallows per-draft failures into a partial-save toast, invalidates `['outfits']` on completion. Subscription path renders a soft pre-paywall affordance behind the redirect-effect; full-failure path shows the M16 i18n empty-state copy + Retry. New `mobile/src/components/WeekPlanPreview.tsx` renders one row per day (date label + outfit preview / generating skeleton / "Couldn't generate {day} — tap to retry"); empty state collapses to a single "Generate week" CTA so PlanScreen stays compact until the user opts in. Modified: `OutfitGenerateScreen` adds a "Generate pool" CTA on the result view that navigates with the same anchor + occasion (so a Restyle-from-piece flow can pivot to a 5-shot pool); `PlanScreen` mounts `WeekPlanPreview` between the "Coming up" HR and the next HR, wiring `onGenerateWeek` → `weekGen.generateWeek({startDate: now})` and `onSwapDay` → `weekGen.regenerateDay(date)`; `RootNavigator` registers `OutfitPool` route + adds `{ anchorGarmentId?: string; occasion?: string; count?: number } | undefined` to `RootStackParamList`. 14 new i18n keys appended to the END of `mobile/src/i18n/en.ts` (`outfitPool.title`, `progressTemplate`, `saveSelectedTemplate`, `generateMore`, `empty.title/body/retry`, `savedTemplate`, `partialSaveBody` + `weekPlan.title`, `generate`, `generating`, `progressTemplate`, `dayFailedTemplate`, `swap`, `savedTemplate`); other 9 locales fall back to en (M33 ports). **Wave-file note (justified):** wave references "generate_outfit" function by name — the actual mobile single-outfit hook (`useGenerateOutfit`) already targets `burs_style_engine` (the underlying engine; `generate_outfit` is a shim), so both new hooks call `burs_style_engine` directly to keep the request envelope identical (mode/generator_mode/occasion/style/weather/locale/prefer_garment_ids/exclude_garment_ids/day_context). Wave's "useSaveOutfit().mutate per selected" line replaced by inline `outfits + outfit_items` insert because `useSaveOutfit` only flips `saved=true` on an existing row id — pool drafts have no persisted id, so the insert IS the save. Verification: `npx tsc --noEmit --skipLibCheck` 0 errors, `npx eslint src --max-warnings 0` 0 warnings, `npx expo-doctor` 17/17 passed. No edge-function changes (wave authorizes none); no migrations. |
| 2026-05-06 | #739 | M14 | **Mobile Launch M14 — 8-mode chat contract + AIChat depth.** Three-phase port of web's `styleChatContract` + `styleChatNormalizer` + AIChat depth into the mobile chat surface. **Phase A — contract**: byte-for-byte ports of `mobile/src/lib/styleChatContract.ts` (full 8-mode `StylistChatMode` union + envelope/policy/state/kind unions + `PersistedStyleChatMessage` + `isStyleChatResponseEnvelope` guard + `collectStyleChatGarmentIds`) and `mobile/src/lib/styleChatNormalizer.ts` (parsers + slot map). New `mobile/src/lib/chatActiveLook.ts` exports `getLatestActiveLook(messages)` + `hasRenderableActiveLook(meta)` against a structural `ChatMessageLike` to avoid circular dep with the chat hook. **Phase B — hook + screen**: `useStyleChat` rewritten end-to-end (~410 LOC). New `UseStyleChatResult` exposes `isHydrating`, `suggestionChips`, `activeLook`, `anchoredGarmentId` + setter, and async `clearChat()`. Hydration on mount via `chat_messages.select(role, content, created_at).eq(mode, 'stylist')` ASC limit 100 → `parseStoredMessage(row)` mirrors web's JSON-tagged stylist-message format and decodes `PersistedStyleChatMessage` into `{content, stylistMeta}`. `sendMessage` now constructs `selected_garment_ids` (from anchor), `active_look` (from latest stylistMeta with non-empty `garment_ids`, threading `anchor_garment_id` + `anchor_locked` flags), and `locale` (`getLocale() ?? 'en'`). SSE parser extended: `type === 'stylist_response'` validates via `isStyleChatResponseEnvelope` and attaches the full envelope to the streaming bubble; `type === 'suggestions'` repopulates `suggestionChips`. `persistMessages` private helper writes BOTH user + assistant rows on `onDone`, JSON-encoding meta-bearing assistant turns as `PersistedStyleChatMessage`. `clearChat` aborts stream → deletes rows → clears local state. Kept all M9 `callEdgeFunction` plumbing via `fetchSSE`, rAF coalescing, AbortController. `StyleChatScreen` rewired: replaced static `MEMORY_FACTS` with `useStyleMemoryFacts()`; new private `MemoryChipRow` component with forget action wired to `useRecordMemoryEvent`; mode-pill `Eyebrow` rendered above each assistant bubble (`stylistMeta.mode` → `chat.mode.<MODE>` i18n key); active-look badge between message list and suggestion chips with garment titles hydrated via inline `useQuery` and a Clear button that fires `sendMessage('clear active look')` (server honors `clear_active_look` flag); anchor row above messages with X to clear; long-press on assistant bubbles with `stylistMeta.active_look` shows an `Alert.alert` "Set anchor" confirm that sets `anchor_garment_id ?? garment_ids[0]`; hydration spinner → empty welcome → restored FlatList depending on state; suggestion chips prefer server-provided `suggestionChips` over the static fallback. **Phase C — memory hooks**: new `mobile/src/hooks/useStyleMemoryFacts.ts` queries `user_style_summaries.summary_json` (defensive accessors over fluid JSON: `style_keywords[]`, `palette[]`, `disliked_garments[]`) PLUS the latest 8 `feedback_signals` rows of type `never_suggest_garment`, hydrates garment titles via a single `garments.select.in('id', ids)` lookup, dedupes by garmentId (feedback_signals wins over summary_json), and caps the merged result at 12 entries. New `mobile/src/hooks/useRecordMemoryEvent.ts` is a typed mutation following the `useAddGarment` pattern — fires `recordMemoryEvent(neverSuggestGarmentEvent(...))` (M10 typed creator + queue-aware dispatcher) and invalidates `['styleMemoryFacts']` on success. **i18n**: 16 new keys appended to `mobile/src/i18n/en.ts` (`chat.mode.*`, `chat.memory.*`, `chat.active_look.*`, `chat.anchor.*`, `chat.history.restored`); other 9 locales deferred to M33 per existing convention. **Deferrals (logged in findings-log)**: free-text "forget I prefer X" preference correction NOT shipped — `reject_outfit` requires an `outfit_id`, no `preference_correction` signal type exists server-side; garment-level forget is fully wired. Multi-thread switcher (`createChatThreadMode` + `ChatHistorySheet`), image upload composer (`pendingImage`), `plan_action` banner (M22 owns), and `useRefineMode` lock-toggle UI all explicitly out-of-wave-spec scope. Verification: `npx tsc --noEmit --skipLibCheck` 0 errors, `npx eslint src --max-warnings 0` clean, `npx expo-doctor` 17/17 passed. Code-reviewer subagent: pending (multi-agent review fires next). No edge-function changes; no migrations. |
| 2026-05-07 | #TBD | M19 | **Mobile Launch M19 � Visual Search in AddPiece.** Adds a third entry mode (Camera / Gallery / Search by photo) to AddPieceStep1. New  wraps the deployed  edge function: resizes the reference photo to the canonical analyze input (longest side 1200 px, JPEG q=0.85, base64), then calls the function with  via M9 . **Contract drift, intentional (matches the deployed function, not the wave skeleton):** the wave file described a  envelope from a storage path; the actual function at  reads  directly and returns  � there are no web product matches today. The hook tracks the deployed contract end-to-end and adapts the response into  so the screen layout stays skeleton-aligned and a future server expansion can flow through the same surface without touching the screen (M18 precedent). Defensive accessors throughout the adapter � malformed rows are dropped,  clamped to [0, 1] after dividing by 100, dedupe by garment_id. -style cleanup is moot here because there is no storage upload (no orphan blob to sweep); the hook still sweeps the manipulator's resized temp file on abort/error/unmount. Standard pattern: AbortController per call, mount-cleanup via ref + abort in useEffect teardown,  ? 'subscription_required' sentinel for paywall surfacing,  ? parsed body  field or  fallback, Sentry capture skips paywall sentinel + caller aborts. New  is the capture/upload UI + two horizontal-scroll result rows. Pre-search state: dropzone-style card with Take photo + Choose from library buttons (uses expo-image-picker for both, mirrors LiveScan permission UX). After picking, swaps to a preview card with the reference image + 'Search this look' CTA + clear (�) button. Searching state: spinner card with 'Finding similar pieces�' caption. Results: two  rows (Your wardrobe / Found online) � wardrobe row hydrates each match via  and renders  (160 px wide), tap ? ; web row renders compact product cards (image + title + price + merchant), tap ? Alert with 'Online import coming soon' + best-effort . Per-row empty state when zero matches. Sticky paywall redirect ref so a 402 routes to PaywallScreen exactly once per mount. Modified:  adds a third  (Search by photo � Find similar) using the existing ;  registers the  route + adds  to . 12 i18n keys appended to the END of : , , , , , , , , , , , . Other 9 locales fall back to en (M33 ports). Verification:  0 errors,  0 warnings, env: load .env.local
env: export SUPABASE_SERVICE_ROLE_KEY VITE_SUPABASE_PROJECT_ID VITE_SUPABASE_PUBLISHABLE_KEY VITE_SUPABASE_URL 17/17 passed. No edge-function changes (wave authorizes none); no migrations. |
| 2026-05-07 | #TBD | M19 | **Mobile Launch M19 — Visual Search in AddPiece.** Adds a third entry mode (Camera / Gallery / Search by photo) to AddPieceStep1. New `mobile/src/hooks/useVisualSearch.ts` wraps the deployed `visual_search` edge function: resizes the reference photo to the canonical analyze input (longest side 1200 px, JPEG q=0.85, base64), then calls the function with `{ image_base64, locale }` via M9 `callEdgeFunction`. **Contract drift, intentional (matches the deployed function, not the wave skeleton):** the wave file described a `{ wardrobe_matches, web_matches }` envelope from a storage path; the actual function at `supabase/functions/visual_search/index.ts` reads `image_base64` directly and returns `{ description, matches: [{detected_item, garment_id, confidence (0-100), reason}], gaps: [{detected_item, suggestion}] }` — there are no web product matches today. The hook tracks the deployed contract end-to-end and adapts the response into `{ wardrobeMatches, webMatches: [] }` so the screen layout stays skeleton-aligned and a future server expansion can flow through the same surface without touching the screen (M18 precedent). Defensive accessors throughout the adapter — malformed rows are dropped, `confidence` clamped to [0, 1] after dividing by 100, dedupe by garment_id. `lastReferencePathRef`-style cleanup is moot here because there is no storage upload (no orphan blob to sweep); the hook still sweeps the manipulator's resized temp file on abort/error/unmount. Standard pattern: AbortController per call, mount-cleanup via ref + abort in useEffect teardown, `EdgeFunctionSubscriptionLockedError` → 'subscription_required' sentinel for paywall surfacing, `EdgeFunctionHttpError` → parsed body `error` field or `HTTP <status>` fallback, Sentry capture skips paywall sentinel + caller aborts. New `mobile/src/screens/VisualSearchScreen.tsx` is the capture/upload UI + two horizontal-scroll result rows. Pre-search state: dropzone-style card with Take photo + Choose from library buttons (uses expo-image-picker for both, mirrors LiveScan permission UX). After picking, swaps to a preview card with the reference image + 'Search this look' CTA + clear (×) button. Searching state: spinner card with 'Finding similar pieces…' caption. Results: two `FlatList` rows (Your wardrobe / Found online) — wardrobe row hydrates each match via `useGarment(id)` and renders `GarmentCard` (160 px wide), tap → `GarmentDetail`; web row renders compact product cards (image + title + price + merchant), tap → Alert with 'Online import coming soon' + best-effort `Linking.openURL(product_url)`. Per-row empty state when zero matches. Sticky paywall redirect ref so a 402 routes to PaywallScreen exactly once per mount. Modified: `mobile/src/screens/AddPieceStep1.tsx` adds a third `SourcePill` (Search by photo · Find similar) using the existing `SearchIcon`; `mobile/src/navigation/RootNavigator.tsx` registers the `VisualSearch` route + adds `VisualSearch: undefined` to `RootStackParamList`. 12 i18n keys appended to the END of `mobile/src/i18n/en.ts`: `visualSearch.title`, `eyebrow`, `takePhoto`, `chooseFromLibrary`, `searchCta`, `searching`, `error`, `wardrobeRow`, `webRow`, `wardrobeEmpty`, `webEmpty`, `webComingSoon`. Other 9 locales fall back to en (M33 ports). Verification: `npx tsc --noEmit --skipLibCheck` 0 errors, `npx eslint src --max-warnings 0` 0 warnings, `npx expo-doctor` 17/17 passed. No edge-function changes (wave authorizes none); no migrations. |
| 2026-05-07 | #TBD | M21 | **Mobile Launch M21 — Assess garment condition (GarmentDetail entry).** Wires the deployed `assess_garment_condition` edge function into GarmentDetail so users can run a wear/fit/repair check on any garment. New `mobile/src/hooks/useAssessCondition.ts` exposes `{ assessment, isAssessing, error, assess(garmentId), reset }` over the M9 `callEdgeFunction` wrapper — AbortController + unmount cleanup, `'subscription_required'` sentinel surfaced via `error`, defensive shape adapter (`toStringArray` + `normaliseScore` clamp/scale). On success the hook invalidates `['garment', user.id, garmentId]` so `useGarment` refetches the persisted columns. **Schema reality (deviates from wave brief):** the wave file referenced a `condition_assessment_jsonb` column on `garments`; that column does NOT exist. The actual schema (per `supabase/migrations/00000000000000_initial_schema.sql` lines 903-904 and `src/integrations/supabase/types.ts`) has two scalar columns — `condition_score numeric(3,1)` (1.0-10.0) and `condition_notes text`. The deployed edge function (`supabase/functions/assess_garment_condition/index.ts`) returns `{ condition_score, notes, should_replace }` and persists the first two via an UPDATE on the `garments` row. The hook normalises the 1-10 server score to a 0-100 UI percentage so the badge tier breakpoints (80 / 50 / 0) match the wave brief, and treats `wear_signals[]` + `repair_recommendations[]` as forward-compat reserved arrays (empty today, populated when the server enrichment lands). The AI's free-form `notes` doubles as the `summary` field on the public assessment. New `mobile/src/components/ConditionBadge.tsx` is a tiered pill (accent for ≥80, neutral border for 50-79, destructive for <50) with the numeric score + tier label + a 2-line caption (first repair recommendation OR summary). Tappable variant adds `accessibilityRole/Label/Hint` pointing at the detail sheet. Modified `mobile/src/screens/GarmentDetailScreen.tsx`: Info tab now renders a Condition block with the badge (when any score is present — either freshly returned from the hook or persisted on the garment row) plus a "Check condition" outline button (size=sm) that runs the assessment inline; sticky paywall ref intercepts the `'subscription_required'` sentinel and routes to PaywallScreen once per screen lifetime, released on `useFocusEffect` regain. New `ConditionDetailSheet` component (RN `Modal` + `animationType="slide"` + scrim, mirrors `GarmentSaveChoiceSheet` so mobile doesn't pull in another sheet library) renders the large numeric score, summary paragraph, wear-signal chips (when present), bullet-listed repair recommendations (when present), and a Re-assess button that releases the paywall latch and re-fires the hook. `persistedAssessment` memo derives a stable `ConditionAssessment` from `garment.condition_score` + `condition_notes` so the badge survives screen reloads and unmount-remount cycles. 12 i18n keys appended to the END of `mobile/src/i18n/en.ts` under the `condition.*` namespace (`checkAction`, `assessing`, `scoreLabel`, `tier.good/fair/poor`, `wearSignals`, `repairTitle`, `reassessAction`, `error.network`, `openHint`, `empty`); other 9 locales fall back to en (M33 ports). Verification: `npx tsc --noEmit --skipLibCheck` 0 errors, `npx eslint src --max-warnings 0` 0 warnings, `npx expo-doctor` 17/17 passed. No edge-function changes (wave authorizes none); no migrations. |
| 2026-05-07 | #TBD | M20 | **Mobile Launch M20 — Import garments from links into AddPiece.** Adds a fourth entry mode (Camera / Gallery / Search by photo / Import from link) to AddPieceStep1. New `mobile/src/hooks/useImportFromLinks.ts` wraps the deployed `import_garments_from_links` edge function. **Contract reality (verified against `supabase/functions/import_garments_from_links/index.ts`):** the function is a one-shot scrape + persist — per URL it fetches HTML, extracts JSON-LD Product → og:image → twitter:image → og:title → <title>, downloads the cover image, uploads to the `garments` Storage bucket, INSERTs a `garments` row with default `category: 'top'` + `color_primary: 'grey'` + `imported_via: 'link'`, and the user refines fields inline from GarmentDetail. Request body `{ userId: string, urls: string[] }` (max 30); response `{ results: Array<{url, status: 'ok'|'failed', garment_id?, title?, image_path?, reason?}>, summary }`. **Storage handling decision (deviates from prompt):** the user spec described re-uploading remote thumbnails to the `garments` bucket from the mobile screen (option 1). The deployed function ALREADY performs the download + Storage upload + DB insert atomically — duplicating that on the client would create orphan files and double-charge the import. We track the deployed contract end-to-end: the screen surfaces the function's returned `image_path` via `useSignedUrl` for thumbnail display, and the saved garment row is reachable via tap-through to `GarmentDetail`. **Why no client-side proposal/confirm screen (deviates from prompt):** the function commits the row before returning — there is no proposal stage to surface client-side. The wave file's "user confirms" copy is aspirational; honoring it would require modifying the edge function (out of M20 scope per CLAUDE.md "no new edge functions"). The screen mirrors the web `LinkImportForm.tsx` UX: paste → per-URL progress timeline → success rows tap-through to GarmentDetail for refinement. **Sequential per-URL processing:** sends one URL per request to surface smooth progress and stay well under the 90s edge-function budget (single page-fetch can take 10s+); matches the web flow. Hook exposes `{ items, isImporting, currentIndex, totalCount, error, submit, reset }` plus a `parseUrlList(text)` exported helper that enforces the https:// allowlist + de-dupes. **URL allowlist tightened to https://** per the M19 web-match precedent (web's LinkImportForm accepts http:// too; mobile rejects). Standard pattern from useVisualSearch / usePhotoFeedback: AbortController per call, unmount cleanup via ref + abort in useEffect teardown, `EdgeFunctionSubscriptionLockedError` → 'subscription_required' sentinel for paywall surfacing, `EdgeFunctionHttpError` → parsed body `error` field or HTTP fallback, Sentry capture skips paywall sentinel + caller aborts. On any per-URL success the hook invalidates `['garments']`, `['garments-count']`, `['insights_dashboard']` (mirrors useAddGarment). New `mobile/src/screens/ImportFromLinkScreen.tsx` renders a multi-line URL textarea (autoCorrect=false, autoCapitalize='none', keyboardType='url') with live count vs MAX_LINKS_PER_BATCH (30), "Find pieces" CTA disabled when empty / over-max / importing, "Importing X of Y…" progress copy threaded into the CTA label. Per-row UI: thumbnail (loaded via `useSignedUrl(image_path)` for success rows; spinner for importing rows; placeholder for waiting/failed), title (or hostname fallback), source URL, status pill (accent for success, destructive for failed, fg2 for importing/waiting), inline error caption for failed rows. Success rows are pressable → `GarmentDetail({ id })`. Sticky paywall redirect ref so a 402 routes to PaywallScreen exactly once per mount. Mid-import back-press confirms before discarding. Modified: `mobile/src/screens/AddPieceStep1.tsx` adds a fourth `SourcePill` (Import from link · Paste a URL) using a new `LinkIcon` (chain-link glyph appended to `mobile/src/components/icons.tsx`); `mobile/src/navigation/RootNavigator.tsx` registers the `ImportFromLink` route + adds `ImportFromLink: { initialUrl?: string } | undefined` to `RootStackParamList` (initialUrl reserved for a future iOS Share Extension hand-off — deferred per the wave file). 23 i18n keys appended to the END of `mobile/src/i18n/en.ts` under the `importFromLink.*` namespace. Other 9 locales fall back to en (M33 ports). Findings: appended a row noting "post-launch: native share-sheet integration deferred per wave file" so future iOS Share Extension work has a tracker entry. Verification: `npx tsc --noEmit --skipLibCheck` 0 errors, `npx eslint src --max-warnings 0` 0 warnings, `npx expo-doctor` 17/17 passed. No edge-function changes (wave authorizes none); no migrations. |
| 2026-05-07 | #749 | M23 | **Mobile Launch M23 — Shopping Chat as 9th chat mode.** Extends the M14 8-mode chat contract with a `SHOPPING` mode and surfaces it as a Style ↔ Shopping segmented control inside StyleChatScreen. **Contract reality (verified against `supabase/functions/shopping_chat/index.ts`):** the deployed function's request body is `{ messages, locale }` only — `selected_garment_ids`/`active_look` are accepted by `style_chat` but are no-ops for shopping; the response is a text-only SSE stream (assistant text via `streamBursAI` deltas) with no `stylist_response` envelope and no structured product cards today. The mobile contract reserves a forward-compat `shopping_results?: ShoppingResultCard[] | null` field on `StyleChatResponseEnvelope` plus a `ShoppingResultCard` envelope type (`{id, title, image_url, price?, merchant?, product_url, reason?}`) and a defensive `parseShoppingResultCards(value)` accessor that rejects non-https URLs (M19 allowlist precedent), drops malformed entries silently, and returns null when nothing survives — so a future server-side product-tool emission flows through the same UI without a screen rewrite. **`mobile/src/lib/styleChatContract.ts`:** added `'SHOPPING'` to `StylistChatMode` union, the new `ShoppingResultCard` + `ShoppingResultCardPrice` types, the `shopping_results` envelope field, and the `parseShoppingResultCards` accessor. **`mobile/src/hooks/useStyleChat.ts`:** new `StyleChatMode = 'style' | 'shopping'` type + `ROUTE_BY_MODE` map (`style → 'style_chat'`, `shopping → 'shopping_chat'`); `currentMode` state + sync `currentModeRef` mirror; `setMode(next)` callback aborts any in-flight stream and drops the streaming-assistant placeholder so a mid-stream toggle doesn't bleed text across modes; resets `suggestionChips` on toggle (chips are mode-specific). `sendMessage` captures `turnMode` at send-time and routes to `ROUTE_BY_MODE[turnMode]`; request body branches by mode — shopping ships only `{ messages, locale }` (stripping anchor + active_look so style-chat anchor state doesn't bleed into shopping prompt history). New SSE chunk branch for `type: 'shopping_results'` (forward-compat — never fires in production today); inline `parseShoppingResultCards(envelope.shopping_results)` extraction when a future stylist_response payload fuses both. New `mergeShoppingResults(env, cards)` helper attaches accumulated cards to envelopes without mutation; new `finalizeEnvelopeForMode(mode, env, text, cards)` synthesizes a minimal `mode: 'SHOPPING'` envelope on `onDone` when the server stayed silent (every other field neutral default — empty `garment_ids` so `getLatestActiveLook` skips it, `render_outfit_card: false`, `clear_active_look: false`). The synthesized envelope makes the `'Shopping'` mode pill render on every shopping assistant bubble even though shopping_chat doesn't emit a `stylist_response` event. **`mobile/src/screens/StyleChatScreen.tsx`:** new Style ↔ Shopping `ModeToggleSegment` row (Chip-style filled-pill for active, neutral border for inactive) inserted between the header and the memory panel; tapping flips `setMode(...)`. New `handleOpenProductLink(url)` handler enforces the https-only allowlist before calling `Linking.openURL`, falls back to an `Alert.alert(t('shoppingChat.invalidUrl'))` on bad URL or open failure, with Sentry capture on the unexpected failure path. `MessageItem` now also renders the new `ShoppingResultCard`s (one per `stylistMeta.shopping_results` entry, after the regular text bubble); the memo equality predicate gained `shopping_results` ref equality + `onOpenProductLink` identity. **New `mobile/src/components/ShoppingResultCard.tsx`** is the card primitive: image (left, 64×64 with neutral placeholder fallback when image_url omitted), title + merchant·reason caption + price (mid column), accent-fill "Open" pill button (right) that delegates to `onOpen(card.product_url)`. Uses `useTokens()` + existing `Caption` primitive; accessibility: `role=button` on Open with `accessibilityLabel` + `accessibilityHint`. **i18n:** 7 new keys appended to the END of `mobile/src/i18n/en.ts` — `shoppingChat.modeLabel.style/shopping`, `shoppingChat.cardOpen`, `shoppingChat.cardOpenHint`, `shoppingChat.cardPriceTemplate` ('{amount} {currency}'), `shoppingChat.invalidUrl`, `chat.mode.SHOPPING` (extends the existing chat.mode.* map so the synthesized envelope's mode pill resolves). Other 9 locales fall back to en (M33 ports). Verification: `npx tsc --noEmit --skipLibCheck` 0 errors, `npx eslint src --max-warnings 0` 0 warnings, `npx expo-doctor` 17/17 passed. No edge-function changes (wave authorizes none); no migrations. |
| 2026-05-07 | #750 | M24 | **Mobile Launch M24 — Pick Must-Haves follow-up to WardrobeGaps.** Adds the "Pick must-haves" follow-up screen so users can convert wardrobe-gap analysis into a prioritised shopping shortlist. **Storage decision (no migration):** persisted as `profiles.preferences.shopping_list_jsonb` per the wave's "default to JSONB-on-profiles unless a real table is justified" guidance + the project rule "Never run a DB migration without the user explicitly asking." Read-modify-write merge mirrors the canonical preferences pattern in `OnboardingScreen.tsx` (lines 181-205) — sibling keys (`onboarding.*`, `language`, etc.) preserved verbatim. JSONB shape: `{ entries: [{ id, gap_id, category, priority: 1|2|3, notes?, added_at }], updated_at }`. Defensive parser at the read boundary — malformed entries silently dropped, entire list defaults to empty when invalid (`parseShoppingList(value)` exported for unit-test reach). New `mobile/src/hooks/usePickMustHaves.ts` exposes `useShoppingList()` query → `{ entries, list, isLoading, error }` keyed on `['shoppingList', user.id]`, and `useSaveShoppingList()` mutation that takes `entries: ShoppingListEntry[]` and writes the merged preferences back. Optimistic update via `onMutate` + rollback in `onError` (matches the `useUpdateGarment` pattern in `useGarments.ts:210-225`); `onSettled` invalidates `['shoppingList', user.id]` AND `['profile', user.id]`; `captureMutationError('useSaveShoppingList')` on error path. New `mobile/src/screens/PickMustHavesScreen.tsx` reads route param `{ gaps: WardrobeGap[] }` and renders one `Card` per gap — checkbox toggle (custom 22×22 box with `CheckIcon` infill), High/Med/Low priority `Chip` row (revealed when checked), collapsible notes `TextInput` (closed by default, opened via "+" affordance). Pre-fills selection state from the persisted list by deriving a stable `gap_id` from `${category}::${item_name}::${index}` so the same gap re-surfaces on later analyses keys identically. Sticky bottom save bar with `useSaveShoppingList().mutate(entries)`; Alert toast on success ("Shopping list saved"). Empty-gap branch (route param `gaps: []`) renders the "Nothing to pick yet" hero card + link back to `WardrobeGaps` — used by the Profile shortcut entry. Stable id generator `newEntryId()` mirrors the offlineQueue helper to avoid pulling expo-crypto for the few entries per user. **Modified `mobile/src/screens/WardrobeGapsScreen.tsx`:** added a "Pick must-haves" CTA below the gap list, disabled (kept visible for layout stability) when zero gaps; only renders when `showCachedAnalysis` so the loading + not-enough-garments states stay clean. Threads the live `gaps` array through `nav.navigate('PickMustHaves', { gaps })` so the picker doesn't re-trigger the rate-limited analysis endpoint. **Modified `mobile/src/screens/ProfileScreen.tsx`:** appended a Shopping list `SettingsRow` to the existing settings card — caption surfaces `{count} items saved` (via `pickMustHaves.savedCountTemplate`) when non-empty, otherwise `profile.shoppingListEmpty`. Tap routes to `PickMustHaves` with empty `gaps: []`, which the screen handles via its empty-state branch. **Modified `mobile/src/navigation/RootNavigator.tsx`:** registers `PickMustHaves` route + adds `PickMustHaves: { gaps: WardrobeGap[] }` to `RootStackParamList` (typed via type-only import from `useWardrobeGaps`). 14 new keys (12 `pickMustHaves.*` + `wardrobeGaps.pickMustHavesCta` + `profile.shoppingList`/`profile.shoppingListEmpty`) appended to the END of `mobile/src/i18n/en.ts`. Other 9 locales fall back to en (M33 ports). Verification: `npx tsc --noEmit --skipLibCheck` 0 errors, `npx eslint src --ext .ts,.tsx --max-warnings 0` 0 warnings (one transient `react-hooks/exhaustive-deps` warning on the `?? []` fallback fixed via `useMemo`), `npx expo-doctor` 17/17 passed. No edge-function changes (wave authorizes none); no migrations. |
| 2026-05-07 | #751 | M25 | **Mobile Launch M25 — Onboarding StyleQuizV4 (full 12-question capture).** Replaces the minimal 5-question `StyleQuizStep` (gender + height + 5-bucket lifestyle + climate/city + archetypes + goal) with the full 12-question V4 capture ported byte-for-byte from web's `src/components/onboarding/StyleQuizV4.tsx`. Persists to `profiles.preferences.style_profile_v4_jsonb` via the existing OnboardingScreen read-modify-write merge (sibling preferences keys — `onboarding.*`, `language`, `shopping_list_jsonb` — preserved verbatim). **Schema source verification:** the V4 type definitions, enum unions, archetype/color/fabric/occasion vocab arrays, default factory, and v3→v4 migration helpers live in `src/types/styleProfile.ts` (not at any `src/lib/styleProfile.ts` path). Mobile is v4-native per the wave directive — the new `mobile/src/lib/styleProfileV4.ts` mirrors the schema (same field names, same enum string literals, same vocab IDs, same `version: 4` constant) without porting any v3↔v4 translators, and ships `defaultStyleProfileV4()` + a strict `parseStyleProfileV4(value)` defensive parser that drops any malformed/unknown enum tokens at the read boundary so corrupt JSONB renders as defaults instead of crashing onboarding. **i18n target verification:** the prompt's stale `mobile/src/lib/i18n.ts` reference resolved to a translation shim (locale dispatcher + interpolation only) that imports the dictionary from `mobile/src/i18n/en.ts` — recent waves all append to the dictionary file, so all M25 keys land in `mobile/src/i18n/en.ts` at the END of the export per the append-only convention. New `mobile/src/screens/onboarding/StyleQuizV4Step.tsx` renders one question per page driven by a `QUIZ_QUESTIONS` table (12 entries, 3 required: identity / archetypes / goal — rest optional). Reuses existing primitives (`Eyebrow`, `PageTitle`, `Caption`, `Chip`, `Button`) and `useTokens()` for all colors — zero hardcoded hex. Multi-select grids render as `Chip` arrays with hard caps (3 archetypes-min/5-max, 3 favorite/disliked colors, 3 preferred fabrics); single-select chip rows use a generic `ChipRow<T>` helper threaded through Q6 / Q8 / Q10. Q1 (identity) keeps the tap-pad height stepper from the M0 quiz so it survives without a slider library. Q2 (lifestyle) + Q7 (formality) render percent sliders via direction-gated `PanResponder` (horizontal capture only — vertical drags fall through to the parent ScrollView, mirrors the M0 slider). Q5 colors render as a 44×44 swatch grid with `CheckIcon` infill (token-aware contrast — `t.fg` over white/cream/beige, `t.bg` everywhere else — no hardcoded hex). Q11 goal keeps the radio-card pattern with label + caption per option. Q12 cultural is a multiline `TextInput`. Required questions block the Continue CTA via a `canAdvance` memo that gates on explicit-touch flags (gender / build / ageRange / goal) so default enum values can't auto-pass; optional questions surface a "Skip" button alongside Continue. Output shape is `StyleProfileV4` (12-question payload) returned via `onComplete`. **Modified `mobile/src/screens/OnboardingScreen.tsx`:** swapped `StyleQuizStep` → `StyleQuizV4Step` import; widened `OnboardingDraft.quiz` from `StyleQuizAnswers` to `StyleProfileV4`; extended the `finish()` preferences merge to write `style_profile_v4_jsonb: draft.quiz` alongside the existing `onboarding.quiz` mirror (only when the quiz actually completed — a skip leaves the slot untouched). The legacy `mobile/src/screens/onboarding/StyleQuizStep.tsx` file is now orphaned but retained in tree per the user's "don't delete" implicit policy (zero importers; eslint passes). 224 new i18n keys appended to the END of `mobile/src/i18n/en.ts` under the `onboarding.quizV4.*` namespace — top-level shell strings (`title` / `intro` / `continue` / `skip` / `back` / `progressTemplate`), per-question prompt + help (`q.<id>.prompt` / `q.<id>.help`), per-question sub-section labels (`q.identity.gender` / `.height` / `.build` / `.ageRange` / etc.), per-choice labels (`choice.<namespace>.<value>`) for all 12 archetypes / 18 colors / 6 climates / 6 palette vibes / 4 patterns / 5 fit-overall / 4 fit-top-vs-bottom / 3 layering / 4 body-focus / 3 care / 4 shopping-frequency / 5 budget / 3 shopping-style / 7 primary-goal label+caption pairs / 8 occasions / 10 fabrics / 6 fabric-sensitivities, plus completion screen copy (`complete.title` / `complete.body` / `complete.cta`). User-facing copy mirrors the web V4 strings verbatim ("Tell us how you dress.", "Twelve quick answers help BURS tune outfits…", "Pick your style words.", "Choose 3–5 that feel true to you.", etc.) so brand voice is consistent. Other 9 locales fall back to en until M33 ports the dictionary. **No edge-function changes, no migrations** (wave authorizes none — the JSONB column already exists from web). Verification: `cd mobile && npx tsc --noEmit --skipLibCheck` 0 errors, `npx eslint src --ext .ts,.tsx --max-warnings 0` 0 warnings, `npx expo-doctor` 17/17 passed. |
| 2026-05-07 | #TBD | M22 | **Mobile Launch M22 — Wardrobe Aging panel on InsightsScreen.** Surfaces the deployed wardrobe_aging edge function as a three-row preview panel below the wear-frequency card. New mobile/src/hooks/useWardrobeAging.ts wraps the function via M9 callEdgeFunction. **Contract reality (verified against supabase/functions/wardrobe_aging/index.ts):** request shape is { locale }; response is { predictions: [{garment_id, months_remaining, health_pct, tip, replacement_reason}] } — NOT three buckets as the wave brief implied. The function focuses on the 5 items closest to needing replacement and emits no unworn signal. The hook bucketises predictions client-side: retire_candidates (health_pct ≤ 30 OR months_remaining ≤ 0), aged (health_pct ≤ 70 OR months_remaining ≤ 6 minus the retire set). Unworn is derived locally via a parallel  PostgREST select so the third row stays meaningful. Pattern: useQuery with key ["wardrobeAging", user.id], staleTime 1h (mirrors server cacheTtlSeconds 3600), gcTime 2h, retry false (15/hr-base rate-limited endpoint). Subscription_required surfaces via WardrobeAgingSubscriptionError typed sentinel; other failures via WardrobeAgingError. Defensive shape adapter throughout — asString/asFiniteNumber/clamp 0-100/strongest-rationale-by-lowest-health for the bucket caption. Empty-corpus servers (predictions=[]) bucket cleanly to count=0 across the panel. New mobile/src/components/WardrobeAgingPanel.tsx is pure presentation: section header (Eyebrow + PageTitle), three rows (label · count badge · rationale caption · chevron), four states (loading skeleton with three placeholder rows / error inline caption / empty upbeat state when totalGarments=0 / ready). Disabled rows when count=0 stay visible at 0.5 opacity for layout stability. New mobile/src/screens/UnusedGarmentsScreen.tsx is the bucket detail: route param { bucketId }, reads useWardrobeAging() bucket → garmentIds → one-shot supabase.from("garments").select("*").in("id", ids).eq("user_id", user.id) hydrate (RLS already enforces the second filter; explicit eq is defense in depth, matches useGarments). Rows sorted back into AI prediction order via Map<id,index>. 3-col FlatList of GarmentCards, tap → GarmentDetail. Modified mobile/src/screens/InsightsScreen.tsx: added WardrobeAgingPanel section between the wear-frequency card and the most-worn list with sticky-ref paywall pattern (WardrobeAgingSubscriptionError → nav.navigate("Paywall") exactly once per error window, mirrors WardrobeGapsScreen). Modified mobile/src/navigation/RootNavigator.tsx: registers UnusedGarments route + adds  to RootStackParamList (string-typed for forward-compat against new bucket ids; the screen guards via isValidBucketId and falls back to "aged"). 14 new keys (10 wardrobeAging.* + 4 unusedGarments.*) appended to the END of mobile/src/i18n/en.ts under wardrobeAging.* and unusedGarments.* namespaces. Verification: npx tsc --noEmit --skipLibCheck 0 errors, npx eslint src --max-warnings 0 0 warnings, npx expo-doctor 17/17 passed. No edge-function changes (wave authorizes none); no migrations. |
| 2026-05-07 | #753 | M27 | **Mobile Launch M27 — Coach tour + photo tutorial.** Adds the post-onboarding first-run UX. (1) New `mobile/src/screens/onboarding/PhotoTutorialStep.tsx` — single-screen "how to take a great garment photo" tutorial inserted between AccentColorStep and StudioSelectionStep. Eyebrow + PageTitle + Caption + side-by-side good/bad illustration tiles + 4 bullet rules (lighting / plain background / full garment in frame / no people). **Illustration approach (deviates from prompt option 1):** the tiles render placeholder colored Views with iconography (TshirtIcon for "good", CameraIcon for "bad", accent-tinted vs destructive-tinted backdrops) rather than ship binary asset PNGs in this PR — there are no `mobile/assets/*` placeholders authored for the tutorial today, and the wave brief explicitly authorizes "simple colored View placeholders with iconography for now." Real assets can swap in via a single component edit when design produces them. Same component shape as AccentColorStep — `{ onComplete, onSkip? }`. (2) New `mobile/src/components/CoachOverlay.tsx` — reusable coachmark primitive: transparent fade-in `Modal`, four-quadrant scrim (top + bottom + left + right strips with rgba(0,0,0,0.62) backgroundColor) cutout around a `measureInWindow`-measured target rect, accent-bordered ring rendered inside the cutout for emphasis, "1 of 4" progress pill anchored to the safe-area top, caption card with Skip + accent CTA buttons positioned BELOW the target by default (flips ABOVE when the target sits near the bottom of the viewport — keeps captions visible on FAB-pinned coachmarks). Centered fallback (no targetRef) when the ref hasn't measured. Accessibility: `accessibilityViewIsModal` + `accessibilityRole="alert"` (RN's role union doesn't include `dialog`; alert is the closest semantic supported on both platforms and matches the GarmentSaveChoiceSheet precedent), invisible target descriptor with `accessibilityLabel={caption}` so VoiceOver / TalkBack get the same context. Uses `useTokens()` everywhere — only the SCRIM_COLOR rgba is hardcoded (per wave brief, mirrors the inline rgba pattern in `tokens.scrimBg`). (3) New `mobile/src/hooks/useFirstRunCoach.ts` — single subscription point for the four per-screen coachmarks. Source of truth: `profiles.preferences.coach_tour_completed_at` (ISO timestamp). Defensive parser at the read boundary. **Cross-screen state strategy:** `currentStep` is lifted into the React Query cache keyed by `user.id` (`['coachTour:step', userId]`) so every screen reads the same value WITHOUT prop drilling or a new context. `advance()` increments via `setQueryData` which triggers re-renders in every consumer of the same key; on step >= 4 it calls a `useMutation` that writes the timestamp via the canonical preferences read-modify-write merge (preserves `onboarding.*`, `style_profile_v4_jsonb`, `accent_color`, `shopping_list_jsonb`, etc. — same shape as M24/M25/M26). `skip()` jumps directly to completion. Optimistic update + rollback on write failure (`onMutate` snapshot, `onError` restore). Defensive gate: `shouldShow` requires `status.isSuccess && completedAt === null` so transient profile load errors default to "don't show" rather than racing the user with an overlay. `captureMutationError('useFirstRunCoach.complete')` on the error path. (4) **Modified `mobile/src/screens/HomeScreen.tsx`:** Today's Look hero card wrapped in a measurable `View ref={heroRef} collapsable={false}`; CoachOverlay rendered as a sibling to ScrollView (so the modal scrim covers the whole viewport, not just the scrollable area); only mounts when `coach.shouldShow && coach.currentStep === 0`. Caption: `coachTour.step.home`. (5) **Modified `mobile/src/screens/WardrobeScreen.tsx`:** ref wraps the whole FlatList content; coachmark surfaces on every return path (error / loading / empty / main) via a single `coachOverlay` constant — centered fallback (no targetRef.current) is intentional for non-grid states. Caption: `coachTour.step.wardrobe`. Step index 1. (6) **Modified `mobile/src/screens/MainTabsScreen.tsx`:** ref wraps the entire BottomNav (FAB lives inside as the gold + button between Wardrobe and Plan). Cutout reads as the whole capsule pill; the caption ("Tap (+) to add a piece") makes the intent unambiguous without threading a ref into BottomNav. Caption: `coachTour.step.add`. Step index 2. Lives at the tab-container level so it renders above whichever tab is active. (7) **Modified `mobile/src/screens/OutfitsScreen.tsx`:** ref wraps the header (Saved looks eyebrow + Outfits title + chips), surfaces on every return path (error / loading / empty / main) like Wardrobe. Caption: `coachTour.step.outfits`. CTA copy `coachTour.done` (final step — advance triggers persistence, overlay never reappears). Step index 3. (8) **Modified `mobile/src/screens/OnboardingScreen.tsx`:** swapped `STEP_COUNT 7 → 8`; SKIPPABLE `[1,2,3,4] → [1,2,3,4,5]` (PhotoTutorial added at index 4, StudioSelection shifted to 5, Achievement to 6, Reveal to 7); imports `PhotoTutorialStep`; renders it at step 4 with `onComplete={advance}` (informational — no draft payload to capture); file-header comment updated to reflect the new 8-step order. 14 new i18n keys appended to the END of `mobile/src/i18n/en.ts` — `onboarding.photoTutorial.eyebrow / title / intro / good.title + 4 bullets / bad.title + 4 bullets / continue / skip`, `coachTour.step.home / wardrobe / add / outfits`, `coachTour.next / skip / done`, `coachTour.progressTemplate`. Other 9 locales fall back to en until M33 ports the dictionary. **No edge-function changes, no migrations** (wave authorizes none — `profiles.preferences` JSONB column already exists; `coach_tour_completed_at` is a sibling key alongside the existing `onboarding.*` / `style_profile_v4_jsonb` / `accent_color` / `shopping_list_jsonb` keys). Verification: `cd mobile && npx tsc --noEmit --skipLibCheck` 0 errors, `npx eslint src --max-warnings 0` 0 warnings, `npx expo-doctor` 17/17 passed. |
| 2026-05-07 | #754 | M28 | **Mobile Launch M28 — Travel capsule end-to-end.** Wires the 3-screen wizard (TravelCapsule → TravelMustHaves → TravelPackingList) to the deployed `travel_capsule` edge function; replaces all FIXTURES / GARMENT_FIXTURES / SECTIONS placeholders with real generated data + persistence. **Persistence boundary verified end-to-end:** read `supabase/functions/travel_capsule/index.ts` (~1235 LOC) to confirm — the POST handler returns `{ capsule_items, outfits, packing_list, packing_tips, coverage_gaps, total_combinations, reasoning, trip_type, duration_days, weather_min, weather_max }` and DOES NOT INSERT into `travel_capsules`. Persistence is the mobile hook's responsibility (mirrors web's `useTravelCapsule` calling `saveCapsuleToDb`). The function exposes a GET handler for listing too, but mobile reads the DB directly so optimistic mutations stay in-sync without a function round-trip. **Schema reality (deviates from prompt):** the wave prompt referenced `must_haves` and `packed_state` as columns; verified in `supabase/migrations/00000000000000_initial_schema.sql:1255-1280` they DO NOT exist on `travel_capsules`. Real columns are: `id, user_id, destination, trip_type, duration_days, weather_min, weather_max, occasions, capsule_items, outfits, packing_list, packing_tips, total_combinations, reasoning, created_at, start_date, end_date, luggage_type, companions, style_preference, result`. Per "Never run a DB migration without the user explicitly asking" + "no migrations (table exists)" wave guidance, both fields piggy-back on the existing `result` JSONB column (web's row stores nested data the same way — keeps platforms compatible). **New `mobile/src/hooks/useTravelCapsules.ts`** exposes `useTravelCapsules()` query keyed `['travelCapsules', user?.id]` (selects all 18 columns, ORDER BY `created_at DESC` LIMIT 50), `useTravelCapsule(id)` single-row lookup helper that derives off the cached list, `useDeleteTravelCapsule()` (optimistic delete + rollback + Sentry capture via `captureMutationError`), `useUpdateTravelCapsuleMustHaves()` and `useUpdateTravelCapsulePackedState()` (optimistic patches with read-modify-write merge on `result` JSONB to preserve sibling keys; rollback on error). Defensive boundary parsers (`parseRow` / `parseOutfits` / `parsePackingList` / `parseMustHaves` / `parsePackedState`) drop rows with malformed JSONB at the read boundary so screens never have to type-check on every read. Surfaces a tri-state `TravelCapsuleMustHaveStatus = 'have' | 'buy' | 'unsure'` mirroring the M24 Pick Must-Haves vocab. **New `mobile/src/hooks/useGenerateTravelCapsule.ts`** wraps the edge function via M9 `callEdgeFunction('travel_capsule', { body, timeoutMs: 60000, retries: 1 })` (function is slow — Gemini tool-use over the user's wardrobe routinely takes 25-45s; 60s timeout + 1 retry per the wave brief). Per-call `AbortController` with `useEffect` unmount cleanup matches `useGenerateOutfit`'s pattern. `EdgeFunctionSubscriptionLockedError` → throws `'subscription_required'` sentinel; "Need at least 5 garments" string-match → friendlier copy in screen. On AI success, INSERTs into `travel_capsules` with the full response envelope folded into the `result` column (including seeded `must_haves` from the user-selected garment IDs + initial empty `packed_state`). Returns `{ capsule_id, capsule_items, packing_list, must_haves, outfits }` so the calling screen can navigate to the new row immediately. Sentry capture skips paywall sentinel + AbortError; otherwise tags `edge_function: 'travel_capsule'` for dashboard segmentation. Invalidates `['travelCapsules', user?.id]` on success. **Modified `mobile/src/screens/TravelCapsuleScreen.tsx`** now renders `useTravelCapsules()` saved list as `SavedCapsuleRow` cards (destination · date range eyebrow + items/outfits caption + delete trash IconBtn with `Alert.alert` typed-confirm). Tapping a saved row → `nav.navigate('TravelPackingList', { capsuleId })`. New trip wizard input (destination + date range + trip-type chips) wired to `useGenerateTravelCapsule().mutate(...)`; on success → `nav.navigate('TravelMustHaves', { capsuleId })`. Inline `Card` + `ActivityIndicator` loading state during the slow generation call so the user has signal that the wizard didn't freeze. POPULAR_DESTINATIONS chip strip preserved; trip-type chips now map to backend `trip_type` strings via `TRIP_TYPE_TO_BACKEND` (Business→business, Leisure→casual, Beach→beach, City→mixed, Outdoor→casual, Winter→winter — matches the edge function's `TRIP_TYPE_CONTEXT` keys). **Modified `mobile/src/screens/TravelMustHavesScreen.tsx`** receives `{ capsuleId }` from route params, reads the row via `useTravelCapsule(capsuleId)`, renders the seeded `must_haves` array as tap-to-cycle tri-state pills (have / buy / unsure). Local working copy + `setHydrated` flag prevents the parent's optimistic update from clobbering mid-flight taps; rollback on persistence failure surfaces `Alert.alert('travelMustHaves.saveFailed')`. "Continue · Packing list" CTA threads `capsuleId` into `TravelPackingList`. Empty-state branch handles legacy capsules with no must_haves seeded. Without a `capsuleId` route param, the screen renders a "back to wizard" fallback rather than crashing. **Modified `mobile/src/screens/TravelPackingListScreen.tsx`** reads the row by id, groups `packing_list` by category via `groupByCategory` (canonical wardrobe order: outer / top / bottom / dress / shoes / accessory), renders one Pressable row per item with a 26×26 checkbox infilled with `CheckIcon`. Per-item state mirrors into a local `packed: PackedState` map; toggle flips instantly via `setPacked`, debounced 300ms (`schedulePersist`) to one PATCH per burst (the user can rip through 25 items without 25 server writes). Cleanup ref clears the timer on unmount. Pull-to-refresh wired through `useTravelCapsule.refetch`. Progress card (packedCount / total + filled bar + 1-piece-left singular) replaces the hardcoded TOTAL=14 from the fixtures. Decorative gradient thumbnails use a stable `hashHue(id)` so re-renders don't reshuffle colors. Empty-state branch + missing-id fallback both ship the friendlier copy via i18n keys. **Modified `mobile/src/i18n/en.ts`** appends 35 keys under `travelCapsule.*` / `travelMustHaves.*` / `travelPackingList.*` namespaces — saved-trip card template, generation loading copy, paywall + not-enough-garments alerts, must-haves status labels (have/buy/unsure), packing-list day-template variants (zero / one / other), share-soon placeholder, etc. Other 9 locales fall back to en (M33 ports). **Modified `mobile/src/navigation/RootNavigator.tsx`** widens TravelMustHaves + TravelPackingList route param shapes from `undefined` / `{ selectedIds? }` to typed `{ capsuleId?: string; selectedIds?: string[] } \| undefined` so the wizard can thread the saved row id through the wizard. Comment block updated to match. **No edge-function changes, no migrations** (wave authorizes none — `travel_capsules` table already exists from web; deploy of the function is an existing artifact). Response envelope contract verified end-to-end: `{ capsule_items: GarmentRow[], outfits: PlannedCapsuleOutfit[], packing_list: { id, title, category, color_primary, image_path }[], packing_tips: string[], coverage_gaps: CoverageGap[], total_combinations: number, reasoning: string, trip_type: string, duration_days: number, weather_min: number\|null, weather_max: number\|null }` (function `index.ts` lines 1212-1226). Verification: `cd mobile && npx tsc --noEmit --skipLibCheck` 0 errors, `npx eslint src --max-warnings 0` 0 warnings, `npx expo-doctor` 17/17 passed. |
| 2026-05-07 | #755 | M29 | **Mobile Launch M29 — Style DNA + wardrobe stats.** Replaces 6 hardcoded ProfileScreen / Settings surfaces with real data + adds ProfileScreen pull-to-refresh. **New `mobile/src/hooks/useStyleDNA.ts`** — query keyed `['styleDNA', user?.id]`, primary read on `user_style_summaries` (single row per user, RLS user_id=auth.uid()) using `select('summary_json, summary_text, confidence, updated_at').eq('user_id', user.id).maybeSingle()`. **Schema reality verified against `supabase/migrations/20260501120000_user_style_summaries_and_memory_ingest.sql`:** the columns are `summary_json jsonb`, `summary_text text`, `confidence numeric`, `version integer`, `dirty_at timestamptz`, `created_at`, `updated_at` — NOT the `archetype text / formality text / color_signature jsonb / vibes text[]` shape the wave brief speculated. The `summary_json` shape is owned by `_shared/style-summary-builder.ts` (StyleSummaryJson interface): `style_archetypes: ConfidenceArray`, `formality_center: number 0-100`, `preferred_colors: ConfidenceArray`, plus avoidance / pairings / etc. Hook re-derives a UI-friendly `{ archetype, formality, vibes, signatureColors, confidence, source, updatedAt }` shape via a defensive parser — first archetype value title-cased, formality_center bucketed via thresholds matching `_shared/style-summary-builder.ts:formalityWordFor` (≥70 Formal / ≥55 Smart casual / ≥35 Casual / else Relaxed), vibes = up to 5 archetype labels, signatureColors = up to 5 lowercased preferred_colors values. **Fallback strategy:** when no row exists OR row's overall confidence < 0.2 (mirrors LIMITED_SIGNAL_THRESHOLD), runtime classifier reads `profile.preferences.style_profile_v4_jsonb` (parsed via existing `parseStyleProfileV4` from `mobile/src/lib/styleProfileV4.ts`) and derives archetype = first picked archetype, formality = mean(floor, ceiling) bucketed via the same 70/55/35 cutoffs, vibes = picked archetypes (up to 3) + paletteVibe (when not 'mixed') + patternComfort hint (love → "Pattern bold", solids_only → "Solids only"), colors = favoriteColors. Empty-state (`'Versatile' / 'Smart casual'`) when neither path yields a result. Defensive parsers throughout — malformed `summary_json` downgrades to fallback rather than throwing; transient DB errors fall through to V4 so a network blip doesn't wipe a useful card. StaleTime 5 min. **New `mobile/src/hooks/useWardrobeStats.ts`** — query keyed `['wardrobeStats', user?.id]`, three HEAD count queries (`select('*', { count: 'exact', head: true }).eq('user_id', userId)`) on `garments`, `outfits`, `wear_logs` run in parallel via `Promise.all`. Returns `{ garmentCount, outfitCount, wearLogCount }`. StaleTime 1 min so a user adding a garment / saving an outfit / logging a wear sees the bumped count without spamming COUNT queries on quick navigation. Cache key intentionally distinct from the per-table `['garments-count', userId]` from `useGarmentCount` so invalidations of either key don't clobber the other. **Modified `mobile/src/screens/ProfileScreen.tsx`** — wires `useStyleDNA()` + `useWardrobeStats()`. Style DNA card: archetype italic Playfair text reads `dna.archetype`; vibes Chip strip reads `dna.vibes` (only renders when non-empty); a Caption above the favorite-colors swatch row surfaces `dna.signatureColors` joined by ' · '; the existing FORMALITY_LEVELS chip strip's active match now compares against `dna.formality` instead of the hardcoded `'Smart casual'`; an "empty signal" Caption appears below the chip strip when `dna.confidence < 0.2`. Stat row reads `stats.garmentCount / outfitCount / wearLogCount`. **RefreshControl** added to the parent ScrollView (was already present via `useMockRefresh`); `onRefresh` now does `Promise.all([dnaQuery.refetch(), statsQuery.refetch()])`. `useMockRefresh` import removed; `refreshing = dnaQuery.isFetching \|\| statsQuery.isFetching`. First-load (no cached data yet) renders the existing `ProfileSkeleton`; subsequent stale-while-refetch shows previous numbers (good UX) instead of blanking. Per-section skeletons handle the rare case where one query resolves before the other (Skeleton placeholders for the archetype text + stat numerals). **Modified `mobile/src/screens/SettingsScreen.tsx`** — wardrobe-count badge added to the header below the PageTitle: a `Caption` rendering `tr('settings.wardrobeBadgeTemplate').replace('{count}', String(wardrobeStats.garmentCount))` ('142 items'). Hidden until the count resolves (no skeleton — the page itself doesn't block). Reads from the same `useWardrobeStats()` hook; cache hits across screens (1-min staleTime). **Modified `mobile/src/screens/SettingsStyleScreen.tsx`** — DNA preview at the top of the existing summary card. The "Quiet luxe" hardcoded headline now reads `dna.archetype`; the hardcoded ARCHETYPES array is gone — the Archetypes section reads `dna.vibes` Chip strip (or shows the `'settingsStyle.dnaPreview.empty'` Caption when the user hasn't picked any). The Formality strip's active chip matches `dna.formality`. The "Edit style words" SettingsRow caption now reads `dna.vibes.join(' · ')` instead of hardcoded ARCHETYPES — full editor lands in M38. Loading skeletons render in place of the title + chip strip while the DNA hook is in flight. New i18n keys: `settingsStyle.dnaPreview.title` ('Your style DNA') replaces the hardcoded "Your style DNA" eyebrow. **Modified `mobile/src/i18n/en.ts`** — 11 keys appended at the end under `profile.styleDNA.*`, `profile.stats.*`, `profile.refresh`, `settings.wardrobeBadgeTemplate`, `settingsStyle.dnaPreview.*` namespaces. Other 9 locales fall back to en until M33 ports the dictionary. **No edge-function changes, no migrations** (wave authorizes none). Verification: `cd mobile && npx tsc --noEmit --skipLibCheck` 0 errors, `npx eslint src --max-warnings 0` 0 warnings, `npx expo-doctor` 17/17 passed. |
| 2026-05-07 | #757 | M30 | **Mobile Launch M30 — Push notifications + Expo send branch.** Registers Expo push tokens on auth-resolve, surfaces real `profiles.notification_prefs` toggles in SettingsNotificationsScreen, and adds the Expo Push branch to the `send_push_notification` edge function. **Migrations (idempotent, 2 files):** `20260507120000_push_provider_columns.sql` adds `provider text NOT NULL DEFAULT 'web'` + `expo_token text` to `public.push_subscriptions` plus a defensive backfill UPDATE and `idx_push_subscriptions_provider (user_id, provider)` index for the per-user provider lookup; `20260507120100_profiles_notification_prefs.sql` adds `notification_prefs jsonb NOT NULL DEFAULT '{"daily":true,"new_outfit":true,"reminders":true}'::jsonb` to `public.profiles`. **New `mobile/src/hooks/usePushNotifications.ts`** exposes three hooks: (1) `useRegisterPushToken` mutation — `Notifications.getPermissionsAsync` first → `requestPermissionsAsync` only when undetermined → `getExpoPushTokenAsync({ projectId })` → upserts a `push_subscriptions` row with `{ user_id, provider:'expo', expo_token, endpoint:<token> }` (mirrors the token into `endpoint` so the existing `(user_id, endpoint)` upsert conflict target keeps working without a new unique constraint). Simulator/web bail via `Device.isDevice`; missing EAS projectId returns `'no_project_id'`. Resolves projectId from `Constants.expoConfig.extra.eas.projectId` with `easConfig.projectId` fallback. (2) `useNotificationPrefs` query keyed `['notificationPrefs', user?.id]` reads `profiles.notification_prefs`, defensively parsed at the boundary — malformed / NULL column / missing key downgrades to `DEFAULT_NOTIFICATION_PREFS` ({daily:true,new_outfit:true,reminders:true}). (3) `useUpdateNotificationPrefs` mutation — canonical M24/M25/M29 read-modify-write merge: re-fetches the column, spreads into `{ ...current, [key]: value }`, writes back so sibling pref keys never get clobbered by a future server-side pref addition. Optimistic update + rollback on error + `captureMutationError('useUpdateNotificationPrefs')`. **Modified `mobile/App.tsx`** — sets `Notifications.setNotificationHandler` at module scope (foreground banner + sound on; badge off — no inbox count today; `shouldShowBanner` + `shouldShowList` set alongside legacy `shouldShowAlert` for SDK 54 forward-compat); exports a `navigationRef = createNavigationContainerRef()` and wires it to `NavigationContainer.ref`; new `usePushTokenRegistration` hook fires the register mutation once per `(app session × user.id)` after `useAuth().isLoading` resolves (de-duped via a `Set<string>` ref so a SIGNED_IN re-fire doesn't re-prompt); new `useNotificationDeepLink` hook reads `Notifications.getLastNotificationResponseAsync` for cold-launch taps + subscribes to `addNotificationResponseReceivedListener` for warm-app taps, parses `response.notification.request.content.data.route` and calls `navigationRef.navigate(route, data?.params)` once `navigationRef.isReady()` (50ms tick retry on cold launch). Both hooks mount inside `ThemedShell` so `useAuth()` is in scope. **Modified `mobile/src/screens/SettingsNotificationsScreen.tsx`** — placeholder ROWS array replaced with three i18n-keyed toggles bound to `useNotificationPrefs` + `useUpdateNotificationPrefs`. Permission state read on mount + on every screen-focus (no native subscription API; refetch via `nav.addListener('focus', ...)`); when `permission === DENIED`, an accent-soft banner above the toggles surfaces `permissionsDenied.title / .body` + an `Open Settings` Pressable wired to `Linking.openSettings()`. Caption above the card pulls `permissionsRequest`. Reuses existing `Eyebrow / PageTitle / Caption / Card / SettingsRow / IconBtn` primitives + `useTokens()`; only the banner styles are new (`StyleSheet` block with borderRadius/padding using `tokens.border` + `tokens.accentSoft`). **Modified `supabase/functions/send_push_notification/index.ts`** — branches on `push_subscriptions.provider`: `'expo'` → POST `https://exp.host/--/api/v2/push/send` with `[{to, title, body, sound, data}]` body (free-tier unsigned; Accept-Encoding gzip per Expo docs); validates the token shape (`ExponentPushToken[…]` / `ExpoPushToken[…]`) before posting; parses Expo's ticket envelope for `status === 'ok'`, treats `DeviceNotRegistered` / `InvalidCredentials` as permanent (cleanup row), `MessageRateExceeded` / 5xx as transient (keep row). `'web'` → existing VAPID flow preserved verbatim (extracted into `sendVapidPush` helper). Per-row try/catch so one provider's 4xx never aborts the whole call; failures aggregate into the response envelope (`{ sent, sent_expo, sent_web, failures }`). New `pref_key` request-body field (`'daily' | 'new_outfit' | 'reminders'`) — when set, reads `profiles.notification_prefs` for the target user via service client and short-circuits with `{ sent: 0, skipped: 'pref_off', pref_key }` when the relevant pref is `false`; missing/malformed prefs default-on so a partial column never silently disables every push. The deep-link `url` field merges into Expo's `data.route` so the mobile listener can navigate on tap (callers passing explicit `data.route` win). Preserves `enforceRateLimit` + `checkOverload` + `RateLimitError` envelope. **Edge function shared-module impact: NONE** — only `_shared/cors.ts` + `_shared/scale-guard.ts` are imported and neither was modified, so other edge functions don't need redeployment. Only `send_push_notification` itself needs a deploy after merge. **Modified `mobile/src/i18n/en.ts`** — 9 new keys appended at END under `settingsNotifications.*` namespace (`permissionsRequest`, `daily.label / .body`, `newOutfit.label / .body`, `reminders.label / .body`, `permissionsDenied.title / .body / .openSettings`). Other 9 locales fall back to en until M33. **Modified `mobile/package.json`** — added `expo-notifications ^0.32.17` (resolved to `~0.32.12` request, npm picked latest matching SDK 54) + `expo-device ~8.0.8`. Both are documented Expo Notifications dependencies. Verification: `cd mobile && npx tsc --noEmit --skipLibCheck` 0 errors, `npx eslint src --max-warnings 0` 0 warnings, `npx expo-doctor` 17/17 passed. `npx -y -p deno-bin@1.42.4 deno check supabase/functions/send_push_notification/index.ts` clean (no errors). **Live APNs verification deferred** to M43 (Apple Developer setup) per wave brief — the code paths land now so the user can wire the EAS dev build immediately when the cert is ready. |
| 2026-05-07 | #752 | M26 | **Mobile Launch M26 — AccentColorStep onboarding.** Adds a personal accent-color picker between StyleQuizV4Step and StudioSelectionStep so the user picks one of 12 curated swatches during onboarding. Persists to `profiles.preferences.accent_color` via the existing OnboardingScreen read-modify-write merge (sibling preferences keys — `onboarding.*`, `language`, `style_profile_v4_jsonb`, `styleProfile`, `shopping_list_jsonb` — preserved verbatim). **Decision (no live tint):** the persisted hex is METADATA only; the live UI accent stays the warm-gold token (`mobile/src/theme/tokens.ts`). Web does live-tint via CSS variables; mobile chooses not to so the step writes the chosen hex and moves on, matching the wave brief ("does NOT change the app's brand accent — it's metadata for color memory"). **Swatch palette source (fresh, not reused):** the 12 swatches mirror web's `ACCENT_COLORS` array in `src/contexts/ThemeContext.tsx` (indigo / petrol / forest / sage / navy / slate / burgundy / rose / terracotta / amber / plum / charcoal) so the persisted hex round-trips identically across platforms. M25's `COLOR_SWATCHES` (18-entry color-DNA palette) is intentionally NOT reused — it's the wrong vocab for the accent picker (color DNA = wardrobe color preferences; accent_color = personal UI tint memory). The hex values are DATA, not design tokens, so the "no hardcoded hex" rule does not apply (per wave brief). New `mobile/src/screens/onboarding/AccentColorStep.tsx` renders Eyebrow + PageTitle + Caption + a 3-column-equivalent flex-wrap swatch grid (44×44 each, 22 radius, `CheckIcon` infill on selection) + accent-fill Continue button. Required-touch gate: Continue is disabled until the user explicitly taps a swatch (a `requiredHint` Caption surfaces under the disabled button). Skip is delegated to the OnboardingScreen header. Token-aware contrast for the check icon via `isLightSwatch(hex)` (perceptual-luminance threshold; identical heuristic ported from M25's `StyleQuizV4Step.QColors`). Reuses `Eyebrow` / `PageTitle` / `Caption` / `Button` primitives + `useTokens()` for borders / fg / bg — zero hardcoded theme hex. Component shape: `{ initial?: { color: string | null }; onComplete: (data: { color: string }) => void; onSkip?: () => void }`. **Modified `mobile/src/screens/OnboardingScreen.tsx`:** inserted AccentColorStep at index 3 between StyleQuizV4Step (index 2) and StudioSelectionStep (was 3, now 4); STEP_COUNT 6 → 7; SKIPPABLE [1,2,3] → [1,2,3,4] (covers Value, Quiz, AccentColor, Studio); progress-bar denominator updates automatically. New `OnboardingDraft.accentColor?: string` field survives the AsyncStorage `burs.onboarding.draft.v1` autopersist effect verbatim (already serialises arbitrary draft shapes). The `finish()` preferences merge writes `mergedPrefs.accent_color = draft.accentColor` only when the user actually picked a swatch — a skipped step leaves any previously-stored accent_color value untouched (read-modify-write semantics). **i18n target verification:** the wave file's stale `mobile/src/lib/i18n.ts` reference resolved to a translation shim (locale dispatcher only) that imports the dictionary from `mobile/src/i18n/en.ts` — recent waves (M22-M25) all append to the dictionary file, so all M26 keys land in `mobile/src/i18n/en.ts` at the END of the export per the append-only convention. 18 new keys appended: `onboarding.accentColor.eyebrow` ('Make it yours'), `.title` ('Choose accent color'), `.intro`, `.continue`, `.skip`, `.requiredHint` ('Pick a color to continue'), and 12 `swatch.<id>` labels (Indigo / Petrol / Forest / Sage / Navy / Slate / Burgundy / Rose / Terracotta / Amber / Plum / Charcoal — verbatim copy from web's `accent.<id>` strings). Other 9 locales fall back to en until M33 ports the dictionary. **No edge-function changes, no migrations** (wave authorizes none — `profiles.preferences` JSONB column already exists from web; the `accent_color` field is a sibling key alongside the existing `theme` / `accentColor` web-side keys, written under the snake_case mobile convention to match `style_profile_v4_jsonb` / `accent_color` naming used elsewhere in mobile preferences). Verification: `cd mobile && npx tsc --noEmit --skipLibCheck` 0 errors, `npx eslint src --max-warnings 0` 0 warnings, `npx expo-doctor` 17/17 passed. |
