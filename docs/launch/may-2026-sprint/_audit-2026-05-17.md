# Day 0 Audit — 2026-05-17

**Auditor:** Claude (this session)
**Inputs:** Glob of `docs/launch/waves/*.md` + git log (last 200 commits across all branches) + `gh pr list` since 2026-04-01 + Copilot analysis pasted by Borna 2026-05-17.

**Headline:** The launch is materially more ready than `CLAUDE.md` and `docs/launch/mobile-launch-overview.md` indicate. **All functional M-waves through M42 have shipped.** Wave R (A–D) fully shipped. Waves S (A–C), T, Q, all N-waves, all G-waves, and the post-N modularization roadmap (Phases 0–6 + audits) all shipped through 2026-05-16. Only two waves remain, both **external setup** (no code in-flight): **M43** (Apple Developer + first EAS build) and **M44** (RevenueCat sandbox + final smoke + store submission). `mobile-w14-submission.md` is the older top-level form of the same submission work; M44 supersedes it.

Copilot's own conclusion: "Launch Readiness: GREEN — platform ready for TestFlight/App Store submission" with no findings flagged at P0 severity.

**`CLAUDE.md` is stale.** It still pins CURRENT WAVE to R with R-C in progress; reality is R-D shipped via PR #843 on 2026-05-15 and the modularization roadmap closed out 2026-05-16. The LAUNCH MODE block we added is still correct, but the "Current wave" table below it should be updated to reflect M43/M44 external setup.

---

## Unshipped waves

| Wave | File | Tracker status (overview.md) | Git merge state | Must-ship for launch? | Reason |
|---|---|---|---|---|---|
| **M43** | `docs/launch/waves/m43-apple-eas.md` | not in tracker (post-tracker wave) | **NOT MERGED** | **Y — external setup** | Apple Developer Program ($99/yr), bundle ID registration, ASC app record, App-Specific Shared Secret, APNs Auth Key, iOS Distribution Certificate, Provisioning Profile, first `eas build --profile development`. Code-side delta is small (`mobile/app.json` `associatedDomains` + `aps-environment` lines). |
| **M44** | `docs/launch/waves/m44-revenuecat-sandbox-submission.md` | not in tracker | **NOT MERGED** | **Y — external setup** | RevenueCat account + project, ASC IAP products (`burs_premium_monthly_119sek`, `burs_premium_annual_899sek`), entitlement mapping to `premium`, sandbox testers, webhook secret, sandbox subscribe/restore/cancel verification, final smoke, store submission. |
| mobile-w14 | `docs/launch/mobile-w14-submission.md` | BLOCKED | NOT MERGED | N — superseded | Top-level pre-wave naming. M44 covers the same submission work with the current wave conventions. Mark as superseded; do not work this file. |

**Everything else is shipped.** Waves M0 through M42 are all in `main`. Waves V0, G1–G6, N0–N16, Q (A–D2), R (A–D), S (A–C), T, plus modularization Phases 0–6 with their audit fix PRs (#852–#871) are all in `main`.

---

## Copilot analysis P0 findings

Copilot did not flag any items at P0. The list below is items I am **escalating** to must-ship-for-launch because they are accessibility-review-relevant (Apple/Play reviewers can flag) or payment-reliability issues. Severity in parentheses is Copilot's own rating.

| # | Finding | File/location | Must-ship? | Mapped to wave or hotfix |
|---|---|---|---|---|
| 1 | Accent button contrast `#FFFFFF` on `#AD8137` is ~4.2:1, below WCAG AA 4.5:1 | `mobile/src/theme/tokens.ts` (accent color) | **Y** — Apple accessibility audit can flag | New micro-wave: **`m45-a11y-contrast-hotfix.md`** — adjust `accentFg` to `#FFFAF4` *or* darken `accent` to ~`#A37A28`. Verify against both themes. ~1 hour. |
| 2 | Trial startup is fire-and-forget; if Supabase momentarily unavailable, trial never starts (Medium) | `mobile/src/contexts/AuthContext.tsx` (`callStartTrial`) | **Y** — affects paid conversion reliability | New micro-wave: **`m46-trial-start-offline.md`** — wrap `callStartTrial` in the existing offline-queue retry pattern (mirror `useAddGarment`'s `persistGarmentWithOfflineFallback` shape). ~2–3 hours. |
| 3 | ~~Image alt text missing~~ — **INVALID** on second look | `mobile/src/components/*Card.tsx`, `*Tile.tsx` | N — already implemented | Verified 2026-05-17 by reading source: `GarmentCard` lines 88–115 build a full `accessibilityLabel` via i18n key `a11y.garmentCard.*` (with `noCategory` / `noColor` / `nameOnly` fallbacks). `OutfitCard` lines 191–202 + 256–265 do the same for outfit + locked-tile state. `PhotoTile` has explicit `accessibilityRole` + `accessibilityLabel`. i18n keys exist in both `en.ts` and `sv.ts`. `GarmentImageTile` is an internal tile inside a Pressable parent that owns the accessible group — correct RN pattern. **No work to do.** |
| 4 | AuthContext subscription cleanup can throw if `subData` undefined | `mobile/src/contexts/AuthContext.tsx` (return cleanup) | N — defensive only | Defer to post-launch. Two-character optional-chain fix; not a blocker. |
| 5 | Large components not memoized (CoachOverlay 17.8 KB, GarmentCard 8 KB, icons.tsx 19.8 KB) | various | N — perf optimization | Defer. No crash, no review flag. |
| 6 | Test coverage gaps on hooks/contexts | mobile `__tests__/` | N — quality, not blocker | Defer. CI gates green at 0 warnings; smoke + unit coverage from N4 (PR #805) is sufficient for launch. |
| 7 | Edge-function security audit not fully visible (Copilot didn't have access) | `supabase/functions/_shared/*` | N — already covered | Wave S-A (PR #849) was the security hardening pass; Copilot didn't see the diff. Verified merged. |
| 8 | Polling latency on render jobs | `useRenderJobStatus` | N — perf, post-launch | Defer. |
| 9 | `OfflineQueuedError` skipped silently in Sentry | `mobile/src/lib/sentry.ts` + `useAddGarment.ts` | N — intentional behavior | Defer. Memory note: this is by design — `OfflineQueuedError` is flow-control, not failure. |

**Net escalation:** two new micro-waves. M45 ships the accent-contrast token fix only (Copilot #1). M46 wraps `callStartTrial` in offline-queue retry (Copilot #2). Copilot #3 was verified invalid — the codebase already has full i18n a11y labels on `GarmentCard` / `OutfitCard` / `PhotoTile`. Both micro-waves are code-only and small; each follows the standard per-PR workflow.

---

## Defer-to-post-launch list

| Item | Reason |
|---|---|
| `mobile-w14-submission.md` | Superseded by M44. Move to `docs/launch/_archive/` after M44 ships. |
| AuthContext subscription cleanup optional-chain fix (Copilot #4) | Defensive; not a launch blocker. |
| Memoize CoachOverlay / GarmentCard / icons.tsx (Copilot #5) | Perf optimization, not blocker. |
| Expand unit test coverage for AuthContext, ThemeProvider, useAddGarment (Copilot recs §5.5) | CI is green; smoke + N4 coverage is sufficient for launch. |
| WebSocket replacement for render-job polling (Copilot rec #10) | Post-launch UX improvement. |
| Code-split mobile navigation (Copilot rec #11) | Post-launch perf. |
| Breadcrumb component (Copilot rec #12) | Post-launch UX. |
| Extract JWT validation helper to `_shared/jwt.ts` (Copilot rec #9) | Post-launch refactor. |
| Document edge-function patterns + ADRs (Copilot rec §7) | Post-launch docs hygiene. |
| Animation tokens (duration/easing) in `tokens.ts` (Copilot rec #1) | Post-launch design-system hygiene. |
| Theme-switch fade animation | Post-launch polish. |

---

## M2 priority order (rewrites Plan A §M2)

Plan A's original M2 priority assumed M40/M43/M44/mobile-w14 were all open. They aren't — only M43 and M44 are. Two micro-waves added from Copilot escalation.

**Actual M2 order (target completion 2026-05-24):**

1. **M45 — A11y contrast + image labels hotfix** (code, ~3 hours, non-native) — fixes Copilot findings #1 and #3. Lowest-risk and unblocks accessibility-review confidence.
2. **M46 — Trial-start offline-queue wrap** (code, ~3 hours, non-native) — fixes Copilot finding #2. Improves paid-conversion reliability.
3. **M43 — Apple Developer + first EAS build** (external setup, Borna-driven, days-not-hours due to Apple cert/profile latencies). Code-side commit limited to `mobile/app.json` edits noted in the wave file.
4. **M44 — RevenueCat sandbox + final smoke + submission** (external setup + sandbox E2E, depends on M43). This wave ends the sprint — it's both M2's tail and M3/M4's entry.

Pixel + Conversions API integration (Plan A §M2's separate sub-task) — **revisit**. The Copilot analysis does not surface any Pixel/CAPI wiring in the current code. This may be worth confirming with Borna: is Meta Pixel + CAPI in scope for launch, or deferred to Plan B's MB1 setup? If deferred, drop it from M2 and let Plan C MC3 render placeholders until Plan B wires it. **Recommend asking Borna before doing the work.**

`mobile-w14-submission.md` — move to `docs/launch/_archive/` as part of M44's tracker-hygiene step. Do not work this file.

---

## Tracker hygiene needed

Same-PR cleanup items (don't open a separate PR for these):
- `CLAUDE.md` — Current wave table still says "R — Android platform parity + on-device background removal" with R-C IN PROGRESS. Update to reflect M43/M44 external setup wave OR simply state "Launch sprint — see `docs/launch/may-2026-sprint/00-overview.md`".
- `docs/launch/mobile-launch-overview.md` — Wave Index table shows M1 as current wave. The N-wave / Q / R / S / T / modularization waves are absent from the index. The sprint-pointer line we added at top is correct, but the body is misleadingly out-of-date. Either bring the table current or add a "see sprint overview for current state" banner above the legacy index.

Both are best handled by the agent running M45 (the first new PR), bundled in the same commit as the M45 work.

---

## Net change to Plan A

| Plan A section | Original | Revised after audit |
|---|---|---|
| M1 R-wave close work | R-C + R-D close | **Already done.** M1 collapses to: audit (this file) + tracker hygiene. |
| M2 wave order | M40 → M43 → M44 → mobile-w14 + Pixel/CAPI sub-task | M45 → M46 → M43 → M44. Pixel/CAPI sub-task needs Borna decision before scheduling. |
| Calendar | 14 days, 5 milestones | Same window. Fewer code-wave hours, more Apple/Play wait time. |
| Risk #2 ("Day-0 audit reveals more open waves than 14 days fits") | Plausible | **Disproven.** Audit reveals less open work than expected. Risk inverts: now the risk is *Apple/Play review queue latency*, not implementation throughput. |

**Recommended next action:** Update CLAUDE.md + mobile-launch-overview.md tracker hygiene as the first commit of M45's PR. Then proceed with M45 → M46 → M43 → M44 in sequence.

---

## Decisions made by Borna (2026-05-17, post-audit)

1. **Meta Pixel + Conversions API — DEFERRED to Plan B.** Not in scope for May 31 launch. Plan C MC3 (ad-performance panel) will render "no data yet" until co-founders wire Meta integration via Plan B. Removes Plan A §M2 "Pixel/CAPI sub-task" entirely.
2. **Tracker hygiene bundles into M45's PR.** No standalone docs commit; CLAUDE.md current-wave update + `mobile-launch-overview.md` banner ship in the same PR as the M45 a11y code. Matches the standing tracker-in-PR rule.

## Open items before M45 implementation can start

- Wave files `m45-a11y-contrast-hotfix.md` and `m46-trial-start-offline.md` need to be created. Standard wave-file template per `mobile-launch-overview.md` "Per-Wave Documentation Convention".
- Contrast fix needs a visual sign-off from Borna. Changing `accent` from `#AD8137` to a darker shade (e.g. `~#9E7423`) gets us to ~4.6:1 against white, but ripples through every accent button, gauge ring, and palette swatch in the design system. Alternative — keep `accent`, change `accentFg` — defeats the white-on-gold design intent. Borna picks before code is written.
- Plan A spec (`A-launch-readiness.md` §M2) and implementation plan (`A-implementation.md` Tasks 6–10) are now stale. They should be revised when the M45 wave file is created — or annotated with "superseded by audit, see _audit-2026-05-17.md" at the top.
